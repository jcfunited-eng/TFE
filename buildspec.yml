version: 0.2
phases:
  pre_build:
    commands:
      - aws --version
      - echo Logging in to ECR
      - aws ecr get-login-password --region us-east-1 | docker login --username AWS --password-stdin 418384447921.dkr.ecr.us-east-1.amazonaws.com
  build:
    commands:
      - echo Building image $IMAGE_URI
      - docker build --build-arg TFE_GIT_COMMIT_SHA="$TFE_GIT_COMMIT_SHA" --build-arg NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY="$NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY" -f web/Dockerfile -t "$IMAGE_URI" .
  post_build:
    commands:
      - echo Pushing image $IMAGE_URI
      - docker push "$IMAGE_URI"
