# C-004 Physical Mosaic Recurrence Sprint

## Task identity

- Active ledger item: **C-004**.
- Acceptance: prove that a later lawful recurrence physically alters a retained
  whole-organism mosaic without a database entity, semantic label, member-set
  lookup, or reinforcement counter acting as cognition.
- Closed predecessor: **C-003**, live-proven on production task 989.
- Production baseline: commit
  `4cb339fdfb0feb6661e4e8167a421ba14f1e889b`, task
  `dsf-ai-task:989`, image
  `sha256:25e2f7fee8d42b62e0efae0722db1caeb2d64c67f9a75b7a9a6c59de52b641d5`.
- Live predecessor evidence: one retained 16-neuron formation spanning sight,
  sound, chemical, and body/fluid participation, with 33 original physical
  bonds and 33 first-recurrence bonds; identity
  `1cc4e70a-f2a0-44c5-a111-f4a5bc915cc1` remains closed.

## Frozen scope

The sprint implements only the **alters** branch of C-004: a recognized
cross-cohort formation must be reached again from a proper partial external
cue through current sparse physical flow, and its bounded recurrence witness
must become the exact newly traversed bond/cue/current-neuron evidence. This
does not claim synaptic strengthening, weakening, dissolution, recognition,
recall, word learning, or semantic identity.

## Architecture-honesty gate

1. **Requested architecture:** recurrence is current distributed physical
   reassembly that may change a formation. It is not a stored success count.
2. **Current code reality:** `settle_organism_mosaic_boundary` upgrades an
   organism-wide original once but then ignores recognized formations. A
   separate cohort-local path increments `reinforcement_count`. The live
   C-003 mosaic spans several cohorts, so that local path cannot reassemble it.
3. **Conflict:** yes.
4. **Mechanisms not extended:** `reinforcement_count` as learning, exact member
   set as identity, semantic labels, database/hierarchy objects, the inert
   legacy electrical-contact plastic bytes, fixed thresholds, or new DSF math.
5. **Single exact next item:** revisit recognized organism mosaics in the same
   cross-cohort physical boundary, require a proper partial cue plus current
   changed members and connecting active bonds, and replace only the bounded
   recurrence witness with that exact current evidence.
6. **DSF scope:** unchanged complete joint seven-field L0-L4 remains the
   authoritative neuronal delivery.
7. **Field loss:** none.

## Input -> transition -> output path

1. One admitted whole-organism source occurrence supplies the exact current
   external partial cue and its causal clock.
2. Complete receptor and internal-neuron paths settle through unchanged DSF,
   MathLoom/Psi/Krimelack, membrane/material, and sparse contact physics.
3. `settle_internal_contact_interval` emits exact current physical neuron
   deltas and the stable bond references whose contacts actually conducted.
4. `settle_organism_mosaic_boundary` resolves those current facts against the
   retained formation's original neuronal structure.
5. The formation is altered only if all retained members were physically
   changed and current sparse bonds connect the proper partial cue through the
   whole retained member population.
6. The successor preserves the original retained fractals and original bonds,
   replaces the prior recurrence bonds and cue with the current physical
   witness, persists one bounded current body, and exposes exact read-only
   evidence.

## Expected output and invariants

- The retained formation count stays constant during a lawful reassembly.
- Its original member lineages, original neuronal fractals, and original bonds
  remain byte-exact.
- Its latest recurrence bond/cue witness changes when a different lawful
  physical route reassembles it.
- A complete replay, empty cue, unrelated cue, disconnected flow, or missing
  member change cannot alter the formation.
- `reinforcement_count` does not increase and is not consulted.
- Work remains linear in the reached formation and active sparse bond frontier.
- Identity, full DSF authority, neuron/contact state, current-only persistence,
  and zero Python cognition callbacks remain unchanged.

## Acceptance-evidence map

| Fact | Producer -> retained state -> observer | Status |
|---|---|---|
| Proper partial cue | `externally_reached_neuron_lineages` + `externally_perturbed_neuron_lineages` -> retained `partial_cue_lineages` -> native/PyO3/Python cue observer | connected; exact Python boundary test passes |
| Current member changes | complete-neuron predecessor/successor delta -> current physical deltas -> native observation | existing |
| Current conducting path | sparse electrical settlement -> active stable bond references -> native formation observation | existing |
| Altered bounded witness | `alter_physical_mosaic_recurrence` -> retained recurrence bonds/cue -> existing mosaic codec -> PyO3 structure and cue observers -> Python wrapper | local production-predecessor probe passes |
| Original structure preserved | retained original fractals/bonds -> codec -> before/after exact comparison | unit, whole-organism, and cold-codec proofs pass |
| Live causal successor | production source occurrence -> native transition -> persisted `CURRENT` -> fresh-process observation | local disposable successor passes; production cutover pending |

## Lifecycle branch matrix

| Branch | Required result | Status |
|---|---|---|
| Pristine/first formation | original plus first proper partial-cue recognition remains lawful | focused native proof passes |
| Recognized retained formation | different proper partial cue changes only the latest recurrence witness | whole-organism and production-predecessor probes pass |
| Cold-restored production predecessor | same behavior from authenticated task-989 body | saved live task-989 body passes; exact current rehearsal pending |
| Invalid/disconnected recurrence | side-effect-free non-admission | focused native proof passes |

## Translation-boundary review

- Upstream native fields are the exact externally reached lineages, exact
  externally perturbed subset, exact complete-neuron physical deltas, and the
  stable references of contacts that actually carried nonzero current,
  carrier transfer, or physical plastic change.
- No Boolean, score, label, counter, database key, owner, lock, validator, or
  Python value was introduced into the transition.
- The successor reuses the existing bounded mosaic codec. No persistence magic,
  migration, stored level, or second authority was added.
- Existing PyO3 structure observation already carries member lineages and both
  bond witnesses. The previously omitted proper cue now crosses a separate
  read-only getter and Python wrapper correlated by the canonical structure
  receipt; no raw neuronal deltas are duplicated across that boundary.
- `reinforcement_count` remains decodable for byte-compatible cold restore but
  current execution no longer increments or consults it.
- Candidate provenance, production task environment, pristine/recurrent
  branches, and response-size measurement remain pre-test/rehearsal gates and
  have not yet been claimed.

## Repeat-failure preflight

| ID | Earliest deterministic check | Applicability/status |
|---|---|---|
| RF-001 | Put this exact worktree first on `PYTHONPATH`; print imported Guala paths before collection. | applicable; pending |
| RF-002 | Resolve/export task-989 environment before the first Guala import; observe mounted roster. | applicable; pending |
| RF-003 | Rebuild/install candidate native extension and print loaded binary provenance. | applicable; pending |
| RF-004 | Run the same path from pristine and exact cold-restored task-989 state. | applicable; pending |
| RF-005 | Enumerate cue, changed members, original bonds, prior/current recurrence bonds, and preserved original structure. | applicable; map above |
| RF-006 | Validate fabric/envelope/resource ordering before native construction. | applicable; pending |
| RF-007 | Resolve controller, interpreter, and required tools before deployment clock. | applicable at packaging |
| RF-008 | Save each disposable committed hop and reproduce a refusal from its immediate predecessor. | applicable to multi-hop proof |
| RF-009 | Reject unused aggregate rational/validation prerequisites; retain only local physical authority. | applicable during diff review |
| RF-010 | Compare persisted `CURRENT` with in-process successor; fresh-start and run one more interval. | applicable at rehearsal/live |
| RF-011 | Measure evidence response bytes; emit bounded structure summaries and exact references. | applicable |
| RF-012 | Execute and observe the actual live recurrence; health/counters alone do not pass. | decisive live gate |
| RF-013 | Check sprint-diff formatting before compilation; do not bulk-format inherited drift in untouched code. | triggered: repository-wide `cargo fmt --check` stopped on pre-existing C-003 drift before compilation; disposition is changed-diff-only check |
| RF-014 | Prove test mocks match the closed predecessor interface before using a broad boundary suite; run the exact new assertion separately. | triggered: 11 inherited tests omit C-003's existing `active_physical_bonds`; the new C-004 cue-observer assertion passes independently |
| RF-015 | Detect virtual-environment availability before choosing the local native install command. | triggered: `maturin develop` refused before compilation because this container has no virtual environment; corrected path is release wheel build plus explicit wheel install |

## Falsified hypotheses and failures

- **Closed:** `reinforcement_count` is not physical strengthening. It is an
  unbounded lifetime observation counter and cannot satisfy C-004.
- **Closed:** the persisted legacy `PlasticSupportState` on electrical contacts
  cannot be used as conductance authority. Current source explicitly preserves
  it while proving that it does not alter conduction; no ratified contact
  strain/coupling law exists in this sprint.
- **Closed:** cohort-local recurrence cannot reassemble the live C-003 mosaic
  because that mosaic deliberately spans several retained cohorts.
- **Container recurrence:** the `/tmp` checkout briefly appeared absent after
  container restart, but Git preserved branch
  `codex/guala-whole-brain-mount-20260811` at C-003 closure. The exact root was
  revalidated before C-004 work; no source was reconstructed from chat.
- **First C-004 gate command:** `cargo fmt --check && cargo test ...` stopped at
  the formatter because many untouched C-003 files have inherited rustfmt
  drift. No compile or test ran. The sprint will validate its own diff and
  record the inherited repository formatting debt rather than rewriting
  unrelated source.
- **First focused C-004 test:** compilation succeeded and ten existing physical
  mosaic tests passed. The new test compared its deliberately unsorted bond
  fixture with the canonically sorted retained witness. The fixture is now
  canonicalized before the comparison; no production transition changed.
- **First whole-organism integration compile:** the added cold-recurrence test
  moved its sample sparse fractal into the original fixture and then reused the
  same test value. The fixture now clones that value at its first use; runtime
  code and persisted state were unaffected.
- **First whole-organism integration execution:** the test supplied current
  deltas in topology enumeration order, while the production delta producer
  explicitly returns stable-lineage order for binary lookup. The fixture now
  uses that exact production ordering; no runtime fallback or sort was added.
- **Python boundary suite:** the new recurrence-cue observer test passed, but
  eleven unrelated prepare tests fail because their historical mock lacks the
  existing C-003 `active_physical_bonds` field. This is predecessor test debt,
  not candidate behavior; RF-014 records it and the sprint does not expand to
  repair those unrelated fixtures.
- **First candidate-extension command:** `maturin develop` refused before
  compilation because the container has no virtual environment. RF-015 pins
  the correct no-venv path: build a release wheel, install that exact artifact,
  and print its loaded provenance.

## Production acceptance

From the authenticated task-989 predecessor, one bounded live partial-cue
occurrence must:

1. preserve the organism identity and original mosaic structure;
2. physically change every member through a current sparse connected route;
3. retain a different exact recurrence bond/cue witness without increasing a
   reinforcement counter or adding a formation;
4. cold-restore that successor byte-exact and complete one further ordinary
   interval from persisted `CURRENT`;
5. report zero Python cognition callbacks and bounded CPU/RAM/state bytes; and
6. expose the exact before/after formation evidence through the live native/API
   observation path.

C-004 remains open until that production behavior is directly observed.

## Candidate test record

- Focused physical-mosaic tests: 11 passed, 0 failed.
- Whole-organism recurrence/cold-codec integration: 1 passed, 0 failed.
- Complete native suite: 573 active tests passed, 0 failed; 13 explicitly
  retired candidates remained ignored.
- Exact Python cue-observation boundary: 1 passed, 0 failed. Eleven unrelated
  historical prepare tests remain stale under RF-014 and are not cited.
- The release wheel was built from this worktree and loaded from
  `/tmp/c004-native.IEDiBkr1/guala_core/guala_core.cpython-311-x86_64-linux-gnu.so`;
  the Python boundary loaded from this exact worktree.
- A disposable candidate run cold-restored the saved live task-989 body at
  tick 52,374. One ordinary eight-hop continuous-world interval preserved the
  single 16-neuron formation's original structure, changed its recurrence
  witness from 33 to 16 active bonds, retained a one-member proper cue, changed
  the canonical receipt from `4255963077db742216162c11514c1855967d1b22e2c427fc408882c1592f83a4`
  to `c38a988c06f82b1eacb3217805ac1d453d286619a8d4bb6d4f65ead69f0ddefa`,
  and kept the retired reinforcement count at zero. The body advanced to tick
  52,382, shrank by 10,396 bytes rather than growing, cold-restored at the same
  SHA/tick/formation evidence, and completed another ordinary interval at tick
  52,383 with zero Python cognition callbacks. Elapsed time was 19.86 seconds;
  peak RSS was 1,632,500 KiB, consistent with the known predecessor runtime
  peak rather than new retained growth. Evidence: `/tmp/c004-recurrent.json`.
