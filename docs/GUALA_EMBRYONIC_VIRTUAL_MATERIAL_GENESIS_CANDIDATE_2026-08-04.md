# Guala embryonic virtual-material genesis candidate

Date: 2026-08-04

Status: test-only falsifiable material model. It is not production anatomy,
resident organism state, deployment, autonomy, cognition, or tutoring proof.

## Architecture honesty gate

1. **Requested architecture:** the first resident complete neurons need exact
   dimensioned material anatomy, finite state, repeatable recovery, and source
   specialization without test constants, heuristics, or semantic wiring.
2. **Current code reality:** exact neuron/cohort cold restoration exists, but
   authenticated production state supplies no complete material constitution.
   A test-only candidate now completes exact open-close-quiescence, conserves a
   reached body/fluid recovery exchange, responds again after recovery, and
   settles four source-specialized neurons through sparse contacts.
3. **Conflict:** yes. The finite body reservoir is not yet replenished by
   food/rest/environment physics, and unit-bearing source-to-receptor-work
   adapters are still absent.
4. **Not extended:** test fixture anatomy, `growth_dna` digests, DSF delivery
   impressions, scalar field sums, semantic field-to-sense mappings, owners,
   locks, database state, or `GLCOG004`.
5. **Single next item:** derive one unit-bearing receptor-work adapter for the
   current source carrier, beginning with the virtual card-light occurrence,
   without treating normalized pixels or DSF magnitude as energy.
6. **DSF evaluation:** the complete unchanged seven-field shared result is used.
7. **Field loss:** none. MathLoom width is the maximum exact balanced-ternary
   numerator/denominator width required by the seven fields for the reached
   gate. No field becomes a score or compatibility vector.

## Exact derived portion

For the reached gate, let `P` be the largest exact balanced-ternary digit count
among every numerator and denominator in `D_k, M_k, R_rev,k, U*_k, C_k, P_k,
B_k`. The smallest current lattice is therefore:

```text
positions = P
typed constraints = 7
rings = 7 * 2 * P
nodes per ring = 3
```

Each neutral ternary ring has one virtual coupling-energy unit `E0 = 1 zJ`.
The exact energy difference between an unmatched and matched trit is:

```text
(+3/2 E0) - (-3 E0) = 9/2 E0
```

Therefore its dissipation quantum is `9/2 zJ`; this is derived from the ring
energy equation rather than tuned against behavior.

The candidate declares virtual base units `L0 = 1 nm`, `G0 = 1 pS`, `C0 = 1
pF`, and `V0 = 1 mV`. A one-open/one-close plastic cycle uses:

```text
closed coordinate = L0
open coordinate = 2 L0
elastic scale = 2 E0
yield stress = E0
initial rest length = L0
returned rest length after opening = 4/3 L0
retained fractal = 1/3 L0
released plastic energy = 3/4 E0
```

The gate's exact open/close energy increments have a common quantum `E0/16`.
One first open consumes 16 quanta and its subsequent close consumes 3. Across
the admitted retained-rest interval `[L0, 2L0]`, the plastic support energy is
monotone from `+E0` to `-E0/4`; with admitted source work in `[-2E0, 0]`, the
largest possible one-transition energy magnitude is `36E0/16`. The mounted
gate capacity is therefore 36 quanta. This is derived from the material energy
surface, not an elapsed-time or firing cap.

For `C0 * V0 = 1 fC`, the exact SI elementary charge requires
`ceil(1 fC/e) = 6,242` carriers. Each candidate membrane compartment begins
with that finite quantity; no carrier is created during settlement.

## Executable result

The test-only model
`one_candidate_neuron_forms_one_exact_retained_fractal_and_quiesces` proves:

- exact current-gate MathLoom width rather than a fixed width;
- all seven DSF facts enter typed Psi/Krimelack rings;
- one exact source-work perturbation opens the finite gate;
- membrane/carrier consequences remain finite;
- the gate closes on the supplied later physical intervals;
- the neuron reaches exact quiescence; and
- the retained plastic fractal is exactly `1/3 nm`.

`candidate_neuron_recovers_through_conserved_fluid_and_repeats_response`
additionally proves exact fuel-in/spent-out/heat-out transfer, recovery without
state reset, and a second physical response. The repeated identical occurrence
creates no second plastic fractal because the retained support is already on
its yield surface; that is a physical habituation result, not a label or flag.

`four_source_candidate_cohort_forms_distinct_member_fractals_and_quiesces`
mounts retina, left cochlea, right cochlea, and body/vestibular source anatomy
with four candidate neurons and a three-contact sparse chain. Two staggered
source-work intervals and their two explicit physical relaxation intervals
produce four member fractals, activity on all three contacts, and exact cohort
quiescence.

The model is compiled only under `cfg(test)`, so it cannot be mounted by the
runtime or deployed accidentally.

## Why this is not yet genesis authority

Recovery anatomy now has finite fuel, spent-material, and heat capacities.
`recovery_fluid_contact` transfers fuel inward and spent material/heat outward
under explicit per-interval contact capacity, debiting and crediting every
quantum exactly once. This proves local refill and removal without reset, but
the finite body reservoir still lacks nutrition/metabolic regeneration and an
environmental heat boundary.

The current live sensory carriers identify physical quantity and unit, but the
camera, microphone, card, and most simulated senses do not yet provide a
ratified local conversion from their reached physical quantity to exact
open-minus-closed receptor work in zeptojoules. Missing work cannot be replaced
with normalized values, DSF magnitude, a sense label, or zero-filled data.

Production genesis requires both remaining omissions to be resolved and then
must pass resident repetition, cold-restart, and bounded resource proofs before
`GLCOG004` is retired.
