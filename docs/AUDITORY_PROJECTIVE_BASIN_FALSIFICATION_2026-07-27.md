# Auditory causal projective basin falsification — 2026-07-27

## Decision

Do not implement or select
`guala.hearing.bilateral_causal_projective_cone.v1`.

The candidate is an exact physically lawful resonance basin, but real
held-out speakers are outside even its larger rational linear span.  A
distance, epsilon, nearest cone, partial-coordinate count, or label-derived
boundary would be a different and prohibited mechanism.

## Candidate

For causally bound complete bilateral trajectories
\(X_1,\ldots,X_n\), the candidate THING basin was:

\[
\mathcal B_T^{(n)}
=
\left\{
\sum_{i=1}^{n}\alpha_iX_i
\;\middle|\;
\alpha_i\in\mathbb Q_{\ge0}
\right\}.
\]

Every trajectory retained exactly 44,800 rational coordinates:

\[
2\ ears\times16\ receptors\times2\ components
\times100\ causal\ frames\times7\ DSF\ fields.
\]

The full typed law and the proven current-neuron loss boundary are in
`docs/AUDITORY_HEARING_PROFILE_LOSS_LOCALIZATION_AND_UPSTREAM_LAW_2026-07-27.md`.

## Exact result

- 18 globally source-disjoint speakers;
- three causal groundings: `down`, `go`, and `left`;
- five causally bound reference speakers per grounding;
- one unheard speaker per grounding;
- held-out pass: 0/3;
- every one of the nine query-to-grounding relations was proved outside the
  rational span;
- every reference matrix had modular rank 5;
- adding each held-out trajectory raised rank exactly to 6.

Because the positive cone is a subset of the rational span, each rank increase
is a conclusive non-membership proof.  No cone solver, numerical tolerance, or
approximation is involved.

## Authority

- corpus SHA-256:
  `49650f2341b26d886b46b3f4fb8fed59e30300b17550f1ee4a768b3106cf93a0`;
- report:
  `/tmp/auditory_causal_projective_span.json`;
- report file SHA-256:
  `7f8b1e963bc00c1895b1200060f96e6017f8b62298116e090d039c50d14538cc`;
- embedded authority:
  `b6305ecd82a95d1e1d4ee0c4d5f101f7c423503ee1bcf37450b1d3f1d3691fed`;
- embedded authority recomputation: passed;
- exact modular arithmetic tests: 3 passed.

Each non-membership record retains the prime, reference and augmented ranks,
and ordered full-field pivot coordinates that witness the rank increase.

## What this proves

Exact positive gain and physical linear superposition are insufficient to
relate an unheard speaker to the causal THING grown from five other speakers.
Repeated exposure truly expands this basin by adding generators; the
expansion still does not contain the unheard vocal trajectory.

Together with the already falsified exact-root, projective equality,
cross-receptor edge equality, interval-hull, order/ray, and recurrent-neuron
relations, this closes the physically declared exact candidates currently
present in the repository.

## Exact remaining deficiency

The current peak neuron cannot learn tolerance: its identity retains one
lane plus four ternary relations, its first two full-field witnesses remain
fixed, and later experience changes only an integer strength.  The new
projective law did learn a genuine expanding full-field basin, but speaker
variation is not generated by positive gain or linear mixing.

An auditory-only semantic boundary for an unheard speaker therefore still
requires an additional admitted law.  That law would have to introduce a
nonlinear physical equivalence, a basin boundary, or a metric.  None has been
derived from the authenticated inputs without becoming a score, tolerance,
nearest relation, tuned quotient, or label-dependent classifier.

Authenticated live object/context continuity can truthfully bind a new vocal
variant to an existing THING after the causal encounter.  It cannot make the
same previously unheard acoustic event select that THING before the binding
evidence exists.

## Production truth

The canonical L0–L4 default remains unchanged.  No hearing profile, runtime
owner, persistence schema, speech-release authority, live route, or
deployment was changed by this falsification.

This is not a solved production-hearing claim.
