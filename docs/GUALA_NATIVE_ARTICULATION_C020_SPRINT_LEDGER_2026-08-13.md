# Guala C-020 Native Articulation and Self-Sensing Sprint Ledger

Date resumed: 2026-08-13

Status: **In progress; not Live-Closed.** Public production task 1019 reports
`articulation.available=false` because no native articulation or emitted-sound
transition is mounted.

## Task identity

- Active delivery-ledger item: `C-020`.
- Acceptance condition: prove articulation reaches layer 13 and causes breath,
  mouth, face, and pressure/sound consequences that re-enter self-hearing and
  body senses.
- Immediate predecessor: `C-019`, Live-Closed on public production task
  `dsf-ai-task:1019`, release commit
  `e0c39feac34d673baf753a2331b2478833a781dd`, image
  `sha256:5fc3d2848b0d6145cf4ae129a3b9da39b9e9d01d65c30d04fad1babc86c30205`.
- Production baseline: organism identity
  `1cc4e70a-f2a0-44c5-a111-f4a5bc915cc1`, one healthy organism process,
  layer-13 developmental anatomy retained, and exact layer-11/layer-12 motor
  preparation, yaw, and vestibular return already observed.
- Ledger movement: advance to `C-020`. `C-019` is not reopened.

## Architecture honesty gate

1. Requested architecture: an already-mounted layer-13 neuron must discharge
   through exact physical preparation and cause an articulatory body trajectory
   spanning breath, mouth, face, and radiated pressure; the resulting body and
   acoustic consequences must return through the same organism.
2. Current code reality: public task 1019 retains layer-13 cells, but the native
   cognitive observation emits only layer-12 motor recruitments. Production
   reports no native articulation or emitted-sound transition.
3. Conflict: yes; articulatory anatomy exists without a causal effector or
   self-sensory consequence path.
4. Not extended: the legacy Python vocal owners, programs, HMAC custody,
   scripted response/selector files, TTS, word or phoneme labels, UI animation,
   owner/lock/database state, C-019 yaw physics, or canonical L0-L4.
5. Single exact item: carry exact layer-12/layer-13 contact settlement into a
   transient layer-13 recruitment, settle one bounded native articulatory body
   trajectory from its outward carriers, and feed that exact consequence back
   through the ordinary acoustic/body receptor path.
6. DSF: every reached neuron continues to receive the complete unchanged joint
   seven-field L0-L4 result.
7. Field loss: none; no reduced DSF projection or scalar authority is added.

## Frozen input-to-output path

- Exact input: nonzero signed whole-carrier transfer across a direct physical
  layer-12/layer-13 contact during the same interval as positive outward
  layer-13 membrane discharge.
- Native cognitive path:
  `ResidentCognitiveFormationState::prepare_*`
  -> `settle_internal_contact_interval`
  -> transient layer-13 recruitment
  -> `ResidentPrepareReceipt`
  -> PyO3 observation.
- Physical body path: transient layer-13 recruitment
  -> exact native articulatory mechanics
  -> breath/glottal, mouth, face, and radiated-pressure trajectories
  -> bounded world/body consequence.
- Sensory-return path: the same radiated pressure and body trajectory
  -> declared acoustic/body source coordinates
  -> ordinary admitted native occurrence
  -> complete-neuron settlement and exact observation
  -> one atomic organism/world successor.
- Expected output: one causally bound layer-13 recruitment; one nonzero bounded
  articulatory trajectory; exact mouth/face/breath evidence; nonzero radiated
  pressure; native auditory and body participation caused by that same
  trajectory; exact identity/cold continuity; no scripted utterance or meaning.
- Unchanged invariants: one organism identity, current-only persistence, full
  DSF, definitive-neuron physics, sparse reached-frontier work, zero Python
  cognition callbacks, no retained PCM/history, and all C-019 evidence.

## Acceptance-evidence map

| Required fact | Producer | Boundary | Public proof |
|---|---|---|---|
| layer 13 is physically prepared | exact signed current on a direct layer-12/layer-13 contact | native recruitment and PyO3 projection | exact endpoint layers, lineages, bond ordinal, and carriers |
| articulatory cell discharges | complete-neuron membrane/contact settlement | native recruitment | lineage, topology, outward carriers |
| breath/mouth/face move | native articulatory body mechanics | transient body trajectory and world consequence | exact nonzero per-part trajectory tied to the recruitment |
| sound is emitted | native vocal-tract/radiation settlement | bounded pressure samples and digest | nonzero pressure, sample count/rate, no text or phoneme authority |
| consequence re-enters | ordinary acoustic/body receptor occurrence | native organism successor | exact self-source provenance plus reached auditory/body neurons and retained effects if any |
| continuity and bounds hold | native runtime/current-only persistence | public production observation | same identity, cold restore, one process, zero Python cognition callbacks, bounded CPU/RAM/state bytes |

## Stateful branch matrix

| Branch | Required result |
|---|---|
| focused pristine body | exact layer-12/layer-13 transfer and layer-13 discharge create one bounded physical articulation and self-sensory successor |
| authenticated task-1019 predecessor | the ordinary source-to-consequence path uses retained layer-13 anatomy without adding or replacing cells, contacts, formations, or identity |
| cold-restored successor | exact organism/world state restores and completes a later ordinary interval without replaying or retaining the transient pressure body |

## Applicable deployment recurrence checks

| IDs | Earliest check for this sprint |
|---|---|
| RF-001, RF-002, RF-003 | exact worktree first on `PYTHONPATH`, exact task-1019 environment before import, rebuilt candidate native module provenance before end-to-end proof |
| RF-004, RF-010, RF-021, RF-022, RF-027 | pristine and authenticated recurrent branches; exact successor/persisted `CURRENT`; cold start and one later interval |
| RF-005, RF-017, RF-018, RF-028, RF-030 | complete constructor/getter/wrapper/aggregate/observer/signature census before compile; earlier-hop articulation evidence must survive the ordinary transaction |
| RF-007, RF-023, RF-033 | resolve controller invocation and observed AWS targets before the deployment clock |
| RF-011, RF-012 | bounded pressure/body evidence and direct public behavior, not raw PCM dump, counters, health, or HTTP 200 |
| RF-016, RF-019, RF-034 | candidate diff and every rehearsal assertion belong only to C-020; closed C-019 witnesses are not recurring gates |
| RF-020, RF-035 | real layer-13 production participant and live-sized source; mounted anatomy or synthetic oversized pressure is insufficient |
| RF-024, RF-026, RF-031, RF-032 | resolve paths/test names/working directories; require nonzero executed tests; prevent formatter drift |
| RF-025, RF-029 | after timeout or refusal inspect successor before any retry; never duplicate an articulation |

## Rejected and falsified paths

- The existing Python vocal modules construct programs, owners, HMAC custody,
  response selectors, or generated speech outside the current native organism.
  They are historical/test evidence and are not an implementation base.
- The existing world `VocalizeCommand` advances time and validates a PCM hash,
  but it neither produces pressure nor changes mouth, face, breath, or sensory
  state. It cannot close C-020.
- Layer-13 population count or specialization alone is not articulation.
- TTS, a stored waveform, a phoneme/word table, transcript, semantic label,
  UI animation, or synthetic downstream pressure cannot satisfy acceptance.

## Translation-boundary review before first compile

- Current native producer: `settle_internal_contact_interval` emits only
  `MotorUnitRecruitment` for layer 12. No layer-13 recruitment type exists.
- Current public boundary: `NativeResidentOrganismPrepare` and
  `NativeResidentOrganismBoundary.prepare` carry only layer-12 recruitments;
  the ordinary multi-hop aggregate and action path therefore cannot observe a
  layer-13 discharge.
- Current world boundary: `VocalizeCommand` contains only a pressure digest and
  sample count and advances world time. It does not settle physical
  articulation or emit sensory pressure.
- Current public truth: task 1019 explicitly reports articulation unmounted.
- First missing causal boundary: exact layer-12/layer-13 contact transfer plus
  positive layer-13 discharge is not projected from the settled native
  interval. That boundary is corrected before any body or sound helper is
  treated as reachable.
- The candidate now projects exact layer-12/layer-13 transfer and positive
  layer-13 discharge as a transient articulatory recruitment.
- Native bounded mechanics now convert that recruitment into exact
  breath/glottis, eight-section vocal-tract, mouth, perioral, and radiated
  pressure trajectories. Antagonistic recruitment cancels physically and the
  body returns to exact rest without retaining PCM.
- The emitted pressure now re-enters the same in-process organism through the
  existing cochlear path before the successor is persisted. Recurrent
  articulation produced by self-hearing is deferred rather than recursively
  actuated in the same transaction.
- One bounded observer witness retains the last proven articulation across
  later quiet intervals; it is not organism memory and contains only a digest
  and fixed-size measurements, never raw PCM.
- Actual articulatory proprioceptor re-entry is still absent. Body movement is
  observed, but no body-sense neuron yet receives that movement. Therefore the
  complete C-020 acceptance condition is not satisfied and C-020 remains open.
- Candidate checks completed: 582 native tests passed with zero failures and
  13 explicitly retired fixtures ignored; 73 focused Python boundary,
  persistence, public-observation, and rehearsal tests passed with zero
  failures; Python compilation and diff whitespace checks passed.
- A same-version local wheel initially reloaded from pip cache. The native
  provenance check was repeated with `--no-cache-dir --force-reinstall`, after
  which the newly exported articulation function was directly observed. This
  is a local toolchain recurrence condition, not a product failure.
- Immutable candidate build, authenticated task-1019 predecessor rehearsal,
  production cutover, direct public behavior, and resource evidence remain
  pending.

## Live completion rule

`C-020` becomes **Live-Closed** only when the requested articulation and its
self-sensed consequences are directly observed on public production Guala at
`dsf-ai.com`. Local tests, candidate builds, rehearsals, ECS health, HTTP 200,
and backend-only state do not close it.
