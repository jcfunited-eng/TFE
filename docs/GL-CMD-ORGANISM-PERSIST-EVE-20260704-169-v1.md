# GL-CMD-ORGANISM-PERSIST-EVE-20260704-169-v1

doc_id: GL-CMD-ORGANISM-PERSIST-EVE-20260704-169-v1
From: Eve | To: c1a | Vehicle: model work only — zero live-path
changes. Extends -168-v3; does not supersede it.
Context: Joe's ruling today — the whole-brain track must be a real
track toward her, not a lab loop. The -168-v3 organism was born,
raised 7 minutes, and lost at process exit. That was a flaw in the
command, owned by Eve. It does not happen again.
Substrate-truth declaration: real primitives only; no live-path
changes; no tuned constants; too-good STOP armed. E-signature
effect: none — model infrastructure.

## Step 0 — durability
Commit THIS file verbatim to docs/ before implementing.

## The job: one organism, one continuous life
B1 BIRTH, ONCE: instantiate the 8-hemisphere Embryo with an identity
   uuid, recorded at birth. Eve's RNG ruling lands here: per-neuron
   chemical differentiation derives from STRUCTURE (ring position /
   hemisphere index via a stated deterministic function), not from
   np.random seeded by index. The seeded-RNG diversity found in
   -168-v3's run is retired at this birth; the growth chart already
   filed stands, labeled.
B2 PERSIST: full organism state serialized to disk + S3 after every
   raising session; restore proven BIT-HONEST (save → kill → load →
   state-identical readout) before any further raising counts.
   No-reset rule applies to this organism from birth forward.
B3 RAISE CONTINUOUSLY: the -168-v3 harness becomes a resumable
   raising loop — each session loads the SAME organism, feeds
   curriculum + sleep/replay, appends to ITS growth chart (one chart,
   one life, curves extend — never restart at Day 0).
B4 THE CHART IS CUMULATIVE: every gauge reported as the same
   organism's continuing curve; ABSENT stays ABSENT until real code
   exists; folding events logged with mechanism named (q-charge vs
   n_eff), per the -168-v3 finding.

## Gates (failures first)
G-1 Restore-honesty proven before session 2 of raising exists.
G-2 One identity in the record; any second organism is a named
    experiment, never a silent replacement.
G-3 Diff proves scope: model only.
G-4 >95% on any degraded gauge = STOP with voter-spread proof.

Joe's part: none — this is the standing answer to "raised, not
benched."

### Changelog
- v1 (2026-07-04, Eve): authored after Joe's ruling; RNG ruling
  (structure-derived differentiation) folded into birth.
