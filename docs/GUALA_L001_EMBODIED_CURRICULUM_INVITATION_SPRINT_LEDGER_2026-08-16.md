# Guala L-001 embodied curriculum invitation sprint ledger

Date: 2026-08-16

Status: **Live-Closed.** A persistent-world participant physically approached
Guala, changed six retinal receptors, and left one exact receipt-bound causal
frontier. Guala's own native reached-versus-foregone sparse routing settled
that frontier as attention without requiring an overt motor response. Only
then did one approved A-card multisensory presentation enter her resident
organism and persist one successor.

## Task identity

- Closed item: `L-001` — replace button-as-learning with an embodied
  invitation and presentation path.
- Production: `dsf-ai-task:1104`, commit
  `a2ac01feacd495cbefcb4c53064c083aba0f5b67`, image digest
  `sha256:c8cf7e8aa09e06c29e1a0e9b8ce8d8c20dfa75f7b416875ce0d9ad27e64e689c`,
  identity `1cc4e70a-f2a0-44c5-a111-f4a5bc915cc1`.
- Cutover completed at `2026-08-16T22:38:36Z`. The final check retained one
  completed deployment with desired/running/pending `1/1/0`.

## Architecture honesty gate

1. Requested architecture: a teacher approaches or calls in Guala's embodied
   world; Guala's own physics may attend or decline; only attended media may
   reach her senses.
2. Current code reality before this increment: participant movement already
   produced exact retinal-origin causal traces, and native attention already
   existed as a changed sparse route set containing transported and foregone
   physical branches. The invitation observer incorrectly required that same
   trace to reach layer-12 motor preparation.
3. Conflict with requested architecture: **yes, corrected.** Motor preparation
   was too narrow because covert attention need not produce overt movement.
4. Mechanisms not extended: motor-only acceptance, retinal-change-as-
   acceptance, labels, semantic commands, scores, timers, reward, queues,
   polling cognition, Python choice, ML, whole-brain scans/copies, duplicate
   state, or a second organism.
5. Single exact next item: `L-002`, the complete novel A-Z live teaching and
   cognitive-chain analysis. L-002 did not begin during this increment.
6. DSF scope: the live lesson retained unchanged full seven-field L0-L4
   delivery; `1,430` complete DSF deliveries were observed.
7. Lost field structure: none. Attention is an exact physical route event,
   not a scalar or reduced DSF projection.

## Corrected boundary

The bounded observer now keeps only the latest reached path for the active
participant receipt during the transaction. It accepts attention only when a
directed transfer on that exact retina-origin path is also an exact nonzero
route in Guala's native changed reached-versus-foregone frontier. An unrelated
attention event cannot match. The witness is transient, creates no retained
organism state, and neither chooses nor advances any route.

The participant frontier may remain active across later native intervals. It
settles as:

- `attended` only on the exact participant-path/native-attention intersection;
- `declined` when that exact frontier expires without such an intersection;
- `observing` while the exact frontier remains active; or
- `not_reached` when the approach changes no retinal receptor.

The settled invitation receipt is single-use. A pre-settlement receipt was
live-refused with HTTP 409 and caused no lesson transition; the exact settled
receipt admitted one presentation and was then consumed.

## Focused falsification

- `22/22` focused invitation, causal-trace, sparse-attention, motor-choice, and
  adjacent observer tests passed in `0.82 s`.
- The positive proof propagated one exact participant receptor path across
  native intervals into a transported route while a simultaneous mounted route
  carried zero whole carriers. No motor recruitment existed in the proof.
- The negative proof supplied a genuine sparse attention event on unrelated
  lineages; it could not accept the participant invitation.
- Existing A-007 motor/choice evidence remained unchanged.
- Two historical lean-teaching pins remain stale against the already-expanded
  sensory roster (`209` versus current `564` transitions, and an old two-
  occurrence expectation versus the current joint occurrence). This L-001
  change does not touch those mechanisms, and they were not patched around.

## Exact deployment rehearsal

The digest-pinned discarded-current-state rehearsal restored the exact live
identity and reported:

- exact cold restore: true;
- source mount read-only and source advanced after baseline: true;
- complete neurons: `321`;
- developmental resting neurons: `196,408`;
- resident state: `59,875,463` bytes;
- 37 articulated axes and 74 terminals, cold-exact;
- Python cognition callbacks/workers: `0/0`;
- rehearsal receipt:
  `133e37ac92d4dfe19866940dc668c94153179ecc55f7245b8f0a0e6af32a661e`.

No migration, motor action, lesson, or invitation was injected by deployment.

## Live embodied invitation

One invitation for `alphabet-a` was made; it was not repeated.

- World revision: `6,858 -> 6,859`.
- Participant target: `(3072, 5142)` mm, heading `219746` millidegrees.
- Retinal receptors changed: `6`.
- Participant causal-intent receipt:
  `b1ce339f1cb901c40ec85e2427a427b7bec82f0befb226d2b1fa0f33d6b8248b`.
- Participant action evidence receipt:
  `33bebffb55aef1bed934e98578a8b6eaf9ec1b7a2a624cb41873c6dc3e3b1a02`.
- Initial sensory successor: tick `137459`, state
  `4de93ea9c3a2851090dcda807f748f43bd2d65780ac4e718d85a4ba03020df35`.
- Native settlement: `participant_causal_path_entered_native_attention`, with
  one exact directed participant-caused transfer and no Python, scripted, or
  semantic acceptance authority.
- Settled single-use invitation receipt:
  `f4e0ab253dee7620689fc8b2a22a1fa19c98954ea2404336177b2d37937e9fb0`.

## Live presentation and retained successor

The approved A surface and tutor pressure reached the same resident organism
only after the attended receipt matched:

- presentation: `full`, `13` bounded multisensory hops;
- surface receipt:
  `849827f6dc371856674ff15fc78f676602e44526cd1a2866e18b779069b16d9d`;
- tutor-audio receipt:
  `51ec5253429f8ca0b68359f7069e9ee8ac85bc3488c5f2c73d6d7a75912a75e3`;
- predecessor state:
  `30e783b69cd5f892339acab80324dad03779da6c0ce6b23f90b6194294addc85`;
- successor tick/state: `137520` /
  `e34c2cc9e6ca1bcdfbafbc2a8e3d1dda53dabcadb750381bed3a09ff63930627`;
- successor bytes: `63,315,110`;
- full DSF deliveries: `1,430`;
- physically transitioned neurons: `4,178`;
- complete-neuron fractal changes: `49`;
- partial-cue reassemblies: `116`, including `8` endogenous;
- recovered neurons: `287`;
- exhausted intervals: `0`;
- durable lesson receipt:
  `0aac21cafd51a15ac4c9580c917c0a65ee5a0ac261b9c56957c41f8c15acf633`.

The invitation now reports `presented` and `presentation_eligible=false`; the
receipt cannot admit a duplicate. Raw CURRENT points to the successor state,
identity is unchanged, Python cognition callbacks remain zero, and task 1104
remains the sole settled process.

## Truth boundary

This proves embodied invitation, Guala-owned native attention/decline, gated
multisensory presentation, retained physical change, and single-use receipt
settlement. It does **not** prove that Guala learned the word "A" or "apple,"
recognized either in a new context, or spoke. Those are L-002/L-005/L-006
behavioral acceptances.

The participant body and room are persistent world state. The card is a
bounded physical multisensory occurrence delivered while Guala remains in
that world; it is not yet a persistent manipulable world object. No stronger
object-persistence claim is made.
