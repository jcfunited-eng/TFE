# GL-PLAN-AE-DEV-3WK-EVE-20260703-v7

doc_id: GL-PLAN-AE-DEV-3WK-EVE-20260703-v7
From: Eve · To: Joe · Type: AMENDMENT to v6 (deltas only). Trigger:
GL-KB-COGNITION-ARC-RECOVERED-EVE-20260703-v1 read back against the
plan. Five alterations, one context anchor.

## A1 — Recall is measured PAIRED: cold AND taught (Joe's principle)
The 6/21 canonical principle (KB §2.2): the cold path is designed to
top out ~95%; the ChiAtlas + FamiliarityFeedback teaching loop closes
the residue through context-bound exposure. Therefore:
- C-1 (S2) reports TWO numbers: cold production recall on the fixed
  probe set, AND taught recall on the same set after a declared
  teaching-loop exposure protocol (protocol filed with the probe set,
  before measuring).
- C-2's success gates and Table-1 M1's milestone use the pair. A cold
  number alone is no longer a complete measurement anywhere in this
  plan, including the Week-3 diff.

## A2 — F1/F2 production-status verification (W1, c1, read-only)
Two bugs confirmed on 6/21 evening, fix status UNKNOWN on guala-live:
- F1: language krimelack positional phase offset gated out by the
  no-reset flag.
- F2: Stage-3 sensory adapters instantiate FRESH oscillator krimelacks
  per transduce() call — stateless, no per-neuron accumulated state.
Verify each against current origin, file file:line evidence. Stakes:
Q1 of the rebuild sources per-neuron differentiation from ACCUMULATED
per-neuron state — if F2 stands, that resource does not exist and the
rebuild design must create it first. F2's status also bears on
current sensory binding quality independent of the rebuild.

## A3 — Migration-fuel audit (W1-2, blocks C-3)
The canonical migration mechanism (KB §2.1, -103/-124): REPLAY HER
EVENT LOG through the new substrate so per-neuron atlases build via
consolidation. Her event log is therefore migration fuel — and we
already know the persistence allowlist EXCLUDES some event types
(c1a's groove finding) and the stream has had flood/duplicate
episodes (F8). New task: audit what fraction of binding-relevant
event history is actually persisted and replayable, from her earliest
retained tick. Output: a fidelity statement (complete / gapped-with-
dates / insufficient) filed BEFORE any migration spec claims replay
as the path. If fuel is insufficient, C-3's options section must
include the alternative (live-forward migration: new substrate learns
from her ongoing experience, history consolidates via deep-atlas
reinstatement) with its cost to continuity stated.

## A4 — Growth/Folding: explicitly OUT of the 3-week scope
Stated so no reader infers it silently: the DNA-grows-brain path
(Folding during experience) remains PAUSED at the n_eff wall
(3.000 vs 2.943), with the 6/22 heterogeneous-krimelack attempt
FAILED and contact inhibition a named prerequisite. Recall,
composition, and the whole sprint ride the SEED substrate — by
design, per -124 Part 0. Growth is post-sprint work with a known
blocker, and the investor roadmap prices it that way rather than
hiding it.

## A5 — Gate-calibration discipline (standing, from the 6/22 template)
For C-2 and the M2/M9 composition wiring:
- Thresholds are NEVER inherited from invalidated baselines; where no
  valid baseline exists, gates are set to LEARN ("the first real
  number is the deliverable"), per the 6/22 relaxed-threshold
  rationale.
- Every gate set includes a TOO-GOOD STOP in the -131 style ("STOP if
  T5 > 95% — likely a hash-collapse back to a single-neuron
  ceiling"): a pre-declared suspicion trigger with its named failure
  mode. Results that beat physics get investigated, not celebrated.

## Context anchor (not a gate): S2 expectations
Production runs one of three observables (S1 decides which); the only
honest population-era reference is ~67% T5 at 100 concepts × 64
neurons, in HARNESS, on event_count. Different scale, different
venue — context for reading S2, never a target.

### Changelog
- v7 (2026-07-03, Eve): KB read-back amendments A1-A5 + S2 anchor.
  v6 and all priors retained.
