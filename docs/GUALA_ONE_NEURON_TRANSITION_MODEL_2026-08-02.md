# Guala one-neuron and three-neuron joint-field transition result

Date: 2026-08-02

Status: the isolated Python binding model and native D1 joint-field,
neuron-binding, local-fractal settlement, persistent mounting, exact-clock
separation, cold restore, and bounded recurrence contracts pass. The corrected
D1 field law is mounted in the deployment candidate but is not live production
until a new task is deployed and observed. It does not supply sparse-neighbour propagation,
DNA expression, mature mosaic formation, autonomy, or tutoring.

## Architecture honesty gate

1. **Requested architecture:** a discrete neuron must receive its complete
   applicable joint DSF perspective, produce a compact causally truthful
   fractal, and provide exact recurrence evidence from which later DNA growth
   can lawfully proceed.
2. **Current code reality:** native `joint_field_l0_l4` executes the joint
   field, binds a topology-preserving local perspective, and settles it into an
   ordered sign/null trit impression. `mounted_joint_fractal` retains that
   result, continuous allocated neuron lineage, exact current field authority,
   and recurrence evidence across atomic organism persistence.
3. **Conflict:** no in the candidate source and persistence path. The currently
   serving production task predates this mount until deployment completes.
4. **Mechanisms not extended:** seven independent Krimelack lanes; scalar DSF
   sums; compatibility vectors; per-port tuples presented as the whole field;
   old threshold or resonance tables; Python cognition; Chi identity; labels;
   scores; ML; and opaque hashes presented as memory or cognition.
5. **Single exact next item:** deploy and live-prove a persisted recurrence.
6. **Field evaluation:** the implementation evaluates the complete joint
   near-v1.3 VTVR field and then binds one declared local restriction. It does
   not use a scalar or compatibility approximation.
7. **Declared fractal loss:** the compact trit impression retains exact
   negative, quiescent, or positive structure in fixed causal order. It does
   not contain the full rational magnitude of every field value. Those exact
   magnitudes and relations remain authoritative through the field and local-
   perspective receipts.

## Corrected binding architecture

The seven DSF names are field families, not seven independent oscillators. For
one causal frame the joint field contains vertex-indexed facts and an edge-
indexed cohesion field. A neuron receives one typed local perspective:

- its declared field vertex and topology coordinate;
- its applicable `D_k`, `M_k`, `R_rev_k`, `U_star_k`, `P_k`, and `B_k` facts;
- every `C_k` physical edge incident on that vertex;
- the complete field-authority receipt and causal frame; and
- a separate receipt for the exact local restriction.

The field vertex is not neuron identity. The binding states which physical
field perspective reached a continuously identified organism neuron during
one transition.

The complete edge body is stored once in an immutable organism-owned field
authority. A local perspective refers to its incident edges without replacing
or flattening the complete field. Three local perspectives must agree at
shared endpoints and reconstruct the tested complete cohesion topology.

## Exact arithmetic boundary

Every rational field value is represented reversibly as a canonical balanced-
ternary numerator and positive denominator. The representation introduces no
binary rounding, ternary truncation, guessed scale factor, or magnitude cap.

The exact words remain MathLoom/field authority. They are not by themselves a
neuronal fractal. The compact fractal is a separate ordered structural
impression derived from the neuron-local exact facts.

## Native local-fractal settlement

For every applicable local vertex fact, and for all four exact facts carried
by every incident cohesion edge, the native settlement applies only the
coefficient-free structural distinction already present in the exact value:

- negative becomes `-1`;
- exact zero becomes `0` (quiescent);
- positive becomes `+1`.

Typed non-rational facts retain their direct structural state: reversal is
positive when present and quiescent when absent; uncertainty availability is
positive when observed and quiescent at genesis.

The ordering is fixed by the typed field schema and incident-edge topology,
not by a score or learned ranking. The neuronal fractal binds:

- continuous neuron lineage;
- the complete joint-field receipt;
- the local-perspective receipt;
- the predecessor-fractal receipt, when recurrence exists;
- the ordered trit impression; and
- exact recurrence evidence.

With a predecessor fractal for the same neuron and topology, recurrence emits
three exact counts: matching non-quiescent coordinates, matching quiescent
coordinates, and contradictory committed coordinates. These facts are DNA-
growth evidence. They are not combined into a score and do not command growth.

The receipt proves custody and causality. It is not the cognitive content; the
ordered trit impression is the compact neuronal artifact.

## Executed one-neuron and three-neuron results

The model uses one simultaneous three-vertex field with sight, sound, and body
perspectives. It first selects one neuron, then binds all three.

For one neuron:

- all applicable exact vertex facts round-trip without loss;
- all incident physical edges remain exact;
- identical cause produces identical field, perspective, and fractal receipts;
- another vertex can change the selected neuron's relational perspective; and
- predecessor recurrence produces exact agreement, quiescence, and
  contradiction evidence.

For three neurons:

- each retains a distinct local perspective and distinct fractal receipt;
- shared endpoint witnesses agree exactly;
- the three perspectives reconstruct all three unique cohesion edges; and
- removing one perspective fails complete tested-topology closure.

The Python reference example used 50, 50, and 36 arithmetic trits for its
three exact local field representations, 136 total. Those are properties of
that small example, not neuron-size constants, thresholds, or growth caps.

## Verification result

Python model, isolated VTVR, and dual-kernel contracts: `13 passed`.

Native joint-field, binding, differential, settlement, recurrence, and
distinct-fractal contracts: `10 passed`.

Combined relevant D1 contracts: `23 passed`.

Full native Guala suite: `292 passed; 0 failed`.

Focused Python native-boundary, persistence, migration, packaging, and
readiness suite: `99 passed; 0 failed`.

## Exact-clock episode correction

One causal capture does not authorize clock interpolation. The browser-camera
retina and microphone cochlear components carry distinct exact source-time
vectors. Therefore a real five-second audiovisual occurrence settles as two
complete joint fields: one 64-vertex sight field and one 32-vertex sound field.
Each field is authoritative only on its own exact clock.

Their shared capture authority may retain one compact
`co_observed_capture_occurrence` candidate containing the two topology and
exact-clock receipts plus the 96 participating neuronal lineages. It explicitly
leaves common physical cause unresolved. On recurrence it may reference the
prior candidate for the same physical topology. This is episode connectedness,
not resampling, recognition, a proto-mosaic, or a mature mosaic.

The real candidate measurement produced 96 first-occurrence fractals with zero
recurrence and then 96 predecessor-aware recurrent fractals. Both occurrences
retained zero mosaics. Production has not passed until those facts survive a
live deployment and restart.

## Conclusions promoted into design

- One simultaneous multidimensional field per exact-clock cohort is upstream
  authority; distinct exact clocks are never silently combined.
- A neuron receives a typed local restriction, not seven independent lanes.
- Cohesion is relational edge state, never one local scalar.
- The exact field remains available by causal reference.
- A neuronal fractal is the compact ordered structural impression, not a hash.
- Recurrence produces exact relation evidence, not a heuristic score.
- Three neuron-local perspectives must agree on shared relations and retain
  complete tested-topology closure.

## Still unproved or unmounted

- live-production invocation and restart proof of the persisted fractal and
  episode-relation candidate;
- sparse-neighbour Krimelack propagation between neuronal fractals;
- energy, phase, winding, refractory, or recovery dynamics beyond the proven
  coefficient-free sign/null settlement;
- the physical law that consumes recurrence evidence to express growth DNA;
- specialization and mosaic admission;
- live production invocation, persistence observation, and restart serving.

These remaining laws may not be filled with old thresholds, labels, scores,
learned weights, guessed coefficients, or serialized compatibility tables.
