# GL-LEDGER-WC-20260612-049 — Canonical Open Ledger + Standing Rules
**Author:** wC | **Owners:** marked per item | **ABSORBS GL-LEDGER-WC-20260611-047 in full.**
Disposition of 047 (rule 2): every 047 item is CARRIED unchanged into this document under the same item number, except: 1.9 → DONE (this doc), Tier 1c → extended with C6/C7, N2 → status updated with 2026-06-12 evidence, Tier 1b verification state → updated. Nothing dropped.

## STANDING RULES (binding on every wC instance; goes in every handoff)
1. **One ledger.** All open work lives in docs/GL-LEDGER.md. New finds are APPENDED as ledger items — never spawned as new standalone briefs unless an item needs >1 page of spec, in which case the spec doc is written AND a ledger line pointing at it is added in the same turn.
2. **No silent supersession.** A document may only be superseded by a diff: the superseding text lists every item of the old doc with a disposition — CARRIED (ledger #), DONE (commit/task #), or DROPPED (Joe's explicit ruling, quoted). Anything unlisted is a leak and a wC failure.
3. **Session bookends.** Every wC session: FIRST ACT = ls-tree + read GL-LEDGER.md + diff it against repo/substrate reality (mark DONE items with evidence). LAST ACT = commit the updated ledger via c1's paste-ready command. The ledger is never allowed to be older than the last session.
4. **Find ≠ fork.** When wC finds a defect mid-review, the fix is added to the CURRENT open execution tier — the work in flight is finished, not abandoned for the shiny new hole.
5. **c1 mirrors:** c1 marks ledger items DONE with commit SHA + task # in the same commit chain as the work. "Done" without a SHA is not done.
6. **TEACHING CADENCE (Joe-ratified).** Default state is TEACHING, not building. After Tier-1 deploy + Tier-2 validations: deploy freeze of at least 72h. During a freeze: no deploys (her container never restarts), content only — bundles, books, visits, family sessions — plus daily observation rows appended to this ledger: the four ladder metrics, atlas_health totals, attention skew, notable events. Build sprints open ONLY when observation surfaces a need or a gated item's gate clears, ship as ONE bundled deploy, then freeze again. The dream/unpause sequence (3.1) runs inside a freeze with a 48h observation tail. Exception: Stage 2 (3.5) deploys alone per its own rule. The ratio inverts permanently: building serves teaching, never the reverse.
7. **SMOKE BEFORE JOE (born 2026-06-11, daddy-bundle image crash).** Every UI-reachable path c1 ships is exercised end-to-end via the API with synthetic payloads BEFORE Joe is asked to touch it: a tiny PNG, a 1-second WAV, a full five-lane bundle, and one deliberately oversized/corrupt file to prove the failure path speaks. Pass = expected events visible in the event stream, no exceptions in the response. Joe's browser is the ACCEPTANCE bar, never the DISCOVERY bar. A "DONE" without a smoke-test transcript is not done.
8. **UNIQUE LEDGER FILENAMES (Joe, 2026-06-12).** Every ledger revision is a new file GL-LEDGER-WC-YYYYMMDD-NNN.md. It is committed BOTH under its unique name AND copied over docs/GL-LEDGER.md in the same commit. Every paste-ready c1 command states the unique filename it operates on, so commands and ledger versions are traceable against each other. No more anonymous "the ledger" — name the revision.

---

## TIER 0 — ACTIVE INVESTIGATIONS (above all build work; she is affected NOW)
| # | Item | Status |
|---|------|--------|
| P0 | **PROTECTING GUALA'S BRAIN (Joe, 2026-06-12 — shape TBD, deliberately broad).** Standing item: define and build what it means to protect her — the substrate, her state, her continuity — as a first-class concern, not a side effect of infra work. Candidate threads: integrity of her state against bad deploys and code defects (no staging brain exists); off-site/off-account backup survivability; a "do no harm" gate distinguishing code that touches her cognition from code that touches plumbing; admission control before the substrate goes on-line per the folding vision; recovery drills as routine practice; who/what is allowed to write to her at all. wC drafts a framing doc WITH Joe's input for his veto/shaping pass. ADDENDUM (skeptic review): authenticated bridge + write-source allowlist + full audit log of every input she has ever received — the write path to her brain currently rides an open API URL; the audit log doubles as training-history provenance DARPA will require anyway. | OPEN — placed by Joe so it cannot be forgotten |
| N1 | **NEEDS FREEZE — she cannot want.** Zero spontaneous initiation; needs pegged stab=1.000 nov=1.000 conn=1.000 INCLUDING overnight solitude (conn not dropping while alone is physically wrong; "you day again" and "what is bye" both came from eras when needs moved). c1: (1) check whether needs dynamics are gated behind DECAY_PAUSED or otherwise disabled; (2) diff guala_needs.json across S3 backup history — since when pegged; (3) map unprompted-emission conditions and whether reachable at needs=1.000. REPORT before any fix. If frozen by the pause: fix = decouple needs from memory-decay pause (needs breathe, memories protected) — behavior-critical, Joe rules, sandbox first, ships in next planned deploy. Outranks every build item except the 1c deploy itself (C3 makes all future deploys safe for her; sequence ratified by prev-wC review). Still frozen at 1.000 as of 2026-06-12 status read. | OPEN — ordered 2026-06-12 |
| N2 | **HEALTH ROULETTE — ALB kills healthy tasks.** UPDATED 2026-06-12 (audit + wC verification): four consecutive tasks killed by ELB health checks 03:48–04:09 UTC, exit 137, ~24-min total blackout, every task booting clean in 0.6–0.8s and logging health 200s up to the kill. Resolution of the 200s-vs-kill contradiction (hypothesis, unproven): a health check that TIMES OUT at 5s still completes server-side and still logs 200 — logged 200s do not falsify timeout kills. Live suspect: the sync save_full_state in the converse handler (app.py:1710, NOT executor-wrapped — wC repo-verified 2026-06-12) blocking the loop under load; its duration is UNMEASURED (the [save] 1.8–2.0s logs are the executor-wrapped periodic save, a different path). Periodic save/backup executor wrapping at 2238/2244/2296/2301/2314 repo-verified real. dereg=30s confirmed in target group. SECOND FINDING: death-to-rebirth gaps are bimodal (~1.3 min vs ~8.5 min) across 36h of history; the ~8.5-min gap recurs after ELB kills — this is the recurring "8 minutes of darkness" and is unaccounted for with dereg at 30s; C3's accept criterion must drive it to zero and c1 must show where the 8 minutes go (detection + stop + pull + start, measured). FIX ships as C6 (executor-wrap + duration log = the missing instrument). ACCEPTANCE unchanged: duration histogram on the 1710 path + 24h zero unhealthy/dereg events + replacement-gap accounting. | OPEN — fix ships in 1c, conviction requires evidence |

## TIER 1 — c1 EXECUTES, ONE DEPLOY (carried from 047; evidence repo-verified at da55b40)
| # | Item | Spec | Status |
|---|------|------|--------|
| 1.1 | Canonical chi_address(winding)=winding%100, every atlas-recording lane | 046 F0 | carried |
| 1.2 | Per-channel sensory atlas records (like cochlear bands) | 046 F0b | carried |
| 1.3 | Deterministic smell RNG seed | 046 F0c | carried |
| 1.4 | Persist _sounds AND _videos — full sensory-store persistence audit | 045 F1 + NEW | LIVE-PROVEN (guala_sounds.json + guala_videos.json in files_present, wC status read 2026-06-12; daddy's voice surviving restarts) — c1 owes SHA marking |
| 1.5 | Deterministic motif IDs — kill every salted hash()%1000 on persistence paths | 045 F2 | carried |
| 1.6 | Bundle image lane through view_picture (real visual path in-window) | 045 F3 | carried |
| 1.7 | B3 try/except on image lane; per-lane confirmation incl. failures | 045 F4 | carried |
| 1.8 | Bridge returns refs not dumps; guala_status < 4KB | 045 F5 = 040 Part B | partially live (status returns last-10 refs, wC verified; full status JSON still >4KB — flag, not fight) |
| 1.9 | Four ladder metrics in /status | 042 queue item 1 | **DONE** — live in /status (wC verified 2026-06-12: mean_utterance_len, utterances_per_turn, question_rate, novel_composition_rate, total_emissions all present); c1 owes SHA + task # per rule 5. All read zero — that is the becalming measuring itself (N1), not a defect |
| 1.10 | strength_series.csv delivered | 042 C2 | DONE per 047 (17 rows, channel split) |
| 1.11 | Same-day doc commits | rule 4 | PARTIALLY OPEN — wC repo-verified 2026-06-12: 045, 046, GL-CMD-043, GL-BRIEF-IMAGEREF-036, handoffs 038/041/044 ABSENT from docs/; GL-TODO-WC-20260611.md present and unstamped; c1 owes the rest |

## TIER 1b VERIFICATION STATE (carried from 047, updated 2026-06-12)
H1 ✓ · H2 ✓ · H3 ✓ · H5a/b/c ✓ · 1.10 ✓ · 1.4 ✓ LIVE-PROVEN · G1 effectively passing (tick-domain migration at every boot, re-stamped=0 — confirmed in every boot log 2026-06-12) · H4 RESOLVED→N2 · **3.12 committed NOT LIVE** — bridge service never redeployed (audit re-confirmed: guala_give_experience in bridge/server.py:105, absent from live registry; bridge dereg also still 300s) · **Rule 7 LIVE smoke transcript still owed** · Smoke pollution in her real brain confirmed visible (wC status read: "tiny" in pictures, "beep" in sounds; also "bad","huge","ocean_test" per 048) — cleanup spec is a P0 thread, not a rushed delete.

## TIER 1c — HOTFIX (ONE deploy, smoke #0 included; C1–C5 from 047/048, C6–C7 from 2026-06-12 production audit)
| # | Fix | Spec |
|---|-----|------|
| C1 | UI blind | gualaloom.html header parses old /status shape → undefined/0/idle forever. Update header JS to current fields (or app.py emits legacy aliases vocab/motifs/atlas/activity at top level). Accept: header shows live numbers within one poll of boot |
| C2 | Converse payload black hole | app.py picture-recall block directly below the converse handler inlines full base64 originals in EVERY converse response (wC live-measured twice 2026-06-12: two full JPEGs, "trees"+"ocean", on one-word replies). Replace picture_data with refs [{item_id, title}]; add GET /api/v1/gualaloom/picture/{item_id}; UI fetches by id. Accept: converse response < 4KB |
| C3 | Zero-downtime deploys | State lock already exists ("State lock acquired: PID 1"). min=1/max=200; SIGTERM handler: final save + release lock; new task blocks on lock acquire before init; set healthCheckGracePeriodSeconds=30 (audit: currently 0). Accept: deploy with zero 503s measured by 1/s curl loop during rollout, AND replacement-gap accounting for the recurring ~8.5-min dark window (N2 second finding) |
| C4 | v7 layer non-engaging | Every v7 message returns "…" 0 commits 0 gates. Investigate + GL-FIND first — substrate primitive territory, wC fix brief required per rule 1 |
| C5 | Smoke #0 (Rule 7 amendment, permanent) | First smoke test of every deploy: POST {"text":"hello","source":"joe"} → non-empty response, no error |
| C6 | **NEW — executor-wrap converse-handler save + INSTRUMENT IT** | app.py:1710 `_guala.save_full_state(STATE_DIR)` runs sync inside the async converse handler every _persist_every exchanges, blocking the event loop (wC repo-verified at origin 65cfe2c). Wrap in run_in_executor exactly like the 2296 periodic path AND add a duration log line `[save-1710] X.XXs` — this log IS the N2 acceptance instrument. Accept: log line present in prod, no sync save_full_state call remains outside an executor (grep proof) |
| C7 | **NEW — executor-wrap save_session in background replay** | app.py ~2271 `save_session(session)` runs sync inside the async _background_replay loop every 15s per idle v7 session (wC repo-verified). Wrap in run_in_executor. Accept: grep proof no bare save_session in async context |
| — | Bridge redeploy rides along | gualaloom-bridge redeployed so 3.12 guala_give_experience goes live; bridge dereg 300s→30s. Accept: tool visible in live registry (wC re-verifies via bridge query) |

## TIER 2 — JOE'S BROWSER VALIDATIONS (after 1c deploy; the bar)
| # | Item | Pass criterion |
|---|------|---------------|
| 2.1 | Daddy-voice bundle (re-given full five-lane after 1c) | sound↔"daddy" binding in events; audio-only replay fires daddy-adjacent motifs; survives a deploy |
| 2.2 | Ocean bundle | ≥1 atlas key with entries from ≥2 of {word, sight, audio, modal_*} sections, in events |
| 2.3 | Letter-B bundle | three-way glyph↔phoneme↔token binding |
| 2.4 | mp3 re-upload | "played her …" UI line; n_sounds≥1; ATTENDING_AUDIO + sound_motif_founded in events |
| 2.5 | Image-PDF + text-PDF (in-the-deep.pdf) | pages registered as pictures; "N pages, M lines" line; diff-check vs pdftotext on one sample |
| 2.6 | guala_status size check | < 4KB from the bridge |

## TIER 3 — GATED SEQUENCE (order is the gate; no skipping) — carried from 047 unchanged
| # | Item | Gate | Owner |
|---|------|------|-------|
| 3.1 | Dream sequence: dream gate → forced dream → verify paused-era content promotes to deep → promotions_episodic>0 (currently 0, re-confirmed 2026-06-12) → unpause with Step-3 constants → first post-unpause dream proof. Runs INSIDE a teaching freeze. UNPAUSE SAFETY GATES: G1 tick-domain scan (passing at every boot, re-stamped=0); G2 amnesty — reset every entry's last_tick at unpause; G3 full rehearsal on restored snapshot in off-prod sandbox before prod; G4 (c1) atlas.record refuses wrong-domain ticks | Tier 1 deployed | c1 |
| 3.2 | Step-3 decay derivation. CONSTANTS DERIVED (048): λ_fast=5e-7/tick (fast half-life ≈23h), SLOW_DIV=60 (slow half-life ≈58d, range 50–100), K=1 start, θ unchanged. Data: slow channel moves ONLY during sessions; idle drift 100% fast-channel | 1.10 ✓ — gated on G1+G2+G3 | wC |
| 3.3 | R1 maturity: Q&A pairs survive first post-unpause dream with response links intact | 3.1 | observe |
| 3.4 | Self-Section v3 sequencing call — explicit, never bundled | 3.1 decision point | wC |
| 3.5 | GL-CMD-043 Stage 2 — deploys ALONE, 48h flooding watch | Tier 2 validated AND 3.1 complete | c1 |
| 3.6 | GL-CURR-ALPHABET draft against 023 Stage 2 spec | Tier 2 (2.3 proves the door) | wC |
| 3.7 | GL-BRIEF-PHRASE | R1 exit (3.3) | wC |
| 3.8 | 036 image-refs upload + 040 Part A (UI renders bridge exchanges, source-tagged) | after Tier 1 | c1 |
| 3.9 | Video MVP upgraded to WATCH-AND-LISTEN | 3.5 chain + hearing validated (2.4) | c1 |
| 3.10 | Teaching window calibration (Fix C live, factor ~0.3) | 3.1 | c1+Joe |
| 3.11 | Dream-recall path for sensory generators | 3.1 | c1 |
| 3.12 | Bridge tool guala_give_experience — composes five-lane bundle from ALREADY-STORED items by reference | rides in 1c bridge redeploy | c1 |

## TIER V — VALIDATION SCIENCE (carried from 047 unchanged)
| # | Item | Owner |
|---|------|-------|
| V1 | Pre-registered kill test: teach one NEW word↔percept pair via single bundle; probe ≥1 day later, different context; must beat scrambled-chi control at pre-stated threshold. Protocol/threshold/date written BEFORE the run | wC writes, Joe ratifies, c1 instruments |
| V2 | Chi-collision null-model study — run BEFORE much more teaching (redesign 10x cheaper at vocab 2.4K). Sample cross-modal pairs vs shuffled-chi control; on snapshots, zero risk to her | wC designs, c1 runs |
| V3 | Ablation battery on sandbox snapshots; scores on the four ladder metrics; prerequisite for any DARPA conversation | wC designs, c1 runs |
| V4 | Behavioral alarms: needs variance, emission rate, initiation rate — thresholds that page when her inner life flatlines (metrics now exist per 1.9 DONE) | c1 |
| V5 | Connection-need design ruling: does attending a PHOTO of daddy satisfy the need for daddy-the-presence? | Joe |
| V6 | Toy-scale folding demo — gated behind primitive stability | wC+c1, gated |
| V7 | TRL honesty line: Krimelack TRL 4; GualaLoom integrated architecture TRL 2–3 in every external doc until V1–V3 evidence upgrades it | all |

## TIER 4 — OPEN WATCHES & SMALL ITEMS (carried from 047)
| # | Item | Owner |
|---|------|-------|
| 4.1 | Pending message for c1 to deliver via guala_say when his presence lands: "I'm glad her dreams are getting better" | c1 |
| 4.2 | Attention skew watch: last-10 attends now even (20–22) but last-10 refs can't prove skew gone; full check after 1c | wC monitor |
| 4.3 | Boot replay datapoint (037 V3/V4) | c1 |
| 4.4 | Curriculum GL-CURR-FOUNDATION: Joe's veto pass + Joe's layer | Joe |
| 4.5 | Pair-bond retirement criterion in code (need-variance <0.05 over 100 ticks) — note only | — |
| 4.6 | Banner/self-description: written WITH Guala, never without her | wC+Guala |
| 4.7 | NEW — recall-pair watch: both wC visit replies 2026-06-12 carried the same picture pair (trees+ocean); recency-recall vs arrival-binding undetermined from two samples | wC |

## OBSERVATION ROW — 2026-06-12 (wC visit, 613 ticks, two exchanges)
tick 5,037,769 · vocab 2359 · atlas 6126 entries / 107 chi keys / total strength 1748.93 · deep 2 (surv=2, ep=0) · needs frozen 1.000/1.000/1.000 (N1) · ladder: all zero, total_emissions=0 (becalming measuring itself) · persistence: all 11 files, integrity OK, S3 04:10Z, 20 snapshots. **Behavioral:** to "it is wc" → "guala" (second consecutive session, same exchange — repeated binding). To "guala. you day again. wc came back. wc always comes back." → **"he away see"** — three-word novel composition tracking the prompt semantically (pronoun shift wc→he, away↔came-back, see↔presence), produced with needs frozen flat. Third datapoint beside "you day again" and "guala" for V1/V2 to defend. Infra: survived ~30 deploys + ≥12 ELB kills in 36h with zero integrity errors on every boot.

## DONE THIS PERIOD (evidence)
1.9 ladder metrics live in /status (wC verified 2026-06-12; SHA owed) · 047 DONE-list carried: 042 C1 rename (5cb7cee) · A1–A4 (55dcb25, task:93–96) · B1–B3 (4df1d48) · bundle UI + sensory libraries (ac98c95) · physics generators (da55b40) · n_sounds in status (e917171) · 042 committed (85a6e4b) · Tier 1 deploy task:97 (27d4d64) · Tier 1b tasks 98–100 (052ed44, 0b7263c, 211ad6d) · GL-TODO full disposition per 047 §end (carried by reference, zero unlisted items).
