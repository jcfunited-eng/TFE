# GL-CMD-LOOM-SCAN-BUILD-EVE-20260702-98-v1

doc_id: GL-CMD-LOOM-SCAN-BUILD-EVE-20260702-98-v1
From: Eve | To: c1b | Deploy vehicle: Deploy 2 image
E-signature declaration: none — read-only instrument (spec §10).
Substrate-truth declaration: zero cognition changes; one new READ-ONLY
endpoint; §9.5 language throughout (mechanisms, never feelings).

## Step 0 — durability (standing rule)
Commit THIS file verbatim to docs/ before executing anything below.

## Scope — production Loom Scan
1. OWN PAGE: dsf_ai_service/static/loomscan.html, linked from the old
   page header. Layout and visual language = the approved prototype
   (docs/GL-MDL-LOOM-SCAN-PROTO-EVE-20260702-v1.html) + the brief
   (docs/GL-CMD-LOOM-SCAN-BRIEF-EVE-20260702-v1.md) exactly. Joe's
   design ruling on record: it ships as its own page.
2. LIVE DATA per the -94 contract: /status poll at 1–2s; events cursor
   via since_tick. Arcs are PER-WORD (the -94 B.2 finding — one
   response_bound per word is real signal, not duplication); dedupe
   only true (kind, tick, input_chi, anchors) repeats.
3. NEW ENDPOINT (the -94 B.3 NONE-EXISTS gap): read-only per-chi
   density — chi key → n_bindings + strength sum. No lock held; same
   introspect discipline as /status. The radial map's dots become real
   positions; the prototype's "placement illustrative" label comes OFF
   for this element only.
4. DEAD PANES: remove both from gualaloom.html per the -94 D line map —
   surgical on the shared 952–978 block (sp-emissions stays live).
5. HEALTH STRIP: engine-truth fields only (persistence =
   engine._last_save_tick); RED shown red; empty panes shown empty —
   the scene strip stays honestly blank until B1 story lanes land.

## T-gates (brief §execution-order step 3)
T1 renders from live data <2s after page load.
T2 dedupe verified against a captured event burst.
T3 empty states honest — no decorative activity, no minimum brightness.
T4 polling load unmeasurable on the substrate (compare converse timing
   with the page open vs closed).
T5 old panes removed; no regression on sp-emissions.
T6 persistence field reads engine truth.
T7 Joe opens it live and says so — his sign-off line goes in the
   report verbatim.

Report: docs/GL-RPT-LOOM-SCAN-BUILD-C1-<date>-98-v1.md — failures
first. If Deploy 2's combined diff gets too big, Eve splits at read
time — build regardless.

### Changelog
- v1 (2026-07-02, Eve): first filed version; previously chat-only,
  which blocked c1b under the Step-0 standing rule. Content unchanged
  from the relayed block.
