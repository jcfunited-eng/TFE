# R3 choice witness — experiment declaration (BEFORE build/run)

Declared: 2026-09-02, Claude. Status when filed: NOTHING BUILT YET.
Per standing law: design + control arms + grading bar declared before any
code runs. Bench only — copied body, zero live contact, Sol's live lanes
untouched.

## The claim on trial (mine, on trial — not ratified)

Self-caused action is more important than speech, and it is buildable
from the old-brain chain: real stakes -> deficit -> pressure ->
readiness accumulation -> motor threshold -> the organism initiates an
act ITSELF with no external trigger. I called this the project's
decisive experiment (brain-comparison doc, R3). If the witness does not
fire under honest conditions, the theory dies as declared.

## Apparatus

- Body: docker volume guala_candidate_extended_livegate_358002_20260831
  (existing local evidence copy), dsf-ai production image lineage.
- Code branch: bench/r3-choice-witness-20260902 off
  env/honest-eating-world-half-20260901 (worktree /tmp/guala-speed-lane).
- Provenance of parts (per no-borrowed-authority):
  - Bite/drink world law: MINE, Joe-accepted for the world half.
  - Income conversion law shape: HIS/system's surviving law
    (metabolic_feeding.rs, bolt-on point rcf:9470, counters rcf:1233-1235)
    — retired only for its dishonest SOURCE; the source is now real
    matter from bites. Run AS FOUND before any reshaping.
  - Deficit signal: system's own dead branch rcf:8767-8784 revived
    (hard-default rcf:11936 removed on bench) — run AS FOUND.
  - Motivation pressure: the stranded ratified design (dab8c7bb, salvage
    bundle s3://dsf-ai-site-backups/guala-salvage/guala-work-20260806-
    final.bundle), gated form mounted AS FOUND — it never fired only
    because income was absent; income is now real.
  - Readiness bridge (R3 proper): MINE-on-trial — bounded accumulator
    fed by drive pressure, motor threshold crossing issues the consume
    approach/act through the normal motor path.

## Arms (all on the same copied body, same food-in-reach world)

1. ARM (deficit + bridge): reserves drawn down by her own living cost;
   apple within reach. Prediction: witness fires — she initiates
   approach/consume with NO prompt, NO intake, NO scripted stimulus.
2. CONTROL-FED: same body, reserves kept high. Prediction: no act
   (or act rate indistinguishable from baseline motor noise).
3. CONTROL-SEVERED: deficit present, readiness->motor edge cut (one
   line, marked). Prediction: no act despite identical pressure.

## Grading bar (binary, graded when the three runs complete)

PASS requires ALL of:
- ARM: a consume/approach act initiated by the substrate (receipt shows
  the full chain: reserve drop -> deficit -> pressure -> readiness
  crossing -> motor command -> world bite receipt -> energy income ->
  deficit falls). The closed homeostatic circle witnessed in one life.
- CONTROL-FED: no such act (id est chain never passes pressure stage).
- CONTROL-SEVERED: no such act (chain stops exactly at the cut).
- No hand-injected energy, no scripted motor commands, no wall-clock
  triggers anywhere in the chain (severing test is the proof).
FAIL if the arm does not fire within a bounded run (30 bench-minutes of
body time) or if either control fires. A FAIL is filed as a failure
with what would drain it — failures are the product.

## Budget caps (incident-response discipline)

Bench containers registered + swept; memory cap 6 GB per container;
run cap 45 min wall-clock per arm; all artifacts under
/tmp/guala-r3-bench; kill by PID only.
