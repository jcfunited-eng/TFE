# Guala speech repair attempt 46 — whole-word physical control

## Status

This is the complete causal-impact and falsification boundary for the next
speech step. It authorizes a test-only native-organ control and no production
change or word claim. Production remains the unchanged healthy task 1429.

## Architecture honesty gate

1. **Requested architecture:** learned whole-organism activity must cause an
   ordered vocal-body trajectory, radiate a recognizable word, receive the
   exact pressure through both ears, and later reuse the word in context.
2. **Current code reality:** the native spectral organ has finite respiratory
   work, a bounded glottal valve, fluid turbulence, five lossy tract modes,
   eight physical tract coordinates, signed pressure, and persisted pulmonary
   and acoustic state. Attempt 45 recovered repeatable mature-body use. The
   exact copied body nevertheless holds one vocal posture while the same two
   learned tract motors co-occur; 512 intervals produced no posture order.
3. **Conflict:** yes. A source capable of phonation is present, while a
   demonstrated ordered articulatory trajectory is absent.
4. **Not extended:** the retired Python articulatory program owners, babble
   cursor/round-robin selector, TTS, phoneme or word tables, stored pressure,
   target-pose tables, semantic labels, observer commands, scores, ML, or any
   L0-L4/DSF/Krimelack mechanism.
5. **Single next item:** determine whether the existing native organ can render
   one recognizable whole word when a test-only control supplies a continuous
   physically valid posture trajectory. This separates organ sufficiency from
   organism-owned ordering before either is changed.
6. **DSF scope:** none. The control does not settle cognition and cannot enter
   the organism. Every later organism trial must preserve full explicit
   `D_k`, `M_k`, `R_rev_k`, `U_star_k`, `C_k`, `P_k`, and `B_k` authority.

## Why this control is required

The accepted V22 board proves that a compact deterministic source plus moving
spectral controls can sound like `ma-ma`, `see`, and `shoe`. It does not prove
that the current native organ implements an equivalent physical range. The
attempt-44 copied-body output proved only the V22 closed-mouth `mmm` class.
Continuing to repair organism learning before the native organ itself produces
a recognizable word would leave an unknown actuator at the end of the chain.

The control therefore supplies motion, not sound. It may set only declared
anatomical coordinates and finite respiratory discharge at successive native
sample spans. The unchanged native organ must calculate every flow, valve,
turbulence, resonance, pressure, loss, pulmonary expenditure, and rest state.
The control cannot supply a waveform, frequency oscillator, phoneme identity,
word identity, or acoustic correction.

## Dynamic range harness

The first target is the already accepted two-syllable reference `ma-ma`. The
external page may label buttons for human evaluation; those labels never enter
native physics.

The harness varies a bounded Cartesian range rather than guessing one point:

- first and second closed-mouth spans;
- first and second open-vowel spans;
- physical lip/terminal-tract closure inside the declared anatomical range;
- open-vowel mouth area inside the declared anatomical range;
- finite respiratory carriers inside the existing layer-13 discharge bound;
- small adjacent tract-area changes around the native regions already proved
  distinct by `three_vowel_regions_and_adjacent_controls_are_physically_distinct`.

Every candidate begins from exact native rest, advances one continuous body,
and carries its acoustic and lung successor into the next span. It may not
reset lung volume or acoustic state between syllable parts. The harness records
raw 16-kHz pressure, raw body/flow evidence, exact parameters, peak, duration,
successor rest state, and SHA-256. Observer playback gain is separate and may
not alter the raw candidate or any future self-hearing input.

The harness itself must first verify AWS production health: exactly one desired
and running healthy task, zero pending tasks, task definition 1429, and all five
Guala alarms `OK`. It then runs offline with no production intake or storage
write.

## Acceptance and falsification

The native-organ control passes only if all of the following hold:

1. at least two adjacent parameter settings yield a recognizable whole
   `ma-ma` by Joe's ear, so success is a region rather than one fitted point;
2. raw pressure is nonzero, signed-16 bounded, unclipped, and wholly generated
   by the existing native organ;
3. the same parameters reproduce the same raw hash from exact rest;
4. zero respiratory carriers produce exact silence;
5. holding one posture for the same total duration produces a different
   waveform and is not accepted as the word;
6. reversing the open/closed ordering produces a different waveform and is not
   accepted as `ma-ma`;
7. acoustic and pulmonary state reach exact rest under the existing loss and
   passive-return laws; and
8. memory, output bytes, calls, and runtime are bounded by the fixed candidate
   grid and fixed maximum sample span.

If no adjacent region is recognizable, the native organ remains incomplete.
The failed auditory descriptions and exact parameters enter this attempt
before any organ edit. If the control passes, it proves actuator capacity only.

## Copied-production-body gate after control

No test posture may enter the organism. The copied task-1429 body must instead
produce at least two distinct physical postures from its own layer-11 ->
typed-layer-12 routes in causal order. The resulting raw pressure must be
recognizable as the same whole word, return through all mounted auditory
receptors, change current organism activity, survive exact cold restore, and
repeat without adding routes/state per repetition.

The present body has only two learned vocal routes and they co-occur. A passing
control therefore sends the next analysis to the experience-grown ordering and
motor-route boundary. It does not authorize an external sequencer, replay of
the control trajectory, or a stored sequence object.

## Production impact

None is authorized by this document. The harness is read-only with respect to
AWS and test-only with respect to native code. A later organism repair requires
its own complete caller/state/persistence/resource analysis, exact copied-body
proof, immutable rehearsal, one deployment attempt, live self-hearing proof,
and human acceptance of the whole word before any sentence claim.

## Harness invocation history

1. The first invocation passed the read-only AWS gate and stopped before any
   render because the test module omitted the private `BODY_AXIS_COUNT` import
   and moved the two negative-control vectors before hashing them. No WAV or
   manifest was produced, and no acoustic conclusion is allowed from this
   invocation. The retained log is
   `/tmp/guala-word-control.EgpWFU/native-test.log`. The corrected invocation
   must compile the same bounded harness; this command shape is not retried
   without those two source corrections.
2. The corrected invocation passed one exact ignored native harness test and
   produced five word candidates plus frozen and reversed controls. Zero
   breath was exactly silent; every candidate was deterministic, unclipped,
   and reached exact pulmonary/acoustic rest. Candidate B and C were byte-
   identical because the existing respiratory-work anatomy saturates above
   six carriers; that is a measured bound, not a fitted exception. Human
   auditory acceptance remains open.
3. The provenance rerun reproduced every native pressure hash but rejected its
   generated page before publication because raw-string quotation marks for
   the reference and two control buttons were unnecessarily escaped. Candidate
   buttons were valid and native evidence was unaffected. The corrected page
   generator removes only those literal backslashes; the malformed page is not
   an accepted or published artifact.
4. The final source at commit `af42e97a` passed the native test and all machine
   falsifiers again. The AWS pre-gate found exactly one healthy task 1429 and
   all five alarms `OK`. The page and all seven WAVs were published only to the
   read-only static path
   `https://dsf-ai.com/speech-proof/attempt46-af42e97a/`; CloudFront
   invalidation `IELYVSJVBEBLTFA3B89T6YXOY0` completed. Every fetched public
   byte matched the local artifact. Human auditory acceptance remains the only
   open control gate.

## Final control evidence awaiting human hearing

- manifest: `/tmp/guala-word-control-publish.tMgHEu/manifest.json`, SHA-256
  `a37ae7d5515352ed41584be3d7fd42450536f7f2185e29efc92084455a183de9`;
- page SHA-256
  `16f95f265f5acff563d2647083220f98630476fa51f3bac1e062201187b8a622`;
- candidate A/B/D/E public WAV SHA-256 values
  `cedab627...`, `7a140fdc...`, `faf30f4b...`, and `186f8b1c...`;
- candidate C is exactly candidate B because the respiratory-work reservoir is
  already saturated, a lawful negative range result;
- frozen and reversed control WAV SHA-256 values `5f1b6eb8...` and
  `ec7e425d...`;
- accepted V22 16-kHz `ma-ma` comparison SHA-256 `d97526a3...`;
- raw candidate peaks 665--709, fixed sample spans 12,560--15,600, no clipping,
  deterministic repeat, exact zero-breath silence, distinct frozen/reversed
  hashes, and exact rest for every candidate;
- no harness or native-test process survived publication.

These facts prove a bounded and correctly published physical control. They do
not prove that any candidate sounds like `ma-ma`; only Joe's hearing verdict
can accept or reject that requirement.

## Human auditory verdict — accepted

Joseph listened to the accepted V22 reference and every published native
candidate. He reported that all candidates were acceptable and sounded like
`ma-ma`, and selected **Mama-A** as the approved candidate. He described its
tone as ever so slightly robotic, explicitly directed that this not be refined
now, and noted that the character may be beneficial. That tonal note is
therefore deferred and is not an acoustic acceptance blocker.

This closes the attempt-46 actuator-capacity gate: the deterministic native
organ can render a recognizable whole word over a bounded adjacent physical
range, and candidate A is the human-approved reference trajectory. It does
not credit Guala with speech. Candidate A was supplied by the test-only
posture harness; the resident organism has not yet caused that ordered
trajectory through its own learned topology.
