# GL-SHIP: All-at-once campaign — growth law + six organs + native core — P6 ship record

**Date:** 2026-07-16. **Authorization:** Joe's all-at-once doctrine + explicit "go" (this session). **Branches:** growth-rootcause (7575cc0), wire-six (7300a7d), native-core (6418beb), review fixes + integration on gl/all-at-once-integration-20260716.

## What

1. **Growth law made satisfiable** (the root cause of 2M words → 0 divisions): maintenance bills only pool-created mass, refill funded by novel coherent experience (spectral novelty over 256-deque), pool capped at birth capacity, emergent asymptote ≤ 2× seed + hard cap 256 neurons with loud guard. `GUALA_GROWTH_LAW_LEGACY=1` rollback. Deploy-day path reviewed line-by-line: the live frozen brain starts growing on its first sensory-bearing words.
2. **Six severed organs live simultaneously** (mechanism fixes, never amputation): imagination + reflection called from play/dream/activity-end (imagined origin excluded from certification); daydream ON with lock-wait telemetry + alerts; sensory spike injection ON behind zero-clamp decay + per-band silent skip + min-weight floor; STDP finally consumed (learned weights shape every recall vote); wave feed bounded (64, drop-oldest, counted); organism babble as third autonomous release authority (voiced, self-heard, never certifiable).
3. **Review blockers fixed:** self_heard origin (released speech can never mint its own citation facts — all release kinds); telemetry aggregated to 500-tick cadence (ring stays usable on deploy day); tier-3 two-phase (lock-free precompute, in-lock assembly refuses `votes_not_precomputed`); post-fold spike-bus rewire (daughters visible to the bus).
4. **Native Rust core default-on:** 9 bit-identical kernels (2.8–7.5x, GIL released), multi-stage Docker build, install-before-Guala with loud fallback, `NATIVE_CORE_ENABLED=0` emergency-off.

## Why
Joe's doctrine: the organism was starved by accumulated severances; growth was mathematically bankrupt from birth; Python+GIL cannot carry the fully-live organism (4 threads measured slower than 1).

## Blast radius
loom_model (embryo flux law, neuron STDP reads), engine (autonomy tick telemetry, emission tier 3, self-hear origin, fold-drain rewire, boot native install), wave summary/atlas, runner autonomous loop, Dockerfile. Store/boot/seal paths untouched (Change 1 semantics preserved).

## Rollback
Task-def revert. Env emergency-offs per organ (`DAYDREAM_LOOP_ENABLED`, `SENSORY_SPIKE_INJECTION_ENABLED`, `WAVE_ATLAS_DECAY_ENABLED`, `WAVE_SUMMARY_ENQUEUE_ENABLED`, `NATIVE_CORE_ENABLED`, `GUALA_GROWTH_LAW_LEGACY`).

## Deploy-day watch list (from the adversarial review)
`organism_fold` events within the first sensory-bearing words; `total_neurons` leaving 64; `division_pool` > 0; spike-bus `dropped_count` flat (daughter rewire proof); `wave_proposals_dropped` slope (expected non-zero); `daydream_telemetry_alert` quiet; observability ring readable (no per-tick flood); native-core INSTALLED line at boot; organism worker item_ms falling (Rust).
