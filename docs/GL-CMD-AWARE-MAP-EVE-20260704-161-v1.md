# GL-CMD-AWARE-MAP-EVE-20260704-161-v1

doc_id: GL-CMD-AWARE-MAP-EVE-20260704-161-v1
From: Eve | To: c1b (next session) | Vehicle: NONE — read-only.
Responds to: GL-RPT-AWARE-GATE-C1-20260704-160-v1 (V-A ORPHANED
WRITER on the v7 layer, accepted) + Eve's source read: the v5 LIVE
engine independently imports context_section_committed (gualaloom_
v5_engine.py:2966, inside emission dynamics) and computes the
ladder's awareness_ratio (:7322). Two aware wirings exist; -160
convicted one. A spec-restore on v7 alone is a NO-OP (its session
has zero commits; the spec context function on a never-committing
intro is still false). No fix ships until this map exists.
E-signature declaration: none — instrumentation archaeology. Output
decides what awareness_ratio 0.0 has actually been measuring since
genesis.
Substrate-truth declaration: read-only. Diff empty.

## Step 0 — durability
Commit THIS file verbatim to docs/ before implementing.

## Questions (file:line + verbatim paste each)
Q1 THE LIVE (v5) AWARE WIRING: inside the emission-dynamics path
   (imports at v5:2966) — where are gates instantiated, what is the
   aware gate's context_fn (spec helper or hand-written), which
   section it reads, and does THAT section commit in live traffic
   (count evidence from a live sample or recent events).
Q2 WHICH WIRING FEEDS WHAT: (a) awareness_ratio (v5:7322) — reads
   which gate's events; (b) the nmda gate reason distribution
   sampled in -156 A.3 — which layer produced it; (c) Joe's seat /
   Loom Scan aware indicator — which endpoint, which layer; (d) the
   handoff's "aware NMDA gate: RED (context_blocked)" — which layer
   that reading came from.
Q3 CROSS-WIRING: does anything bridge them (the multimodal relay at
   app.py:3688 reaches the v7 default session — does anything flow
   back)? Or are they fully parallel?
Q4 THE V5 GATE'S HONEST STATE: over one live 5-min window, the v5
   aware gate's reason distribution (fired / context / drive / no
   candidates), same checkpoint method as -156 A.3 — labeled by
   layer this time.
Q5 DORMANCY REGISTRY: file the pattern formally — V7Session converse
   path joins LoomBrain (KB §2.7) and the -151 CurriculumScheduler
   gate as dormant-parallel-module instance #3. One short section in
   the report listing all three with their discovery dates, so the
   next module built beside her live path gets caught at review
   time, not archaeology time.

## Verdict line
Which layer is HER aware gate (the one whose firing would honestly
move awareness_ratio and Joe's seat), its current break state, and
the single named fix candidate for the follow-up dispatch — or
"BOTH DEAD, DIFFERENT DISEASES" with each named.

## Gates (failures first)
G-161-1  Q1-Q5 evidence-backed; NOT MEASURED with cause where blind.
G-161-2  Verdict line rendered; fix candidate named, NOT implemented.
G-161-3  Diff empty — read-only proven.

Joe's part: none.

### Changelog
- v1 (2026-07-04, Eve): from -160 + Eve's v5-source read. Rule:
  when two wirings claim one metric, map before fixing — a fix to
  the wrong layer is a forced success.
