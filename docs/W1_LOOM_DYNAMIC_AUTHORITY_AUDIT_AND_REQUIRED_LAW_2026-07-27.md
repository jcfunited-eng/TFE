# W1 Loom dynamic-authority audit and required state law — 2026-07-27

## Decision

No existing real `LoomNeuron` dynamic can lawfully carry the authenticated
bilateral full field into a learned causal THING under the current hearing
contract.

The new receptor bridge advances exact phase and winding inside each
auditory Krimelack. That state presently terminates there. It does not alter
the neuron's DSF, coupling matrix, membrane, synapses, psi state, or causal
THING mosaic.

The legacy dynamics are not a lawful continuation:

- `LoomNeuron.step()` obtains familiarity from chi `match_score`, maps chi
  and a reduced DSF into a 16-value psi injection, and commits by psi
  probability thresholds;
- `receive_coupling_spike()` flattens one DSF to an array, repeats it into
  psi dimensions, and mutates the familiarity dead zone;
- membrane/STDP learning uses wall-clock timing and multiple explicitly
  heuristic thresholds and amplitudes;
- cluster routing uses chi familiarity, a fixed familiarity threshold, and
  a fixed two-neuron novelty pool.

None may be attached to the authentic bridge and then described as
full-field substrate hearing.

## Architecture honesty gate

1. Requested architecture: actual Loom neurons must develop bounded,
   deterministic bilateral co-resonance from repeated causally grounded
   experience and release only a genuinely heard THING.
2. Current code reality: all 656 exact neuronal trajectories enter real
   H1/H6 auditory Krimelacks, but no lawful downstream state transition
   consumes them.
3. Conflict with requested architecture: yes.
4. Mechanisms not extended: `LoomNeuron.step`, `match_score`, ChiAtlas,
   `_map_inject`, psi settle/commit, flattened `DSF.to_array()` coupling,
   wall-clock STDP, membrane heuristics, resonant-spectral projections,
   static auditory motifs, and contextual THING clamping.
5. Single exact next item: implement the finite recurrent law specified
   below as an isolated auditory state kernel, then run the same
   source-disjoint held-out walk-up before production promotion.
6. Field evaluation: every pressure/phase D/M/R/U/C/P/B lane remains
   independent and authoritative; the exact physical pressure/phase and
   bilateral lanes remain present as well.
7. Reduced approximation: neuronal winding pulses are a receptor response,
   not a replacement for the full field. They discard sub-threshold
   magnitude. Every pulse must therefore retain a receipt link to its exact
   rational lane trajectory, and no winding-only result may claim that the
   original field was evaluated in full.

## Executable audit

The audit advanced one exact rational D_k lane in a real H1 auditory
Krimelack and compared all downstream state before and after.

| State | Result |
|---|---|
| Exact receptor phase/winding | changed |
| Neuron `_last_dsf` | unchanged |
| Coupling matrix | unchanged |
| Membrane potential | unchanged |
| Incoming learned synapses | unchanged |
| Psi state | unchanged |

Source inspection independently confirmed every prohibited dependency
listed above.

Authorities:

- report: `/tmp/w1_loom_dynamic_authority.json`;
- report SHA-256:
  `7c226ae06cf078d1f0353a28bcd7ae687ea59072208e88df7178c30bfd05103c`;
- embedded authority:
  `2dc059b56c7db83a37b9ec8e863b8613a4af03c3af56e2656896f343acf18dd6`;
- audit tool:
  `tools/audit_w1_loom_dynamic_authority.py`;
- tool SHA-256:
  `7bbb0ec5195e1e1d11584cde43c4fe42d47a660ef34836bb268a73518cdd1070`.

## Required upstream state law

Name:
`AuthenticatedBilateralL6PulseClosure`.

This is an auditory neuronal state law after frozen L0-L4 and exact receptor
integration. It is not a word matcher.

Let \(V\) be the 656 authenticated receptor lanes already delivered by the
bridge. At causal receptor frame \(t\), lane \(i\) produces its exact integer
winding increment

\[
z_i(t)\in\mathbb Z.
\]

Every \(z_i(t)\) remains linked to the exact rational lane values, durations,
field receipts, and W1 settlement receipt that caused it.

Let \(G=(V,E)\) be fixed before observing an utterance. Its edges are only:

1. lanes delivered to the same actual Loom neuron;
2. the existing Loom ring-neighbor connections in H1 or H6;
3. homologous H1/H6 receptor connections already authenticated by the
   bilateral bridge.

No result-selected edge, nearest edge, learned acoustic distance, or
post-observation topology is permitted.

For one physical edge \(e=(i,j)\), a causal transmission witness exists at
frame \(t\) exactly when:

\[
z_i(t)\ne0
\quad\land\quad
z_j(t+1)\ne0.
\]

The witness retains the ordered pair
\((z_i(t),z_j(t+1))\), not a score. Parallel windings remain integer pulse
multiplicity; sign remains excitation direction. Causal-frame adjacency is
the delay law, so there is no wall clock, interpolation, timing window, or
new delay constant.

For a causal THING \(T\), each physical edge/direction maintains bounded
source-disjoint witness counts. An edge may become a learned coupling only
after:

1. at least two distinct authenticated causal occurrences witness it, which
   is the definition of recurrence rather than a tuned count; and
2. canonical L6 reports

\[
n_{\mathrm{eff}} <
\left\lceil n_{\mathrm{start}}/e\right\rceil
\]

over all source-disjoint lived occurrences admitted for \(T\).

An occurrence that has the edge supplies one non-null collapsed constraint.
An authenticated quiescent occurrence supplies one quiescent collapsed
constraint. Missing or unverified evidence supplies no collapse. Divergent
causal consequences fission the edge; they never average or compete by
weight.

During a new hearing, every retained THING graph receives the same receptor
pulses. Context and THING identity cannot inject activation. State advances
one physical frame at a time. For target lane \(j\), each learned incoming
edge supplies one constraint: did the prior bounded activation
\(a_i(t)\in\{-1,0,+1\}\) and current exact receptor winding \(z_j(t+1)\)
instantiate that edge's causally learned ordered winding direction?

Canonical L6 evaluates that finite incoming constraint set. The target state
is:

\[
a_j(t+1)=
\begin{cases}
\operatorname{sgn}(z_j(t+1)), & \text{if incoming constraints L6-lock},\\
0, & \text{otherwise.}
\end{cases}
\]

Thus internal activity is bounded to one ternary pulse per lane per frame.
Winding multiplicity remains in the authenticated source witness but is not
summed into an amplifying state. A learned edge transmits only on the next
authenticated frame, and a target cannot fire without its own simultaneous
receptor event. There is no same-frame reverberation, self-sustaining
activity, accumulated numeric energy, or unbounded cascade.

The finite graph advances for the finite number of physical receptor frames;
no convergence tolerance or iteration cutoff is needed. A THING terminal
settles only when canonical L6 locks its bounded set of causally learned
incoming auditory terminal constraints:

- exactly one settled terminal: `resolved`;
- no settled terminal: `unknown`;
- more than one settled terminal: `ambiguous`;
- missing authority or exhausted capacity: `unresolved_resource`.

## Exact invariants

- Frozen L0-L4 bytes and equations do not change.
- Explicit D/M/R/U/C/P/B fields are never packed into a vector, averaged,
  ranked, scored, or replaced by winding.
- Every pulse is exact integer state with a reachable full-field receipt.
- Every transition advances exactly one authenticated causal frame.
- Learning authority is one real causal THING encounter, never transcript
  text, command labels, filenames, chi, psi, or context prediction.
- Query evolution runs all THING graphs in parallel; no context selects a
  candidate graph.
- Shared structure is quiescent; divergent consequences fission.
- State contains bounded counters, locked physical edges, and authority
  receipts, not raw PCM or an unbounded tuple history.
- Capacity exhaustion fails closed; no eviction or partial recognition.
- Cold restore must reproduce identical canonical state bytes and the same
  query settlement.
- Overlapping voices may produce multiple locks only if the upstream
  binaural settlement physically preserves both sources. Otherwise the
  result is unknown, never fabricated separation.

## Minimal scoped change

No L0-L4 kernel change is required or authorized.

The smallest required state-kernel change is one new bounded owner,
`dsf_ai_service/substrate/w1_loom_auditory_co_resonance.py`, consuming
`W1LoomAuditoryDynamicsOccurrence` after the bridge has assembled all lanes
on the common frame grid.

Only two live integrations are needed:

1. the bridge submits each completed occurrence to the owner for query
   evolution;
2. after `_attempt_lived_thing_vocal_learning()` has proved exactly one
   held physical THING, that same occurrence and THING receipt authorize
   plasticity.

Persistence must be a separate authenticated bounded state envelope. The
legacy `LoomNeuron.step`, membrane/STDP, chi/psi path, static motif owner,
and causal THING route remain untouched.

## Promotion boundary

This law is concrete but not yet proven to recognize speech. It must not be
called solved or deployed as recognition until it uniquely resolves every
predeclared held-out grounded word, rejects the unrelated tone and wrong
groundings, survives cold restore byte-identically, and remains
unknown/ambiguous under unproved mixtures.
