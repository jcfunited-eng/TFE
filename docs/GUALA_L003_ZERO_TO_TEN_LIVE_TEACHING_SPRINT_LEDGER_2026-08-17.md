# Guala L-003 zero-through-ten live-teaching sprint ledger

Date: 2026-08-17

Status: **In progress.** No L-003 lesson has been attempted. The zero-card
asset and tutor pressure were packaged in task 1105, but the first read-only
world check exposed an unrelated candidate expansion of the world object
capacity. Production correctly refused to rebuild Guala's retained world.
The correction is to remove that expansion before any teaching.

## Frozen task identity and acceptance

- Active item: `L-003` — teach and analyze the complete 0-10
  number/counting deck, including zero, through distinct physical
  experiences.
- Live-closed predecessor: `L-002`; task 1104 gave all A-Z cards one durable
  attended presentation and retained full physical cognitive-chain evidence
  without claiming learned recognition.
- Production baseline at sprint start: task 1104, commit
  `a2ac01feacd495cbefcb4c53064c083aba0f5b67`, one organism identity
  `1cc4e70a-f2a0-44c5-a111-f4a5bc915cc1`.
- Current live candidate: task 1105, commit
  `bcfba6aa42a10ce98514f840c16b03f9ba159796`, image
  `sha256:a6a30c7c53fd24b1c78e6662da250665107585cfdf2e67497d209a461d8c3d3a`.
  The native organism restored and advances, but the persisted world is
  unavailable because this candidate changed its configured object capacity.
- Acceptance: exactly one attended, durable, full physical presentation for
  each `number-00` through `number-10`, with per-card and aggregate full-DSF,
  neuron-transition, post-quiescence fractal, recurrence, recovery, exhaustion,
  persistence, identity, and resource evidence. Delivery does not by itself
  prove counting, number recognition, a learned word, or speech.

## Architecture honesty gate

1. Requested architecture: an external tutor offers eleven brief physical
   counting experiences; Guala's own native causal attention admits or
   declines each offering, and the same resident organism receives the actual
   light, pressure, contact, chemical, body, DSF, neuronal, recovery, and
   persistence consequences.
2. Current code reality: the live lesson path reads exact signed raster and
   WAV bytes from the 61-experience external manifest and settles them through
   the already-live L-001/L-002 causal path. It does not require adding every
   packaged card raster to Guala's persistent home inventory.
3. Conflict with requested architecture: **yes in task 1105.** The candidate
   broadened `approved_curriculum_physical_surfaces.py` from the existing 36
   A-Z/1-10 surfaces to 61 surfaces and increased
   `EmbodimentWorldAuthority.DEFAULT_MAX_OBJECTS` from 64 to 67. That expansion
   belongs to later word work, is not required by L-003, and prevents the
   retained 64-capacity home from restoring.
4. Mechanisms/files that will not be extended: the persistent world object
   roster or capacity; word-card placement; neuron/DSF physics; Python
   cognition; semantic labels; scripted speech; scores; ML; queues; duplicate
   brains; fallback restore; or whole-brain scanning/copying.
5. Single exact next item: revert the out-of-scope physical-roster/capacity
   expansion while retaining the zero raster, zero tutor pressure, 61-card
   external manifest, exact packaging, and manifest validation; then prove the
   authenticated predecessor world cold-restores before redeployment.
6. DSF scope: every eventual lesson will use the unchanged complete joint
   seven-field L0-L4 delivery. This correction does not evaluate or alter DSF.
7. Lost field structure: none; no field is reduced, flattened, scored, or
   replaced.

## Exact causal and translation path

`number-NN` transport ID -> signed external manifest lookup -> exact raster
SHA/WAV SHA and sample-count verification -> embodied participant approach ->
participant-caused retinal change -> native changed reached/foregone route
intersection -> single-use invitation receipt -> one shared physical lesson
clock -> visual/cochlear/contact/chemical/body occurrences -> unchanged full
joint DSF -> reached-frontier neuron settlement -> post-quiescence fractal and
recurrence evidence -> recovery -> atomic native `CURRENT` publication ->
durable lesson receipt -> bounded public observation.

The transport ID, manifest object ID, filename, and lesson receipt do not enter
the receptor signals and have no semantic authority.

## Acceptance-evidence map

| Required fact | Producer to retained/public boundary |
|---|---|
| Tutor physically invited | persisted-world other-body action -> retinal occurrence -> exact participant action receipt -> native sparse-path intersection -> invitation receipt |
| Exact zero/counting media | signed manifest SHA/sample count -> verified raster/WAV bytes -> shared lesson occurrence |
| Full physical settlement | native episode -> complete seven-field delivery -> physical neuron transition -> post-quiescence fractal/reassembly/recovery totals |
| One durable presentation | single-use invitation receipt -> atomic native successor -> `last_card_lesson_receipt` |
| Identity/resource bounds | raw native `CURRENT`, live observation, ECS one-owner census, state-byte trajectory, zero Python cognition callbacks |

## Preflight recurrence table

| ID | Applicability | Observed evidence/action |
|---|---|---|
| RF-001 | Exact worktree import path | The first focused pytest command omitted `PYTHONPATH`, failed before collection with two import errors, and produced no candidate evidence. The identical command with `/tmp/guala-a0114-resumed` first on `PYTHONPATH` passed 6/6. |
| RF-002 | Process-fixed sensory/world roster | Task 1105 used the intended sensory/world flags; candidate source changed the world capacity itself. |
| RF-004 | Retained/cold-restored branch | Native organism rehearsal passed, but the separate retained world branch was omitted and then failed live. It becomes mandatory before the next build. |
| RF-012 | HTTP/health is not behavior | Deployment and manifest HTTP passed; `/api/v1/world/observation` returned 503, so teaching was stopped. |
| RF-016 | Stable active item | Active item remains L-003; the word-surface expansion is rejected as scope drift. |
| RF-025 | Gateway ambiguity | Any teaching timeout will be reconciled once against the durable receipt and never blindly retried. |
| RF-033 | Exact AWS target | Exact live target is `tfe-web-cluster` / `dsf-ai-service-lb`; the initially guessed cluster name failed read-only and was corrected from the controller. |
| RF-044 | Multiplicative work | No new neuronal/card scan loop is introduced; the 61-surface persistent-world expansion is removed. |
| RF-048 | Persisted world omitted by candidate rehearsal | Added after task 1105: cold-restore the exact authenticated world and execute its read-only observation path before packaging any release that touches world configuration or physical rosters. |

## Failed paths and disposition

- `2026-08-17T00:17:16Z`: task 1105 deployed once after the native organism
  rehearsal passed. It retained the same identity, exact native cold restore,
  417 complete neurons, 196,312 developmental resting neurons, zero Python
  cognition workers/callbacks, and a 76,938,043-byte rehearsal state.
- The public 61-card manifest then verified `number-00` through `number-10`,
  including zero surface SHA
  `8a6fea71e5b46cb4791a125c9954183eba5da3fdc2d1775f0d3606d500b446fa`
  and tutor-pressure SHA
  `de064659338a2ab3f61ff5af64782b8c862fda0cce2c57945037f6268f79360d`.
- The first read-only world request returned 503:
  `ValueError: embodiment state limits changed`. The retained world was not
  replaced, no participant invitation was sent, and no lesson was attempted.
- Root cause: the sprint treated the 60 already-packaged external curriculum
  rasters as 60 already-persistent W1 objects. Source truth shows only the 36
  A-Z/1-10 surfaces belonged to that approved construction roster, while the
  production home independently retains 15 furniture/material objects. The
  zero lesson path verifies and presents media directly from the signed
  external manifest; expanding the world capacity is unnecessary.
- Disposition: restore the prior approved-surface roster and 64-object bound;
  keep only the exact zero media/manifest/package prerequisite. Add the
  candidate-world cold-restore check to the durable recurrence register.
- Exact predecessor proof after the correction: a read-only process in the
  sole task-1105 container applied only the candidate's restored 64 capacity
  and 36-surface construction roster before importing the production world
  owner. It restored the actual persisted `world.glworld` at revision 6888,
  object capacity/count `64/15`, body count `2`, region/portal counts `4/4`,
  16 retained applied receipts, and returned the ordinary public world
  observation schema with HTTP 200. It did not call the native organism
  runtime, persist state, rebuild the world, or create a second brain.

## Resource and ownership invariants

- One ECS service owner, one desired/running task, zero pending tasks.
- One organism identity and one raw native `CURRENT` lineage.
- No Python cognition worker or callback.
- No retained raw lesson media, duplicated 52-78 MB organism image, growing
  curriculum queue, semantic database, or full-neuron scan.
- Serial teaching only; the next card is not offered until the previous card
  has a durable outcome.
