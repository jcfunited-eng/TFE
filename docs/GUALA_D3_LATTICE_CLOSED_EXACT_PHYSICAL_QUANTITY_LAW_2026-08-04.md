# Guala D3 lattice-closed exact physical quantity law

Date: 2026-08-04

Status: ratified representation boundary for isolated falsification. It is not
yet a receptor, membrane runtime, Krimelack transition, resident organism
mount, D3 completion, or production authority.

## Architecture honesty gate

1. **Requested architecture:** exact bounded local neuronal physics whose RAM,
   scalar width, and transition work do not grow merely because the organism
   ages.
2. **Current code reality:** unrestricted rational membrane recurrence cannot
   have fixed residency; denominators can grow on every step. The live resident
   runtime therefore stops truthfully at receptor ingress and claims no
   membrane or Krimelack transition.
3. **Conflict:** yes until each mounted physical producer proves lattice
   closure and a finite physically derived state domain.
4. **Mechanisms not extended:** arbitrary-precision persistent rational state,
   rounding, truncation, retained denominator growth, clamps, voltage wrapping,
   guessed numeric limits, fallback coefficients, or hot-path validation.
5. **Single exact next item:** implement and falsify one isolated membrane
   quantity boundary with no persistence or runtime mount.
6. **DSF evaluation:** none. Complete explicit D1 remains unchanged upstream.
7. **Declared loss:** none. A producer that does not close on the admitted
   lattice remains unavailable instead of being approximated.

## Why unrestricted rational recurrence is impossible here

An exact recurrence can produce a new denominator at every generation. For
example:

```text
V_next = (V + E) / 2
```

can produce denominators `2^t`. A fixed numerator/denominator payload must then
truncate, overflow, or grow. None is acceptable for resident neuronal state.

Therefore Guala may mount only a physical preparation whose complete reachable
transition family is closed on an admitted exact lattice. This is a physical
admission requirement, not a semantic cap.

## Representation

Immutable anatomy carries canonical reduced positive ratios:

```text
charge_quantum = q_num / q_den coulombs
time_quantum   = t_num / t_den seconds
capacitance    = (A * c_m) farads
current_quantum = charge_quantum / time_quantum
```

Resident membrane state carries only:

```text
charge_quanta: signed fixed-width integer
```

The physical charge and potential are derived exactly:

```text
Q = charge_quanta * charge_quantum
V = Q / capacitance
```

An admitted current of `j` current quanta for `n` time quanta transfers exactly
`j*n` charge quanta:

```text
outward_charge_quanta = j * n
charge_next = charge_prior - outward_charge_quanta
```

There is no rational multiplication or reduction in the reached transition.

## Cold anatomy admission

Before a state exists, admission must prove:

1. every ratio is positive, reduced, dimensionally named, and fixed width;
2. `C=A*c_m` is derived by cross-cancellation and fits the representation;
3. the potential factors for both physical domain extremes fit fixed width;
4. current quantum is derived from charge and time rather than independently
   chosen;
5. every allowed producer's `I*dt` is exactly divisible by charge quantum;
6. every intermediate and successor fits the declared integer widths;
7. the charge domain comes from finite ion/reservoir anatomy or another
   ratified physical invariant; and
8. every allowed producer maps its reachable predecessor domain back into that
   domain without a clamp.

If an ohmic or kinetic producer cannot prove these facts for every reachable
state, it is `MissingLatticeClosureLaw`. It is not rounded onto the lattice.

## Event admission and reached transition

An event-local admission may bind one already-authenticated predecessor charge,
one exact aligned physical transfer, and its exact in-domain successor into a
private fixed-width value. That admission is physical preparation; it does no
hashing, persistence, database access, or semantic decision.

The reached transition consumes the admitted value and returns its successor.
It is infallible and contains no clamp, wrap, threshold, check, receipt,
serializer, allocation, loop, callback, or owner.

State remains unchanged when event admission fails.

## Finite domain and recovery

A finite charge domain may not be guessed from a convenient integer width. It
must be derived from physical inventory and anatomy. An unlimited constant
inward current has no finite invariant domain and is unavailable until an
explicit finite ion reservoir, depletion/recovery reaction, or other closed
restorative producer is ratified.

Nutrition, rest, or sleep cannot reset charge or energy. They must cause typed
body/fluid conversions and conservative pass-offs into local recovery paths.

## Falsification order

### One neuron

- zero current is exact quiescence;
- one aligned inward and outward transfer is exact;
- potential is derived from charge and capacitance;
- a non-lattice half-step is refused without mutation;
- constant drive leaving the physical domain is refused, not clamped;
- 100,000 admitted quiescent/restorative transitions retain fixed state size;
  and
- source shape contains no owner, lock, database, hash, receipt, codec,
  serializer, history, score, or semantic table.

### Three neurons

- one explicit sparse contact only;
- all currents use one immutable predecessor generation;
- equal-and-opposite charge transfer conserves total charge;
- endpoint and edge order cannot change the result;
- fan-out cannot create charge; and
- no topology inference or count-based mosaic claim.

### Four neurons

- synchronous opposing fan-in/fan-out;
- a cycle cannot amplify charge;
- depletion/recovery closes the cycle or remains unavailable;
- successors publish atomically;
- byte-exact restart reproduces the next transition; and
- storage stays fixed while work follows only reached contacts.

Only after these proofs and a truthful calibrated receptor family may this
representation enter the resident organism. Krimelack remains a separate
unratified transition.
