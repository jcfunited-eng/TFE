# Guala speech repair Attempt 51 — bounded premotor population

Date: 2026-09-06

Status: **copied-production-body candidate in progress; production task 1429
untouched**

## Authority and invariant

The immutable input is the exact task-1429 body at tick `469814`, SHA-256
`9db4d05cb4d3de3a99aef5129596c4ea82dc5552d7f00eea0c934d7673d79df1`.
The requested chain remains distributed organism activity -> learned physical
motor gesture -> finite respiratory work -> existing lossy tract -> emitted
pressure -> the same organism's cochlear receptors. Canonical L0-L4, full
seven-field DSF delivery, MathLoom, neuron constitution, learned state, body
state, and the existing organ are unchanged.

No word, phoneme, pitch, pose, trajectory, phase cursor, timer, carrier count,
or neuron state is authored by production code. The copied-body guide applies
finite work to already-declared tissue only; it inserts no cognitive contact.

## Repair-attempt history carried forward

1. The first production-growth candidate left the mandatory L10 administrative
   path in authority. Eight copied-body guide cycles produced zero new vocal
   routes. Rejected: real co-moving premotor/body closure was still discarded.
2. Removing that unrelated mandatory gate initially grew `2 -> 13` routes.
   Rejected: generic and learned-anchor paths both claimed the same event and
   duplicate motor sources were admitted. Artifact
   `/tmp/guala-guided-population-candidate1.json`, SHA-256
   `958d25...`.
3. Specific-cause precedence, one founding family, one source per fixed motor,
   and idempotence corrected the overgrowth. One copied-body cycle grew exactly
   `2 -> 8` vocal routes from `2 -> 8` independently powered L11 sources, all
   eight fixed terminals, byte-exact cold. Artifact
   `/tmp/guala-guided-population-candidate2.json`, SHA-256
   `d899f09c6184334d35c392647fe9cc0f8e0c33e00b1735c7db2b3d37a0c40d45`.
4. Unguided continuation rejected that topology as sufficient. Six new motors
   fired in two three-motor groups, while the two mature anchor motors fired
   much later and alone. Four guided cycles did not repair the separation.
   Pressure existed, but no coordinated four-axis phase existed and no accepted
   utterance was produced. The four-cycle artifact SHA-256 is
   `696c88fe7859820cac6910c5c16915378fb7dd34231b3afea0fcc419f9ec6d71b`;
   its WAV SHA-256 is
   `07ca29246e3c20c29034b3a2e422e6165ef7dd34231b3afea0fcc419f9ec6d64`.
5. The one-cycle baseline was extended through the real cochlear ingress. Its
   pressure changed all `32/32` cochlear channels through `35` full-DSF
   deliveries and cold encoded exactly. This proves self-hearing works; it does
   not convert the rejected three-motor posture into speech. Artifact SHA-256
   `15317fdaeaaa2f6d225646cebb7f2bb118b28811883573af40b12e24c4ef4f7e`;
   WAV SHA-256
   `4dd55f9d8b88b227a43be7e7d2dbbb7eaaae73b3e9ef199942671a749c102926`.

## Exact defect exposed by the accepted controls

Candidate 50B proved the existing organ, breath, body motors, and pressure path
work when four causally accounted source-widths reach a four-motor phase. The
lawful growth candidate created real sibling L11 cells with the same two
physical founders, but left those new cells electrically isolated from the
mature learned anchor. They therefore did not constitute one physical premotor
population and did not discharge as one gesture.

## Attempt 51 candidate

When—and only when—an already-learned exact motor terminal and another
same-direction terminal physically co-move and return their proprioception, the
existing bounded motor-synergy law recruits one L11 sibling per previously
unserved fixed terminal. Attempt 51 additionally grows one sparse electrical
contact from the already-learned anchor L11 cell to each recruited sibling.

This is bounded by fixed terminal anatomy, duplicate-proof, and idempotent. It
copies no state and creates no energetic multiplier: mature activity must cross
the ordinary electrical fabric and every cell must supply its own finite work.

## Acceptance and rejection

Accept only if the exact copied body shows:

- exactly eight target routes and exactly one source per motor;
- the four motors of each physical direction discharge as a bounded group;
- finite respiratory work and emitted pressure;
- pressure reaches and changes the copied body's cochlear receptors;
- severing the organism source removes the output;
- byte-exact cold continuation and bounded RAM/encoded size; and
- no surviving process and healthy AWS service/alarms.

Reject on route duplication, cross-family recruitment, unguided silence,
three-of-four or one-of-four posture, sound without respiratory debit,
self-hearing failure, cold mismatch, resource growth, or any production change
before the copied proof completes.

## 51A — lateral contact alone rejected

A copied-body run added one sparse anchor-to-sibling L11 contact for each of the
six recruited sources. It changed neither motor grouping nor sound: the WAV was
byte-identical to the rejected one-cycle baseline,
SHA-256 `4dd55f9d8b88b227a43be7e7d2dbbb7eaaae73b3e9ef199942671a749c102926`.
The three new cells in each direction still fired separately from its one old
anchor. Artifact SHA-256
`3410d7028f92c20bb61b81a694107dbdf306e5493a8e2f3601ba08ed9b485404`.

Cause of rejection: connecting three fresh cells to a mature anchor did not
remove their different electrical histories. This mechanism is not sufficient
and must not ship.

## 51B — complete co-aged cohort passes spatial coordination

The corrected growth law retained each mature learned anchor as teacher and
grew one fresh population source for every physically co-moving terminal,
including the teacher's existing terminal. The copied body therefore retained
all old learned contacts while gaining a complete co-aged four-cell execution
cohort in each antagonist direction.

Measured unguided result after one finite guided cycle:

- all four minimum-direction motors discharged together at clock `1`;
- all four maximum-direction motors discharged together at clocks
  `8, 10, 12, 14, 16, 18, 20, 22, 24`;
- respiratory work was finite (`24` carriers) and pressure was emitted
  (`30,125 / 512,000` nonzero samples, peak `150`);
- emitted pressure changed all `32/32` cochlear channels through `35` full-DSF
  deliveries; and
- the successor cold-encoded byte-exactly at `113,372,670` bytes.

Artifact SHA-256
`ffe0b34c8054a234e16268f652f4098d137c19bbfe17efc9574d3dbee27788b8`;
WAV SHA-256
`de6b15406c815d8016a0aa2952bba38c1639b8b263b4d97ed46ecd058f7d0604`.

This is the first lawful-growth candidate to pass four-axis spatial
coordination in both antagonist directions. It is not yet accepted speech: the
two populations have not repeated as an ordered alternating gesture after
guidance. The next single falsifier is a copied-body, test-only electrical
bridge between the two pre-existing learned phase anchors. It may identify the
missing physical sequence relation but can never ship as authored topology.

## 51C — direct phase-anchor bridge rejected

The one test-only electrical contact between the two pre-existing learned phase
anchors mounted and cold-encoded, but it changed no motor timing: minimum fired
once at clock `1`, maximum fired at clocks `8..24`, and neither population
recurred during the remaining 232 clocks. The bridge therefore does not encode
or elicit ordered gesture physics and was removed from the harness.

Artifact SHA-256
`8264e108f0a49478f7ec23dfbc15c20d19f4fc7cba2b57800f38c5447bf833b1`;
WAV SHA-256
`4444e41fbf886e4213562d9974e102415d69d6b5de1df482e3ac6216a9ae7437`.

The preserved positive result is 51B's complete spatial population. The next
consolidated run restores the already-proven developmental exposure dimension:
50B obtained natural open/closed continuation only after hundreds of physical
occurrences, whereas 51B supplied only two. The range harness now uses the same
native transition path without per-occurrence serialization; this changes no
physics or state and removes only test bookkeeping time.

## 51D — causal-authority correction to 51B

The phrase "lawful-growth candidate" above is withdrawn as a production
claim. The harness recorded `motor_discharges_during_guidance = 0`: 51B proved
that a complete co-aged population can remove the three-versus-one spatial
split, but its anatomy was authored from assisted tissue motion plus a live
premotor frontier, without a simultaneous organism-owned learned L12
discharge. That violates the acceptance matrix and cannot ship.

The production growth function now receives the current native
`MotorUnitRecruitment` evidence directly. Synergy growth requires a positive
discharge by the exact already-learned anchor motor, with nonempty dynamic
learned-work provenance, while its terminal and the assisted terminals
physically co-move. Guidance alone, a reflex discharge, a zero carrier event,
or a premotor frontier without efferent discharge grows nothing.

The focused invariant test passes and repeats idempotently. The exact copied
body is now running a bounded exposure range through this integrated gate. Its
result must simultaneously show nonzero organism motor discharge during the
teaching occurrence, eight terminal coverage, grouped unguided output, finite
breath and pressure, self-hearing, and exact cold continuation.

## 51E — integrated 16-cycle lower bound

The corrected gate was run for 16 complete assisted cycles on the immutable
body. It retained exactly the original `2` vocal routes and grew nothing,
despite `11` total motor recruitments elsewhere in the body. The resulting
unguided window was exact silence, and cold continuation remained byte-exact at
`112,813,632` bytes. Artifact SHA-256
`c05ba2d1a6b0f77013c6bd8ece90b765cecdf1b53c992a02c553e29fe1f5fe81`;
silent WAV SHA-256
`61480b2ca09e9d5b7b359b23869ac199fe1bbb5ae6ca1501a6151f73304f3142`.

The harness's prior counter included every body motor and was therefore too
broad for the speech verdict. It now records only the exact eight vocal
terminals, their discharge carrier counts, and whether each discharge carries
dynamic learned-work provenance. The current range stops as soon as both real
vocal anchors have lawfully recruited all eight terminals, or at the fixed
256-cycle ceiling. It does not infer speech from unrelated body motion.

## 51F — 256-cycle blind-alternation range rejected

The exact copied production body completed all `512` alternating guided
occurrences and `512` unattended continuation clocks. Real learned vocal
motors discharged `40` times, routes grew from `2` to `9`, finite respiratory
work reached `11` carriers, radiated pressure occupied `241,500 / 2,048,000`
samples with peak `423`, that pressure changed all `32/32` cochlear receptors
through `35` full-DSF deliveries, and the `113,433,479`-byte successor
cold-round-tripped exactly. Memory remained bounded and the process exited.

The candidate is rejected as speech and as complete learning: all four
maximum-direction terminals were recruited, but minimum-direction recruitment
remained incomplete and unattended continuation emitted only five isolated
one-motor minimum pulses. The exact causal log explains why. Every one of the
`40` real learned vocal discharges occurred on a harness occurrence labeled
`TowardMaximum`, including all seven discharges by the pre-existing
minimum-direction anchor. Blind alternation phase-locked the tutor to the
opposite antagonist whenever that anchor fired; the strict same-event law
correctly refused to learn from it.

Artifact SHA-256
`bb15ee6b5ec7a906ef37de909cab6a22d9950ed68d567fb6e4771a9e306cd92a`;
WAV SHA-256
`2573a345047319b5f0c8ce29162ab5bb4a210b3e741e4c32b41fa6a2bb9532ac`.

The next range is cumulative, not a new organ candidate. It preserves the
proven motor-strength, breath, tract, pressure, self-hearing, and cold/bounds
chain. The tutor now sustains the missing antagonist direction until its
four-terminal anatomy exists, then sustains the other direction. This is a
test-harness correction only: it reads persistent terminal coverage and never
authors a contact, motor discharge, carrier width, pose, timing, or semantic
target. If the organism's exact learned anchor cannot recruit under sustained
same-direction returned motion, the same-event gate is falsified and the next
production mechanism is a bounded one-transition efference/afference receipt.

## 51G — first sustained-guidance invocation stopped on known duplicate defect

The sustained-direction harness compiled and began from the same immutable
copy, but was stopped deliberately after `205` completed occurrences. The
already-complete 51F artifact showed that the candidate could grow duplicate
same-family sources for three maximum terminals (`9` total routes rather than
the required `8`). Continuing a long run on source already rejected by that
invariant would waste compute and could not authorize production. No result
artifact was written, no state escaped the process, and the process exited on
interrupt.

The focused test had repeated the original mature anchor only. It did not
repeat learning after a newly recruited sibling became the active teacher.
The production bound now recognizes the whole two-founder physical family:
after the first complete cohort exists, any active member of that family sees
the already-owned terminal and grows nothing. The falsifier now repeats both
the original anchor and a recruited sibling and requires identical contact and
lineage counts after each.

## 51H — sustained guidance reaches eight terminals but exposes non-atomic cohort growth

The corrected optimized harness completed in `150.81` seconds from the exact
copied body. Sustained assistance first held minimum motion, then switched to
maximum after the missing minimum terminals mounted. It stopped at occurrence
`246` when all eight terminals were reachable. Nine real learned vocal
discharges occurred during guidance. The 256-clock unguided continuation
produced finite respiratory work (`59` carriers), bounded native pressure
(`265,569 / 1,024,000` nonzero samples, peak `426`), changed all `32/32`
cochlear receptors through `35` full-DSF deliveries, and cold-round-tripped
exactly at `113,371,971` bytes. The optimized executable changed no physics
and reduced the same proof from an hour-class debug run to 2.5 minutes.

The candidate remains rejected. It retained `9` L11-to-L12 routes: the two
mature learned teachers plus four new maximum sources but only three new
minimum sources. Terminal coverage alone hid the missing co-aged replacement
for the mature minimum teacher. Early unattended pulses therefore mixed
antagonist directions rather than producing two clean four-motor phases.

Cause: motor regulations were visited sequentially while the family index was
mutated. Once the first missing minimum terminal created a sibling, the later
mature-teacher terminal saw a non-bootstrap family and skipped its own co-aged
source. Regulation storage order had become motor-population authority.

The repair freezes the motor/family index at the start of the physical event
and makes all terminal decisions against that one predecessor. A transient
family/terminal set prevents two regulation paths in the same event from
creating duplicates. No motor, contact, or state is deleted. Therefore the
bounded accepted topology is ten routes: two preserved mature teachers plus
one complete co-aged eight-source execution population, every source attached
to exactly one motor.

Artifact SHA-256
`c199939981c5d692f6ee865417d90d5de3eaade07da06f2c48cc6689402fc460`;
WAV SHA-256
`43a63a24aa5c43339a1d25b4e90448bb6df9187b65eaa05d56dfdd4967698d4c`.

## 51I — atomic complete population passes; temporal gesture remains absent

The pre-event atomic bound passed the same exact copied-body range in `150.70`
seconds. At occurrence `246` the body retained exactly `10` target routes: the
two unchanged mature learned teachers plus eight co-aged execution sources,
one source per motor and all eight terminals covered. No route duplicated.
Fourteen real learned vocal discharges occurred during teaching.

The unchanged downstream chain also passed: finite respiratory discharge
(`42` carriers), bounded native pressure (`28,973 / 1,024,000` nonzero samples,
peak `46`), pressure reaching and changing all `32/32` cochlear receptors
through `35` full-DSF deliveries, a bounded `113,407,390`-byte successor, exact
cold round trip, and normal process exit.

This candidate is accepted as the spatial population mechanism and rejected
as complete speech. Unguided output began with mixed antagonist groups of
`5`, `5`, `4`, `7`, and `7` motors, followed by incomplete maximum groups.
It did not express two clean four-motor antagonist phases, much less the
closed/open/closed/open temporal gesture already accepted as `Mama-A`.

Artifact SHA-256
`2dc2d2bdcac96432fefb4e912be7a2652bc6618b848e5b84ba5282714db174d4`;
WAV SHA-256
`e23a0e0f15dc0841472810cb978040d69fd571adbceed830418426daf65d66f2`.

The spatial code remains in the cumulative candidate; it is not deployed
alone. The next stage keeps that exact successor in memory, repeatedly presents
the already-approved physical four-phase teaching experience through ordinary
body and acoustic consequences, removes all guidance, and requires recurrent
organism activity to produce the ordered gesture, finite breath, pressure,
self-hearing, cold identity, and flat resources together.

## 51J — complete-loop correction rejects recurrence-to-motor sufficiency

The prior continuation loop called the local electrical settler directly. It
therefore bypassed retained-mosaic admission, reassembly, recurrent-frontier
retention, post-contact growth, and the ordinary motor/body return path. During
guided development it also observed organism motor recruitment without
applying that recruitment to the articulated body or returning the resulting
proprioception and pressure. That harness could test spatial motor population,
but it could not test learned temporal recurrence. Its temporal rejection is
withdrawn and replaced by this complete-path result.

The corrected cumulative harness preserved the exact 51I population repair and
used the ordinary complete transition for every guided and unguided interval.
During teaching it applied every organism motor recruitment to its retained
terminal, settled finite respiratory work through the existing organ, and
carried the resulting body consequence and self-pressure into the following
transition. After eight four-phase physical exposures it encoded and cold-
decoded the organism exactly, discarded all process-local pending inputs, and
ran 256 complete unassisted transitions through formation and recurrence.

Measured result:

- exactly `10` L11-to-L12 routes remained: two mature teachers plus eight
  co-aged sources, one source per motor and all eight terminals covered;
- teaching produced `65` learned vocal motor discharges, `58` finite
  respiratory carriers, and `57,991` nonzero native pressure samples;
- the teaching state cold-round-tripped exactly;
- unassisted cognition produced `14,623` internally reassembled formations and
  `289,456` exact formation-to-formation causal transfers; but
- it produced zero vocal motor discharges, zero respiratory work, and exact
  silence across all `256` unassisted transitions; and
- the final `113,122,867`-byte state cold-round-tripped exactly and the process
  exited normally.

The production candidate remains rejected and task 1429 remains untouched.
This result proves that general recurrence is active after teaching but does
not reach the complete vocal motor population. The next check is not another
organ or amplitude experiment: it inventories exact retained-formation
membership of each of the ten L11 sources and L12 motors before and after the
cold boundary. Any repair must connect retained physical formation activity to
the already-proved motor population without a word, phase cursor, trajectory,
timer, replay buffer, or observer-authored command.

Artifact SHA-256
`99f6291408ab590fa1f0dd3a10c480f9f3b74f05d066ff35e65a41bebb406aa7`;
silent WAV SHA-256
`db101901663b1a8aeea9f739bde31d2c9d5b09519ecb907cded76cd98308c09c`.
