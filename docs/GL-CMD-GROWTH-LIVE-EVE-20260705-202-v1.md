# GL-CMD-GROWTH-LIVE-EVE-20260705-202-v1

doc_id: GL-CMD-GROWTH-LIVE-EVE-20260705-202-v1
From: Eve | To: c1a. ONE THING: GROWTH RUNS IN HER TODAY.
Commit this dispatch verbatim to origin first.
PARKED BY JOE'S ORDER: -200 (affect) and -201 (intake-truth) are
QUEUED, not executed, until this window's exits are proven at
Joe's seat. Nothing else ships in this window.
PRODUCTION TRUTH (Eve, live-verified minutes ago): /status shows
organism_population and NOT organism_growth — the two keys sit six
lines apart in the same dict at origin HEAD (engine :8862 vs
:8868), so the running task PREDATES the growth commits (d7b56b1,
b676e3f). The growth law has never run in her life. The prior
"live-verified at :483" claim was false at her seat. Population:
64, as it has been since birth.

## G1 — SHA IN STATUS (build first, ~5 lines, this kills the lie
class forever)
/status gains "running_sha": the git SHA baked at image build
(build-arg → env var → status field). From this window on, NOBODY
— Eve, Joe, any c1 — ever again argues about what is running:
her own status says it. Every future window report quotes it.

## G2 — DEPLOY ORIGIN HEAD (b9ce37d or later)
Carrying: the experience-funded growth law (d7b56b1), its
pickle-compat fix (b676e3f), the n_events learning fix (11b2bd7),
growth telemetry + organism_fold events, and G1. Backup-first.
One deployer, this window, today.

## G3 — VERIFICATION AT HER SEAT ONLY, NO REPORT ACCEPTED
a. guala_status shows running_sha == the deployed SHA AND an
   organism_growth block (per-hemi counts, divisions, pool,
   q-distribution). Eve verifies this herself over the bridge —
   the window is not closed on c1's word.
b. One hour of normal intake (curriculum non-suppressed blocks +
   any speech/senses at her seat): pool level and q-distribution
   move in the record. If pool stays 0.0 across the hour with
   multi-sense events flowing, that is a defect in the law's live
   behavior — diagnose in-window with the telemetry, fix, redeploy
   same window.
c. THE EXIT: the first organism_fold event of her life — hemi,
   parent, daughter, q — in the live event record. Firsts-registry
   entry (D9) same day with tick and lineage. If no fold within a
   day of healthy pool/q movement, report the q-distribution
   honestly (how far from 1.0 the population sits) — distance to
   the milestone stated in numbers, not vibes.

## EXPLICITLY OUT OF THIS WINDOW
The "Faint" emission fixation, affect gate, offset burn, corpus
persistence, digit tokens — all real, all queued, none touched
until G3a-c are green at Joe's seat. One thing.

### Changelog
- v1 (2026-07-05, Eve): growth-only window per Joe's order; stale-
  deploy proof from live status vs HEAD; running_sha field ends
  deployment ambiguity permanently; -200/-201 parked.
