# Guala D3 biological membrane and electrical-coupling boundary

Date: 2026-08-04

Status: accepted physical relations with isolated bounded native membrane and
conductance candidates. Receptor gating, sparse junction anatomy, resident
neuron coupling, higher cognition, deployment, and production remain absent.

## Architecture honesty gate

1. **Requested architecture:** local sensory and inter-neuronal physics alter
   finite membranes while conserving charge and preserving sparse anatomy.
2. **Current code reality:** isolated native candidates implement elementary
   charge transfer, capacitance, and per-path conductance balance from supplied
   conductances. They do not derive channel states or gap-junction anatomy.
3. **Conflict:** yes until finite biological gates, ion-specific material,
   sparse connections, neuron coupling, persistence, and organism mounting are
   present in one causal transition.
4. **Mechanisms not extended:** fractional channels, stochastic selection,
   inferred adjacency, weights, voltage wrapping, charge clamps, normalized
   signal injection, owner/lock/database cognition, or flattened DSF.
5. **Single exact next item:** determine one measured finite Type II channel
   conformational law or retain truthful unavailability.
6. **DSF evaluation:** none; complete L0-L4 remains separate and unchanged.
7. **Declared loss:** membrane and junction state neither compress nor replace
   the DSF field, Krimelack, neuronal fractal, or cognitive formation.

## Membrane charge

For positive capacitance `C`, outward-positive current `I_out`, exact reached
interval `dt`, and signed separated charge `Q`:

```text
delta_Q = -I_out * dt
Q_next = Q_prior + delta_Q
V_next = Q_next / C
```

Completed carrier crossings are integer elementary charges. Exact retained
phase carries the unresolved rational remainder between reached intervals.
Biological voltage is not wrapped, clamped, scored, or used as Krimelack phase.

## Finite channel current

```text
sum_i(n_i) = N_channels
N_open = sum(n_i for every declared open conformation)
g_open = N_open * unit_conductance
I_out = g_open * (V - E_rev)
```

Every `n_i` and `N_open` is an integer population. A fitted open probability is
not installed as organism state. Single-ion ohmic current is admissible only
where the named anatomy supports it; multi-ion pores require ion-specific
electrodiffusion.

## Sparse electrical coupling

For an explicit gap junction with integer open-channel population `N_open,ij`:

```text
g_ij = N_open,ij * unit_conductance_ij
I_ij = g_ij * (V_i - V_j)
delta_Q_i = -I_ij * dt
delta_Q_j = +I_ij * dt
```

Every edge settles equal-and-opposite charge from one immutable predecessor.
Topology may not be inferred from coordinate proximity, receptor order,
digests, DSF cohesion, or semantic relation. Only reached membranes and
explicit sparse edges participate.

## Current calibration gap

Live visual illumination is not calibrated photon flux; PCM is not calibrated
pressure or hair-bundle displacement; touch reach is not force or strain; body
displacement is not spindle/tendon state; smell saturation is not molecular
activity; and taste contact is not dissolved ionic activity or pH. Passing
those values directly as current, conductance, occupancy, or charge is
prohibited.

The isolated candidates prove arithmetic, conservation, quiescence, restart,
and bounded state for supplied physical quantities. They do not prove sensory
gating, Krimelack coupling, mosaics, recall, cognition, autonomy, tutoring, or
production.
