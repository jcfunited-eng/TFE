# GL-CMD-RECALL-REACH-EVE-20260704-159-v1

doc_id: GL-CMD-RECALL-REACH-EVE-20260704-159-v1
From: Eve | To: c1a | Vehicle: Part A/D offline (replay harness).
Parts B/C ride the -155 pattern (before/after on identical snapshot;
sleep window only if the diff demands one).
Responds to: GL-RPT-RECALL-PROVENANCE-C1-20260704-158-v1 — verdicts
7× NEVER-CANDIDATE (section-routing gap: standalone caption →
listen-only commit at engine:1750-1751; recall queries only
subject/verb/object at :3512), 3× NOT-IN-SNAPSHOT, 0× CANDIDATE-LOST.
Your refusal to pick a prohibited remedy was correct; this CMD is the
Eve ruling you stopped for. (-158-v2 was authored but WITHDRAWN
UNSENT — superseded by your Part A before dispatch; noted here for
the record, no repo cleanup needed.)
E-signature declaration: restores E3/E5 reach for experience-bound
words — P1 joint-attention naming (single word + referent + presence)
is currently unreachable by recall; this is the defect's canonical
weight.
Substrate-truth declaration: the fix extends WHICH sections recall
reads — no role-guessing for standalone words (labeled-taxonomy
class, prohibited), no taught-binding boosts, no new constants, no
scoring change. Variant chosen by pre-declared measurement, not
preference. Plus the -57 §1.2 index-invariant restoration (F-3).

## Step 0 — durability
Commit THIS file verbatim to docs/ before implementing.

## Findings this CMD acts on
F-1 ROUTING GAP (from -158, primary): standalone-caption words
    commit to listen only; _recall_from_atlas queries
    subject/verb/object only. Taught-in-isolation words are
    structurally unreachable. 7/10 probes convicted on this alone.
F-2 EXCLUDED-SELF (Eve, verified at source, secondary): converse
    recall passes exclude_words=input_words_lower (:3514/:3543) —
    the probe word can never be its own return. Once F-1 is fixed,
    bare-caption probes hit THIS wall next. Measurement design must
    account for it (Part D); the exclusion itself is the associator's
    design and is NOT changed here.
F-3 INDEX BYPASS (Eve + c1b flag, standing): Section.receive commits
    via atlas.record directly (:703), bypassing _atlas_record;
    _word_to_chi_index rebuilds only at boot (:6679-6692). Live
    commits are invisible as recall cues until restart.
F-4 NOT-IN-SNAPSHOT ×3 (from -158): three taught bundles never
    persisted to the measured snapshot. Unexplained = untraced
    state-loss class; traced in Part D.
F-5 (record only, no action): same word carries multiple historical
    chi values across her lifetime (parameter drift) — cross-filed to
    C-2 rebuild inputs and the A3 fuel audit.

## Part A — offline A/B (blocking; replay harness, same snapshots)
Two variants, identical everywhere except recall's section surface:
  VARIANT L  — recall queries subject/verb/object + listen
  VARIANT LI — recall queries subject/verb/object + listen + intro
For EACH variant, against the SAME snapshots as the Day-2 numbers:
A.1 The full triple: cold (30-probe set) · experience-bound (10) ·
    quality (rule verbatim, --quality-report).
A.2 Candidate-set size stats per probe (mean/max) vs baseline —
    the crowding cost of the wider surface, in numbers.
A.3 Determinism: two runs per variant, identical outputs.
PICK RULE (declared now): highest experience-bound quality wins,
subject to cold ≥ baseline (2/30) — cold regression disqualifies;
tie → VARIANT L (smaller surface). If BOTH variants leave quality at
0 with bare captions (F-2 predicts they may), the tiebreak metric is
probe-word PRESENCE in the final candidate set (reachability, which
is what F-1 broke) — reachable-but-excluded counts as the fix
working, and Part D's captions carry quality from there.

## Part B — ship the winning variant
-155 discipline: minimal diff; before/after per A.1 filed; no other
recall behavior changed (weighting, count≥2 evidence rule, exclusion
all untouched); new dated GL-RECALL-DAILY line with the post-fix
triple and variant named. Emission-path regression check: one live
converse sample window, mean_utterance and commit-origin counts
non-degraded (or NOT MEASURED with cause).

## Part C — index-invariant restoration (F-3)
Route Section.receive's commit through _atlas_record /
_index_word_at_chi, restoring -57 §1.2's stated invariant. Evidence:
a word committed live is recall-cue-resolvable WITHOUT restart after
the fix, provably not before; boot-time rebuild still yields an
identical index; diff proves scope.

## Part D — records and the three missing bundles
D.1 Trace the 3 NOT-IN-SNAPSHOT probes: bound-then-evicted (show
    decay math to <0.02), window-never-committed (show the gate that
    blocked), or save-path loss (name it — state-loss class, escalate
    to Eve immediately if so). Verbatim evidence per probe.
D.2 Append context note to GL-RECALL-DAILY-20260703 Day 2 (append
    only, entry untouched): "0/8 structurally pinned — routing gap
    (-158 F-1 primary) + excluded-self (-159 F-2 secondary) + bare
    captions; see -158/-159."
D.3 Measurement design rule, on the record: future experience-bound
    sets use multi-word captions (≥3 non-stopword words) via
    --captions. Coherence rule text UNCHANGED.

## Gates (failures first, NOT MEASURED where true)
G-159-1  Part A tables for BOTH variants filed BEFORE any Part B
         commit; pick rule applied verbatim, shown.
G-159-2  Part B before/after triple on identical snapshots; diff
         proves scope; emission regression checked.
G-159-3  Part C live-commit cue-resolution proven before/after;
         index rebuild determinism preserved.
G-159-4  D.1 verdicts evidence-backed per probe; any save-path loss
         escalated same-report.
G-159-5  All prohibitions held: no role-defaulting, no boosts, no
         constants, no scoring/exclusion changes. Diff is the proof.

Joe's part: veto line only — this widens what her recall can reach;
if any part of it smells like shortcutting her cognition path, kill
on sight.

### Changelog
- v1 (2026-07-04, Eve): the ruling -158 stopped for. Variant by
  measurement, not preference; role-defaulting rejected as
  taxonomy-class; F-3 fix carried in; the 3 missing bundles traced;
  measurement design corrected without touching the rule.
