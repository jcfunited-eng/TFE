# C-010 Cross-Context Generalization Sprint

Date: 2026-08-12

## Frozen item and production baseline

- Active delivery-ledger item: **C-010** — prove repeated cross-context
  relations become generalized learned structure without scripted concepts or
  word meanings.
- Immediate predecessor: **C-009**, closed live on task definition
  `dsf-ai-task:997`. This sprint advances the ledger and does not reopen C-009.
- Production organism identity:
  `1cc4e70a-f2a0-44c5-a111-f4a5bc915cc1`.
- Production image:
  `sha256:c64000ab674cfc21d4d69016e9e2505ba87ec507a52c37f66c39400b2855202b`.
- Production census: one desired/running healthy task, zero pending tasks, one
  completed PRIMARY rollout, 4,096 CPU units, and 16,384 MiB.

## Architecture honesty gate

1. **Requested architecture:** distinct lived contexts must be able to reach
   and causally use the same distributed learned relation. The relation is
   resident neuron/contact/formation structure, never a named concept.
2. **Current code reality:** task 997 already emits the same two-formation
   physical relation during unattended world intervals and during the bounded
   C-009 body-cue proof. Formation receipts change when their latest recurrence
   witness changes, while the native relation builder already has the exact
   stable member structures and bonds in hand.
3. **Conflict:** no organism-physics conflict is established. The first missing
   boundary is bounded truthful observation of the stable relation structure,
   not another memory or concept mechanism.
4. **Not extended:** retired reinforcement/relation counters, concept or word
   objects, semantic groups, similarity, hierarchy, archive/history lookup,
   parent/member containers, owners, locks, databases, or receipt-driven
   cognition.
5. **Single exact item:** while the native relation builder already holds the
   reached member sets and active bonds, emit one stable digest of that exact
   physical structure beside its transient formation receipts. Carry it
   unchanged through the existing observer after commit, then prove the same
   relation in an unattended self-caused context, an externally caused
   world/body context, and a later unattended context.
6. **DSF scope:** C-010 does not alter or reduce L0-L4. Every reached neuron
   continues to receive the complete explicit seven-field delivery.
7. **Field loss:** none. The new value is read-only evidence computed after
   physical commitment and never re-enters settlement.

## Exact input, path, transformations, and output

Input A is one ordinary unattended production interval whose own native motor
discharge moves the body and returns through the mounted world and vestibular
path. Input B is one externally caused yaw at the same lawful world position
through `/api/v1/world/move`. Input C is the next ordinary unattended interval
with a self-caused motor/body consequence. Their provenance is distinct even
when their learned physical relation is shared.

Path:

1. The existing world/sensory or vestibular input reaches the native organism.
2. Existing neuron/contact settlement reassembles retained formations and emits
   transient exact relation receipts plus shared lineages or active bonds.
3. The native relation builder sorts only those participating member sets,
   shared lineages, and active bonds and emits one domain-separated SHA-256.
4. The hop commits to its exact successor and the existing observer carries
   that digest beside the transient receipts without rereading retained memory.
6. The production acceptance harness compares the digest across Inputs A, B,
   and C while also requiring distinct causal provenance, actual organism
   transition, cold continuity, one process, and zero Python cognition
   callbacks.

Expected output: the same stable learned physical relation is causally active
in externally caused and self-caused contexts, survives the intervening context
and ordinary persistence, and is later reused without a label, concept object,
history scan, or new formation duplicate.

## Acceptance-evidence map

| Required fact | Producer | Retained authority | Last required observer |
|---|---|---|---|
| Contexts are distinct | unattended intake and external world-move provenance | existing world/body and organism successors | exact intake, world revision, ticks, and motor/manual cause |
| Relation is physical | current formation reassembly plus active sparse bond/shared lineage | current neuron/contact state and retained formation structures | per-hop relation evidence |
| Identity is stable despite changing receipts | exact retained member sets plus exact active bonds | existing retained mosaics and sparse fabric | stable structural-relation SHA-256 |
| Cross-context causal use | same structural relation is active in A, B, and C | same continuing organism | three exact production observations in causal order |
| No scripted concept | source and diff audit | no new cognitive state | no label, concept, score, counter, archive, or group object |
| Durability and bounds | ordinary publication and restart law | one current organism body | identity/tick/state bytes, one process, zero callbacks |

## Translation-boundary review

- Upstream evidence is the native relation's formation receipts, exact shared
  lineages, exact active stable bonds, and the exact committed successor.
- The stable digest is produced from the exact relation frontier that produced
  each hop's receipts; a later hop cannot be substituted for that occurrence.
- Downstream evidence is a bounded relation digest and counts. No raw neuron or
  fractal body crosses the public boundary.
- The digest, receipt, Python wrapper, HTTP surface, and sprint document remain
  outside cognition and cannot influence settlement.
- One transient native observation tuple gains the stable digest. No codec,
  retained state, DSF field, neuron transition, contact transition, or organism
  topology changes.

## Durable preflight recurrence register

| ID | C-010 applicability |
|---|---|
| RF-001 | Exact worktree is first on `PYTHONPATH`; loaded paths are printed before tests. |
| RF-002 | Task-997 environment is resolved before process-fixed imports. |
| RF-003 | Not triggered unless native source changes; no native change is currently authorized. |
| RF-004 | Pristine control and authenticated recurrent body both exercise relation resolution. |
| RF-005 | Decisive: stable relation evidence must reach the public observation before packaging. |
| RF-006 | Existing task-997 resource admission remains unchanged and is checked before restore. |
| RF-007 | Controller interpreter and every command are resolved before deployment timing. |
| RF-008 | Each relation is resolved against its immediate committed hop successor. |
| RF-009 | No aggregate becomes transition authority. |
| RF-010 | Persisted `CURRENT` and in-process successor are compared; fresh restore advances once. |
| RF-011 | Only bounded counts and digests cross the observer boundary. |
| RF-012 | Exact live cross-context actions are required; health and HTTP 200 cannot close C-010. |
| RF-013 | Only changed files are formatted/checked before inherited drift. |
| RF-014 | Mock and compiled interfaces remain unchanged unless the trace disproves that assumption. |
| RF-015 | Not triggered unless a native wheel is built. |
| RF-016 | C-010 is active, C-009 remains closed on task 997, and the complete diff must remain C-010-only. |
| RF-017 | Every relation producer, wrapper, accumulator, test fixture, and public consumer is censused before the first test. |
| RF-018 | Decisive: an earlier-hop relation must survive a later quiet hop in the complete intake evidence. |
| RF-019 | Candidate rehearsal assertions map only to C-010 before immutable build. |
| RF-020 | Existing formations and sparse bond must physically participate; mounted presence is insufficient. |
| RF-021 | Every assertion stays bound to the exact successor of its declared context. |
| RF-022 | Not triggered unless a retained value domain changes; this sprint currently changes no retained state. |
| RF-023 | Triggered during target census: the historical null-name JMESPath failure recurred. The durable register now requires unfiltered AWS retrieval followed by validated-string `jq` filtering; the corrected command passed. |
| RF-024 | Triggered before packaging-test collection: historical test filenames were guessed rather than resolved. The durable register now requires `rg --files` proof for every pytest target before invocation. |
| RF-025 | Triggered during live context B: the state-changing request outlived the public gateway and returned 504 after committing. The successor was checked before any retry; no duplicate experience was sent. |

## Failed and rejected paths

- A formation receipt is not stable learned-relation identity because its
  bounded latest recurrence witness lawfully changes. Receipt equality is
  rejected as C-010 proof.
- A first local implementation resolved relation receipts by enumerating every
  retained formation after each active hop. Pre-deployment review rejected and
  removed it because its work would grow with total learned memory. The
  candidate instead hashes only the relation participants already reached by
  the native builder.
- The inert historical reinforcement and mosaic-of-mosaics counters cannot
  prove use, strengthening, transfer, or generalization.
- The first task-environment query repeated the known null-name JMESPath
  failure. It changed no state. RF-023 now prevents that command shape.
- The first import-path command loaded the intended worktree but its optional
  census print treated `_authored_growth_dna()` as an object rather than its
  actual tuple return. The shell stopped before test collection and changed no
  state. C-010 tests do not depend on that unrelated accessor.
- An intentionally broader inherited-fixture run reported the already-known
  RF-014 drift: public-observation mocks omit the production-authorized
  vestibular source-site observer, unattended fixtures name a retired cadence
  constant, and cold-probe mocks supply placeholder bytes that are not a real
  organism envelope. The C-010 relation suite passed 4/4 in that same run; the
  inherited failures do not exercise or contradict the changed boundary and
  are not being patched inside this sprint.
- A packaging-boundary command guessed historical test filenames and pytest
  stopped before collection. No candidate test ran or changed state. RF-024
  now requires resolving every exact target from the active worktree first.
- The live external-yaw request returned HTTP 504 after the bounded native
  intake outlived the public gateway. It was not repeated. A read-only
  successor check proved the requested `+64` millidegree yaw committed as
  world revision `1180`, and the following self-caused `+52` millidegree yaw
  advanced that exact pose to revision `1181`. RF-025 now requires checking
  the committed successor before any retry.

## Completion boundary

C-010 closes only after one immutable candidate is live and direct production
evidence shows one stable structural relation across the three declared
contexts, with the same organism identity, bounded state/work, exact ordinary
persistence, one process, and zero Python cognition callbacks. Local tests or
a digest without causal context do not close it.

## Pre-build evidence

- The native relation proof changes one recurrence witness so both formation
  receipts change, while the new stable structural receipt remains identical.
- Complete native verification passes `407` tests with zero failures and `13`
  explicitly retired fixtures ignored.
- The exact affected Python/native translation, rehearsal-controller, and
  deployment boundary passes `72` tests with zero failures.
- Candidate wheel SHA-256:
  `9e863ec3cab57ca557b34970b94a50e7ab47f496714a6e467fbc048c1f30013b`.
- The rejected whole-memory observer is absent from the candidate. Runtime
  work is proportional only to the formations and bonds in the already-reached
  relation; no additional native call, retained field, codec byte, database,
  owner, lock, counter, label, or Python cognition callback is introduced.

## Deployment and live-production closure

- One production deployment attempt ran from `2026-08-12T18:10:31Z` through
  `2026-08-12T18:28:31Z` and completed in 18 minutes.
- Live task definition: `dsf-ai-task:998`.
- Live commit: `13d9771e14f675f9f6d7d348b13a18c7ccdbff12`.
- Live image:
  `sha256:51d3999679b7e019a76ec75b0335ece71f44d3c3ba8c51b1f88793fcc92c8eaa`.
- ECS reports one desired/running healthy task, zero pending tasks, zero failed
  tasks, and one completed PRIMARY rollout.
- Candidate cold rehearsal restored identity
  `1cc4e70a-f2a0-44c5-a111-f4a5bc915cc1` byte-exact at tick `57277`, with
  state bytes `43385280`, zero Python cognition callbacks, and stable relation
  SHA-256
  `f3fb4081cec9181e576a02549c0b1dbb97be1c14bfa60dfd62aa161dcff91c8a`.
- **Context A:** ordinary unattended intake at organism tick `57349` moved her
  body by a self-caused `+85` millidegree yaw at world revision `1179`. The
  same stable relation SHA-256 was active.
- **Context B:** one externally caused request at the same position requested
  exactly `+64` millidegrees, from heading `81401` to `81465`. It committed as
  world revision `1180`; no retry was sent after the client-side 504. The next
  native relation occurrence followed A at tick `57350` with the same stable
  relation SHA-256.
- **Context C:** later ordinary unattended intake advanced through tick `57358`
  and moved her body by a self-caused `+52` millidegree yaw, from the exact B
  heading `81465` to `81517`, at world revision `1181`. Relation occurrences
  through ticks `57352`, `57354`, and `57356` retained the same stable relation
  SHA-256.
- A and C have `continuous-environment:<occurrence>` provenance; B is the only
  externally submitted world move and truthfully reports `chose_to_go=false`.
  The organism identity remained unchanged. State grew only 224 bytes from
  the first A observation (`43385280`) to the first post-B/C observation
  (`43385504`), and live observation reports zero Python cognition callbacks.
- C-010 is therefore **closed in live production**. This proves reuse of one
  distributed learned physical relation across distinct causal contexts. It
  does not claim a named concept, word meaning, language, or complete Guala.
