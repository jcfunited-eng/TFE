# Guala Layer-13 Articulatory Specialization Sprint — 2026-08-11

## Architecture honesty gate

1. Requested architecture: specialize one persistent layer-13 articulatory
   neuron only from genuine ordered vocal and body physics.
2. Current production reality: task 976 has reached layers 0–12, including one
   reusable layer-12 motor route, but no reached layer-13 neuron and no native
   articulation transition.
3. Conflict: yes; the last declared base specialization is absent.
4. Do not extend retired Python vocal owners or receipts, scripted speech,
   semantic labels, action selectors, databases, locks, dense reserve scans,
   or Python cognition.
5. Single exact item: mount or reuse one layer-13 neuron only when one admitted
   interval physically changes an acoustic receptor, body-regulation,
   delayed-ordering, and pre-existing motor participant together.
6. Participating neurons continue to receive unchanged full joint seven-field
   L0–L4 delivery.
7. No DSF field structure is reduced or lost.

The recovered and persisted cognitive-chain authority at
`docs/GUALA_COGNITIVE_CHAIN_SPECIFICATION_AND_DESIGN_2026-08-08.md` governs
this sprint together with the definitive-neuron model,
developmental-connectivity law, whole-brain segment map, and current executable
source. Layer 13 remains developmental anatomy; no semantic language unit,
thought, utterance, or meaning is mounted by this change.

## Change-impact ledger

| Required fact | Exact sprint authority |
|---|---|
| Input | The stable lineages whose complete physical neuron states changed during one admitted interval. The candidate convergence requires concurrent layer 1 acoustic, layer 8 body-regulation, layer 11 ordering, and already-reached layer 12 motor material. |
| Function/file path | `ResidentCognitiveFormationState::prepare_occurrences` in `native/guala_core/src/resident_cognitive_formation.rs` supplies the changed-lineage frontier after one-hop contact settlement and existing layer 8/11/12 development. One new local helper in the same file may claim layer 13 from that frontier. Generic cohort and sparse-contact codecs already persist reached cells and contacts. |
| State transformation | If and only if all four physical participant classes changed, claim one quiescent layer-13 cell and retain sparse contacts to those exact participants. The new cell is mounted after settlement and cannot emit sound in the interval that creates it. Reaching the same participant set later reuses it. |
| Expected output | First lawful convergence consumes exactly one resting cell, adds exactly one reached layer-13 neuron, and adds only participant-to-layer-13 sparse contacts. Missing any required participant creates nothing. Repetition creates no neuron or contact. |
| Explicit non-claim | Developmental articulatory specialization is not articulation, speech, a word, thought, choice, emitted pressure, or autonomy. Those require later breath/larynx/mouth/face actuation and self-heard consequence. |
| Production acceptance | One real admitted vocal/body occurrence reaches the living task-976 successor, grows layer 13 exactly once, cold-restores exactly, preserves identity and prior state, reports zero Python cognition callbacks, and repeats without growth. |
| Translation-boundary review | The changed-lineage frontier is produced directly while complete native neuron predecessor/successor states settle; no Python classification supplies it. Layer resolution comes from persisted `ReachedNeuronMount` anatomy. S-018 reuses the transient mounted-anatomy view already built by the layer-12 function, then touches only changed lineages, existing layer-13 candidates, and their sparse contacts. It adds no Boolean/counter authority, serialized type, field, codec, receipt, owner, lock, database object, Python/native call, dense reserve operation, or new full-field evaluation. Generic reached-cohort and sparse-contact persistence already carries the successor. Energy/material/identity are unchanged except for the existing exact resting-cell claim and existing authored contact genesis law. |
| Observed evidence | Focused native proof passes: without the motor participant, no cell or contact is created; with one acoustic, one layer-8 regulation, one layer-11 ordering, and one pre-existing layer-12 motor participant, exactly one layer-13 cell and four sparse contacts are retained; reordered repetition adds no cell or contact. The existing lived optical+binaural+vestibular integration path also reaches one layer-13 cell and survives exact encode/decode cold restoration. Full native verification passes 569 tests with zero failures and 11 explicitly retired tests ignored. Packaging, rehearsal, deployment, and live proof remain unproven. |
| Rejected or falsified paths | The retired Python vocal-owner/receipt graph is not an input to neuronal development. A layer number, card ID, transcript, tutor label, or audio-file identity cannot trigger specialization. Repository-wide `cargo fmt --check` is currently blocked by inherited formatting differences across unrelated native files; S-018 does not rewrite those files. |
