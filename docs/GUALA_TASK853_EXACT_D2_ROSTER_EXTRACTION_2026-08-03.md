# Guala task-853 exact D2 roster extraction

Date: 2026-08-03

Status: authenticated read-only production extraction; no organism transition,
deployment, topology admission, or production write occurred.

## Architecture honesty gate

1. **Requested architecture:** preserve the actual D2 neuronal predecessor and
   use only its causally retained facts when admitting future physical anatomy.
2. **Current code reality:** task 853 persists a `GLMFAB03` legacy materialized
   container whose nested mounted state is `GLJNFT02`. The nested body retains
   exact neuron lineages and four-field receptor-port keys but no authenticated
   membrane, channel, fluid, body, control-volume, or boundary anatomy.
3. **Conflict with requested architecture:** yes. The lineage/port predecessor
   is authentic, but the physical anatomy required by D3 is absent.
4. **Mechanisms not extended:** legacy familiarity classes, pseudo-mosaics,
   `GLJNFT03` enriched receptor fields, inferred coordinates, test anatomy,
   Python parsing, owner records, or migration as inspection.
5. **Single exact next item:** review and ratify the typed initial virtual
   biology that may be admitted as a one-way physical-topology successor while
   retaining this exact D2 predecessor as unresolved source evidence.
6. **DSF scope:** the canonical Rust restore rebuilt both retained complete
   L0-L4 fields and validated their exact field/fractal authorities before
   projecting the roster.
7. **Field loss:** none in validation. The extraction output is deliberately a
   narrow observation of lineage and legacy port identity; it is not a reduced
   DSF decision authority.

## Production custody

The source was the immutable current generation served by ECS task definition
`dsf-ai-task:853`:

- organism identity: `1cc4e70a-f2a0-44c5-a111-f4a5bc915cc1`;
- recovery generation: `d44d203f-5d1e-4f9e-869c-795751fc7597`;
- recovery tick: `23723846`;
- persisted body bytes: `4148843`;
- persisted body SHA-256:
  `b1f538e25d0bf59584266172ccb473b2b2db6ad7ddf1fc1f7ffa542bd2cc7e14`;
- outer schema: `GLMFAB03`, version 3;
- nested mounted schema: `GLJNFT02`, version 2.

The independently verified live persistence record carried the same byte count
and state SHA-256. The transferred bytes were rehashed locally before decoding.
The temporary container file and the exact temporary versioned S3 transfer
object were deleted after verification; the S3 prefix retained neither an
object version nor a delete marker.

## Generation counters

The outer `GLMFAB03` generation decoded as `13`. The inner `GLJNFT02` generation
decoded as `2`.

These are counters owned by two nested historical serialization mechanisms.
They are not neuron counts, organism identity, cognitive age, experience count,
or proof of learning. Their difference proves that they cannot be silently
equated. It is possible that the outer legacy container advanced without an
inner mounted-neuron transition, but this extraction does not establish the
historical reason. Future topology admission must retain both observed values
without rewriting either to create cosmetic continuity.

## Exact retained roster

The canonical decoder returned 96 unique lineages, all with the fixed lineage
prefix `GLNLINE1` and sequential big-endian ordinals `1` through `96`.

### Sight

- count: `64`;
- sense code: `0`;
- topology indexes: `0` through `63`;
- sensor ID: `browser-camera-retina-8x8`;
- substreams: row-major `receptor-r0-c0` through `receptor-r7-c7`;
- lineage ordinals: `1` through `64`;
- exact lineage byte range:
  `474c4e4c494e45310000000000000001` through
  `474c4e4c494e45310000000000000040`.

For sight topology index `i`, row is integer `i / 8`, column is `i mod 8`, and
the retained lineage ordinal is `i + 1`. This statement reports the extracted
canonical order; it does not create retinal geometry or membrane anatomy.

### Sound

- count: `32`;
- sense code: `1`;
- topology indexes: `0` through `31`;
- sensor ID: `microphone-gammatone-cochlear-field`;
- substreams: `erb_00_pressure`, `erb_00_phase_advance`, continuing by ERB band
  through `erb_15_pressure`, `erb_15_phase_advance`;
- lineage ordinals: `65` through `96`;
- exact lineage byte range:
  `474c4e4c494e45310000000000000041` through
  `474c4e4c494e45310000000000000060`.

For sound topology index `i`, ERB band is integer `i / 2`; an even index is the
retained pressure substream and an odd index is the retained phase-advance
substream. The lineage ordinal is `i + 65`. This reports retained ordering only;
it does not establish cochlear mechanics, ear-fold directionality, hair-cell
anatomy, or membrane physics.

## Explicit absences

The body contains no authenticated production D3 values for:

- enriched receptor coordinates, physical quantity, unit, calibration,
  relevance, or input-map binding;
- membrane identity, capacitance, charge bounds, or gap contacts;
- channels, reaction sites, synapses, fluid lanes, or control volumes;
- body, interoceptive, proprioceptive, vestibular, touch, taste, or smell
  receptor anatomy;
- motor, articulatory, facial, eye, or mouth anatomy;
- neocortical, hippocampal, prefrontal, amygdalar, sleep, dream, emotion, or
  fluid-brain physical configurations; or
- a causal anatomical compartment graph.

Those absences must remain unresolved until reviewed virtual-biological laws and
initial values are admitted. Neither the sight/sound labels nor the sequential
lineages can be promoted into those missing structures.

## Native extraction proof

The added read-only native seam uses this single path:

```text
exact GLMFAB03 content SHA-256
    -> existing bounded outer parser and legacy-arena skip
    -> borrowed GLJNFT02 slice
    -> existing canonical decode and full physics validation exactly once
    -> exact lineage plus four retained port fields
```

It refuses a changed content hash, outer `GLMFAB04`, inner `GLJNFT03`, malformed
or trailing bytes, and insufficient storage or working-memory admission. It
does not encode, migrate, publish, or infer anatomy.

The exact task-853 replay passed in a debug build after `191.57` seconds. The
separate installed native forward test measured approximately `11.5` seconds
and `348640` KiB peak RSS. The logical working-memory admission argument is not
an operating-system RSS ceiling; future migration admission must use measured
platform headroom.

This extraction is a one-time historical transition dependency. It must not run
on normal boot or every causal event.
