# Guala speech repair Attempt 53 — learned cue to vocal ordering

Date: 2026-09-07 UTC
Status: active analysis; no candidate edit, build, deployment, or speech claim
Worktree: `/tmp/guala-speech-existing-organ`
Branch: `sol/speech-existing-organ`
Starting HEAD: `735489406577f90edf6c2fb7fb1889f87eae8107`

## One active defect

After teaching and cold restore, a lawful retained partial cue reaches Guala's
existing cognitive formations, but no layer-11 vocal ordering source supplies
learned work to an existing layer-12 vocal motor. The downstream typed motor
tissue, breath recruitment, vocal pressure, and cochlear return have separate
component proofs only. Guala cannot presently speak.

## Architecture honesty gate

1. Requested architecture: one preserved deterministic organism in which a
   learned sensory cue reaches resident ordering activity, existing vocal motor
   tissue, body-owned pressure, and self-hearing through ordinary causal physics.
2. Current code reality: learned L11-to-L12 work is offered only when the same
   L11 motor source sends a real settled transfer into its founding L7 route.
   Attempt 52 added an L11-to-L11 current-experience-to-family-hub contact, but
   the complete copied-body run still produced no post-cue learned work.
3. Conflict with requested architecture: yes. The retained cue/cognition path
   and the already-developed vocal motor path remain causally disconnected in
   the lived post-restore condition.
4. Mechanisms that will not be extended: the retired production shell; direct
   formation-to-motor contacts; observer or Python cognition; scripted words,
   phonemes, trajectories, waveforms, sequence objects, or cursors; Chi identity;
   ML; score or lookup-table control; artificial population multipliers; fixed
   carrier conversion; L0-L4; or any compatibility proxy for the DSF field.
5. Single exact next item: trace and correct the one omitted physical
   learned-cue-to-L11-ordering transfer, then run one decisive copied-current-
   production-body speech gate.
6. Field authority: the unchanged full joint seven-field DSF delivery remains
   authoritative; no reduced approximation is being evaluated.
7. Lost field structure: none.

## Preserved production baseline

- Live task at attempt start: `dsf-ai-task:1434`.
- Immutable image digest:
  `sha256:fb194f7cd2fbee77e492cd1ed2aa01ef4ea6342b4a955138b75b976c946dce95`.
- Preserved organism identity:
  `1cc4e70a-f2a0-44c5-a111-f4a5bc915cc1`.
- UI-001 is live and truthful; it does not prove speech or cognition.
- Attempt 52 is the immediate speech predecessor and remains failed.

## Successes carried forward — do not rediscover

- S-001: Attempt 47I replaced fixed one-carrier conversion with exact,
  state-varying learned source work when an L11-to-L12 route actually fires.
- S-002: Attempt 47K proved a 32-ms persisted tissue activation changes vocal
  posture; duration is not the missing speech boundary.
- S-003: Attempt 50 proved downstream recruitment capacity: lawful scale one
  recruited eight vocal antagonists; the prohibited scale-four control also
  recruited breath and pressure. The multiplier may not ship.
- S-004: Attempt 51 grew a bounded ten-source premotor population from real
  learned discharge and returned consequence. During teaching it drove eight
  terminals, finite breath, and 57,991 nonzero pressure samples.
- S-005: Attempt 51's cold-restored partial cue continued cognitive formation
  activity. Therefore cue ingress and formation continuation are not the
  unresolved boundary.
- S-006: Attempt 52A found that synergy growth had discarded the current lived
  experience ordering route. This withdrew the false need for a new cognitive
  layer or direct formation-to-motor shortcut.
- S-007: Attempt 52B lawfully retained that current-experience route and grew
  one bounded L11-to-L11 link to a unique active-family hub. Its focused
  anatomy/idempotence invariant passed; it added no direct motor authority.
- S-008: The existing body-owned vocal organ, exact pressure custody, and
  cochlear return remain proven components when causally recruited.
- S-009: P-005's lean five-route production shell and UI-001's five-route
  embodied access surface are live without the retired shell.

## Failures carried forward — prohibited repeats

- F-001: This attempt initially treated the truth skill's relative
  `scripts/require-guala-root.sh` reference as a worktree-relative path. The
  helper is not in the worktree; its permanent development-tool location is
  `/root/.codex/skills/guala-project-truth/scripts/require-guala-root.sh`.
  The failed lookup made no source, state, or production change. The exact
  helper was subsequently located before retry, invoked once with
  `/tmp/guala-speech-existing-organ`, and returned that exact root plus HEAD
  `735489406577f90edf6c2fb7fb1889f87eae8107` and the expected candidate
  modifications. Permanent preflight: resolve and run this helper once when
  establishing the active worktree. It is development/deployment protection,
  not organism code; it must not enter or repeatedly execute in the production
  shell, runtime, cognition, or body.
- F-002: Attempt 52's first exact-name invocation selected zero tests. Verify
  the test name by listing it before invoking it.
- F-003: A prior fixture reversed learning chronology. Teaching must precede
  the retained partial cue.
- F-004: A broad synergy predicate grew duplicate contacts. Growth authority
  must remain exact and idempotent.
- F-005: An active sibling became a second family attachment point. Family
  identity must not be inferred from whichever sibling happens to fire.
- F-006: In-event mutated topology made hub selection order-dependent. Growth
  decisions must use one immutable pre-growth boundary.
- F-007: A full-loop run accidentally used the slow debug executable. Verify
  the release binary path before the decisive run.
- F-008: `/usr/bin/time` is absent. Do not wrap the run with that path.
- F-009: Attempt 52B's L11-to-L11 anatomical join was present but insufficient:
  256 positive continuation clocks produced zero learned vocal pulses, zero
  respiratory carriers, and zero new pressure. Do not repeat that candidate.
- F-010: Silence, reversed, discontinuous, and severed controls were all
  silent. That is not positive speech evidence and need not be rerun before a
  positive causal chain exists.
- F-011: After correctly listing the fully qualified Rust test name, this
  attempt mistakenly invoked its short name with `--exact`, repeating F-002.
  The redundant command was stopped during compilation and changed no source,
  organism state, or production. Permanent command gate: an exact test command
  must paste the fully qualified name from the immediately preceding `--list`
  output, use `--lib`, and is successful only when its terminal result says
  `1 passed; 0 failed`; exit status alone is not acceptance.
- F-012: The first candidate patch used a comment anchor not present in the
  source and was refused by `apply_patch`. No file changed. The exact local
  source was reread before the corrected patch; do not reuse remembered patch
  context.
- F-013: Candidate 53A widened the already-conserved learned-work source from
  outgoing L11-to-L7 transfers to outgoing L11-to-L7-or-L11 transfers. It
  added no state, contact, neuron, field, timer, or authored output. The exact
  focused invariant passed `1 passed; 0 failed; 608 filtered out`, but the
  complete copied-body chain rejected it. After cold cue, 256 clocks produced
  zero learned vocal pulses and zero respiratory carriers. Twenty-seven tiny
  nonzero pressure samples at peak 5 were noncausal residue: silence, reversed,
  and discontinuous controls each produced 24, while the learned-contact-
  severed control produced 27. Candidate 53A was reverted immediately. Do not
  retry lateral-source-layer widening.

## Attempt 53A artifact record

- Candidate source SHA-256:
  `bc4a848786d463767e8deaef9be4451ea63b3345833e674820c2127ac861ec20`.
- Candidate diff SHA-256:
  `5ea51f07092394e95675f29b21595282159e399eb8fc6cd1bec1002a33b811a4`.
- Exact release test binary SHA-256:
  `76c8050854143950e6d18eaf8775b225c7177365f27ff61a2b66736aa56f344b`.
- Complete run time: 294.22 seconds, one exact test passed as a harness.
- Report: `/tmp/guala-integrated-full-loop-v8.json`, SHA-256
  `7314a7667f9316c80c39c46ba9ead784bdf88e9e24c5d8446dd2dbff21aa23da`.
- Teaching state: `/tmp/guala-integrated-full-loop-v8-teaching.cognitive`,
  SHA-256
  `3a09357644fb1e087f43e411ee294d8e3384d5a0f6077922de31682bb99ef0d9`.
- WAV: `/tmp/guala-integrated-full-loop-v8.wav`, SHA-256
  `ba15c0ece7899808c66818838bf0f0cdb3e20f0ea84adcf838a353af3ceec58c`.
- Preserved successes: exactly ten bounded routes and ten sources; all eight
  target terminals; 42 guided learned discharges; 40
  guided respiratory carriers; 57,322 guided nonzero pressure samples; exact
  cold round trip; exact cochlear return when the noncausal residue was
  explicitly admitted for observation. None is post-cue speech.
- Pre-run AWS: account `418384447921`; task1434 desired/running/pending
  `1/1/0`; primary completed; task and container healthy; all five Guala
  alarms OK; public main page HTTP 200. Production was not changed.
- Harness correction: `https://dsf-ai.com/health` truthfully returned 404
  because health is an origin/container route, not the public CloudFront
  surface. ECS task/container health plus the known public page replaced that
  guessed check. Do not call the public `/health` path again.

## Static trained-state truth after 53A

- S-010: The already-emitted 53A teaching state was placed into the exact
  original GLORUN envelope without changing its cognitive bytes. The existing
  measurement-only probe cold-decoded it; no teaching or candidate transition
  was repeated.
- S-011: The trained state contains exactly ten vocal motor-ordering sources,
  15 L11-to-L12 contacts, and eight L11-to-L11 contacts. Every one of the ten
  motor-ordering sources has zero retained-mosaic memberships.
- S-012: All eight L11-to-L11 contacts are only the two motor-family stars:
  source suffix `05f6` to four motor siblings and source suffix `06ba` to four
  motor siblings. There is no non-route/current-experience ordering endpoint
  and no contact from retained memory into either family.
- F-014: The first read-only `jq` reduction used the wrong scope while indexing
  an edge and exited with `Cannot index array with string left_lineage`. No
  artifact or state changed. Binding the edge before indexing produced the
  exact static result above.
- F-015: Attempt 52's claim that its new current-experience lateral anatomy was
  present in the complete copied-body state is false. Its focused artificial
  fixture grew such a contact, but the real teaching chronology grew none.
  Candidate 52B was a real-body no-op at its claimed boundary. Do not extend
  its family-hub selection or cite its unit test as lived anatomy.
- F-016: Candidate 53B's new exact memory-to-ordering invariant passed, but the
  immediately following motor-family regression test failed at the single
  assertion requiring Attempt 52's obsolete current-experience-to-family
  contact. The surrounding bounded motor-family assertions had reached their
  expected counts before that assertion. Blame confirms the failed expectation
  was added with Attempt 52, not with the original bounded family test. The
  artificial current-experience fixture and its two obsolete assertions were
  removed with the already-rejected production mechanism; the valid bounded
  family behavior remains under the same exact test.
- F-017: The AWS preflight first named nonexistent cluster
  `dsf-ai-cluster` and received `ClusterNotFoundException`. No AWS state
  changed. The account's returned cluster list named the exact cluster
  `tfe-web-cluster`; the service/task check was then run there. Do not guess
  the cluster name from the service name again.
- F-018: The first combined Candidate-53C patch used an exact source anchor for
  the work-direction change but a stale multi-hunk anchor for the already-read
  Attempt-52 block. `apply_patch` refused the entire combined edit; no file
  changed. The two exact local regions were reread and applied separately.
- F-019: The first combined Candidate-53D flow/test patch incorrectly expected
  `&fabric` where the exact test call contains `&mut fabric`; `apply_patch`
  refused the whole edit, so no file changed. Production and test regions were
  reread and applied as separate exact hunks. Do not compose multi-region
  patches from remembered call syntax.

## Candidate 53B causal impact map

The smallest direct architecture already exists: retained memories own a
layer-9 recurrent cell; motor actions own layer-11 ordering sources; learned
motor contacts join those sources to typed layer-12 motors. Candidate 53B
removes the failed Attempt-52 current-experience-to-family attachment and,
only when one exact retained L7/L9 memory route and one exact body-owned learned
motor discharge co-occur, grows one ordinary sparse contact from that retained
L9 cell to the exact L11 ordering source that caused the discharge. The route's
founding L7 must equal the active memory association and its learned bond must
name the discharged motor. This is retained memory -> ordering -> motor, never
formation -> motor.

- Production source touched: only
  `native/guala_core/src/resident_cognitive_formation.rs`.
- Runtime transport/schema touched: none.
- Persistent codec/state fields touched: none; the ordinary sparse contact is
  already encoded by the existing fabric.
- L0-L4, DSF fields, neuron physics, motor work, body, pressure, and cochlear
  return: unchanged.
- Growth bound: at most one idempotent L9-to-L11 contact per physically
  co-occurring retained-memory/learned-motor-source pair; no new neuron and no
  artificial width.
- Removal: the Attempt-52 block that tried and failed to attach a transient
  current-experience ordering cell to a selected family hub is deleted rather
  than retained alongside the direct memory link.
- Decisive falsifier: after the same finite teaching chronology, the static
  trained state must contain actual L9-to-vocal-L11 contacts; cold partial cue
  must then produce learned motor pulses, breath, new pressure, and self-return,
  while the severed copy remains silent.

## Overengineering assessment

- O-001 confirmed: the active cognitive source file is 32,846 lines and its
  test probe is 6,685 lines. The current compiler reports 113 warnings,
  including unused/dead compatibility surfaces. This is evidence of substrate
  bloat, not a reason to add another layer.
- O-002 confirmed: Attempts 51 and 52 added 755 production/test lines to the
  cognitive source and 913 lines to the probe after the last source-work
  boundary. The result grew ten motor routes but still produced no learned
  post-cue action. Those additions are not accepted architecture merely because
  they are deployed.
- O-003 confirmed: the existing motor-work path recognizes source work only
  when an active L11 ordering cell sends backward into L7. It ignores ordinary
  exact work released when a causally reached premotor family member sends
  through its bounded L11 lateral contact. That special-case restriction is
  why the Attempt-52 anatomical join cannot enter the already-proven motor-work
  transducer.
- O-004 confirmed: a one-assertion Rust regression change forces a complete
  optimized/LTO crate relink taking 2 minutes 24 seconds and emits 113 warnings
  before a zero-second focused test can run. This build/test coupling materially
  magnifies iteration cost and hides relevant failures in dead-surface noise.
  It is a cleanup target; it is not evidence that any neuron physics should be
  simplified or that L0-L4 may change.
- Retention rule: keep only the existing exact work conservation, bounded motor
  residue, typed motor discharge, respiratory recruitment, pressure, and
  self-hearing mechanisms that have direct component proof. Family-selection,
  compatibility, and observer fields remain removal candidates; none may be
  extended to make the correction pass.

## Current source finding

`resident_cognitive_formation.rs` computes a `LearnedMotorWorkOffer` only when
an L11 cell that already owns an L11-to-L12 motor bridge is itself the sender
of a nonzero settled transition into L7 across its founding frontier. The
Attempt-52 L11-to-L11 link does not by itself satisfy that source condition.
This explains why correct new anatomy could coexist with zero learned motor
work after the cue. The exact lawful repair is not yet selected; the remaining
source/settlement path must be mapped before one edit is authorized.

## Candidate 53B result — rejected and fully removed

Candidate 53B passed its exact memory-to-ordering causal/idempotence invariant:
`1 passed; 0 failed; 608 filtered out`. After removal of the obsolete
Attempt-52-only fixture expectation, the unchanged bounded motor-family test
also passed exactly with the same result. These are component successes only.

The complete tick-469814 copied-body run finished normally in 294.57 seconds
but rejected the candidate. The teaching state and silent WAV were byte-for-
byte identical to the prior failed run. Consequently Candidate 53B grew no
memory-to-vocal-ordering contact in the real chronology: the proposed retained
L7/L9 route and a learned motor discharge never satisfied the same-interval
growth condition. After the cold partial cue, 256 clocks produced zero learned
vocal pulses, zero respiratory carriers, zero nonzero pressure samples, and no
self-hearing. The severed, silence, reversed, and discontinuous controls also
remained silent.

Preserved real successes from the same run: ten bounded vocal routes and ten
sources; all eight vocal terminals; 65 guided learned motor discharges; 58
guided respiratory carriers; 57,991 guided nonzero pressure samples; exact
teaching and final cold round trips; and no tutor-authored later motor command.
These prove the tutor-time motor/body/pressure chain, not learned speech.

Artifacts before the source was reverted:

- Candidate source SHA-256:
  `506dc5b62cfb60809ebb6880e438d8f7b04093de12e015eed5c2947d7c53f4e0`.
- Candidate diff SHA-256:
  `33a95dfebaa0cf7e2518a1a891f16b0c748922b7bee8b2c209f206dc256f1e6b`.
- Exact release test binary SHA-256:
  `4f160ce00d2a398392d149257672ce867681b5121f30fcf6af21c25d5872a364`.
- Report: `/tmp/guala-integrated-full-loop-v9.json`, SHA-256
  `122bac50dc51929f673839ef60de7ba9b299b3e7aeae2dc44bfe924bacda8954`.
- Teaching state: `/tmp/guala-integrated-full-loop-v9-teaching.cognitive`,
  SHA-256
  `8df93f015d971752be40f1ac5615172368fad51deaa40a1d09b0a9f1da60b5a8`.
- WAV: `/tmp/guala-integrated-full-loop-v9.wav`, SHA-256
  `db101901663b1a8aeea9f739bde31d2c9d5b09519ecb907cded76cd98308c09c`.

The candidate production source and its tests were then reverted byte-exactly
to HEAD. Only this attempt ledger remains uncommitted. Candidate 53B must not be
deployed, retried, or extended with another coincidence gate.

Post-run custody remained unchanged: production task definition 1434 was
desired/running/pending `1/1/0`, rollout `COMPLETED`, task and container
`HEALTHY`, image digest
`sha256:fb194f7cd2fbee77e492cd1ed2aa01ef4ea6342b4a955138b75b976c946dce95`,
and no Guala alarm was in `ALARM`. No Cargo, Rust compiler, or copied-body test
process survived the run.

## Static memory/action seam after 53B rejection

- The trained body contains 181 retained mosaics. Their retained members span
  layers 0 through 11, so the memory system is active and is not globally
  restricted to raw receptors.
- None of the two layer-7 associations that found all ten target vocal motor
  routes (`...046a` and `...0574`) occurs in any retained mosaic.
- None of the 15 layer-11 sources with a learned layer-12 motor bridge shares
  a layer-7 founder with any layer-11 route that has a layer-9 recurrent-memory
  neighbour. Thus no unique stored-topology join exists for Candidate 53B to
  discover later.
- Only three layer-11 cells occur in retained mosaic membership, and none is a
  target vocal ordering source. No layer-12 cell occurs in any retained mosaic.
  This confirms that adding more mosaic bookkeeping is not a narrow speech
  repair.

O-005 confirmed: the current speech construction built a retained-memory
system and a learned motor system with disjoint founders, then added a special
current-experience/family-hub attachment to compensate. That attachment is
absent in the lived body and introduces another timing conjunction. It is
overengineered glue and remains scheduled for deletion, not extension.

## Candidate 53C causal impact map

The existing learned motor bridge is already physically exact, persistent,
bounded, severable, and connected to a typed motor. Its work transducer accepts
only one direction on its exact founding L7/L11 contact: L11 sending back into
L7. A real external or recurrent cue naturally crosses that same learned route
in the opposite direction, L7 into L11. The narrow candidate admits that
incoming founding-contact work to the same existing exact work-conservation
and intrinsic-motor settlement. This knits cue ingress to the already-learned
ordering/motor bridge; it creates no new contact, neuron, field, state, timer,
cursor, memory, or speech-specific instruction.

The incoming route is eligible only when the L7 founder is the exact causal
seed, the exact founding bond is the advancing frontier crossing, the L11
receiver is the exact source owning the already-learned L12 bond, and released
work is positive. The existing exact conductance split, motor input residue,
gate settlement, source-work debit, severing control, and bounded body remain
unchanged. The old Attempt-52 current-experience-to-family-hub growth block and
its artificial test fixture are deleted in the same candidate because the
complete trained-body audit proved that mechanism a no-op.

## Candidate 53C result — directional change rejected

The exact bounded motor-family regression passed after the Attempt-52 glue and
fixture were removed: `1 passed; 0 failed; 608 filtered out`. The complete
copied-body run then finished normally in 285.83 seconds and rejected the
incoming-direction hypothesis. It again produced zero post-cue learned vocal
pulses, zero breath, zero pressure, and no self-hearing. The teaching state and
silent WAV were byte-identical to the prior failures. The 13-line incoming
direction branch therefore never became physically eligible and was removed;
it must not be retried. The 52-line deletion of proved-dead Attempt-52 glue and
its artificial expectation remains valid cleanup and is carried separately.

Candidate 53C artifacts:

- Source SHA-256:
  `475331b941b6211e81fc0f9de8c69843978e721fe837453f257d61681ef8f58e`.
- Diff SHA-256:
  `3fb7bbc30d0c9a798a3db367b379a9e1bc116b83c3b819bf8520de8385d77785`.
- Release test binary SHA-256:
  `981e223ee733fad7d71cf65bff69857b24114dcc07de7c6d4a9cd0adb4f2670c`.
- Report: `/tmp/guala-integrated-full-loop-v10.json`, SHA-256
  `ec67381206feacf9575db728f821ecdb79eaeed42e66bb4d39034aa8a73923e2`.
- Teaching state: `/tmp/guala-integrated-full-loop-v10-teaching.cognitive`,
  SHA-256
  `8df93f015d971752be40f1ac5615172368fad51deaa40a1d09b0a9f1da60b5a8`.
- WAV: `/tmp/guala-integrated-full-loop-v10.wav`, SHA-256
  `db101901663b1a8aeea9f739bde31d2c9d5b09519ecb907cded76cd98308c09c`.
- Preserved tutor-time successes: ten routes/sources, all eight terminals, 65
  learned discharges, 58 respiratory carriers, and 57,991 nonzero pressure
  samples.

## Exact coexisting-sense exclusion found after 53C

The 16 vocal proprioception/load receptors used by the cue each connect to
their exact layer-6 integration cell. Those 16 layer-6 cells have zero layer-7
contacts in the trained body. The two layer-7 cells founding all ten target
motor routes instead have large old fan-ins (39 and 71 layer-6 neighbours) and
no contact to any current cue integration.

The source cause is explicit in `mount_reached_cross_sensory_association`:
`externally_energized_by_occurrence` keeps each admitted source episode in a
separate slot, then requires at least two sensory/body layers inside that one
slot. In the teaching and cue protocol, exact body consequence and exact sound
are separate episodes admitted to one coexisting organism interval. The body
slot has only layer 5; the sound slot has only layer 1. Both are rejected by
`sensory_layer_count < 2`, so the physical co-occurrence can never create its
layer-7 association. This is a deterministic exclusion, not a timing guess.

O-006 confirmed: the system advertises coexisting multi-sensory settlement but
the association builder partitions the coexisting inputs back into isolated
episode buckets before testing cross-sensory reach. Later motor-family and
memory glue attempted to compensate for an association that the earlier gate
made impossible. This is overengineered and causally wrong.

## Candidate 53D causal impact map

Use one exact union of all externally energized integrations admitted to the
same `advance_coexisting_admitted_transition` interval when evaluating a
cross-sensory assembly. The unchanged gate still requires at least three
actually settled layer-6 integrations and at least two real sensory/body
layers. The resulting one ordinary layer-7 association is returned to each
contributing occurrence so the existing occurrence-local body regulation and
motor learning can use the same physical coexperience. No history, clock,
semantic label, score, new state field, new neuron layer, or speech exception
is added. Existing exact assembly identity and idempotent contact reuse remain.

## Candidate 53D pre-build translation and downstream-flow review

The initial source draft exposed and prevented one late integration failure
before compilation: `mount_reached_cross_sensory_association` can mount a new
layer-7 cell and new fabric contacts, while the following consumers previously
would have received the topology index built before that growth. The candidate
now rebuilds the existing `ResidentTopologyIndex` immediately when the cohort
or fabric shape changes. This is reuse of the existing transient topology
authority, not another index, state field, or compatibility surface.

The complete bounded path reviewed before the first build is:

1. `advance_coexisting_admitted_transition` retains each authenticated source
   occurrence while admitting them to one organism interval.
2. `mount_reached_cross_sensory_association` maps each externally energized
   receptor to its existing local layer-6 integration, unions only the
   integrations participating in that interval, and requires at least three
   settled integrations across at least two actual source layers.
3. One ordinary layer-7 cell plus sparse layer-6 contacts is mounted or exactly
   reused. The refreshed topology makes that cell and its bonds visible to all
   remaining consumers in the same transition.
4. `exact_moved_body_regulations_by_occurrence` retains only body regulations
   whose exact paired terminal actually moved. `exact_occurrence_affective_pairs`
   permits the interval's one unambiguous association to meet those moved
   regulations even though body and sound arrived through separate authenticated
   source occurrences.
5. `mount_reached_affective_reach_indexed` uses the refreshed topology to mount
   or reuse the existing layer-7/layer-10 physical route. Later ordinary
   electrical intervals—not direct seeding—must carry current onward through
   ordering, the existing learned layer-11/layer-12 motor bridge, respiratory
   recruitment, vocal pressure, and cochlear self-return.
6. The generic cohort and fabric successor codec already persists these cells
   and contacts. The focused test retains its existing exact encode/decode
   equality check. No schema, FFI field, Python cognition callback, transport,
   observer authority, or production startup path changes.

Conservation and bounds: no new energy or carrier source is introduced; the
candidate changes only which truly coexisting source integrations may author
existing developmental anatomy. Growth still consumes the bounded resting
population and reuses byte-identical assemblies. Work remains linear in the
already-admitted occurrence members plus existing incident contacts, with no
history scan or retained cache.

Pre-build success S-053D-01: the stale-topology late failure was found by
following the candidate into its immediate consumers and corrected before any
compile or broad run. Its permanent falsifier is that every anatomy/fabric
mutation must either leave the transient topology shape equal or rebuild the
existing index before the next topology consumer. A test pass with a stale
binary or an isolated helper remains insufficient.

The completed source-only review found no second state owner, mutation schema,
new settlement authority, unbounded retained collection, direct formation-to-
motor shortcut, semantic speech instruction, or reduced DSF proxy. The first
reviewed candidate fingerprint was
`7260cab722df6673f800a0ee4502b3d4580eac154fd1b19d610eb67fdc387677`;
verification returned the same value before this ledger entry was appended.

Focused success S-053D-02: frozen candidate fingerprint
`363a8b61c366baa4a65145dd21d40061bed801ba055b61a21e0b91ac068ae8b0`
compiled into exact release test binary SHA-256
`586b65277d25780b62b04c2ea8ad3e58d70379e4f611579a2d828b998ece1567`.
The fully qualified test
`resident_cognitive_formation::tests::varied_multisensory_occurrences_mount_only_exact_settled_assemblies`
reported exactly `1 passed; 0 failed; 608 filtered out`; post-test fingerprint
verification was identical. It proves coexisting occurrences form one exact
association, exact recurrence reuses it, a different assembly remains distinct,
and generic encode/decode is exact. It is component evidence, not speech.

O-007 confirmed: this one zero-second test required a 3 minute 26 second
optimized whole-crate rebuild and emitted 114 inherited warnings. This is
measured build/test bloat. It is not required neuron physics and must not be
copied into a lean successor, but removing it is outside the frozen speech
slice.

F-020: two guessed public observation paths,
`/api/v1/guala/native-observation` and `/api/v1/gualaloom/observation`, returned
404. The live interface source authoritatively names
`/api/v1/guala/observation`; that route returned schema
`guala.lean_actor_observation.v1`. Permanent rule: read the deployed route
constant before a public probe; never reconstruct it from obsolete handoffs.

F-021: ECS Execute Command truncated a base64 body stream to about 10 KiB, so
interactive session output cannot copy the 2.4 MiB compressed CURRENT body.
That transport is rejected and must not be retried for material body custody.

F-022: the first pre-signed S3 PUT used boto3's legacy Signature V2 default and
the production container received HTTP 403. No object was written. The
corrected Signature V4 request succeeded once; legacy signing must not be
retried.

Read-only production envelope at `2026-09-07T22:32:43Z`: AWS account
`418384447921`, cluster/service `tfe-web-cluster/dsf-ai-service-lb`, task
definition `1434`, task `cb396f0fe3f346ccb5070e3c0b0b83a3`, image digest
`sha256:fb194f7cd2fbee77e492cd1ed2aa01ef4ea6342b4a955138b75b976c946dce95`,
desired/running/pending `1/1/0`, rollout `COMPLETED`, and zero alarms in
`ALARM`. The correct public observer showed identity
`1cc4e70a-f2a0-44c5-a111-f4a5bc915cc1`, live tick 520593, persisted tick
520590, zero Python callbacks, no pressure, and no durability/cleanup error.

Copied-current success S-053D-03: a read-only ECS command selected paired
CURRENT tick 520650 and uploaded only its immutable compressed body to
`s3://guala-incident-bench-20260831/speech-attempt53/current-20260907.glorun.gz`.
The production-reported raw body receipt/size
`8886267ca22999754189a62fca715eb5b7837954ea98723ac778fdb6cfffb1db` /
135,507,851 bytes and compressed receipt/size
`f570fa9ca65cba1b36b841bb89f1b87f7ae0b706f6f884ac8e0da16b31bd375b` /
2,437,381 bytes matched the local download and decompression exactly.
Production CURRENT, EFS, actor, and task were not changed or paused.

## Candidate 53D newest-body run — stopped at first causal failure

The frozen release binary ran only
`resident_cognitive_formation::reservoir_probe::reservoir_probe_dump` against
the authenticated tick-520650 body with the recorded 256 guided cycles, eight
four-phase sequence repetitions, 1,500 tutor carriers per terminal, and 256
maximum post-cue clocks. It stopped after 151.02 seconds at candidate tick
520906 before cold cueing or report emission:

`complete learned sequence retained its first pressure phase`

The option was absent because all eight target vocal terminals never became
present at the start of a learned-sequence phase within the bounded guided
window. This differs from the tick-469814 predecessor runs, which did complete
the guided population and produced 65 learned discharges, 58 respiratory
carriers, and 57,991 nonzero tutor-time pressure samples before failing only
after the cue. Artifact `/tmp/guala-integrated-full-loop-v11.log` is retained;
the incomplete teaching body is not acceptance evidence and no JSON/WAV was
emitted. No process survived.

F-023 disposition: Candidate 53D is neither accepted nor yet attributed as the
cause. The run exposed a same-body comparison requirement before another
candidate edit: execute the unchanged HEAD predecessor on the exact same
tick-520650 body and chronology. If HEAD also cannot complete guided growth,
the current body/harness boundary is the first failure; if HEAD completes it,
Candidate 53D regressed tutor-time development and must be removed. Running an
older body or extending the cycle bound cannot answer this and is prohibited.

Same-body attribution success S-053D-04: unchanged HEAD
`735489406577f90edf6c2fb7fb1889f87eae8107`, exact release binary SHA-256
`166586283922da42f178a1502d3198b99e35e1165c15490b4866daee2b737471`,
passed its one exact predecessor regression, then failed on the identical
hard-linked tick-520650 body at the identical generation 520906 and identical
assertion after 159.05 seconds. Candidate and predecessor partial teaching
states are byte-identical: SHA-256
`a801cb41b4b2e36557df9a35acd44b98bdc8c4a65b34f8ff9b7f1e7ac69df38b`,
135,527,905 bytes. Therefore Candidate 53D did not regress this chronology,
and also did not engage during it.

Static retained-state success S-053D-05: an observation-only envelope carrying
that byte-identical partial cognitive successor showed that tick 520650 and
tick 520906 have exactly the same 1,833 reached cohorts, 9,526 fabric contacts,
182 retained mosaics, and every layer-boundary contact count. In particular,
L11-to-L12 remained 7. The only two target vocal routes are the pre-existing
section-0/toward-maximum and section-7/toward-minimum routes; none of the six
missing target routes grew.

The 16 exact target vocal proprioception/load receptors all have their existing
layer-6 integrators, but all 16 integrators have zero layer-7 association
contacts before and after the 256 guided cycles. The existing guided-growth
harness admits only body consequence until body-owned pressure already exists;
it supplies no external tutor sound during the population-growth phase. Thus
it cannot exercise Candidate 53D's corrected body-plus-sound association on
this lived body. The older tick-469814 run's late route growth depended on
pre-existing incidental structure and is not a lawful teaching contract.

F-024 disposition: do not raise the 256-cycle bound, retune carriers, or reuse
the older body. Add one bounded external non-semantic pressure occurrence to
the same guided body interval using test-only ingress, then rerun Candidate
53D and unchanged HEAD on the same tick-520650 body. The sound is tutor input,
not substrate state, meaning, phoneme, word, motor command, or trajectory
authority. HEAD must remain unable to form the cross-source association while
Candidate 53D must causally progress beyond this exact zero-growth boundary.

F-025: the first proposed tutor-pressure artifact was extracted from the prior
v10 output WAV on the unverified assumption that the file retained guided
pressure. Exact sample inspection found 4,000/4,000 samples were zero: that WAV
contains only the silent unguided output. The artifact was rejected and
overwritten. Permanent rule: inspect the physical content and provenance of an
artifact before using it; report metadata does not establish its semantics.

F-026: the next artifact-generation command invoked `sox` without first
checking tool availability; this environment has no `sox`, so the command
failed before changing the artifact. Permanent harness preflight now requires
resolving the exact executable before invocation. `/usr/bin/ffmpeg` was then
confirmed present and generated the bounded test input successfully.

Tutor-pressure input success S-053D-06: the test-only external input is exactly
4,000 little-endian signed 16-bit samples at 16 kHz (250 ms), with 3,990
nonzero samples, range -512..512, and SHA-256
`00c6ec35000e84c01ceb41f5349559156ea70ed398614a1c4a14328e3afb166c`.
The harness now rejects the wrong byte length and all-zero pressure before
cognition runs, co-admits this one non-semantic hearing occurrence with the
guided body consequence, and records its sample/nonzero counts in any emitted
report. It supplies sensory work only; it does not author meaning, phonemes,
words, motor direction, timing, contacts, or state.

## Candidate 53D external-pressure current-body result — rejected

The harness-integrated candidate fingerprint was
`0a2eda2e35d044fd40bb374c97fa529eec5a6d3c172c84063c4daecd733a4bbf`.
Its exact release test binary SHA-256 was
`651ac3635957458c684c6089af2594b6293e746bc75e594839ada37eb2574c2f`.
The exact focused cross-sensory regression passed again: `1 passed; 0 failed;
608 filtered out`. The oversized crate required 2 minutes 55 seconds and
emitted the same 114 inherited warnings for the zero-second test.

The exact candidate then ran against only the authenticated tick-520650 body
and the S-053D-06 pressure input. It again stopped at candidate tick 520906 on
`complete learned sequence retained its first pressure phase`, after 186.44
seconds. No acceptance JSON or WAV was emitted and no process survived. The
partial cognitive state is 135,649,990 bytes, SHA-256
`bde96f54af39900223f123824a939ba0955f4295599ee84a2d69775e9bce4d6e`;
the 454,487-byte causal log has SHA-256
`2f94408c3c838f25ec944646db05f070cf15754111b276a45a3370e8f48fce7d`.
It differs from the byte-identical candidate/HEAD silent-input state, proving
the tutor pressure entered and changed cognition, but not that speech worked.

Static success S-053D-07: the exact partial cognitive bytes were placed into
an isolated copy of the original envelope for decode-only census. The
17,684,967-byte report SHA-256 is
`9bf94ff1c2b1e006fcbdd406dbed20ba73cc64cc070070fd0f0bb5dc33daa0f2`.
Relative to the silent-input partial state, the pressure-bearing run grew
1,833 -> 1,836 cohorts and 9,526 -> 9,586 contacts while retaining exactly 182
mosaics. Boundary deltas were L6->L7 +44, L7->L10 +1, L8->L10 +12, L7->L11
+1, L10->L11 +1, and L11->L12 +1. This is real bounded developmental change.

The one new route is ordering `...0850` -> motor `...00c5`. Motor `...00c5`
is the already-existing vocal-tract section-0/toward-maximum terminal, already
owned by mature ordering route `...05f6`. Thus the added sound/body experience
did not grow a missing terminal; it grew a second ordering route to an already
reached motor. The new association `...077a` collapsed 44 sound/body layer-6
integrations into one cell; its one affective route `...13f5` widened across
12 body regulations, and the existing founding-route walk selected the first
already motor-connected regulation. The exact guided toward-minimum body
consequence therefore became another section-0/toward-maximum route. More
cycles or more sound would repeat the wrong topology and are prohibited.

F-027: an inspection command invoked unavailable `xxd` without executable
preflight. It failed read-only. This repeated the already-recognized missing-
tool class and is not excused by having no state effect. Resolve every optional
harness executable with `command -v` before invocation; use no remembered tool
assumption.

F-028/F-029/F-030: the first static reduction indexed an object root as an
array; after correcting the root, the next reduction guessed that
`electrical_fabric` was an object rather than an array; a later combined query
guessed that `motor_reachability` was an array rather than an object. All three
failed read-only and changed no artifact, but F-028 repeats the exact jq-scope
class already recorded at F-014. Permanent query gate: before any reduction,
emit and retain `type` and `keys` for the root and every nested field used;
never combine type discovery with speculative indexing, and never reuse a
remembered report shape.

F-031: the Candidate-53E formatting preflight invoked crate-wide
`cargo fmt --check`. It exited nonzero after about 30 seconds because the
baseline crate contains thousands of unrelated historical formatting
differences across files outside the candidate. It changed no file. This broad
check cannot distinguish candidate formatting from inherited debt and must not
be rerun during this speech slice. Candidate whitespace is governed by the
exact source diff plus `git diff --check`; any future whole-crate formatting is
a separately authorized mechanical cleanup, never a speech prerequisite.

F-032: the Candidate-53E pre-run public observation request repeated the exact
F-014/F-028 schema error: it selected remembered root fields before emitting
the live object's `type` and `keys`, producing several `null` values. The GET
was read-only and changed no organism or production state, but those nulls are
discarded as evidence. This is the third recurrence of an already-recorded
operator mistake. The next request is restricted to schema discovery; no
field selection is permitted until the actual keys are retained.

Candidate 53D is rejected as a complete current-body speech repair. Its
component success and its lived failure are both retained: coexisting external
sound and body can form persisted cross-sensory anatomy, but the current
single-association/single-affective founding walk collapses simultaneous body
terminals and chooses an old motor. The unchanged-HEAD rerun with the new sound
input is unnecessary because the candidate itself failed the positive gate;
no baseline result could convert that failure into acceptance.

## 2026-09-08 architecture pause — keep, discard, create

Joe required a stop in serial repair activity and a fresh accounting of what
the organism is trying to accomplish, which mechanisms are necessary, and
which accumulated mechanisms obstruct that result. This section is the frozen
decision boundary before another source change or copied-body run.

The requested result remains one lean physical loop:

`heard tutor pressure plus exact guided vocal-body consequence -> retained
sensorimotor relation -> later partial acoustic cue -> organism-owned vocal
motor discharge -> breath and pressure -> cochlear/body return`

### Keep — directly proved physical components

- The unchanged full joint seven-field L0-L4 delivery and definitive neuron.
- Continuous organism identity, current neuronal/body state, sparse contacts,
  exact persistence, and cold restoration.
- Existing typed layer-12 vocal motor tissue, layer-13 articulation, finite
  respiratory recruitment, accepted pressure production, and cochlear return.
- Exact dynamic learned-work conversion when a lawful ordering-to-motor route
  actually fires.
- Exact body-effectors and occurrence-local proprioceptive/load evidence; the
  body source already preserves one physical occurrence per moved axis.
- The completed five-route production shell and truthful UI. They are not the
  speech defect and will not be rebuilt or widened.

### Discard — falsified or causally unnecessary mechanisms

- Candidate 53D's union of every coexisting occurrence into one layer-7
  assembly returned to every occurrence. It erased which body movement was
  paired with the tutor sound and collapsed 12 regulations into one route.
- The cross-source `exact_occurrence_affective_pairs` fallback that permits one
  interval association to attach to every moved body regulation without the
  association's own ordinary carrier transfer. It existed to compensate for
  the earlier occurrence partition and is both unnecessary and unsafe once
  occurrence identity is preserved.
- Attempt 52's current-experience-to-family-hub glue, Candidate 53B's
  memory-to-ordering coincidence, and Candidate 53C's reversed L7-to-L11 work
  admission. Each was a real-body no-op or failed the complete causal gate and
  must remain absent.
- More tutoring cycles, larger carrier values, broader motor fan-out, direct
  authored motor bridges, semantic speech labels, another stored sequence
  system, and any Python cognitive controller.

The 32,814-line `resident_cognitive_formation.rs` and 6,720-line probe are
confirmed maintainability debt. Source line count alone is not executed work,
so splitting files cannot be represented as a speech repair. Production-cost
suspects inside the monolith are repeated topology reconstruction, mounted-
population scans, compatibility branches, and test/build coupling. They remain
owned cleanup candidates, but none will be mixed into this causal correction
without a separate measured successor-equivalence proof.

### Create — one occurrence-preserving correction, no new subsystem

Restore the existing cross-sensory association builder's per-occurrence
identity while allowing simultaneous context from physically different
sensory/body layers to participate. For each exact occurrence, its own
integration lineages remain local; coexisting integrations at other physical
layers may join its assembly; integrations belonging to sibling occurrences
on the same layer may not. Assembly identity remains only the sorted exact
lineage set. Thus one tutor-sound occurrence can participate in each distinct
vocal-body occurrence without collapsing those body occurrences together.

The unchanged minimum remains three actually settled layer-6 integrations
across at least two real layers. New layer-7 cells are not seeded. Later
ordinary carrier transfer must activate each retained association before it
can pair with that occurrence's exact moved layer-8 regulation. No new state,
layer, schema, energy, timer, score, label, history, or special speech command
is introduced. The transient topology refresh after real anatomy growth is
retained because later consumers otherwise inspect a stale shape.

This design differs causally from rejected Candidate 53D: shared sensory
context is copied into each exact assembly definition, but same-layer body
occurrences are never unioned, and one association is never assigned to every
body regulation. It differs from Attempts 52/53B/53C because it repairs the
earliest exclusion boundary instead of adding downstream glue.

Single next item: restore baseline from rejected Candidate 53D, implement this
one association-scoping law, delete the compensating cross-source fallback,
source-review the frozen diff, then run the existing focused occurrence test
and one complete copied-current-body acceptance path. Any failure rejects the
candidate; it does not authorize another patch layer or a longer run.

### Candidate 53E frozen source review

Candidate 53E changes only
`native/guala_core/src/resident_cognitive_formation.rs` in the production path.
It replaces Candidate 53D's all-occurrence union with the occurrence-preserving
coexisting-context rule above, removes the compensating cross-source affective
fallback and its test-only assertions, retains the required transient topology
refresh, and retains the already-authorized removal of Attempt 52's failed
family-hub glue. No L0-L4, DSF, neuron constitution, body, pressure, codec,
schema, Python, shell, UI, or production file changes.

Source review classification: no architectural rejection found. The exact
input is the already-authenticated occurrence-indexed external receptor
lineages plus the actually settled layer-6 set. The output is only sorted
lineage assemblies and sparse contacts. Existing state is mutated only inside
the uncommitted successor construction; any error rejects that successor. The
relation adds no energy and new cells remain electrically unseeded. Work and
growth are proportional to the exact association memberships produced from
the currently reached interval, with no retained raw input, history scan,
second index, owner, lock, counter, score, or semantic instruction. Generic
cohort/fabric encoding and cold restoration are unchanged.

The first complete-worktree freeze after source review was
`ad97ab56e6a519749bce10119be897f3b84769353937aa9c1ccd88633a7edf68`
and verified unchanged before this ledger-only review entry. Because the
fingerprint includes this untracked ledger itself, recording the value changes
the complete-worktree fingerprint; the production-source diff remains frozen
and will receive its own byte hash immediately before build and again before
the copied-body run.

Candidate 53E production-source diff SHA-256 before build was
`3a87ed1868b6ac1e3e08ff441b45985128196c798227994394320a5884ee0bb3`.
The release build completed in 2 minutes 43 seconds with the inherited 114
warnings. The exact built test binary was
`native/guala_core/target/release/deps/guala_core-3b4e6ae94e95937d`, SHA-256
`79ac09ec7a6a8a84301d902fdf67a8c2d31ded11ec3f7151e4564bedaf29eba8`.
Its immediately listed fully qualified test name was then invoked exactly:
`resident_cognitive_formation::tests::varied_multisensory_occurrences_mount_only_exact_settled_assemblies`.
Result: `1 passed; 0 failed; 608 filtered out` in 0.01 seconds. The test proves
distinct same-layer occurrences retain distinct associations while sharing
coexisting other-layer context, and repeated assembly reuse adds no duplicate.
This is focused component evidence only; it does not prove learned speech.

The immediate post-run production envelope remained healthy and unchanged in
identity/code: task definition 1434, desired/running/pending `1/1/0`, rollout
`COMPLETED`, identity unchanged, live/persisted ticks 520754/520750, no
checkpoint or cleanup error, zero Python callbacks, no pressure, and zero
alarms in `ALARM`.

## Candidate 53E copied-current-body result — rejected

The exact release binary SHA-256
`79ac09ec7a6a8a84301d902fdf67a8c2d31ded11ec3f7151e4564bedaf29eba8`
ran only
`resident_cognitive_formation::reservoir_probe::reservoir_probe_dump` against
the authenticated tick-520650 body and the exact bounded tutor-pressure input
SHA-256
`00c6ec35000e84c01ceb41f5349559156ea70ed398614a1c4a14328e3afb166c`.
The run stopped after 189.41 seconds at candidate tick 520906 on:

`complete learned sequence retained its first pressure phase`

Result: `FAILED; 0 passed; 1 failed; 608 filtered out`. All eight target vocal
terminals again failed to become present during the bounded guided chronology,
so no learned-sequence phase began and no later cold cue or control ran. No
acceptance JSON or WAV was emitted and no probe process survived. Production
was not touched.

The incomplete teaching state is retained only for read-only causal census:
`/tmp/guala-integrated-full-loop-v13-teaching.cognitive`, 136,544,568 bytes,
SHA-256
`ec702cc61057f0acb70402ce0e84e62d329a715e7896429af76b4b314533dbd8`.
It is not acceptance evidence. Its different bytes and size relative to
Candidate 53D prove Candidate 53E changed the learned anatomy reached during
the same input chronology; they do not prove the change is useful or that
speech works.

Preserved component success S-053E-01: the exact focused occurrence test
passed and proves the new builder keeps same-layer body occurrences distinct
while sharing simultaneous sound context. Preserved end-to-end failure F-033:
that component correction did not close the current body's guided
vocal-population boundary and is rejected as a speech repair. No longer run,
larger carrier value, deployment experiment, or downstream patch is authorized
by this result. The only next evidence step is a read-only census of the
retained partial state against Candidate 53D's recorded topology.

Static causal success S-053E-02: an isolated envelope carrying only the failed
teaching cognition was generated without changing its bytes, then decoded by
the existing measurement probe. The envelope is 136,545,662 bytes, SHA-256
`f5fc57ca0399da3c9b5892080e39603710d5db15955a767c0fb4112b36130dc9`.
The 18,578,369-byte static report SHA-256 is
`26f5f5cf23d03b1c52e2a57278b5ee8b142137061d0a397e2499bf184f1c52dc`.
Relative to Candidate 53D under the same body and tutor pressure, Candidate
53E grew 1,836 -> 1,859 cohorts and 9,586 -> 9,906 contacts while retaining
exactly 182 mosaics. Boundary deltas were L6->L7 +288, L7->L10 +7,
L8->L10 +8, L7->L11 +7, L10->L11 +7, and L11->L12 +3. The three new motor
contacts are ordering/motor lineage pairs `...085e`/`...00e1`,
`...086c`/`...00fd`, and `...087a`/`...0509`; no L11->L12 contact was removed.

This is genuine partial causal progress, not speech: the harness sustained
only TowardMinimum while the minimum population was incomplete, and Candidate
53E added exactly the three routes missing from the two-route starting body at
the final guided generation. The current harness checks its single global
256-cycle ceiling before permitting the now-required TowardMaximum phase, so
the stopped result cannot test the other three missing terminals. This matches
Attempt 50's retained correction that a 256-occurrence ceiling did not prove a
universal negative, while differing from Candidate 53D's prohibited longer
run: Candidate 53D created one route to an old wrong-direction motor; Candidate
53E created three distinct exact-occurrence motor routes and reached the
direction boundary only at the ceiling.

The initial F-033 prohibition against any longer run is therefore narrowed by
the subsequent census: it still prohibits blind repetition, more carriers,
more sound, or an unbounded extension, but does not prohibit the harness's
already-coded hard maximum of 512 cycles needed to give each of the two
sustained directions one bounded 256-cycle range. One immutable Candidate 53E
run at that existing hard maximum is the next and only authorized evidence
step; failure rejects the candidate without another patch.

F-034: the first schema inspection of the v13 static report again attempted
`.records[0]` in the same query that asked for the root type, before proving the
root was an array. The root was an object and `jq` rejected the indexing. This
was read-only, but it is a fourth recurrence of the recorded schema-scope
operator error. The discarded query supplied no evidence. Subsequent queries
were strictly staged: root type/keys, `records` type, array length/member type,
member keys, nested types, then nested keys, and only then values. Permanent
command gate: a schema-discovery command may inspect exactly one previously
proven container level; it may not speculate about a child in the same command.

O-008: the generic static measurement probe took 169.55 seconds and emitted
large volumes of cloned-transition diagnostics merely to produce a read-only
topology census. That is confirmed test-harness/build coupling and
maintainability debt, not organism work and not a reason to add another probe
mode during this speech slice.

## Candidate 53E bounded second-direction result — rejected conclusively

The one authorized extended gate reused the identical source, exact release
binary SHA-256
`79ac09ec7a6a8a84301d902fdf67a8c2d31ded11ec3f7151e4564bedaf29eba8`,
authenticated tick-520650 body SHA-256
`8886267ca22999754189a62fca715eb5b7837954ea98723ac778fdb6cfffb1db`,
8,000-byte tutor pressure SHA-256
`00c6ec35000e84c01ceb41f5349559156ea70ed398614a1c4a14328e3afb166c`,
1,500 carriers, and 256 post-cue clocks. Only the already-coded hard guided
ceiling changed from 256 to 512 so both sustained directions could receive one
bounded range.

The run crossed the prior tick-520906 stopping boundary, completed all 512
guided occurrences, and stopped after 375.39 seconds at tick 521162 on the
same assertion:

`complete learned sequence retained its first pressure phase`

Result: `FAILED; 0 passed; 1 failed; 608 filtered out`. The complete eight-
terminal vocal population still did not exist, so sequence training, cold
partial cue, autonomous discharge, pressure, self-hearing, and controls never
ran. No JSON or WAV was emitted and no probe process survived. The incomplete
teaching cognition is 136,530,691 bytes, SHA-256
`b3ecdaaef9921416fe2e5b578004cdf8eba25b08bed2ba43bfa0a2492e3cb21a`;
it is retained as failure evidence only.

F-035 disposition: Candidate 53E is rejected conclusively as the speech repair.
Its distinct-occurrence association component and the three routes grown in
the first bounded direction remain recorded successes, but neither another
cycle extension, carrier/sound retuning, another downstream bridge, nor
deployment is permitted. The next architecture decision must use the retained
53E census and this complete two-direction failure; it may not rediscover or
rerun Candidate 53E.

Immediate post-failure production verification remained healthy and untouched:
AWS account 418384447921, service desired/running/pending `1/1/0`, task
definition 1434, one PRIMARY `COMPLETED` deployment, and zero Guala alarms in
`ALARM`. The public lean observation remained available with unchanged identity
`1cc4e70a-f2a0-44c5-a111-f4a5bc915cc1`, live/persisted ticks 522565/522562,
three pending intervals, no outstanding checkpoint or durability block, no
checkpoint/cleanup error, 221 full-DSF deliveries, 1,701 physically changed
neurons, zero Python callbacks, and no external or self-heard samples. Speech
therefore remains functionally failed in production.

F-036: the final read-only census attempt placed the v14 teaching cognition in
an isolated 136,531,785-byte envelope, SHA-256
`683e8fe3cc3f9995390d8da9bce8c9990519e2c5e112c49b09f518c7ac6c37af`.
The generic measurement probe then spent 115.29 seconds executing unrelated
cloned population-range branches and panicked at `reservoir_probe.rs:1523` on
`population-range contact settlement: NeuronLineageAuthorityChanged` before
writing any report. No JSON exists and no process survived. This is a harness
failure, not additional speech evidence, and reinforces O-008: the generic
probe is not a safe lean topology reader for this later state. It will not be
patched or rerun in Attempt 53. The authenticated v14 cognitive bytes and the
full 512-cycle gate failure remain the evidence boundary.

## Candidate 53F — moved-body-only association authority

Architecture re-review found one narrower source of Candidate 53E's excess
anatomy. Candidate 53E allowed every energized occurrence to found an
association, including the sensory-only tutor-pressure occurrence. Its v13
state consequently created seven L7-to-L11 ordering routes for four moved
vocal axes, while only three exact new motor routes resulted. The shared sound
context is necessary input to each body-owned experience; a second sound-owned
hub for the same interval is not.

Candidate 53F adds no subsystem. It derives the already-existing exact moved
body regulations before association growth, permits an occurrence to found an
association only when that exact occurrence owns at least one authenticated
moved-body regulation, and supplies those same exact moved regulations to the
ordinary occurrence-local affective pairing. Coexisting sound integrations may
still join each eligible body occurrence at different physical source layers.
A sensory-only sibling now returns no association. The transient topology
refresh remains after actual growth. No new state, layer, neuron kind, schema,
energy, timer, score, label, history, motor command, output bridge, or Python
callback is introduced.

Requested architecture: tutor pressure plus exact moved vocal-body consequence
must become one retained body-owned sensorimotor relation and later drive native
vocal action, pressure, and cochlear return. Current code reality before 53F:
Candidate 53E created body-owned relations plus unnecessary sensory-only
relations and failed the complete two-direction gate. Conflict: yes. Mechanisms
not extended: speech output, motor fan-out, cycle/carrier/input range, semantic
labels, Python cognition, shell/UI, and all rejected Candidate 52/53A-53E
downstream glue. Full joint seven-field DSF remains unchanged; no field
structure is reduced or lost. Single exact next item: source-review this
removal, pass the one focused association regression, then run one copied-body
gate at the established 512-cycle two-direction bound. Failure rejects 53F
without another patch layer.

Candidate 53F source review: the production change remains inside
`resident_cognitive_formation.rs`; the existing test-only probe chronology is
unchanged. Exact moved-body regulation derivation is reordered ahead of L7
growth but still uses the same unmodified predecessor topology and authenticated
body consequence evidence. New L7 cells remain quiescent and unseeded. The
candidate removes eligible occurrences and pairings rather than adding work;
its growth is bounded by actual moved-body occurrences and exact assembly
members. Generic persistence and cold restoration are unchanged. No source
conflict was found in this frozen review.

Candidate 53F source-plus-probe diff SHA-256 before build was
`e7bd4535c09493c8f2ef554593329318394156b1738c0bb52e03bc1c5050e455`.
The release build completed in 2 minutes 53 seconds with the inherited 114
warnings. Exact binary
`native/guala_core/target/release/deps/guala_core-3b4e6ae94e95937d` has SHA-256
`f69194c2df82bd09223828e7e403757b11384ce9dbd9298d702922ec89fb56f1`.
The fully qualified focused test name was listed immediately before exact
invocation. Result:
`varied_multisensory_occurrences_mount_only_exact_settled_assemblies` passed
`1 passed; 0 failed; 608 filtered out` in 0.00 seconds. It proves two eligible
moved-body occurrences retain distinct coexisting-context associations, the
sensory-only sibling creates none, and exact recurrence creates no duplicate.
It does not prove vocal population, pressure, or speech.

F-037: the first attempt to append this build record used remembered line
wrapping instead of rereading the ledger tail, so `apply_patch` rejected its
context and changed nothing. This repeats F-012's stale-patch-context class.
Permanent documentation gate: reread the exact destination lines immediately
before every ledger patch; do not use remembered wrapping even within the same
turn.

## Decisive acceptance gate

One immutable candidate must start from an authenticated copy of the newest
production CURRENT body, preserving exact identity and learned state. After
the existing lawful teaching chronology and cold restore, a retained partial
cue must cause, after the cue ends:

1. nonzero exact learned source work from a physically active L11 ordering
   route into at least two opposing typed vocal motor phases;
2. variable body-owned vocal motor discharge and finite respiratory carriers;
3. new pressure not copied from or simultaneous with tutor ingress;
4. exact cochlear self-return caused by that new pressure;
5. persistence and cold continuation without identity or learned-state loss;
6. unchanged full DSF delivery, zero Python cognition callbacks, bounded
   CPU/RAM/storage/contact/neuron growth, and no surviving local probe;
7. silence for the severed causal control. Other negative controls are run
   only after the positive chain passes.

ECS health, a focused unit test, motor posture alone, tutor sound, or a silent
negative control cannot satisfy this gate.

## Deployment rule

No deployment will be used to discover the next boundary. Deployment is
eligible only after the copied-production-body positive chain passes. Live
success then requires exact task/digest/commit match, preserved identity and
tick continuity, body-owned pressure plus self-hearing observed in production,
healthy AWS custody/resources, checked rollback, and a checked Slack completion
notification. Until then, the truthful verdict remains: speech failed.

## Candidate 53F decisive copied-body result — rejected

The immutable Candidate 53F release binary SHA-256
`f69194c2df82bd09223828e7e403757b11384ce9dbd9298d702922ec89fb56f1`
ran the exact reservoir probe against authenticated copied CURRENT body
SHA-256
`8886267ca22999754189a62fca715eb5b7837954ea98723ac778fdb6cfffb1db`,
using the unchanged 8,000-byte tutor-pressure input SHA-256
`00c6ec35000e84c01ceb41f5349559156ea70ed398614a1c4a14328e3afb166c`,
512 guided cycles, eight sequence repetitions, 1,500 carriers, and 256
post-cue clocks.

The run crossed the earlier tick-520906 stopping boundary and completed the
full bounded guided range. At tick 521162 it stopped after 333.76 seconds on
the same required assertion:

`complete learned sequence retained its first pressure phase`

Result: `FAILED; 0 passed; 1 failed; 608 filtered out`. The complete learned
vocal population still did not form, so sequence training, cold partial cue,
autonomous motor discharge, new vocal pressure, cochlear self-return, and
causal controls did not run. No JSON or WAV was emitted and no probe process
survived. The incomplete teaching cognition is 135,518,876 bytes, SHA-256
`3ce203335fcbe2b64164da1cfd74a643832df204a6a1d7deeac0a4cf2d1da211`;
it is failure evidence only.

S-053F-01 component success: the moved-body eligibility restriction passed
the exact focused regression and reduced the live-body contact frontier from
Candidate 53E's approximate 9,700-contact range to approximately 9,300. That
is evidence that sensory-only duplicate association anatomy was removed. It
is not evidence of speech and must not be rediscovered as a complete repair.

F-038 complete-mechanism failure: Candidate 53F did not close the learned
cue-to-vocal-action chain. It is rejected as the speech repair. No Candidate
53G, cycle extension, input retuning, downstream bridge, or deployment is
authorized by this result.

F-039 process/design-review failure: the pre-build review proved local
occurrence ownership, persistence reuse, boundedness, and absence of direct
motor or Python shortcuts, but it did not prove the complete physical route
from L7 association through L10 ordering, active L11 work, both antagonistic
vocal motor phases, respiratory pressure, and cochlear return on the copied
production body. Calling that review "no source conflict" overstated its
scope. The focused regression likewise proved only association selection.
Permanent gate: no further speech code candidate may be built from a local
causal inference. Before another edit, the existing source and retained
failure evidence must be reviewed as one end-to-end design, with every
required physical transition and its executable falsifier identified before
implementation. Candidate testing then verifies that proved design; it must
not be used to discover the design one late assertion at a time.

F-040 ledger-operation failure: the first closure append targeted a remembered
final sentence instead of rereading the exact current file end immediately
before applying it. The patch was safely refused and changed nothing, but this
repeated F-037 despite its permanent gate. The destination was then reread and
this append used the displayed final two lines. A stated documentation rule is
not evidence that it is being followed; its precondition must be visible in
the immediately preceding operation.

Post-failure production verification at 2026-09-08 remained untouched: AWS
account `418384447921`, task definition `dsf-ai-task:1434`, one PRIMARY
`COMPLETED` deployment, desired/running/pending `1/1/0`, and zero Guala alarms
in `ALARM`. The public lean observation remained available under identity
`1cc4e70a-f2a0-44c5-a111-f4a5bc915cc1`; live/persisted ticks were
522850/522846 with four pending intervals. A checkpoint was outstanding at
that instant but durability was not blocked and checkpoint/cleanup errors
were null. The last unattended occurrence delivered 221 full-DSF records,
physically transitioned 1,436 neurons, used zero Python callbacks, and emitted
no pressure or external/self-heard samples. Production speech therefore
remains functionally failed.
