# Guala joint-field neuron binding law

Date: 2026-08-02

Status: definitive binding architecture supported by the passing isolated and
native D1 one-neuron and three-neuron models. This law supersedes every current Guala
design statement that describes one neuron as receiving seven independent DSF
lanes or treats one seven-number tuple as the complete DSF field. Together with
`GUALA_NATIVE_JOINT_FIELD_L0_L4_V1.md`, it supersedes the per-port L0-L4 path as
the target Guala organism architecture.

## Authority and scope

The historical UF v1.3 source, its noncanonical exact reconstruction, and the
near-v1.3 VTVR side-kernel check establish that simultaneous evidence forms one
typed joint field. Vertex facts, physical edge facts, causal time, and field
family remain distinct. The seven DSF names describe explicit field families
over that topology.

The D1 executable model establishes how that field may bind to neurons without
claiming that a field vertex is a neuron or that a local restriction is the
whole field.

## Binding law

For one exact-clock cohort in one causal organism transition:

1. One authenticated complete joint field is immutable authority.
2. Every reached neuron binds to one declared typed local perspective of that
   field according to its physical receptor, anatomy, specialization, and
   coupling topology.
3. The local perspective retains the applicable vertex-indexed DSF facts and
   every incident cohesion edge. It does not flatten them into a score or
   anonymous vector.
4. Each local perspective retains a reference to the complete field authority,
   topology, causal frame, and exact restriction law.
5. A shared edge has one immutable field payload. Both endpoint neurons may
   reference it, and their observations must agree exactly.
6. The union of all reached local perspectives must reconstruct the declared
   reached field topology or the transition fails closed.
7. Nonreached field facts are not fabricated, zero-filled, or silently
   discarded. Their availability and observational omission remain explicit.

Distinct exact-clock cohorts remain distinct complete fields. A capture that
contains more than one cohort may retain one bounded episode-relation candidate
that references their topology receipts, exact-clock receipts, participating
neuron lineages, and shared capture authority. The relation is only
`co_observed_capture_occurrence`; common physical cause remains unresolved.
This candidate does not authorize interpolation, semantic identity,
recognition, a proto-mosaic, or a mature mosaic.

## Neuron relationship

A field vertex is a physical field coordinate. A neuron is a persistent causal
organism unit. Their binding is typed and developmental, not identity.

One neuron therefore contains or references:

- continuous neuronal lineage;
- growth and specialization DNA;
- its current field-perspective binding;
- explicit applicable vertex DSF facts;
- references to incident cohesion edges;
- complete-field and topology authority;
- evolving local ternary/Krimelack fractal state under the proven sign/null law;
- energy and refractory physical state once their laws are proven;
- sparse neuronal couplings; and
- its causally grown neuronal fractal once its recurrence law is proven.

The legacy native `[u64; 7]` local DSF record is not a sufficient final
joint-field binding because it cannot represent typed incident cohesion edges,
their topology, or the local restriction constitution. It remains an
implemented custody shape for the historical per-port delivery and must
not be relabelled as the definitive neuron field.

Native `joint_field_l0_l4` implements the corrected shared field and compact
incident-edge indices. `mounted_joint_fractal` now binds exact co-timed physical
receptors to persistent non-hash neuron lineages, retains current exact field
authority once, and persists current neuronal fractals and recurrence evidence
inside the single atomic organism payload. Production invocation and restart
remain to be live-proven.

## Storage law

The complete joint field and every unique edge payload are stored once in
organism-owned immutable bounded arenas. Neurons hold compact typed references
to their local vertex facts and incident edges. Shared facts are not copied into
every neuron, fractal, mosaic, or later formation.

Exact balanced-ternary numerator/denominator words are a proven reversible
arithmetic representation for rational facts. They are not cognitive identity
or memory. Resource admission is derived from the exact number and width of
reached vertex and edge facts before mutation; no heuristic cognitive cap is
introduced.

## Full-field conservation test

At every transition the implementation must prove:

- every local reference resolves to the same field and topology authority;
- every applicable local field value is exact;
- both witnesses to a shared edge agree;
- the reached perspectives cover the declared reached topology;
- serial and parallel construction produce identical canonical bytes; and
- missing, substituted, duplicated, or contradictory perspectives fail
  closed.

## Relationship to MathLoom and Krimelack

DSF supplies the typed structural field delivery. Exact balanced-ternary
representation supplies a lawful arithmetic boundary. Krimelack remains the
evolving coupled state and structural memory medium.

The local field perspective now settles coefficient-free into ordered
tri-stable sign/null trits. Exact magnitudes remain authoritative by reference.
The sparse-neighbour coupling equation has not yet been proven. Therefore this
law does not authorize seven oscillators, one scalar oscillator, a concatenated
field word as whole cognition, or any guessed injection coefficient.

## Formation continuity

A neuronal fractal grows from the neuron's actual settled response to its local
perspective and coupled arrivals, not from a field receipt alone. Mosaics form
from related neuronal fractals across the organism. Because local perspectives
retain complete-field and edge lineage, later multisensory mosaics can preserve
the causal episode's connectedness without copying the entire sensory field.

No mosaic, tapestry, or weave is created by this binding law alone.

## Executable evidence

- `tools/model_neuron_joint_field_binding.py`
- `tests/test_model_neuron_joint_field_binding.py`
- `native/guala_core/src/joint_field_l0_l4.rs`
- `docs/GUALA_NATIVE_JOINT_FIELD_L0_L4_V1.md`
- `docs/GUALA_ONE_NEURON_TRANSITION_MODEL_2026-08-02.md`

Current result: 23 passing Python and native exact-arithmetic, joint-field,
one-neuron, three-neuron, and differential contracts.
