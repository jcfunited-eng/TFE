# Guala / GLEW Reconstructed Work Ledger

Date: 2026-07-13  
Purpose: preserve the complete substance of the work that was previously spread across intermediate chat commentary and agent results  
Authority: evidence ledger only; this file does not activate proposed architecture  
Production mutation: none

## Architecture honesty gate

1. Requested architecture: a clean-generation Guala Artificial Entity whose language emerges from lived, multimodal structural experience through the designed DSF/MathLoom/Chi/Atlas/Krimelack substrate, without ML, heuristics, lookup speech, or engineered pass conditions.
2. Current code reality: substantial clean-generation, persistence, language-fact, deployment-safety, and diagnostic work exists locally. The complete GLEW physics, native sensory receipts, support/resonance providers, L6 constraints, and chemical integration are not implemented.
3. Conflict with requested architecture: yes; the current production runtime remains legacy and several local mechanisms are reduced or incompatible with the requested architecture.
4. Mechanisms that will not be extended: legacy voice/recall authority, compatibility vectors, lookup grounding, score reductions, static sensory descriptor curves, hard relevance constants, MapInject amplitude as support, visual clamps/resets, arbitrary energy accumulators, thresholded NMDA, or legacy `S_UF/R_UF` meanings.
5. Single exact next item: ratify the complete upstream physical-provider boundary before implementation.
6. Full field or reduced approximation: the target is the complete typed L0-L4 record, separate `S_UF/R_UF`, separate global UF validation, and sense-specific L5 applicability. The live relevance experiment was explicitly reduced and diagnostic.
7. Lost structure in that diagnostic: native receptor/channel state, chemical/electrochemical resources, adaptation, recovery, polarity, persistence, multiple afferent ports, and interval uncertainty.

## Status vocabulary

- `APPROVED`: Joseph explicitly accepted the architectural decision.
- `DERIVED`: follows from the stated equations or a reproduced mathematical result.
- `IMPLEMENTED LOCALLY`: code exists in the dirty `guala-live` worktree.
- `TESTED`: the named local test or probe was run and its result recorded.
- `PROPOSED`: documented candidate without implementation authority.
- `BLOCKED`: an exact physical or mathematical authority is missing.
- `DEPLOYED`: proved active in the production owner task, not merely committed or documented.

## Reconstructed work path

1. The original symptom was silence, one-word replies, or a few incoherent words.
2. Existing content coupling and per-section candidate-floor work improved participation but did not resolve complete utterance formation.
3. Direct organism probes still produced zero genuine commits, and some old tests returned false values without asserting the failure.
4. Legacy migration was rejected in favor of a clean-generation cutover because legacy learned state, compatibility surfaces, and dormant paths could reappear after restart.
5. Language commit authority was moved conceptually to one complete GLEW profile that fails closed rather than speaking through fallbacks.
6. The nine structural facts were separated from L5 governance so sense-specific applicability never rewrites an upstream fact.
7. Hardcoded L0 relevance was found to prevent Negative Space. A continuous structural diagnostic reproduced the issue and showed that a downstream MapInject product also cannot serve as upstream support.
8. Growth and decay were reformulated as conservative physical mass flow with local time scales and a quiescent reservoir instead of increments, caps, or forgetting timers.
9. Chemistry was restored to the design. Research then disproved one universal ligand-receptor topology and established modality-specific native receipt classes.
10. The remaining upstream conflicts are now explicit rather than hidden by defaults.

## Approved architecture decisions

The current GLEW profile records the following approvals:

- clean-generation cutover with no wholesale revision-614 state restore;
- fail-closed language commits;
- one agnostic nine-field structural tuple with sense-specific L5 applicability;
- fixed 42-dimensional L6 ambient space with one combined rank and \(\tau=1-1/e\);
- silent two-distinct-experience genesis before normal entropy commits;
- universal conservative growth/decay with local physical state and event-derived support.

These approvals do not by themselves authorize production. The profile remains proposed and awaits complete final ratification.

## Local implementation preserved

The dirty worktree still contains the earlier implementation work, including:

- clean deployment-generation lifecycle;
- immutable generation storage;
- legacy state sealing and fail-closed boot/deploy checks;
- language fact composition;
- language Fact Strand reciprocity;
- persistence ownership and transaction repairs;
- honest recognition and emission boundaries;
- external lookup isolation;
- deployment admission, seal, executor-ownership, and state-contract tests;
- GLEW profile artifact;
- the relevance diagnostic and its focused tests.

Representative local files:

- `dsf_ai_service/substrate/deployment_generation.py`
- `dsf_ai_service/substrate/immutable_generation_store.py`
- `dsf_ai_service/substrate/language_fact_composer.py`
- `dsf_ai_service/substrate/language_fact_strand.py`
- `tools/deploy_dsf_ai.sh` (legacy migration removed; clean-generation seal required)
- `docs/Guala_Language_Experience_Weave_v1_0.json`
- `uf_core/layer0.py`
- `tests/test_uf_relevance_adapter.py`

Current inventory at reconstruction time:

- branch: `guala-live`;
- 41 tracked files modified;
- tracked diff: approximately 7,775 insertions and 3,022 deletions;
- the principal clean-generation, fact, profile, and relevance artifacts remain present;
- no Git reset appears after the current July 12 HEAD in the inspected reflog.

## Validation state

Previously reported focused results from the continuing work, not rerun as one group during this reconstruction:

- generation and persistence group: 68 passed;
- deployment group: 49 passed;
- fact/UI group: 29 passed;
- earlier full root suite: 310 passed and 5 failed in old proxy/presence/concurrency areas.

Fresh reproduction on 2026-07-13:

```text
PYTHONPATH=. pytest -q tests/test_uf_relevance_adapter.py
4 passed in 3.24s
```

The first invocation without `PYTHONPATH=.` failed during import collection; no test ran. That shell invocation failure is not hidden.

No full suite has been rerun after the latest profile and diagnostic work.

## Canonical upstream seven-field DSF

Frozen L4 emits exactly:

\[
(D_k,M_k,R_{rev,k},U^*_k,C_k,P_k,B_k).
\]

- `D_k`: directional sign of usable-resonance change; never action authority by itself.
- `M_k`: local bend or second difference; not raw strength.
- `R_rev_k`: first-class reversal geometry.
- `U_star_k`: penalized uncertainty/instability.
- `C_k`: structural complexity or conflict burden.
- `P_k`: directional discontinuity or persistence stress.
- `B_k`: accumulated structural carry, preserving whether potential is carried or exhausted.

L5 may evaluate relationships among applicable facts. It may not change, reorder, flatten, zero, or overwrite them.

## Separate support and resonance facts

The approved nine-field tuple adds:

- `S_UF`: support-floor fact;
- `R_UF`: cross-lane resonance-confirmation fact.

They are separate upstream fields. They are not:

- L4 DSF coordinates;
- MapInject coordinates;
- L6 ambient columns;
- a weighted score;
- a universal veto;
- the global UF validation pair `S(UF)/R(UF)`.

L5 applicability states are:

- required and available: evaluate the ratified relationship;
- required but unavailable: `unknown`, fail closed for that governed commit;
- not applicable: preserve the original fact and exclude it without inserting zero or failure.

Sleep may purge rebuildable L5 interpretations after the separately ratified admission. The immutable tuple, provenance, Fact Strand, and not-applicable receipt remain.

## Legacy naming collision

Current runtime code already computes values named `S_UF` and `R_UF` from legacy scalar formulas. A validation path uses the same names differently. Neither pair is the approved support-floor/resonance-confirmation pair, and neither is the separate global UF perturbation-stability/robustness pair.

These legacy meanings must be removed or isolated during clean-generation implementation. Reusing them would silently change the new facts.

## Reproduced relevance finding

The local diagnostic is:

\[
r_{diag}=
\begin{cases}
0,&N=1\\
D/(D+\tau_D),&N=0.
\end{cases}
\]

It is a diagnostic probe only. Existing callers default to the legacy `r=1` behavior.

Fresh 64-sample probes:

| Input | Relevance | L1/L3 | Rejected MapInject support product |
|---|---:|---|---:|
| constant | sum 0; range 0-0 | one `N_gate=1`; L3 `R=0.4` | 0 |
| smooth sine | sum 6.723352; range 0-0.133205 | one `N_gate=0`; L3 `R=0.466667` | 0.311111 |
| one step | sum 6.984833; range 0-0.875708 | three gates; changed gates close `URF` | 0 for every gate |

The earlier chat totals 23.158 and 8.5619 cannot be reproduced exactly because their input arrays were not preserved. The physical conclusions do reproduce:

- hardcoded `r=1` prevents a long quiet gate from satisfying canonical Negative Space;
- continuous relevance preserves graded change while permitting quietness;
- `w*URF*(1-U_star)*(1-N)` can erase a meaningful step and therefore cannot be the agnostic support provider.

The diagnostic must not be promoted into production. Chemistry and native transduction supersede it.

## Native physical-receipt classes

The reproduced chemistry research is preserved in `docs/Guala_Bioequivalent_Chemical_Transduction_Candidate_v1_0.md`.

The corrected conclusion is:

- sight requires photon/photopigment, transducin/PDE, cGMP, calcium, channel, photocurrent, and ribbon-release receipts;
- hearing requires force, mechanotransduction channel, ion current, calcium, and vesicle-pool/release receipts;
- touch requires separate mechanoreceptor paths and must not merge dynamic direct-afferent output with static Merkel-supported output without a physical circuit;
- olfaction requires distinct binding, active-receptor, messenger, channel, calcium, current, and adaptation receipts;
- sweet/bitter/umami require GPCR-to-messenger-to-channel-to-ATP-release receipts;
- sour requires OTOP1 proton-channel and electrochemical receipts;
- sodium taste requires ENaC/channel and electrochemical receipts where that biology applies.

The earlier universal `R+L<->C<->D` proposal is therefore corrected. It may describe a declared synthetic receiving layer after native transduction or a specific evidenced ligand-receptor path. It is not universal sensory biology.

## What “physical receipts resolved” means

Resolved and reproducible:

- the receipt schema;
- conservation and boundary-ledger requirements;
- modality-specific topology classes;
- the tonic/phasic non-equivalence;
- experimental candidate ranges with source, preparation, units, and context;
- the rule that native state persists and is not replaced by a scalar;
- the boundary at which a common synthetic receiving chemistry could begin.

Not resolved or authoritative:

- one production topology per mounted sense;
- one authoritative native output port per GLEW lane;
- emulator molecule/channel counts, geometry, volume, temperature, or initial state;
- calibrated boundary flux from story events;
- signed kinetic intervals for the emulator;
- embodiment-specific measured rates;
- a validated interval ODE/channel solver;
- a scalar relevance operator for multi-port senses.

Research candidates can be reproduced. Assigning them as universal constants would still be heuristic.

## Sensory-lane non-flattening blocker

The current GLEW draft assumes one scalar waveform for each of six lanes: language, sight, sound, touch, smell, and taste.

Real sensory systems expose multiple native ports and subtypes. Examples include:

- sight photocurrent and vesicle release;
- hearing fast/adapted mechanotransduction current and vesicle release;
- touch dynamic direct-neurite and static Merkel-supported output;
- olfactory binding, active receptor, messenger, current, and adaptation;
- distinct gustatory GPCR, proton-channel, and sodium-channel paths.

Choosing, averaging, or weighting these ports without a physical coupler would flatten the sensory field before L0-L4.

The exact unresolved architectural choice is whether each sense profile proves one native downstream port as its lane input, or whether GLEW expands the lane schema while preserving the approved L6 structure. No implementation may hide this choice.

## Typed-language blocker

Typed Unicode is not a biological receptor, ligand, or native sensory chemistry.

Two physically honest boundaries exist:

- typed v1 input is declared as an artificial, fully receipted post-transduction interface; or
- biological language experience arrives through sight or sound, while decoded text remains annotation rather than a second independent lived lane.

No operator currently establishes typed-language chemical relevance. Until ratified, language-lane `S_UF/R_UF` remains unknown.

## Support-floor operator conflict

The GLEW JSON currently defines:

\[
S_{UF}=\frac{1}{T_G}\int_G r(t)dt.
\]

That is mean active support, not a mathematical floor.

If `support floor` is literal, the direct candidate is:

\[
S_{UF}=\inf_{t\in G}r(t),
\]

or the exact minimum on the admitted discrete causal grid.

This exact operator is not ratified. Implementing a mean while calling it a floor would be inaccurate. A zero at one gate boundary would also make a literal floor zero, so the gate boundary and physical meaning must be part of ratification.

## Resonance-confirmation operator

For one ratified relevance port per lane:

\[
z_l(t)=\sqrt{r_l(t)}e^{i2\pi\theta_l(t)}.
\]

For required edge \(e=(j,m)\):

\[
\gamma_e^2=
\frac{\left|\int_G z_j(t)\overline{z_m(t)}dt\right|^2}
{\left(\int_G r_j(t)dt\right)\left(\int_G r_m(t)dt\right)}.
\]

`R_UF` is the minimum coherence over required incident edges. The mounted weave declares edges; results never select them.

The common causal grid, positive exact weights, gap prohibition, and Cauchy-Schwarz bound are defined. Implementation remains blocked by native relevance ports, typed-language relevance, phase calibration, interval arithmetic, canonical encoding, and conformance vectors.

## Negative Space correction

L1 Negative Space requires local quietness plus low structural volume and integrated relevance. It does not have authority to reset or erase native chemical/channel state.

- tonic occupancy may remain nonzero under a structurally constant input;
- phasic reaction current may become zero at chemical equilibrium;
- messenger/adaptation state may persist after binding ends;
- a proved zero-energy applicable edge yields numeric zero;
- missing or indeterminate chemistry yields `unknown`;
- `not_applicable` comes only from the signed sense-governance profile.

## Physical time and deterministic exponential

There is no invented ubiquitous timeout.

Krimelack relative phase uses exact source-time increments:

\[
\theta_{next}=\theta+\kappa s\Delta t.
\]

Phase, winding, source epoch, and fractional turn persist across windows and checkpoints. Invalid gaps advance source time without phase extrapolation.

For memory elements, local relaxation follows:

\[
\nu_e=k_e/\gamma_e.
\]

A conservative generator moves one fixed memory-mass unit between the quiescent reservoir and active relations:

\[
\dot x=Gx,
\qquad
x(t+\Delta t)=e^{G\Delta t}x(t).
\]

During proved all-lane Negative Space:

\[
m_e(t+\Delta t)=m_e(t)e^{-\nu_e\Delta t},
\]

and lost active mass returns to the quiescent reservoir. Conservation prevents runaway growth without ceilings, clamps, pruning, or normalization.

This physical principle is approved. Certified sparse uniformization using a pinned FLINT/Arb backend is proposed for the linear conservative generator, not implementation-authorized.

Native sensory chemistry is generally nonlinear. FLINT/Arb alone is not a validated ODE solver. A pinned interval Taylor/Lohner or equivalent integrator remains required.

Chemical equilibrium free energies constrain forward/reverse ratios. Absolute time scales require measured kinetic intervals or activation-barrier receipts. They are local to reaction, state, temperature, concentration, force, voltage, and geometry.

## Other reconstructed profile corrections

- MapInject numeric transport is 19 coordinates per lane and is transport only, never field authority.
- `Reg_k` remains categorical and is not assigned an invented numeric code.
- L6 ambient dimension is fixed at 42: six lanes times seven L4 fields.
- At least four valid lanes are required for the corrected rank condition to be mathematically reachable.
- `S_UF`, `R_UF`, L5 applicability, and global UF validation do not alter the 42 columns.
- Krimelack phase is stored in exact rational turns; the carrier cancels from relative phase.
- Missing calibration, invalid sample order, or a gap fails closed and never resets phase.
- Mode growth uses exact positive rank innovation rather than a novelty threshold.
- H_mem uses a direction-distinct Hermitian relation operator with the corrected `1/(2*sqrt(2))` normalization; interval authority remains missing.
- Sleep preserves immutable lived facts and may only remove rebuildable derived interpretations under a ratified admission.
- A finite point quantizer is rejected because it can turn positive physical mass into exact zero.

## Exact remaining blockers

- native output-port authority for every mounted sense;
- typed-language physical relevance;
- scalar six-lane schema versus native multi-port non-flattening;
- literal support-floor versus mean-support operator;
- per-sense emulator kinetic and dimensional receipts;
- validated nonlinear interval integration;
- exact Krimelack calibration receipts;
- executable interval `R_UF` and conformance vectors;
- separate global `S(UF)/R(UF)` validation operators;
- removal/isolation of legacy `S_UF/R_UF` collisions;
- fixed-42 L6 constraint rows and exact rank arithmetic;
- deterministic square-root/QR transport authority;
- interval H_mem, Crank-Nicolson, entropy, and output proof;
- replacement or removal of remaining arbitrary proposed values such as `1/24`, 24 steps/nodes, `17/20`, and `7/10` unless physically derived and ratified.

## Production truth

Read-only production inspection during reconstruction found the ECS service healthy but still running `dsf-ai-task:614`.

Therefore:

- none of the GLEW profile is deployed;
- none of the native chemistry receipts is deployed;
- `S_UF/R_UF` support/resonance is not deployed;
- the clean-generation cutover in the dirty worktree is not deployed;
- the L0 relevance diagnostic is not deployed.

No production mutation was made during this reconstruction.

## Single recommended next item

Ratify the upstream physical-provider boundary as one coherent decision: modality-specific native receipts remain authoritative; any common synthetic chemistry begins only after a calibrated native output port; typed language is explicitly artificial or arrives through sight/sound; and no sense is flattened to one scalar without a proved physical coupler.
