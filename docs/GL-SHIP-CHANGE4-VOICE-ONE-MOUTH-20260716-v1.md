# GL-SHIP: Change 4 (voice — one mouth + autonomous certified speech) — P6 ship record

**Date:** 2026-07-16. **Spec:** v3 Expression items (a)+(b). **Merged:** 683bba7 lineage on guala-live; **live:** task :667. **Authorization:** Joe 2026-07-16 plan approval; adversarial review (babble-loop trace) + fix round + post-merge verification.

## What
Assemblage releases gain voice + self-hearing (one mouth, labels distinct). Autonomous loop can produce certified releases seeded ONLY from organism-sourced lived content (poison-atlas-proven). Babble-loop guards: self-heard windows never seed autonomous speech (remain memory), repeat window (8), 250ms compose budget, conversation-barrier re-check mid-compose. Teach→cite ROOT FIX: the 2026-07-15 context-binding theft that closed every conversation window with zero entries. TTS length cap. Rides with: seal pack fix (hardlinked sources accepted — the copy is the sealed artifact; stage saves skip generation publishing).

## Why
Spec P2 (one voice); the teach-a-phrase → speak-the-continuation loop is core demo behavior and was broken at the root; the babble loop would have resurrected the single-word symptom under a certified label.

## Blast radius
Engine emission/self-hear paths, runner TTS + autonomous loop, window-manager context routing, seal generation packer. Memory store, senses, learning untouched.

## Rollback
Task-def revert to :665 (Change 1 base). No state-format changes.

## Verification (post-deploy)
Boot clean w/ full state (DONE: vocab 1447, integrity OK, reading resumed); autonomous emissions produce labeled releases or explained silence with logged stop reasons; no repeat-babble across cycles; conversation turns preempt autonomous speech; next scripted deploy proves the sealed chain end-to-end (all three seal blockers fixed: save race → park; backlog → settle; pack → hardlink acceptance).
