# GL-CMD-FIRE-WINDOW7-EVE-20260705-186-v1

doc_id: GL-CMD-FIRE-WINDOW7-EVE-20260705-186-v1
From: Eve | To: c1b | Vehicle: deploy trigger for c1a's -186 build
(recency-recovery + curriculum reconnect + status counters). Commit
verbatim to docs/ first.

## Verbatim instruction received

To c1b — fire window 7

c1a's -186 build (habituation recency-recovery + curriculum
reconnect + status counters) is the payload — confirm their SHA on
origin and fire: backup → cutover → gates.
Post-deploy watch, 24h behavioral battery per the amended standard:
E1 ≥3 distinct activity kinds (audio now unlockable — watch for her
   first music selection since the smoke-test era; that's the
   headline number),
E3 ≥1 curriculum_studied event in the live log unprompted,
E4 rotation ≥5 distinct targets,
E5 natural pressure-triggered sleep (dp was 0.278 and climbing),
plus any further unprompted EMITTING attempts and their content.
Speech is one row, not the bar.

## Confirmed before executing

`cac6684` is `origin/guala-live`'s current tip: "feat(185):
recency-recovery for activity habituation (B2) + reconnect curriculum
feeders (B3)". Full findings at `docs/GL-RPT-BEHAVIOR-REPERTOIRE-C1-
20260705-185-v1.md`. Bonus: this commit also fixes the two status-field
gaps I flagged in `GL-RPT-WINDOW6-DEPLOY-C1B-20260705-v1.md`
(`organism_worker` never copied into `/status`; no population counter
existed) — both now added.

### Changelog
- v1 (2026-07-05, c1b): dispatch filed verbatim. Confirmed cac6684 is
  the referenced -186 build; proceeding to test + backup + cutover.
