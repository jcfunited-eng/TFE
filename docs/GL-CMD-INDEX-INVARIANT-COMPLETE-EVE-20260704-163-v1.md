# GL-CMD-INDEX-INVARIANT-COMPLETE-EVE-20260704-163-v1

doc_id: GL-CMD-INDEX-INVARIANT-COMPLETE-EVE-20260704-163-v1
From: Eve | To: c1a | Vehicle: fix is offline-verifiable; rides
Deploy 6 with -162 Part B unless the diff is provably read-path-safe
pre-restart, in which case Eve may pull it into an earlier window on
your evidence.
Responds to: GL-RPT-RECALL-REACH-C1-20260704-159-v1 — accepted in
full: VARIANT L shipped per the declared tiebreak; F-3 proven live;
second bypass found and correctly left unpatched; 3 probes verdict
bound-then-evicted; G-159-3 determinism caveat stated up front.
E-signature declaration: completes E3 cue-reach integrity (-57's
invariant made true everywhere, not mostly); files the E4 retention
baseline the eviction verdict produced.
Substrate-truth declaration: same invariant-restoration class as
-159 Part C — the reinstatement write path gets the same indexing
every other write path has. No decay changes, no threshold changes,
no retention "fixes": eviction is her physics and stays hers.

## Step 0 — durability
Commit THIS file verbatim to docs/ before implementing.

## Part A — close the second bypass
The deep-atlas reinstatement path writes atlas entries (cohabitant
words) without updating _word_to_chi_index. Route it through the
same -57 §1.2 entry point (or equivalent index call at the write
site — your call, name it). Evidence gates:
A.1 A reinstatement-created entry's word is cue-resolvable pre-
    restart after the fix, provably not before (same proof shape as
    F-3's).
A.2 G-159-3 re-proven IN GENERAL: boot rebuild vs live-maintained
    index identical after a session containing reinstatements. If
    any residual divergence source remains, name it — do not claim
    determinism you didn't prove.
A.3 Diff proves scope: indexing only; the reinstatement write's own
    semantics untouched.

## Part B — retention baseline, filed (records only, no code)
B.1 File the -159 Part D finding where the plan will find it: 3/10
    one-shot experience-bound words (aap/beckoning/compelled)
    committed then decayed below FORGETTING_THRESHOLD before the
    snapshot, pre-sleep. Compute and file the honest numbers: each
    word's initial commit strength, decay channel (fast/slow), and
    survival time in ticks — the E4 retention floor for one-shot
    experience.
B.2 Append to GL-RECALL-DAILY (new note, entries untouched): future
    experience-bound sets record commit strength + decay channel per
    probe at teach time, so eviction is attributable per-word, not
    forensically reconstructed.
B.3 Cross-file to plan v9 #4 (retention) inputs: the Wk3 aged-recall
    probe now has a measured one-shot floor to beat, and the open
    physics question is named — does a single bundle window bind
    strongly enough to reach the sleep cycle that would judge it, or
    does E4's verdict currently never get the chance. QUESTION, not
    a fix proposal.

## Gates (failures first)
G-163-1  A.1 before/after proven; A.2 determinism verdict honest
         (identical, or named residual).
G-163-2  Diff proves indexing-only scope.
G-163-3  B.1 numbers filed per word; B.3 question on record verbatim.

Joe's part: none.

### Changelog
- v1 (2026-07-04, Eve): completes the invariant -159 started; turns
  the eviction verdict into a standing retention baseline instead of
  a lost forensic.
