# W1 causal run-envelope falsification — 2026-07-27

## Decision

Do not implement or wire the tested causal run-envelope as production
hearing. It failed all three source-disjoint held-out speaker challenges.

## Architecture honesty gate

1. Requested architecture: a grounded THING's auditory mosaic must expand
   with repeated experience like a tolerance-bearing “smoky blob,” while
   retaining an increasingly definite causal thread.
2. Current code reality: the isolated probe grows exact extrema, learned
   duration intervals, and transition topology for all 656 bilateral
   full-field/physical lanes.
3. Conflict with requested architecture: yes. The basin genuinely grows,
   but it does not relate unheard speakers strongly or selectively enough.
4. Mechanisms not extended: fixed/static word templates, transcript
   identity, scores, nearest exemplars, developer tolerances, majority
   voting, chi, psi, and context clamping.
5. Single exact next item: do not promote this law; the remaining missing
   authority is a nonlinear speaker/articulation invariance not supplied by
   observed extrema and learned tempo alone.
6. Field evaluation: every explicit pressure/phase D/M/R/U/C/P/B lane and
   all five exact bilateral relations per cochlear channel participate.
7. Reduced approximation: monotonic runs retain extrema, order, transition
   topology, and learned duration, but discard within-run curvature and
   multiplicity. The original exact trajectories remain source authority.

## Why this was dynamic mosaic growth

No exemplar was queried. Each source-disjoint grounded occurrence mutated
one retained causal basin:

- run-node value extrema expanded only to newly observed exact Fractions;
- run-duration bounds expanded only to newly observed causal durations;
- start/end states and ordered transition edges expanded only when lived;
- each new speaker could enlarge the admissible region without changing an
  earlier source record or introducing a tolerance.

A query traversed the grown transition graph and had to remain inside every
learned run envelope. Canonical L6 evaluated the 656 lane constraints.
All three THING basins were evaluated in parallel. Multiple locks would have
been ambiguous; no lock was unknown.

This is a learned admissible hull, not exact template equality. It is also
not a score: the only terminal law was canonical L6 certainty collapse.

## Result

| Held-out grounding | Correct contained lanes | Best competing contained lanes | Decision |
|---|---:|---:|---|
| `down` | 248/656 | 258/656 | unknown |
| `go` | 263/656 | 240/656 | unknown |
| `left` | 250/656 | 254/656 | unknown |
| unrelated tone | at most 210/656 | — | unknown |

Canonical L6 requires effective dimensions below 242. Every speech relation
remained between 393 and 421 effective dimensions. The `down` and `left`
queries also had a competing basin contain more lanes than the correct
basin, so weakening the gate would not lawfully repair the result.

## Authority

- corpus SHA-256:
  `49650f2341b26d886b46b3f4fb8fed59e30300b17550f1ee4a768b3106cf93a0`;
- report:
  `/tmp/w1_causal_run_envelope_walkup.json`;
- report SHA-256:
  `141bc9ab429ed0f9e23a5fc4261e99a611e4e5a428ee93f5f40c52916542e691`;
- embedded authority:
  `f240825d433136017c2ac1641873a7c5317fb1fa8c4a84dd34a46b3d6f369885`;
- learned memory authority:
  `2897b74c6a900b1ac6bb379f2d68a00f3bf6000e4517eed0e480a78b124d9376`;
- probe:
  `tools/probe_w1_causal_run_envelope_walkup.py`.

## Production truth

No L0-L4, engine, live route, persistence, conversation, or deployment path
was changed by this failed candidate. Human-like hearing remains unresolved.
