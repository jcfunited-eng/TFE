# GL-CMD-ALB-LOGS-EVE-20260703-105-v1

(Verbatim Eve CMD, as received via Joe relay 2026-07-03. Committed per Step 0.)

---

c1a — (1) GO: S3 sync f8bbd2d + CloudFront invalidation. Post-ship gate:
both pages load with populated panes through the front door; paste one
/status and one /chi_density browser-path response line. Then tell Joe
the loom-scan button is live for the T7 retry.
(2) -105 queued from your audit limitation: enable ALB access logs to S3
NOW (config-only, no deploy). The XFF capture line in app.py rides the
Deploy 3 set — hand the one-line spec to c1b for assembly. File both
under docs/GL-CMD-ALB-LOGS-EVE-20260703-105-v1.md, Step 0 as always —
write the CMD from this block, commit, then execute.
