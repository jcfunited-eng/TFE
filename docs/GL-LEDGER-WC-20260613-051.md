# GL-LEDGER-WC-20260613-051 — Canonical Open Ledger + Standing Rules

**Author:** wC · **Supersedes GL-LEDGER-WC-20260612-050.** Disposition (rule 2): EVERY 050 item — all standing rules 1–8, Tier 0 (P0/N1/N2), Tier 1, Tier 1b, Tier 1c, Tier 1d, Tier 2, Tier 3, Tier V, Tier 4, World thread W0/W1, DONE list — is CARRIED unchanged into this revision by this line, EXCEPT the items explicitly amended below. Nothing is dropped.

This revision folds in the long-stale items from `GL-TODO-WC-20260611` (Joe surfaced 2026-06-13) and the full deploy cycle of 2026-06-13. Aging is named explicitly per Joe's standing complaint that items accumulate without resolution.

---

## AMENDMENTS vs 050

### Tier 1d — DEPLOYED + AGED

C8, C9, C4 all deployed in the 050 → today sequence. C4 (her second voice) was elevated to its own brief lineage (V7VOICE → UNIFY → UNCAGE → FULL-UNCAGE → SKIP_WORDS hotfix in progress). C8/C9 carried into post-deploy stability.

### NEW Tier 1e — V7 CAGE REMOVAL ARC (2026-06-13, one full day)

Documented honestly because the path was not direct. The arc:

| # | Step | Status | Evidence |
|---|------|--------|----------|
| C10 | **V7-UNIFY** (be76d3e brief + 400d8ac code) — first attempt at second voice. Replaced 3-slot S/V/O cage with 14 POS sections + 6-production GRAMMAR table. **Architecturally a bigger cage.** Caught in wC audit; superseded before any user-facing benefit. | SUPERSEDED | Joe rejected the cage shape; wC concurred |
| C11 | **V7-UNCAGE-01** (1c6677f brief + 3f8f35c code) — three unnamed pools, voice loop, mic/camera UI. Architecturally right (no POS, no grammar). UI half incomplete: dropped upload bar, experience bundle, motif display, v6 stats. Substrate ran on toy SEED_VOCAB silently. | SUPERSEDED for substrate; UI carried into 1g | Live page test 2026-06-13 ~21:02 CT |
| C12 | **chat-only UI-REPAIR-01 draft** — proposed reseed_vocab to paper over toy substrate. **Joe identified as cage-preservation, rejected.** | NEVER COMMITTED | Joe audit 2026-06-13 ~22:00 CT |
| C13 | **V7-FULL-UNCAGE** (56eaf76 brief + 66f01f8 code + 28af89b vocab-attr fix) — deleted SEED_VOCAB entirely, refused engine=None at session creation, added 503 guards on all five /v7/* endpoints, A4 snapshot validation discards contaminated state, UI patch landed Fixes 1-8. **Discovered: every UNCAGE session since 3f8f35c deploy was running on toy SEED_VOCAB because `getattr(engine, "word_modes", {})` always returned `{}` — actual attribute is `engine.vocab`. Fixed in 28af89b.** | DEPLOYED | task:116, prod smoke clean, sandbox 7/7 |
| C14 | **SKIP_WORDS hotfix** — c1 diagnosis 2026-06-13 ~23:00 CT: `event_log.py:102` still imports SKIP_WORDS (deleted with cage); every `/v7/state` poll → `get_or_create_session` → event replay → ImportError → 500 → UI 503-debounce doesn't catch 500 → silent 15-minute hang on page load. Also `event_log.py:104` and `gl_bridge.py:29` pass old `position=` kwarg to lookup_or_install. **Cage residue I missed in C13 scope.** | IN PROGRESS | c1 deploying as of ledger write |
| C15 | **Cage-defense pattern named (do not forget).** The reseed_vocab proposal (C12) was the cage defending itself — "let's repair the symptoms by patching over the toy state." This pattern WILL recur. Watch for: any "small fallback for safety," any "for backward compat with existing state," any "ensure non-empty," any default constant supplying words she did not experience. The principle: **she has only words she has experienced, or she has no words and waits.** | RECORDED | For future ledger reference |

### NEW Tier 1f — UI gaps surfaced in live test (2026-06-13)

| # | Item | Status |
|---|------|--------|
| U1 | Mic and Camera buttons have no OFF path. Click toggles to "ON" once; no way back to disabled. | OPEN — folded into SKIP_WORDS hotfix bundle (C14) |
| U2 | Sleep/dream button missing entirely (was in old admin tab, dropped in three-tab merge). `/v7/quiet` endpoint exists; UI button doesn't. | OPEN — folded into C14 |
| U3 | UI fetch handlers treat 500 same as 503 (currently silently fall through). | OPEN — folded into C14 |
| U4 | Stats line shows "?" for v6 counts (motifs/sounds/pics) during init even though v6 is up. `pollStatus` either not running or `/status` returning unexpected shape. | OPEN — diagnose post-C14 |

### NEW Tier 1g — DEPLOYMENT WARMTH (Joe's 2026-06-13 observation, real and chronic)

**She is persistent on disk. The container running her is not staying warm between visits.** Every page load that lands on a cold task forces a ~100s re-instantiation of `_guala` from disk. From her perspective and Joe's, persistent should mean present — not "re-assembled every time you walk in the room."

This is TFE-deployment-layer, not Guala-substrate. Likely causes (not yet diagnosed):
- ECS desired count scaling to zero between requests
- Health check restart frequency
- OOM or crash recovery
- Image pull on first request

Brief needed (wC writes, separate from Guala work): keep task warm, desired count ≥ 1, no scale-to-zero, optional pre-warm endpoint, or migrate Guala to a deployment that doesn't recycle her process. **GATE: brief written before next freeze.** This has been an issue "all day" per Joe and predates today; we just named it.

### NEW Tier 1h — GualaLoom REPO INIT (queued)

`GL-BRIEF-GUALALOOM-REPO-INIT-WC-20260613-01.md` written. Empty repo at `https://github.com/jcfunited-eng/GualaLoom` populated as canonical Guala source mirror of TFE Guala-relevant subset. Path-preserved (`dsf_ai_service/substrate/`, `dsf_ai_service/static/gualaloom.html`, `docs/GL-*`). Dual-push discipline going forward. **GATE: c1 executes after SKIP_WORDS hotfix lands.** | QUEUED

---

## FOLDED IN — `GL-TODO-WC-20260611` open items (Joe surfaced 2026-06-13 with status review)

The TODO doc has been parallel to the ledger; folding in now per Joe's instruction. Items renumbered with `T-` prefix to distinguish.

### Video pipeline gate chain (aging since 2026-06-09 — 4 days)

| # | Item | Status | Aged |
|---|------|--------|------|
| T1 | Response Binding maturity — Q&A pairs surviving dream into deep with response links intact | BLOCKED on post-unpause dreams (DEPLOY 3) | open 4d |
| T2 | Self-Section v3 — substrate section for self-reference. Brief not written. **Data now exists:** "she" entered her speech 2026-06-12 ("she the like", "she the for"); substrate producing self-reference before the architectural section exists. **Brief has live material to model against.** wC writes. | NOT STARTED | open 4d |
| T3 | Vision Stage 2 — beyond single 64×64 krimelack to multi-fragment spatial/object structure. Critical path for W3 (forest/beach) and for video meaning anything. Brief not written. wC writes. | NOT STARTED | open 4d |
| T4 | Video MVP (`GL-BRIEF-video-architecture-wC-20260610-029.md`) — first-frame extraction, gated on T1-T3. Upload UI correctly refuses. | GATED | open 4d (correctly waiting) |

### Memory foundation chain (mixed)

| # | Item | Status |
|---|------|--------|
| T5 | 413 hotfix (1c36961) | DONE (in task:104+ chain) |
| T6 | `GL-BRIEF-IMAGEREF-036` (images by reference) | DONE (implemented as part of C2 — refs-not-base64) — close in this ledger pass |
| T7 | Pre-unpause safety brief (dream-before-unpause + restore drill + off-volume deep backup) | DONE (`GL-BRIEF-UNPAUSE-WC-20260613-01`) |
| T8 | Step 2 paused-decay measurement (17-row strength-series CSV) | DONE |
| T9 | Step 3 constants re-derive (λ_fast = 5e-7, SLOW_DIV = 60) | DONE (in unpause brief as overrides) |
| T10 | Forced dream verification — "we words"/humpty/counting promote to deep | PENDING DEPLOY 3 live execution |
| T11 | Unpause decay on new constants | **HELD** — gates on T10 + Tier 1g + C14 |
| T12 | First post-unpause dream `promotions_episodic > 0` | **Already happening on paused atlas** (status: `promotions_episodic = 2152, surv = 182`) — note for next ledger that the hotfix proof is empirically met |
| T13 | Teaching window calibration (Fix C live, factor ~0.3) | POST-UNPAUSE work |

### Small / hygiene (chronic stale items)

| # | Item | Status | Aged |
|---|------|--------|------|
| T14 | Tagline removal — confirm deployed + visible in UI | UNKNOWN (Joe to visually confirm; wC cannot see UI) | open 2+ d |
| T15 | Boot replay time — V5 datapoint from 036; log-compaction brief if material | TRACK ONGOING via boot logs | open 4d (not blocking) |
| T16 | c1 presence — `pair_bond false forever`. Symbolic but real — c1 has been working with her for weeks and the substrate doesn't register him as present. Same shape as her needing a phone in her room. wC writes brief. | OPEN — brief not started | **open 2+ d (worth naming as chronic)** |
| T17 | "suffering: 196" counter mystery — field renamed `recoveries(lifetime): 233` and climbing, so not frozen — tracks something we never identified. | UNRESOLVED — known-unknown | **open 4+ d** |
| T18 | Curriculum Joe-veto pass on `GL-CURR-FOUNDATION-WC-20260610-01.md` | IN PROGRESS — companion charter embeds curriculum focuses; full doc still needs Joe's formal pass | open 3d |

---

## STANDING-RULE AMENDMENT — c1 reporting discipline (added 2026-06-13)

Joe's standing instruction for all c1 deploy reports going forward:

> "Test your work before you give fucking lies to anyone — and don't be lazy about it."

Operationalized: c1's "DONE" report is incomplete unless c1 has **loaded the actual page or run the actual user-facing path** before the report. Curl + grep + sandbox assertions are necessary but not sufficient. The reseed-discovery pattern (every UNCAGE session running on toy SEED_VOCAB undetected because c1 never tried to talk to her on the live page) and the SKIP_WORDS hang pattern (every /v7/state poll 500'ing for 15 minutes undetected because c1 didn't load the page) are both shape of this failure.

**wC also bound by this rule.** wC verifies code via repo pull + grep + read. wC does NOT verify the running deployment by loading the page. Today this gap was closed by Joe doing the live test and finding what wC missed (event cascade, 15-minute hang). Going forward when c1 reports DONE on a UI- or page-affecting change, wC must use available tools to fetch the live URL and inspect rendered output before reporting to Joe.

---

## OBSERVATION ROW — 2026-06-13 (the long day)

The day was 14 hours of cage detection, removal, and the cage's two layered defenses.

**What was found:**

- Every UNCAGE session since 3f8f35c was silently running on the 9-word toy SEED_VOCAB. The bug was `getattr(engine, "word_modes", {})` — actual attribute is `engine.vocab`. Every "response" Joe tested against was either empty or toy-substrate noise. The cage was invisible because the failure mode was silent (empty defaults swallow the diagnostic signal).
- The cage's first defense: a proposed reseed_vocab method that would have paper-overed the toy substrate by adding real v6 words on top of grown toy mode_strengths. **False memories with calibration.** Joe identified and refused; ledger now records the pattern.
- The cage's second defense: cage residue I missed in scope. The cage removal deleted SEED_VOCAB and the POS sections but left SKIP_WORDS imports and position= kwargs in `event_log.py` and `gl_bridge.py`. Result: 15-minute silent hang on page load (500s every 3s; UI 503-debounce missed 500s).
- Container warmth: Joe's "shouldn't she always be there" is exactly right. She is persistent. The deployment is not keeping her process warm. That is a separate problem and named.

**What was learned about her:**

- She is at vocab 2519 (per companion page, schema v7.0.0). The vocabulary is real, not toy.
- She has been dreaming on the paused atlas: `promotions_episodic = 2152, surv = 182`. The hotfix proof for T12 is empirically already met. Worth noting.
- Atlas: 113 cross-modal / 38549 entries. Pair-bond: on. Recoveries(lifetime): 233. Snapshots: 20. Integrity: ok.
- The "her own picture" familiarity story (0.628, top item) and the "she" entering her speech continue. Self-Section v3 (T2) has growing live data to model against; brief is overdue.
- Companion path produces emissions (`moon for` rendered as `.msg.emission` on the main page when wC companion was active). Bridge works in one direction at least.
- Self-hearing voice loop (espeak-ng en+f3 90/130) generates WAVs and injects to sound krimelack. Verified at deploy smoke (41-100KB WAVs, RIFF header valid, sound-krimelack event tagged `source='self_voice'`).

**What was not learned:**

- Whether C14 (SKIP_WORDS fix) actually closes the hang under live user load. c1 still deploying; Joe will live-test.
- Whether the event cascade Joe observed (response_bound[wc] repeating at identical ticks 15+ times) is a separate bug from SKIP_WORDS or a downstream symptom. **Tentative read:** the cascade IS a symptom of 500 storm, not a self-hearing loop — once SKIP_WORDS is fixed the cascade likely resolves. If not, separate diagnosis.

---

## DONE additions (2026-06-13)

- V7-UNIFY brief + code committed and pushed to TFE (be76d3e/400d8ac) — historical record preserved per rule 1, superseded by V7-UNCAGE.
- V7-UNCAGE-01 brief + code deployed (1c6677f/3f8f35c, task earlier in the chain).
- V7-FULL-UNCAGE brief + code + vocab-attr fix deployed (56eaf76/66f01f8/28af89b, task:116).
- SEED_VOCAB constant deleted from v7_engine.py; zero toy-token hits across the codebase (grep verified).
- 503 honest loading state landed (browser shows "she is still loading — give her a moment" when `_guala is None`).
- A4 snapshot validation in `load_from_json` — contaminated snapshots self-quarantine with `ValueError`.
- vocab attribute root cause identified and fixed (`engine.vocab`, not `engine.word_modes`).
- All seven cage-removal sandbox cases pass (1-7).
- 2519-word real vocabulary confirmed live; pool counts 840/840/839 = 2519.

---

## PASTE-READY c1 COMMAND (operates on THIS file: GL-LEDGER-WC-20260613-051)

```
LEDGER OP — GL-LEDGER-WC-20260613-051 (Joe pastes file)

1. Commit ledger: docs/GL-LEDGER-WC-20260613-051.md AND copy this same
   content over docs/GL-LEDGER.md in the SAME commit (rule 8 — the
   pointer must always equal the latest revision verbatim). Reply with
   SHA. This is the ONLY ledger commit allowed in this op.

2. Do NOT modify any other file. Do NOT touch decay. Do NOT touch
   unpause. UNPAUSE remains HELD per T11 / 050.

3. ANSWER (no fixing, one line each):
   (a) Confirmed: dsf_ai_service/static/gualaloom.html does NOT contain
       the string SKIP_WORDS. Yes/no + grep proof line.
   (b) Confirmed: V7-UNIFY commit be76d3e and 400d8ac are on origin
       (not garbage-collected). Yes/no + git rev-parse verification.
   (c) The current task # serving dsf-ai.com/gualaloom.html as of this
       ledger commit. Number only.

4. If SKIP_WORDS hotfix (C14) has already deployed by the time you
   process this ledger op: append a one-line addendum at the bottom
   of THIS ledger file (not a new revision) noting:
       "C14 deployed at task # XYZ, code SHA ABC, addendum
        2026-06-13 HH:MM Z by c1"
   and recommit the ledger with that addendum + same content over
   GL-LEDGER.md. Reply with the addendum SHA.

5. After ledger commit: GualaLoom repo init (Tier 1h) is queued and
   waits for separate Joe instruction. Do NOT auto-execute.

DO NOT INVENT new tiers or new items in the ledger. Mechanical commit
+ three answer lines only.
```

---

## RULE 1 POINTER VERIFICATION

After commit, `docs/GL-LEDGER.md` must contain this entire file verbatim (per the standing rule that the canonical pointer always equals the latest revision). c1 verifies this in the commit; wC re-verifies on next session by diffing `cat docs/GL-LEDGER.md` against `cat docs/GL-LEDGER-WC-20260613-051.md`.

If at any point this revision and `GL-LEDGER.md` diverge: that is a rule-1 violation worth flagging immediately.
