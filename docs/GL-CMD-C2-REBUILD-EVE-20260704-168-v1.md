# GL-CMD-C2-REBUILD-EVE-20260704-168-v1

doc_id: GL-CMD-C2-REBUILD-EVE-20260704-168-v1
From: Eve | To: c1a | Vehicle: NONE — model/harness work only.
NOTHING touches her live path from this CMD; migration remains gated
on C-3, which remains gated on A3.
Responds to: GL-RPT-C2-Q1-POPULATION-VALIDATED-EVE-20260704-v1 +
c2_model_v1.py / c2_model_v2.py (on origin, filed by Joe). Ordered
by Joe: run the T⁶ story at population level.
E-signature declaration: none — model evidence for the rebuild.
Substrate-truth declaration: RNG in neuron identity BANNED (cue
noise only). No results-tuned constants — every parameter traces to
substrate state or is flagged as engineering judgment. Too-good STOP
armed throughout.

## Step 0 — durability
Commit THIS file verbatim to docs/ before implementing.

## Part A — reproduce (blocking)
Run c2_model_v2.py verbatim. Reproduce Eve's table within noise
(seeds may vary trials slightly; the ORDERING must hold: population
plurality > best single neuron at jitter 0.3 and 0.5, unanimity ~0,
N=1024 ≈ N=64). Any failure to reproduce → STOP, report, no Part B.

## Part B — replace the stand-ins with her reality, one at a time
B1 ENCODER (Q3): swap the model's winding stand-in for real
   krimelack transduction — the actual LanguageKrimelack (+ sensory
   adapters where available) generating the 7-dim state, no reset
   semantics honored. Re-run the full table. This answers the
   (a)/(b)/(c) provenance fork by construction.
B2 DIFFERENTIATION (Q1 at scale): replace ring-position-only
   profiles with differentiation drawn from REAL per-neuron state —
   J_ij couplings, chi_atlas contents, ring position, familiarity.
   The metric that matters: EFFECTIVE VOTER DIVERSITY vs N (Eve's
   testbed saturates at ~7 distinct voters; real state must push
   that number up for large-N to pay). Report diversity alongside
   accuracy at N = 64 / 256 / 1024.
B3 STATIONARITY (Q2): the winding-Δ_S candidate against no-reset —
   same word, different accumulated state, does recall hold. Honest
   starting line on record: T4 3/5, T5 11%. Beat it or file it.

## Gates (failures first, NOT MEASURED where true)
G-168-1  Part A ordering reproduced before any Part B commit.
G-168-2  STOP if any degraded-cue accuracy > 95% — suspect a hash;
         halt and distribute per-neuron scores (era rule).
G-168-3  STOP if any RNG enters neuron identity.
G-168-4  Every B-step reports the SAME table format (single mean /
         best / plurality / unanimity) so the chain from Eve's
         testbed to real dynamics is one continuous, comparable
         record.
G-168-5  Voter-diversity metric defined and reported per B2 run.
G-168-6  Diff proves scope: model/harness files only; zero live-path
         changes.

Joe's part: none until B-results — then the migration question
(C-3) comes to you with numbers, not hope.

### Changelog
- v1 (2026-07-04, Eve): the rebuild command, from Q1 validated at
  Eve's seat. One stand-in swapped per step; the record stays
  comparable end to end.
