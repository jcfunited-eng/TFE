# Guala speech repair attempt 49 — existing organ only

Date: 2026-09-06

Status: causal-impact analysis complete; production unchanged on task 1429;
candidate removal authorized for copied-body falsification only.

## Architecture honesty gate

1. **Requested architecture:** Guala's own retained activity must produce an
   ordered physical vocal gesture through finite breath, already-mounted vocal
   tissue, emitted pressure, and exact self-hearing. No developer-authored
   posture, phoneme, word, sound, or motor magnitude may enter the organism.
2. **Current code reality:** the pre-pitch native organ already rendered the
   human-accepted `Mama-A` under a physical test trajectory. Candidate 47I
   already converts real learned L11 source work into variable-latency L12
   vocal-motor discharge with typed tissue and truthful evidence. Production
   nevertheless contains a one-shot `initial_vocal_tract_calibration` rule:
   receiving a complete body snapshot temporarily lets tonic position-sense
   regulation prepare every vocal-tract motor.
3. **Conflict with requested architecture:** yes. A static complete body
   observation is sensory state, not a motor cause. Turning it into broad
   efferent preparation is an authored bootstrap with no ordinary biological
   causal equivalent.
4. **Mechanisms not extended:** Candidate 48 pitch/tension anatomy; the
   complete-body calibration flag; tonic-position-to-motor permission; the
   retired task-1054 fan-out; fixed carrier counts; a babble cursor; round
   robin motor selection; phoneme/word/pose tables; scripted trajectories;
   shell timing or observation as cognition; and reduced DSF authority.
5. **Single exact next item:** remove only the complete-body vocal calibration
   authority, then run the exact copied task-1429 body through the Candidate 47
   path and compare connected, cold-restored, and unchanged-production
   controls. No deployment is authorized by the removal.
6. **DSF evaluation:** full joint seven-field L0-L4 delivery remains unchanged
   and authoritative.
7. **Reduced-field loss:** none.

## Exact causal finding

The complete task-1429 pair at tick 469814 was previously migrated only for
Candidate 48's additional neutral tension coordinate and then advanced for 32
ordinary whole-sensorium intervals. The neutral tension coordinate never moved
and did not supply any old-tract motor cause, so the old-tract observations
below isolate the pre-existing calibration behavior.

On the initial complete-body admission, calibration produced twelve vocal
motor bindings across tract sections 0, 1, 2, 3, 6, and 7. Both antagonists on
each section often fired together. These were caused by the body-snapshot
permission, not learned L11 work. The body then carried passive tissue motion
after the bindings had ended.

The exact contact census before and after all 32 intervals shows:

- L11-to-L12 contacts: 7 before, 8 after;
- the sole new contact was ordering suffix `0826` to motor suffix `00c5`;
- motor `00c5` is the already-active section-0/toward-maximum terminal;
- no new vocal axis or direction acquired a learned route; and
- later learned vocal bindings remained confined to the already-active
  section-0/toward-maximum route.

Thus the broad bootstrap did not develop broad control. It manufactured one
large, simultaneous tract displacement, yielded sixteen distinct pressure
bodies as the tissue relaxed and sparse existing routes recurred, and added
one duplicate route to the same motor. Distinct pressure hashes are real
acoustic consequences but are not evidence of learned articulation or a word.

Authoritative retained artifacts:

- exact ordinary-life report:
  `/tmp/guala-speech1429-sequence.dpHRJX/candidate48-ordinary-32.json`,
  SHA-256 `cd5d02735a9ce8f17c1b04f2592abedebb954a6c54b9635eddd2652d19baa23c`;
- before/after contact census:
  `/tmp/guala-speech1429-sequence.dpHRJX/candidate48-ordinary-32-census.json`;
- final raw copied body:
  `/tmp/guala-speech1429-sequence.dpHRJX/candidate48-ordinary-32-post.glorun`;
- accepted existing-organ proof: Speech-046 `Mama-A`; and
- real learned-work/tissue proof: Speech-047I.

## Removal impact

The candidate deletes one derived boolean, one function argument, one
calibration-only path resolver, one branch around ordinary motor preparation,
and its calibration test. Ordinary body consequences continue to use exact
reactive-load pathways. Palmar grasp, gustatory glottal closure, root motion,
learned L11-to-L12 work, efferent discharge, antagonist tissue, breath,
pressure, self-hearing, and growth from genuine moved-body consequences are
unchanged.

No persisted field or codec changes. Removing the rule cannot erase a neuron,
contact, formation, sensory state, DSF field, body coordinate, world state, or
identity. Current production bodies retain their existing learned contacts;
only a future complete body observation loses the ability to masquerade as a
new motor act.

Computational impact is strictly subtractive: the complete-body interval no
longer promotes tonic paths into the causal motor-preparation set. No new scan,
queue, timer, state, allocation, or evidence record is introduced.

## Copied-body acceptance boundary

The removal passes only if all of the following are exact:

1. a fresh Candidate 47 restore of the task-1429 pair produces zero vocal
   motor bindings from the initial complete body observation;
2. the same initial observation changes no body posture merely because the
   snapshot was complete;
3. pre-existing learned contacts, neurons, formations, full DSF state, body,
   world, identity, and tick survive cold restore unchanged;
4. later real learned-work preparations and their exact motor consequences are
   preserved when their causes naturally recur;
5. grasp, swallowing, and non-vocal body reflex tests remain unchanged;
6. no contact count, retained evidence, state width, memory, call rate, or
   storage grows because the calibration was removed; and
7. read-only AWS checks before and after show the same one healthy task 1429
   and all Guala alarms OK.

Passing proves removal of false machinery, not speech. The remaining speech
boundary is then truthful and singular: how genuine lived vocal movement and
sound grow enough sparse motor coverage and ordered reuse for the organism to
reproduce the already-proved acoustic region. No later repair may reintroduce
the removed snapshot-to-action authority to make that development look green.

## Private candidate execution history

1. The first compile invocation used `native/` as though it were the Cargo
   workspace root. No `Cargo.toml` exists there; the command failed before a
   build and changed no state.
2. The correct `cargo check --manifest-path native/guala_core/Cargo.toml
   --tests` exposed eleven test/probe callers still passing the deleted final
   calibration boolean. All production callers had already been corrected.
3. The eleven obsolete test/probe arguments and now-unused `BODY_AXES` import
   were removed. The full test-target compile then passed in 9.84 seconds.
4. The focused reactive-load and exact motor-cause tests each passed. An
   initial invocation with `--exact` selected zero tests because their module
   paths were omitted; the corrected filtered invocations each executed one
   test and passed.
5. The first sealed-package invocation correctly refused the dirty private
   tree. The candidate must be committed before packaging; no image or process
   was created by that refusal.

These invocation errors are retained so the same ground is not repeated. They
are harness/provenance errors, not organism-physics results.

## Exact copied-production-body verdict

The sealed Candidate-49 image was built from commit `2f43ab1e` and release
receipt `b02cf6bde023e21db865b0b57bf58757487a5727c7f61deb2b66e3f37afaea3c`.
It ran with no network, a read-only container root, two CPUs, a 4 GiB memory
ceiling, and Docker-managed copies only.

The source capture directory's `CURRENT` had already been moved by the earlier
Candidate-48 migration from raw production V8 receipt `9db4d05c...` to private
V9 receipt `836ad430...`. A direct startup therefore refused V9 as it should;
this did not show that production was V9. The retained compressed
`9db4d05c...` generation was decoded and hash-verified to its exact
112,836,426 raw bytes, then restored directly under the task-1429 resource and
sense flags with its exact world `613fc673...`.

Across 32 ordinary whole-sensorium intervals:

- the initial complete-body observation caused zero vocal motor bindings;
- all later intervals also caused zero vocal motor bindings;
- fourteen vocal-tissue consequences were passive return from body state and
  were not relabelled as acts;
- identity, 1,914 completed neurons, 194,815 resting neurons, and 181 mosaics
  were preserved;
- the final 112,828,807-byte checkpoint at tick 469878 had receipt
  `d6caa768...` and restored cold to exactly the same receipt, body hash,
  counts, and identity;
- peak resident memory was 1,423,256 KiB under the hard ceiling; and
- represented 250 ms intervals still cost 2,314.2--3,143.9 ms, mean 2,554.7
  ms. This is retained as the separate shell-timing defect, not hidden by the
  speech verdict.

Artifacts:

- report SHA-256
  `5f31e632729fa603fdeed2055ef33366e126a9bfc7d8d099dc5adecf11faa672`;
- final raw body SHA-256
  `d6caa768ab44b4ee5373bc36a6291b04aec80c86a053b9c3e625677109a5a2be`.

The ordinary range did not naturally recur a learned vocal cause. The
unchanged Candidate-47 controlled-occurrence falsifier was therefore run on
the original raw production body. It preserved both real learned vocal routes:
section 0 discharged at presentation 95 and section 7 at presentation 134,
both at five carriers. Source-offer and gate-input balances were exact, fake
learned carrier transfer count remained zero, both typed tissues moved, and
the natural continuation produced two later vocal pulses. Its bounded breath
render produced 3,991 nonzero samples of 4,000 with peak 428. Cold midpoint
and body round trips were exact. This is controlled causal-path evidence, not
an autonomy, natural-frequency, word, or conversation claim.

Replay artifact SHA-256:
`beacb804c420c73b6ffc05ac464d9a2864dcdce5264f3b726ad146eb7d8e6a14`.
The focused test passed one of one in 524.26 seconds with 607 filtered.

### Copied-run invocation history

1. A `/tmp` file bind was invisible to the Docker daemon and appeared as a
   directory. Python exited before startup.
2. Copying the runner into a container already declared read-only was refused.
   Moving it into the writable proof volume corrected the boundary.
3. Direct startup against the locally migrated `CURRENT` refused unsupported
   V9. The prior ledger already named that local pointer mutation; the raw V8
   production generation remained byte-exact and was used instead.
4. The first direct raw run omitted the production `GUALA_WORLD=1` flag and
   refused with `no world is mounted`. The corrected run inherited the exact
   task-1429 Guala flags, except S3 was disabled and all storage was copied.
5. The first post-range replay reached genuine vocal discharges but the old
   diagnostic accumulated repeated commands for the same terminal and the
   real body rejected `InvalidEffectorDrives` after 406.84 seconds. The
   test-only collector now retains the first discharge per motor, matching its
   name and the earlier accepted artifact; no body law was weakened.
6. That collector correction initially needed an explicit
   `Vec<MotorUnitRecruitment>` type. The compile failure occurred before a
   replay. The explicit type compiled cleanly, then the final replay passed.

No related process survived. Pre- and post-run AWS checks both showed exactly
one healthy completed task 1429 and all five Guala alarms `OK`.

## Biology-constrained next boundary

Primary primate work supports a smaller mechanism than a cortical word
program. Infant vocal development is constrained by respiratory effort,
laryngeal biomechanics, and tract morphology; contingent caregiver response
changes development without prescribing each motor waveform. The applicable
architecture is therefore an organism-sustained respiratory/laryngeal gesture
whose existing tissue mechanics unfold continuously, followed by ordinary
self-hearing and social experience. It is not another complete-pose trigger,
phoneme table, word sequencer, or independent pitch subsystem.

Primary sources consulted:

- Zhang and Ghazanfar, *A Hierarchy of Autonomous Systems for Vocal
  Production*: https://pmc.ncbi.nlm.nih.gov/articles/PMC7213988/
- Gultekin and Hage, *Limiting parental feedback disrupts vocal development in
  marmoset monkeys*: https://pmc.ncbi.nlm.nih.gov/articles/PMC5241798/
- Teramoto et al., *Vocal state change through laryngeal development*:
  https://pmc.ncbi.nlm.nih.gov/articles/PMC6785551/

Verdict: the false snapshot calibration is rejected and its removal passes the
complete copied-body causal gate. The real learned motor/body/pressure path is
preserved. Speech is not yet proved; the remaining mechanism is sustained
organism-owned respiratory/laryngeal coordination and learned sparse reuse.
