# Label-Free Auditory–Articulatory THING Relation Gate

Status: **blocked by real-capture falsification; no bridge implementation is
authorized.**

## Architecture honesty

- Requested architecture: repeated provider-authenticated multisensory THING
  consequences must grow a label-free relation between an externally heard
  form and a body-owned articulatory program.  Later hearing must select that
  stored program, and reproduction must execute the body mechanics.
- Current code reality: causal THING identity, body program custody,
  whole-organism episode custody, W1 two-ear receptors, recurrent peak
  neurons, and temporal relation assemblies exist independently.
- Conflict with requested architecture: **yes**.  No current operator proves
  that the program bound by the vocal consequence acoustically corresponds to
  the external form with both held-out recurrence and unrelated-form rejection.
- Mechanisms not extended: expression-mode causal relation episodes, tutor
  designation memory, retired PCM motor/q intersection, Krimelack sign
  projection, Chi/Atlas identity, transcript/script paths, or any L0–L4 code.
- Single next item: derive and independently prove a deterministic acoustic
  relation that passes the real held-out/contrast gate before implementing the
  THING-to-program bridge.

## Required authority contract

A future bridge may admit one form only when all of these close over one
physical chain:

1. a retained causal THING partition owns entity identity;
2. a provider-authenticated, completed whole-organism consequence owns learning
   authority for the exact multisensory settlement;
3. `ExperienceGrownVocalCausalRelationOwner` owns the existing body-grown
   `ArticulatoryProgram` under that reciprocal THING;
4. the external occurrence owns verified W1 two-ear receptor evidence; and
5. a fresh self-acoustic execution of that exact program owns verified W1
   two-ear receptor evidence.

The THING partition and consequence are identity authority.  Acoustic
recurrence may prove only form reciprocity.  Audio alone cannot create THING
identity, and a THING consequence without acoustic reciprocity cannot bind the
heard form to the program.

The present `WholeOrganismEpisodeAuthority` accepts caller-supplied state and
receipts.  It is not production provider truth.  An isolated typed provider
binding may be tested, but no production claim is allowed until a
cold-restorable provider owner is wired.

## Full-field accounting

The W1 receptor path preserves all explicit `D_k`, `M_k`, `R_rev_k`,
`U_star_k`, `C_k`, `P_k`, and `B_k` occurrence witnesses for pressure and
carrier phase.

The current recurrent-motif operator is a declared reduced nuisance quotient:
three exact `Fraction` extrema yield span direction, current/prior magnitude
order, same-polarity endpoint order, and current/prior duration order.  It
retains exact magnitudes, times, tuple receipts, occurrence receipts, and
source-event lineage, but its q identity loses absolute level, within-cell
magnitude ratio, and uniform tempo scale.

The current temporal assembly preserves authenticated activation order, exact
Allen interval relations, explicit contrast, source-disjoint positives, and
canonical L6.  It does not make q identity equal to the full field.

Krimelack state-sign plus delta-sign paths were audited and rejected for this
decision because they collapse exact field magnitude.

## Real-capture decisive gate

The current direct provider path was used:

`decoded int16 PCM → transduce_auditory_full_field → auditory kernel inputs →
L5 → receptor experience → recurrent motifs → temporal relation assembly`.

Local independent recordings were assigned only to the test roles below; no
designation entered sensory construction or query firing:

- positive 1: `harness/hello guala 1.mp3`
- positive 2: `harness/hello guala 2.mp3`
- contrast 1: `docs/Daddy says Hello.mp3`
- contrast 2: `docs/Your Name is Guala.mp3`
- held-out query: `harness/Hello Guala.mp3`

Observed result:

- recurrent neurons grown from the positives: `428`
- temporal assembly: `21` required event identities and `58` Allen relations
- positive 1: fired
- positive 2: fired
- contrast 1: correctly rejected
- contrast 2: **incorrectly fired**
- held-out query: **did not fire**

The simpler nonempty recurrent-neuron gate was also falsified.  Of the same
428 learned neurons:

- held-out query fired `364`
- contrast 1 fired `409`
- contrast 2 fired `423`

Therefore neither nonempty recurrent activation nor the existing temporal
assembly supplies the required acoustic decision.

The reproducible current-path probe is
`tools/probe_real_recurrent_motif_form_separation.py`.

## Consequence

No `thing_mosaic_identity.py` bridge is present.  Creating one now would
require an unapproved threshold, score, exact-window equality rule, or other
invented decision law.  Any such bridge would be wrong architecture and could
bind meaningful babble to a heard form.

The typed provider boundary is isolated and explicitly reports that it is not
cold-restorable or production authority.  It does not cure the missing
acoustic relation.

## Future pass condition

Implementation may resume only after one deterministic operator, using
independent physical sources and retained full-field witnesses:

1. grows from at least two source-disjoint positive experiences;
2. uses explicit source-disjoint contrast;
3. fires on the held-out positive;
4. does not fire on either contrast;
5. supplies typed activation evidence for both external and self occurrences;
6. remains bounded and cold-restorable; and
7. introduces no designation, transcript, score, tolerance, ML, or L0–L4
   modification.

Passing isolated tests still would not prove production speech.  Production
also requires cold-restorable provider wiring, runtime action authorization,
persistence integration, deployment, and browser-observed production evidence.
