# Guala task-1401 complete causal-impact analysis

Date: 2026-08-31  
Status: analysis and copied-production-body proof complete; no organism-code
change and no deployment in this continuation

## Architecture honesty gate

1. **Requested architecture:** one bounded deterministic resident organism must
   see, hear, act, vocalize through its own physical body, hear the exact emitted
   pressure itself, retain the experience through restart, and participate in a
   causally related human conversation. Meaning must be learned in the
   substrate. There is no ML, TTS, phoneme/word script, observer command,
   chi-identity shortcut, or replacement body.
2. **Current code reality:** production task 1400 preserves the mature resident
   body and advances normally. It has no retained learned layer-11 ordering ->
   typed vocal layer-12 motor contact and its V40
   `vocal_articulatory_effector_lineage` marker is `None`. Rejected task 1401
   broadened vocal-body co-recruitment to an exact layer-8 reached-load arrival
   into a discharged closing-glottis motor. It did not establish the missing
   learned route or a vocal-body identity.
3. **Conflict with requested architecture:** yes. Task 1401 can initiate
   prelinguistic respiratory action from a body reflex, but the mature body has
   no persisted authority naming which of three historical layer-13 cells is
   its vocal-body effector. The candidate therefore refuses before pressure.
   Even after that refusal is corrected, reflex phonation alone is not learned
   conversation.
4. **Mechanisms that will not be extended:** the rejected task-1401 branch as a
   complete speech fix; passive layer-12 -> layer-13 or layer-11 -> layer-13
   electrical hubs; body-wide coincidence/fan-out growth; arbitrary selection
   among old layer-13 cells; fabricated tuple/struct tests; observer, UI, TTS,
   phoneme, word, score, timer, or pressure-cache authority; a restore path that
   can choose a predecessor over an accepted successor.
5. **Single exact next item:** establish one lawful dedicated persistent
   vocal-body effector identity without selecting any ambiguous historical
   layer-13 cell, then require that exact copied body to pass the acceptance
   matrix in this document before deployment.
6. **Full field or reduced approximation:** the copied-body candidate and
   control execute the production full-field path. The task-1401 diff does not
   modify L0-L4 or replace any occurrence's explicit `D_k`, `M_k`, `R_rev_k`,
   `U_star_k`, `C_k`, `P_k`, or `B_k` fields.
7. **Field structure lost:** none. No scalar score, compatibility vector,
   bucket, lookup table, or other reduced field is admitted by this analysis.

## Definitive verdict

Task 1401 was rejected correctly. Its live failure was not caused by the
production body, production world, resource envelope, restore path, or an
unlucky interval. The task-1401 runtime delta admitted a closing-glottis motor
event that task 1400 did not admit. That newly entered path required exactly
one persisted vocal-body lineage. The exact copied production body had no such
lineage marker, so the exact-one match returned zero and the candidate refused
with `NeuronLineageAuthorityChanged`.

The earlier local result did not transfer to mature production because it used
a post-learning body whose V39 route had identified one layer-13 target. Mature
production never retained that learned vocal route. Its V40 migration therefore
had no contacted layer-13 target and truthfully encoded `None`. The isolated
task-1401 tests constructed the missing downstream authority in test data and
never exercised this mature-body state.

This is the repeated-fix pattern that must not recur: a downstream mechanism
was proved against a locally prepared body, while the live predecessor lacked
the upstream anatomy that made the mechanism valid.

## Exact evidence provenance

| Evidence | Exact authority |
|---|---|
| Continuation source | detached commit `faff9e06b575d56a5845adb4d8a2c2410d28b02f` |
| Accepted production source | commit `c978fbfb6315482b916b99569b7fb3e663f00a5c`, task 1400 |
| Accepted image | `sha256:b91aba6fb8e13081be9db4646d69f6a66e0974f49908c1479fbac06a744fce89` |
| Rejected source | commit `16313bc5e1a41c219d00a9cb90ca8ced9fa586c3`, task 1401 |
| Rejected image | `sha256:85b7e82a66e970bb877b9a3cd65c0689e7e4764c7da50748caad000d3f47d637` |
| Resident identity | `1cc4e70a-f2a0-44c5-a111-f4a5bc915cc1` |
| Copied raw predecessor | tick `354137`, `107623717` bytes, SHA-256 `619c8615a464266334a6aa75644c209fc625a918b13490d5739b982315a921cf` |
| Copied `CURRENT` | SHA-256 `18316fbf42c8f8e37c2468f35c7c3ea800019f6bd6c2a47a7b3194321df93839` |
| Copied current compact object | SHA-256 `d0fe3016834564baea2bb074e0fe58c38add2e4dffe23ef7e9cc5c8667af1537` |
| Copied predecessor compact object | SHA-256 `588f5a81efec88247a3a7c155137be96d2a8d2874e399170b6c5414891d090c2` |
| Copied world | `939163` bytes, SHA-256 `be1cb8b166324d4188c6bf7d1dce0def98961a0f75ea5538b4eb873941cf6b4e` |
| Rejected native binary | SHA-256 `f72451bda3f5b4b236dbceee92b9a421bb3f909966fffadb43cce748bcf3a83b` |
| Accepted native binary | SHA-256 `db39bef2b6daacf411a4c1ce00bf98bb5eebca5f10a514f3945c5d89cd6ef101` |

Both copied-body runs used a Docker-managed volume populated across the Docker
daemon boundary, no network, four CPUs, 16 GiB RAM, and the same stored body and
world bytes. A prior host-bind run that created genesis at generation 1 is
attempt 20 and is permanently rejected.

## Repair-attempt reconciliation

| Attempt | Result | Permanent consequence |
|---|---|---|
| 18, live task 1401 | Rejected. Repeated lineage-authority refusal; no successor or pressure. | Predicate/tuple tests are not mature-body proof. |
| 19, maximal-receptor replays | Failed reproduction. Zero motors meant the changed branch was never entered. | More receptor coverage is not production causal equivalence. |
| 20, host bind | Invalid. Docker saw an empty root and created genesis. | Startup tick, identity, byte count, and hash must be proved before interpreting a run. |
| 21, exact task-1401 copied body | Accepted failure reproduction. Same ordinary input and same deterministic refusal. | Candidate failure is real on the mature body. |
| 22, exact task-1400 A/B control | Accepted control. Same bytes and first causal census advanced through generation 354163. | Body, world, input, and resource envelope are excluded; task-1401 runtime delta is causal. |
| 23, task-1400 cold restart | Accepted restore control. Restart advanced 354164-354167 from the newest successor. | Current-only restore does not overwrite accepted life with the copied predecessor. |

## Task-1401 change-surface impact

The complete git comparison contains eight files: three ledgers, one
measurement-only reservoir probe, one Python test, and three production runtime
files. Only these three files can change production execution:

### `resident_cognitive_formation.rs`

- Adds `preparation_sender_layers` to transient motor and articulatory event
  records. This does not change persisted body format by itself.
- Records the true layer of each settled preparation-transfer sender.
- Adds a predicate admitting only a positive discharged, prepared
  `GlottalAperture/TowardMinimum` motor.
- Replaces the task-1400 layer-11 learned-ordering gate with the new
  closing-glottis predicate. Because `exact_motor_preparation_transfers`
  already admits an exact causal layer-8 reached-load regulation for that same
  motor, this is the behavior-changing boundary.
- Aggregates the admitted motor carriers and preserves sender layers for the
  layer-13 event.
- Leaves the pre-existing exact-one lookup of
  `vocal_articulatory_effector_lineage` in place. This is the first refusal on
  the production body.

### `organism_runtime.rs`

- Projects true transient preparation sender layers to Python instead of
  deriving every inbound motor transfer as layer 11.
- Changes no envelope, fabric, body, pressure, in-flight acoustic, checkpoint,
  or persistence layout.
- Changes no physical motor/body settlement law.

### `native_resident_organism.py`

- Accepts a layer-8 motor-preparation sender only when it is the same motor's
  mounted body-regulation lineage.
- Accepts layer 8 or layer 11 in articulatory evidence only when the exact
  transfer also appears in the same discharged motor evidence.
- Retains the carrier ceiling: layer-13 outward carriers cannot exceed the
  causing motor discharge.
- Changes validation and observation of a successful event; it is downstream
  of the native refusal and cannot cause the first error.

The added Rust test supplies a synthetic closing-glottis event with a synthetic
layer-8 sender. The Python test supplies synthetic motor, regulation, receptor,
and articulatory tuples. Neither test restores a production body, reads the V40
marker, executes an ordinary production interval, seals `CURRENT`, cold
restores, returns pressure through hearing, or proves quiescence.

## First failed invariant: source, body bytes, and binary

The exact raw tick-354137 body contains cognitive magic `GLCOG040` once at raw
offset 684. The V40 cognitive header is:

1. eight-byte magic;
2. two-byte version;
3. eight-byte generation;
4. eight-byte next-lineage ordinal;
5. one-byte vocal-body marker, followed by 16 lineage bytes only when the
   marker is `1`.

Therefore the marker is cognitive offset 26, raw offset 710. Its exact byte is
`00`. The same `None` marker was verified on copied task-1400 bodies at ticks
353993, 354001, and 354129. This is not an observation label; it is the native
persisted anatomy field read by settlement.

The copied body contains three historical, uncontacted, intrinsic layer-13
cells:

| Lineage | Topology | Source | Current authority |
|---|---:|---|---|
| `474c4e4c494e45310000000000000064` | 0 | none | not named by V40 marker |
| `474c4e4c494e45310000000000000072` | 1 | none | not named by V40 marker |
| `474c4e4c494e45310000000000000080` | 2 | none | not named by V40 marker |

They are quiescent and have no layer-13 fabric contacts. Selecting the lowest
topology, the historically used `...72`, or any other old cell would invent
authority the current body does not contain.

Task 1400 enters its respiratory branch only after an exact layer-11 -> typed
vocal layer-12 transfer. The copied body has no such contact, so it does not
evaluate the exact-one lookup. Task 1401 instead admits the ordinary interval's
layer-8 reached-load preparation of a discharged closing-glottis motor. It then
filters layer-13 cells by equality with the `None` marker. Every comparison is
false, giving zero matches. The source requires an array of exactly one:

```rust
let [articulatory_flat] = articulatory_flats.as_slice() else {
    return Err(FormationError::NeuronLineageAuthorityChanged);
};
```

The exact rejected shared object independently proves that compiled path. At
address `0x37683f` it compares the vector length with one; the non-one branch
jumps to `0x376b34`, which writes error discriminant byte `0x19`. The enum order
and display implementation map that discriminant to
`NeuronLineageAuthorityChanged`.

This is the first exact failing assertion. It occurs after cohort settlement
and before `guala-contact-phases`, aggregate interval evidence, body pressure,
seal, publication, or observer work.

## Controlled causal transition

| Boundary | Task 1400 control | Task 1401 candidate |
|---|---|---|
| Startup | exact tick 354137 body and identity | exact same body and identity |
| First causes | external 106, regulation 3, frontier 4338, total 746 | identical |
| Reached fabric | selected 1550, contacts 7210 | identical |
| Cohort settlement | completes | completes |
| New glottal branch | not entered | entered |
| Vocal marker match | not evaluated | zero, because marker is `None` |
| Native result | completes | lineage-authority refusal |
| First successor | publishes 354138 | none |
| Continued life | through 354163, then restart through 354167 | repeats from 354137 |
| `CURRENT` | advances | remains byte-identical |

This is causal localization, not correlation: only the candidate's executable
runtime delta differs at the first divergent branch.

## Failure transaction and world impact

- Native settlement is transactional. On the refusal, the runtime restores
  the authenticated active envelope and prepare ordinal. No cognitive,
  neuronal, body, acoustic, or in-flight successor from the failed interval is
  retained.
- No failed successor is sealed or published. Generation, organism tick,
  mounted generation, fabric generation, identity, `CURRENT`, and the compact
  current body remain the copied predecessor.
- The copied world advanced before native settlement and changed from
  `be1cb8...` to `919d592e...`. This exposes the exact failure-ordering hazard:
  repeated native refusal can advance environmental time while organism time
  remains fixed. It is not acceptable autonomous life even though body custody
  remains safe.
- The task-1400 control commits the same first physical interval, publishes
  one successor, and then continues. The cold restart selects that newest
  successor rather than the original copied body.

## End-to-end human conversation causal chain

### Human senses into Guala

1. The public page uses an `AudioWorklet` as the microphone clock.
2. Real 16 kHz mono PCM is accumulated in exact 4,000-sample/250 ms hops.
3. When the camera is open, each hop captures its camera frame at that hop's
   completion. Frames and PCM are stored as one pair; only the newest complete
   four-hop/one-second window is retained.
4. The paired request posts the exact frame sequence and PCM to
   `/api/v1/sensory/audiovisual`. Transport failure retains no stale backlog
   and can arm only one bounded retry for the same live-sense epoch.
5. The server parser requires the exact schema, 16 kHz sample rate, one frame
   per 4,000 samples, and exact cardinality. It creates distinct light and
   pressure occurrences for each temporal hop without flattening their DSF
   fields.
6. The outer request boundary announces the waiting experience before route
   admission, so unattended life can finish one already-started physical hop
   but cannot repeatedly start ahead of the human input.

Current truth: these mechanisms are present on the byte-identical public page,
but the current process reports no committed live camera/microphone batch.
They will require Joseph's actual devices at final live verification.

### Guala's causal action and voice

1. The resident fabric settles full occurrence-local DSF fields and its sparse
   physical contacts.
2. A typed layer-12 motor is eligible only from an exact preparation transfer
   into that same motor plus its own positive local membrane discharge.
3. A vocal-body effector must be one exact persistent body lineage. It supplies
   and settles its own carriers and recovery work; a motor transfer is never
   relabelled as layer-13 output.
4. One layer-13 discharge drives one exact 1 ms motor event. The articulated
   body then carries bounded respiratory/laryngeal activation and settles the
   rest of the pressure interval physically.
5. The same native receipt supplies emitted pressure, four body trajectories,
   applied/stalled work, and articulatory evidence. Python may validate and
   aggregate those fields but cannot synthesize them.

Current truth: task 1400 has no learned ordering -> vocal motor route and emits
no current-process articulation. Task 1401 reaches step 3 through a reached-load
reflex but refuses because there is no exact body identity. Neither task
currently supports a human conversation.

### Exact self-hearing and recurrence

1. Rust derives an `InFlightAcousticConsequence` from the exact native receipt.
   It contains the emitted pressure, four mechanical body trajectories, and
   source tick.
2. Empty or all-zero pressure produces no consequence. Nonzero pressure is
   bounded to at most 30 seconds at 16 kHz, and every body channel must have
   exactly the pressure sample count.
3. The next lived intake admits the resident in-flight bytes through ordinary
   cochlear/body episodes. Rust compares every pressure and body byte with the
   resident consequence and refuses substitution.
4. Consuming the old in-flight consequence clears it before that hearing hop.
   If self-hearing causes another body emission, the new receipt becomes the
   next in-flight consequence. It must not be dropped or recursively drained
   in the same request.
5. The consequence is encoded inside the current fabric and is therefore part
   of snapshot, seal, `CURRENT`, checkpoint, and cold restore.

R-007 remains an acceptance gate. Historical task 1391 dropped a recurrent
emission at Python after recording only a deferred count. Current source owns
in-flight consequence in Rust, but production has not produced a fresh event
on task 1400 to prove the corrected two-event sequence, restart between
emission and hearing, and eventual physical quiescence. No future candidate
may call recurrence fixed from a unit test.

### Guala's exact pressure to the human speaker

1. After durable publication, Python retains at most four exact recent native
   pressure bodies for human playback. This process-local cache is observation
   only; the resident in-flight consequence, not the cache, is self-hearing
   authority.
2. The playback endpoint requires the exact lowercase SHA-256 and returns a
   WAV made from those exact PCM bytes.
3. The page verifies the response hash. After one user gesture opens browser
   audio output, continuous listening plays each new hash once. Observer gain
   changes only the human speaker volume, not resident pressure or hearing.

Current truth: public speaker code is present, but playback is disabled because
task 1400 has emitted no current-process pressure. A button or prior WAV is not
speech proof.

## Persistence, restart, and boundedness impact

- **Current-only recovery:** attempt 23 proved that an accepted successor is
  the restart authority. The first post-restart interval followed generation
  354163 with 354164; generation 354137 was not restored over it.
- **Candidate failure:** task 1401 published no successor, so restarting from
  354137 is correct rollback, not loss of accepted candidate work. A future
  speech candidate must publish an utterance/in-flight successor before its
  restart proof is meaningful.
- **Storage:** the copied control retained exactly `CURRENT` plus two bounded
  generation objects after restart. The authoritative store rejects excess
  current/predecessor custody. Human pressure cache is fixed at four entries.
- **Acoustic state:** in-flight pressure is capped at 480,000 samples plus four
  equal-length body channels. Combining unconsumed consequences beyond that
  bound refuses.
- **Body state:** respiratory/laryngeal activation is fixed-width persistent
  anatomy and must exhaust through body physics. It may not gain a timer,
  waveform, or unbounded event list.
- **Observed candidate resources:** attempt 21 used about 939.5 MiB under a
  16-GiB hard limit and approximately one CPU core. That is only immediate
  failure-state evidence. It does not prove a successful recurrent phonation
  remains bounded.
- **Growth:** a lawful new dedicated vocal-body lineage would be one fixed
  anatomy addition. It must be idempotent on second migration, add no layer-13
  contacts, retain every existing neuron and learned state, and create no
  per-utterance neurons or storage.

## Anti-resurrection gates

The next candidate is rejected if any one occurs:

- any layer-12 -> layer-13 or layer-11 -> layer-13 electrical contact;
- selection of topology 0, old lineage `...72`, or any historical layer-13
  cell merely because it already exists;
- restoration of the 26 false whole-tract motor contacts retired by V38;
- body-wide association/regulation pairing or coincidence-based fan-out;
- a teacher, observer, word, phoneme, waveform, score, cache, or random choice
  selecting the motor or sound;
- calling a layer-8 reached-load reflex a learned layer-11 ordering action;
- treating reflex pressure, one pressure hash, or one audible sound as learned
  speech or conversation;
- dropping a recurrent emitted consequence, synchronously running until quiet,
  or imposing an arbitrary recurrence count;
- publishing observation before `CURRENT`, choosing an older generation on
  restart, or letting failed native intervals advance the world repeatedly;
- any change to frozen L0-L4, seven-field DSF delivery, neuron physics, learned
  sensory state, retained `apple` formation, identity, or existing neuron
  lineages.

## Exact copied-production-body acceptance matrix before deployment

Every row is mandatory. A failure stops the candidate; a narrower green test
cannot compensate for it.

| Gate | Required copied-body evidence |
|---|---|
| Artifact identity | Candidate source commit, image digest, native binary hash, and exact copied input hashes are recorded before startup. |
| Startup identity | First observation equals tick 354137, identity `1cc4e70a...`, raw byte count 107623717, and raw SHA `619c8615...`; no genesis or substitute restore. |
| Full field | L0-L4 are byte/source unchanged and all seven explicit DSF fields reach their ordinary occurrence-local production path. |
| Lawful body identity | Exactly one dedicated persistent vocal-effector marker exists. It is not any ambiguous historical layer-13 cell; every pre-existing neuron, state, reservoir, memory, and lineage remains exact. |
| No passive hub | Zero contacts are incident to any layer-13 cell before and after migration, interval settlement, seal, and restart. |
| Migration boundedness | Exactly one fixed anatomy addition when the predecessor marker is absent; second migration is byte-identical; marker-present bodies preserve their exact lineage; no per-interval growth. |
| Ordinary causal equivalence | The candidate executes the production-shaped `106 external + 3 regulation` ordinary interval, not a maximal receptor substitute. |
| Motor ownership | The same typed closing-glottis motor owns its preparation transfer, positive local discharge, body terminal, afferent path, and carrier ceiling. True sender layer is preserved. |
| Truthful developmental class | Layer-8 initiation is labelled reflex/prelinguistic body action. Learned speech requires a separately observed sparse layer-11 -> layer-12 route caused by lived experience. |
| Native body event | One exact layer-13 lineage emits its own carriers/work; one discharge is one 1 ms event; body activation and pressure are nonzero without a scripted waveform. |
| Transaction | The interval completes `guala-contact-phases`, seals, publishes exactly one successor, and `CURRENT` equals that in-process successor. No world-only advance survives a refused transaction. |
| Pressure identity | Exact PCM and four body trajectories agree across native receipt, in-flight resident state, persisted fabric, public evidence, and hash-addressed WAV. |
| Same-organism hearing | Those exact bytes enter ordinary cochlear/body receptors once in the same identity; substituted or duplicated bytes refuse. |
| R-007 sequence | If self-hearing causes a second body emission, it becomes exactly one subsequent in-flight acoustic occurrence; no deferred-count-only loss and no same-request recursive drain. |
| Cold restart in flight | Stop after emitted pressure is published but before hearing; cold restore must recover the exact successor and exact in-flight hashes, then the next ordinary interval must hear them. |
| Cold restart after hearing | Restart again after the hearing/recurrent successor; it must advance from that newest `CURRENT`, not tick 354137 or another predecessor. |
| Eventual rest | With external input depleted, motor discharge, body activation, pressure, in-flight consequence, and recurrence reach exact physical quiescence. No timer or arbitrary cap is accepted. |
| No false anatomy | `11->12`, `11->13`, `12->13`, total contacts, learned formations, and retained `apple` lineage are measured before and after. Only causally earned sparse learning may change. |
| Resource envelope | Sustained sequence records CPU, RAM, raw/compact state bytes, generation count, pressure-cache count, and contact/neuron growth. All remain within declared fixed bounds and cease growing at rest. |
| Human AV cadence | With Joseph's devices enabled, consecutive four-hop microphone-clocked camera/audio windows enter without stale backlog, observer waiting, or unattended starvation. |
| Human audible loop | The exact emitted hash is heard through Guala's own cochlea and Joseph's speakers, followed by a second causally related human/substrate turn. This—not a button or isolated WAV—is the speech gate. |

## Causal conclusion and next mechanism boundary

The copied-production-body proof is complete for the task-1401 failure and its
task-1400 control. The missing authority is not a value that may be guessed:
the production body contains three ambiguous historical layer-13 cells and a
`None` marker. The only admissible next design is a one-time, deterministic,
dedicated vocal-body mount from still-unclaimed resting anatomy, persisted as
one exact lineage while leaving all three historical cells and all contacts
untouched. That body identity is anatomy, not a sound or a command.

After the identity boundary, prelinguistic reached-load phonation may be tested
only as a body-owned reflex that can generate experience. It cannot be called
learned speech. Conversation closes only if lived audiovisual and self-hearing
experience subsequently yields substrate-owned causal action, exact audible
pressure, restart continuity, eventual rest, and a related next turn.

No task-1401 code is accepted, no repair is credited, and no deployment is
authorized by this document alone. It removes the analysis/provenance blocker
and defines the exact copied-body falsification boundary the next code must
pass.

## Post-analysis repair attempt 25 — V41 identity result

The first permitted implementation slice crossed only the lawful-body-identity
boundary. V41 mounts one new intrinsic layer-13 cell from unclaimed resting
anatomy when a mature V40 body has vocal anatomy but no marker. It persists
that exact lineage, creates no electrical contact, never selects a historical
layer-13 cell, preserves a valid existing marker, and leaves a genuinely
pre-embodied body unmounted.

The exact copied tick-`354137` production body proved:

- cognitive bytes `107623033 -> 107625626`;
- complete neurons `1810 -> 1811`;
- all three historical layer-13 cells retained exactly;
- new dedicated lineage `474c4e4c494e4531000000000000008e`;
- every pre-existing cohort, neuron state, contact, frontier, mosaic, and
  hippocampal entry retained exactly;
- no layer-13 contact; and
- byte-identical second migration.

One testing false-green was explicitly rejected: sharing Cargo output between
the clean control and candidate reused the wrong binary and reported zero
matching tests. With distinct build outputs, both new V41 tests executed and
passed. The complete serial library suites then gave untouched `faff9e06`
`549 passed, 16 failed, 11 ignored` and V41
`551 passed, 16 failed, 11 ignored`, with the exact same 16 failing names.
V41 therefore introduces zero library-suite regressions; its two new tests are
the two additional passes. The inherited failures remain visible.

This evidence satisfies only the lawful body identity and bounded one-time
migration rows. All pressure, self-hearing, recurrence, restart-in-flight,
quiescence, resource, learned-speech, synchronized-device, audible-speaker,
and related-next-turn rows remain mandatory before deployment.

## Post-analysis repair attempt 26 — copied-body aggregate/interval refusal

The first copied-production-body rehearsal of local commit
`2440a9ed56834afeca70ed19e41680608f63b57e` is rejected. The exact local-only
image was `sha256:685153cbf504cef24bdfa2f4377301386e09c3a86b554d1f9f00d0ec647a3b16`,
its embedded native extension SHA-256 was
`838704df5ac27ff277533b227c43ffa58b4986efe2dbda63a08792edfe66bc71`,
and its verified release archive SHA-256 was
`dfd6409662c8eb72020b064b2971d742f9a35df14025ce9b586af08f90912947`.
It ran locally with no network, four CPUs, 16 GiB RAM, and the unchanged
production identity. Nothing was pushed, registered, deployed, or written to
production.

Before startup, Docker volume `guala_candidate_2440a9ed_354137_a` contained
only the exact copied tick-`354137` authority: `CURRENT` SHA-256
`18316fbf42c8f8e37c2468f35c7c3ea800019f6bd6c2a47a7b3194321df93839`,
current compact-object SHA-256
`d0fe3016834564baea2bb074e0fe58c38add2e4dffe23ef7e9cc5c8667af1537`,
predecessor compact-object SHA-256
`588f5a81efec88247a3a7c155137be96d2a8d2874e399170b6c5414891d090c2`,
and world SHA-256
`be1cb8b166324d4188c6bf7d1dce0def98961a0f75ea5538b4eb873941cf6b4e`.
The candidate therefore did not start from genesis or a substituted body.

V41 identity migration succeeded and the prior lineage-authority failure did
not recur. Native then completed in-process physical transitions through
generations `354138-354140`, then `354141-354144`, and then
`354145-354148`. Python refused the third aggregate before commit with the
exact first error `RuntimeError: motor-unit recruitment repeated a lineage`.
The error occurred twice. Immediately after each refusal, the next native
sequence began again at generation `354138`, rather than `354149` or
`354145`. This is direct evidence that the refusal discarded the whole
unsealed trajectory back to the persisted tick-`354137` body and allowed the
unattended loop to recompute previously covered intervals while the world had
already advanced. This is the restart/resurrection failure shape the repair
history is intended to prevent.

The exact source boundary is now localized. Rust retains each interval's
`motor_unit_recruitments` and `articulatory_unit_recruitments` inside its
corresponding `CausalIntervalEvidence`, and Python already validates motor to
articulatory causality independently inside each interval. Rust also extends
the same interval events into top-level presentation aggregates. Python then
re-validates that flattened multi-interval motor aggregate by constructing a
single lineage map and incorrectly requires one lineage to occur at most once
across the entire unsealed trajectory. A motor lineage may discharge again in
a later physical interval; global uniqueness therefore destroys interval
identity. Removing the guard alone is forbidden because it would allow an
articulatory event to borrow a matching motor discharge from a different
interval. The admissible repair is interval-scoped causality plus exact proof
that the top-level presentation sequence equals the ordered concatenation of
those already validated interval records.

The stopped forensic volume is preserved. Its post-stop authenticated
`CURRENT` is 158 bytes, SHA-256
`a99c8b6c93a6fba3c8cacad0a75808c04cc16425331a9131852e4843bc00c713`,
and names tick `354144`, raw state byte count `107551779`, raw state SHA-256
`3d8247510e90eb8bba9276e1340810785fa4184365bea4c7ed2d4f01fe3c4cd6`,
and predecessor raw SHA-256
`6e6d2001836e7ac6c329855d441a40fd6873e145b23ea2c81e0ae3c3ed17f784`.
Exactly two compact generation objects remain; their stored SHA-256 values are
`fc1ef234fbd650d5a908ed826d85a5a1e1020a426eb0d95d8ede6aa9aef289f5`
and `3e7ff2fc59d0832c7012ad8153fe8dccdaea6cb99f8dff7ef29603339537804b`.
The world is 939123 bytes with changed SHA-256
`0e4b4989010348c50e0218250e6bea12e62d75ed203983b5d981ed36deb18bde`.
Those post-stop bytes are failure evidence and will not be reused as a clean
candidate input.

Attempt 26 proves neither native pressure nor self-hearing. The visible
transport stopwatch reported `native_self_hearing=0.0ms` on the completed
pre-failure calls, and no exact pressure/body hash was captured through the
refused transaction. The next rehearsal must start from a newly populated
volume with the four original hashes and must prove interval-local motor/body
ownership, publication, exact pressure custody, restart, recurrence, and
quiescence before any production action or human device request.

## Post-analysis repair attempt 27 — interval fix passes; speech proof refuses

Commit `5f0df59481cc9c2e47c9b70d19949879ccd474b9` packaged as verified archive
SHA-256 `882f176eee3bf4af3f92590a5f45d49d6599a05aa736fe347eb7b667acac6583`
and local image
`sha256:2dda998b5c368b4b83fc41b5a80d5c6437e3491b753221b32c9de16e2552ca34`.
A fresh volume contained exactly the four original copied-production files
and hashes. The interval-scoped repair crossed the former `354145-354148`
failure and continued monotonically through `354182` with no refusal or
rollback; the attempt-26 defect is closed on the mature body.

The candidate is nevertheless rejected as speech proof. At tick `354158` the
public record exposed nonzero 12000-sample pressure SHA-256
`cdb9c197b47ebfc2a86e6adabea602353d9490a04f54bb39fdeae1474f2fd15b`
and four nonquiescent body ports, while the same record exposed zero layer-13
recruitments, zero applied motor quanta, zero self-hearing hops, zero
self-hearing transitions, and zero self-hearing fractals. It still labelled
the event `native_articulation_and_self_hearing_committed` and described
self-hearing before persistence. All nine completed transport records through
shutdown reported `native_self_hearing=0.0ms`. That observation claim is
false and no speech or self-hearing credit is allowed.

The stopped copied body is preserved at authenticated tick `354182`, state
bytes `107639965`, pointer SHA-256
`416a651e8b9f68fa8a4fdd6eb4f8616d00e9f9a59c17fa2b9a90b028b88dd8ba`,
raw current SHA-256
`a5f4e5bc323dd12d39a8eabe3bfa5d4b571c3ee628ebdd4976db9616a6c5b0f6`,
raw predecessor SHA-256
`705c3fa2d0fb19ec0b1f64a2c5b27c6fed253a9cde11615cf74abba39d2911da`,
and world SHA-256
`0cb24f9dbb463243b685f2f4911ddf46c60a7db23fe85ce02c71d91b8df8fc65`.
Production remained unchanged.

The deliberately reduced repair target is prelinguistic embodiment, not
learned speech: one existing layer-8 reached-load closing-glottis discharge
may recruit the dedicated vocal body. Layer-11 learned ordering, a word,
meaning, curriculum, and conversation are not prerequisites for this boundary.
The required causal chain is therefore layer 8 -> typed glottal motor ->
dedicated vocal body -> exact pressure -> the same organism's cochlea and the
human speaker. Calling this reduced boundary learned speech remains forbidden.

Cold inspection of an exact clone of the stopped tick-`354182` body proved
that its current native envelope retained an in-flight occurrence from tick
`354180`: 40,000 pressure bytes (20,000 samples), SHA-256
`96b9644dbcb2c36e5cc935f4755f7e82328cd6507ed5ddd6f624e2427b61b556`,
and 160,000 articulated-body bytes, SHA-256
`8bf4af3c1ded996fbc16d76361cb2ef777c9a8a5408e562ac053c577c0f7cc87`.
This proves exact emitted/in-flight custody only; it is not hearing evidence.

Restart attempt 27 then resumed the original stopped volume from tick
`354182`. Its first native successor was `354183`, so it did not resurrect the
original tick-`354137` production copy. Nevertheless, every completed
transport through at least generation `354225` reported
`native_self_hearing=0.0ms`; the retained occurrence was not admitted through
the cochlea. Shutdown was requested after the failure was established. The
ten-second graceful deadline expired while unsealed work continued through
`354250`, so Docker terminated the isolated process and only checkpoint
`354225` is authoritative. Its `CURRENT` SHA-256 is
`4d57aa352a03ce71695edb251570440f6277b58184d591771f50b30beacc45d9`,
current raw state SHA-256
`1777ec33deffca349aa06971ee35c93a53e86be243b77a23ab851dfcdb161035`,
predecessor raw SHA-256
`c886d63cde21c2b1c04b41395f99e9f23731be908cd50aad9ba3c64e85dd2446`,
and world SHA-256
`5297e993c1a532445edecb8374b7fd88c0c1284273dc45187907807e6d274683`.
The isolated volume is preserved; production remained untouched. This is a
restart-consumption failure, and repeating unattended intervals cannot close
it.

The restart-consumption failure is now exactly localized on another untouched
clone of the authenticated tick-`354182` body. Before startup, `CURRENT` was
SHA-256 `416a651e...`, state bytes were `107639965`, raw state SHA-256 was
`a5f4e5bc...`, and the source-`354180` consequence had the exact pressure and
body hashes above. Calling the real production `_startup()` with
`GUALA_CURRENT_FORMAT_MIGRATION=1` returned no boot error and retained identity
and tick, but published a different tick-`354182` body: pointer SHA-256
`4579bd979188d09de44ecd6b8d7320c6769125f35055826a5890cfd74c1048dc`,
state bytes `107439953`, raw state SHA-256
`3637dc55d062e69b0610ad5310257f0e527cf8494f08401f8d28a6486969645c`,
and all three in-flight accessors returned `None`. The exact byte loss is
`200012`: the 12-byte in-flight header plus five signed-16 channels over 20,000
samples. This is exact copied-production-body proof that migration deletes the
complete retained occurrence before unattended intake can see it.

The causal source is one argument in
`migrate_resident_organism_exact_energy_envelope`. The parser already decodes
`parsed.in_flight_acoustic`; the migration tuple preserves identity, tick,
fabric generation, joint, migrated cognition, vestibular body, and articulated
body, but omits that decoded field and passes literal `None` to
`encode_fabric`. Startup always invokes this current-format migration when the
release switch is `1`. The later intake correctly checks the resident
in-flight field first and returns immediately when it is `None`. Thus neither
the cochlear admission code nor waiting is the first failure.

The bounded repair surface is one native migration owner: carry the already
validated `Option<InFlightAcousticConsequence>` through the same re-encode.
Legacy/pre-acoustic bodies continue to decode `None`; current bodies retain
their exact bounded field; migration performs no hearing and does not consume,
duplicate, synthesize, scale, or interpret pressure. Required proof is: a
current-format envelope with nonzero in-flight pressure migrates with identical
source tick, pressure samples, four body trajectories, organism identity,
lived tick, fabric generation, joint, cognition, vestibular body, and
articulated body; an exact copied tick-`354182` clone then retains the two exact
hashes after real migration startup and consumes them exactly once through the
existing cochlear admission path. No L0-L4, DSF, neuron, contact, learned
route, curriculum, world, browser, or persistence schema change is admitted.

Verification command attempt 1 supplied a repository-wide `cargo fmt --check`
before the focused test. Existing formatting drift outside the changed file
made that check exit first, so no test executed. The unexecuted suffix also
repeated the already-recorded invalid short-name-plus-`--exact` form. It is not
evidence. No formatter was run and no unrelated file was changed. The test
must be invoked by its resolved fully qualified name without that failed
precondition.
