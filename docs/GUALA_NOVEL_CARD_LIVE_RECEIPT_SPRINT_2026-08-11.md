# Guala novel-card live receipt sprint — 2026-08-11

## Frozen scope

Make one best-evidence previously unseen approved card complete through the
live production request boundary and leave one bounded, durable,
intake-specific observation receipt.  Do not alter neuronal physics, L0--L4,
the lesson's exact clocks, its mounted sensory roster, organism identity, or
its post-quiescence formation law.

## Change-impact ledger

| Boundary | Exact statement |
|---|---|
| Input | One approved card surface, its paired tutor pressure waveform, the exact shared source clocks, and the current production organism predecessor. |
| Function path | `native_production_app.py::_card_lesson_hop_episodes` -> `native_joint_source_episode.py::encode_native_joint_source_episode` -> native `settle_native_joint_source_episode` -> `_perform_card_lesson_intake` -> `_perform_admitted_intake_locked` -> native prepare/commit -> one native body publication -> public observation. |
| Current transformations | Nine 250 ms source episodes are constructed for number 7. Each episode currently re-encodes approximately 105 ports and repeatedly materializes the same exact timestamps, zero phases, unit relevances, and repeated mapped values. The organism then settles each reached episode and publishes one successor after the final hop. |
| Expected output | The same byte-exact source episodes and the same native successor as the unoptimized path, but the live request completes below the 60-second gateway boundary. One compact external observation receipt remains addressable after continuous environmental transitions overwrite `last_transition`. |
| Invariants | Unchanged full joint seven-field DSF; identical source episode bytes; identical hop count, clocks, sensory participants, native successor, neuronal fractals/formations, identity, and conservation; no semantic card meaning, Python cognition, queue, owner, database, or append-only history. |
| Production acceptance | From the exact authenticated production predecessor, present one approved card with no evidence of prior presentation to the current body; receive a successful live response; retrieve its intake-specific receipt after at least one later unattended transition; cold-restore the successor; prove exact cognitive-chain evidence and bounded CPU/RAM/state/storage/Python calls. |
| Starting production evidence | Task 969, commit `7c4825e13e1ad61984f7151c06da4d78cafa69a3`, one healthy process. A Z request crossed the 60-second gateway boundary, while the organism continued. Only the later continuous transition remained observable, so Z attribution was unprovable. |

## Translation-boundary review

- The exact number-7 production-body model uses nine quarter-second episodes
  and 105 source ports per episode.
- Profiling episode construction recorded 957,078 rational encodes and about
  41 million Python calls.  The repeated values are custody representations,
  not separate physical causes.
- Exact production-body timing before correction: 9.539 seconds constructing
  source episodes plus 55.849 seconds settling/publishing, 65.387 seconds
  total.  The result committed nine hops, 954 complete DSF deliveries, 84
  neuronal fractals, 1,476 physical neuron transitions, and one successor.
- No neuron, contact, formation, energy, or DSF law requires repeated Python
  string construction of identical rationals.  Removing that repeated
  translation work changes no source byte or physical state.
- A per-episode Python rational cache was modeled and rejected.  Although it
  reduced calls, it still performed tens of thousands of rational operations
  and retained the wrong per-receptor translation architecture.
- The accepted boundary carries all nine exact clocks and nine contiguous
  port-major binary64 signal bodies through one native batch call.  Rust
  reconstructs and validates canonical GLJSRC02 from the immutable mounted
  receptor anatomy.  It does not alter a source value or organism law.
- The measured zero rational count is specifically zero Python rational
  translations.  Exact per-sample values still enter native rational physics;
  those physical values are required and are not counted as independent
  causes.  The boundary creates one native batch transaction, not one call per
  rational, sample, receptor, neuron, or hop.

## Failed hypotheses retained

- The Z timeout was not proof that the native organism refused the lesson.
  Later state changed, but the single latest-transition observer made exact
  attribution impossible.
- Static card raster decoding is not the dominant delay.  Exact rational
  translation and native settlement dominate.
- An asynchronous lesson job or queue is rejected: it would add an owner-like
  transport mechanism and retain the double-teach ambiguity rather than fix
  the causal request path.
- Response streaming is not the first correction: keeping a slow request
  alive would hide cognitive-budget waste instead of removing it.

## Evidence log

- `local model / pre-change`: exact production body SHA
  `3d708f084b751576d8df54075ff433fa6bac9a85a8a1855eba7bfdf875d0617f`,
  tick 38,656, 24,463,215 bytes.
- `production-shaped translation`: 105 sensory ports and nine hops; warm
  construction uses one Python-to-native compact batch call, zero Python
  rational encodes, 124,656 total profiled Python calls, and 1.077 seconds.
  The prior production-shaped profile recorded about 41 million calls and
  957,078 rational encodes.
- `byte and state parity`: all nine new GLJSRC02 bodies are byte-identical to
  the deployed encoder.  From the exact predecessor above, both paths produce
  byte-identical hop evidence and the exact successor
  `0a225e686cf89937d940a977b13dbd191a47a5cad3e6a41444e4ff2fd5196ce5`,
  tick 38,665, 24,465,080 bytes.
- `durable receipt`: one fixed, digest-bound JSON observation record replaces
  its predecessor atomically; it is outside cognition, never appended, and is
  not overwritten by later continuous sensory transitions.
- `live deployment`: commit
  `5c27fb206ad7ffcd8a6b10f95c8e55cc1fe12208`, task definition
  `dsf-ai-task:970`, immutable image
  `sha256:1110b54787061d0d8fe1f492710ee3447662791ac75b8af65427e0bfd0e6e5a4`.
  The release rehearsal cold-restored the exact live body with the same
  identity, zero Python cognition callbacks, one process, 179 reached complete
  neurons, and 196,550 developmental resting neurons.
- `live number-07`: HTTP 200 in 50 seconds.  Nine hops produced 954 complete
  DSF deliveries, 1,136 physical neuron transitions, and 110 post-quiescence
  neuronal fractals.  The successor was
  `b22ca9621dcc249eb591feb3b93561aa78eea5fb9b637d3236b87d1525d1aa00`,
  tick 39,529, 24,425,182 bytes.  The immediately observed pre-lesson body was
  24,423,305 bytes, so observed retained growth was 1,877 bytes.  The body
  still reported one mosaic, no reassembly, and no evidence of a learned word;
  none is claimed.
- `receipt continuity`: receipt
  `47fc24c009d2221198f1581f930c14ecafb156d7735091e1ac20adbda27d70e8`
  remained attributed to `number-07` after a later unattended interval.  A
  same-image process replacement then cold-restored the body and the exact
  receipt together.  After restart, unattended sensing advanced to tick
  39,593 while the state remained 24,425,182 bytes; identity, image, commit,
  and zero Python cognition callbacks remained exact.
