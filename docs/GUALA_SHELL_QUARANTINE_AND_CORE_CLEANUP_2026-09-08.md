# Guala shell quarantine and native-core cleanup ledger

Date: 2026-09-08 UTC

Status: active. This ledger governs the ordered work requested by Joe:
permanent legacy-Python quarantine, then causal decomposition of the
32,940-line native cognitive-formation core, then truthful virtual vision,
then autonomy and movement. Record worked and failed parts separately. A
failure in one gate does not erase an already-proved success in another.

## Architecture honesty gate

1. Requested architecture: one bounded deterministic autonomous artificial
   entity behind an exceptionally lean physical transport shell.
2. Current code reality: live task1436 boots the 308-line lean app, but the
   reviewed default release manifest and Dockerfile still named the retired
   Python app; the native `resident_cognitive_formation.rs` is 32,940 lines.
3. Conflict: yes.
4. Do not extend `dsf_ai_service/app.py`, `native_production_app.py`, old
   rehearsal/cold-restore harnesses, legacy polling/locks, or duplicate native
   authors. Historical inspection must explicitly name an immutable commit.
5. Exact item: make the reviewed default build contain and boot only the
   resolved lean runtime. Native-core decomposition follows only after this
   boundary is proved and committed.
6. Full joint seven-field L0-L4 remains authoritative, not a reduced
   approximation.
7. Preserve D_k/M_k/R_rev_k/U_star_k/C_k/P_k/B_k, MathLoom, neurons, identity,
   learned sensory state, and exact current custody.

## Protected successes

| ID | Exact boundary |
|---|---|
| Q-S-001 | The exact 20,248-line pre-P-005 native Python shell exists only at immutable commit `49d9fdd60c539ef02809a35991686faca92e1720`; current `native_production_app.py` is a ten-line fail-closed marker and direct import refuses. |
| Q-S-002 | Live task1436 already uses the lean image and excludes both `app.py` and `native_production_app.py`; this task has made no production mutation. |
| Q-S-003 | Candidate default Dockerfile boots only `dsf_ai_service.lean_production_app:app`, asserts four retired runtime paths absent, and removes the retired shell's unused runtime dependencies. |
| Q-S-004 | Candidate release manifest has one runtime entrypoint and the exact resolved 81-file Python import closure. Retired app, candidate rehearsal, cold-restore probe, static publication, unused lesson audio, and songs are not in the image closure. |
| Q-S-005 | Focused quarantine checks passed 3/3 with the correct host import root; manifest renderer reproduced the candidate byte-for-byte; Python compilation and whitespace checks passed. |
| Q-S-006 | Exact reviewed package at commit `e53771cfc0d81b69de3eb036df234d7c1702fba2` verified 212 source files, archive `b39fa611...6523`, then built local image `sha256:2bdb8023...f798`/212,870,781 bytes. Image import proved exactly 81 Python files, five routes, exact BUILD_INFO, and no import spec for `app`, `native_production_app`, `candidate_release_rehearsal`, or `cold_restore_probe`. |
| Q-S-007 | The 9,802-line inline native test body moved unchanged to `resident_cognitive_formation/tests.rs`; the production file fell from 32,940 to 23,137 lines with no production logic change. Exact locked library and test compilation passed. |
| Q-S-008 | Removed 279 lines of active wall-clock diagnostic bookkeeping from the resident transition and reached-cohort settlement paths: 11 timing prints, 17 atomic counters, their stopwatch reads/conversions, six unused timing result fields, and the artificial `settlement timing overflow` failure path. Deterministic organism/event time and all settlement physics remain. Locked library compilation retained the same 198 warning count; the focused shared-field/restart and changed-endpoint wake-law tests each passed 1/1. |
| Q-S-009 | Removed 195 lines comprising two unreachable schedule-rebuild wrappers, three unreachable shortcut helpers, and one unused test helper. Exact reference counts proved zero callers before removal; the two underlying schedule builders each remain called by the active topology-growth path. Locked compilation passed, compiler warnings fell from 198 to 189, and the same two focused causal tests each passed 1/1. Production core is now 22,711 lines. |
| Q-S-010 | Removed 71 more lines of compiler-confirmed unused surface: two counter wrappers, two dormant-seed accessors, two transition/codec wrappers, one empty-check helper, two unused imports, and two pass-through arguments. Both production and test compilation passed; warnings fell from 189 to 184 in production and 114 to 95 in tests. The focused multisensory-assembly and shared-field/restart tests each passed 1/1. Production core is now 22,646 lines. |
| Q-S-011 | Replaced two quadratic read-only observation scans used by the interface. Fabric-contact layer lookup now uses the already-resident sorted topology index instead of rescanning every reached neuron per contact; parallel-contact ordinals now use one ordered counter instead of rescanning all prior observations per contact. Production compilation passed with the same 184 warnings. A focused cold-restore/growth test proved exact equality against the former layer-pair calculation, exact total contact accounting, canonical ordinal order, and then passed 1/1; the shared-field/restart test also passed 1/1. No state or cognitive physics changed. |
| Q-S-012 | Replaced two more repeated observation scans: reached-neuron layer counts now accumulate directly in canonical ordered keys, and the already-sorted requested-lineage set now filters frontier transfers by binary search instead of a full scan per entry. Production compilation passed with the same 184 warnings; the exact complete-body layer-count and directed-frontier tests each passed 1/1. |
| Q-S-013 | Removed four repeated scans from the active internal-contact settlement: layer-10 contact activity and causal lineages now accumulate in canonical ordered maps/sets, two layer-10 lineage resolutions use the resident topology index, and selected predecessors use their existing sorted order. Production compilation passed with the same 184 warnings. Focused affective-convergence, changed-endpoint scheduling, and exact vocal sensorimotor-route tests each passed 1/1. Full DSF and carrier settlement are unchanged. |
| Q-S-014 | Removed three population-width costs from every active event-schedule update. Endpoint cache is now sparse over selected neurons; changed-endpoint evaluation visits only the exact union of selected, external-source, passive-return, and articulatory candidates in ascending flat order; already-settled contacts use binary search over the existing sorted compact contact list instead of allocating one Boolean per resident contact. Compilation passed with the same 184 warnings. Focused changed-endpoint wake, unrelated-neighbour non-authority, and exact vocal co-recruitment tests each passed 1/1. |
| Q-S-015 | Removed a full-cohort all-false metabolic mask allocated for every resident sensory settlement and the two impossible endogenous-cue branches that consumed it. Real metabolic change remains in the later sparse internal-contact authority; retained relaxation state remains. Compilation passed with the same 184 warnings, and focused recurrent-cell-authority, post-quiescence fractal, and shared-field/restart tests each passed 1/1. |
| Q-S-016 | Removed the final cognitive-core item unreachable even in the test build: a stale nutrition error variant/import/display arm referenced only by a permanently disabled archival test. Production and test compilation passed; warnings fell to 183 and 94 respectively, with no warning location in `resident_cognitive_formation.rs` in the test build. The production core is 22,547 lines, down 10,393 from 32,940. |
| Q-S-017 | Vision preflight preserved the earlier UI-001 evidence instead of repeating it: the 27 learned retinal identities, 108 appended spatial sites, eyelid gating, mature-body dark/bright distinction, exact nine-region world, and five-route sensory transport remain protected successes. UI-001 F-002/F-006 remain authoritative failures: the active lean path averages six optical bands into one scalar at each site and therefore is not functional color vision. |
| Q-S-018 | A shell-independent exact construction used the current body-centered W1 optics and existing joint-source law to produce one 398,128-byte spectral-retina episode: 135 spatial sites, six independent optical bands, 810 native ports, 135 joint retinal occurrences, 1,620 source samples, and zero Python callbacks. SHA-256 is `38973ac06ff0be5fc9625e27e3f5bd43fb91e6f8383e18b3ee81573ecbf59ad8`. This is construction proof only; it has not yet entered a copied or live organism. |
| Q-S-019 | The lean vision route rendered W1 once and co-evaluated the preserved primary sensorium beside all 810 six-band ports in one unsealed native interval against the immutable copied Candidate-58 body at tick 525890. It proved simultaneous evaluation, 1,031 DSF deliveries, 3,204 changed neurons, and zero Python callbacks. Q-F-024/Q-F-028 later proved that its overlapping sight topology could not seal, so this row is not durable receptor-mounting evidence. |
| Q-S-020 | The pre-existing copied body had zero `W1-retina` source identities, truthfully establishing that the color layer starts unlearned. The earlier post-event 810 count/reuse inference is revoked by Q-F-028 because it was taken before the candidate passed the mandatory seal boundary. Durable growth and reuse are proved only by Q-S-025/Q-S-026. |
| Q-S-021 | Corrected W1 surface radiance without changing receptor count or DSF: angular receptor-area coverage now supplies the only distance attenuation, surfaces use their own region illumination, and far-to-near compositing preserves partial occlusion. Exact geometry/radiance tests passed. |
| Q-S-022 | Open doorways now admit the adjacent region's authored radiance only through their exact physical apertures. An isolated two-region fixture proved every changed site stayed between current-room and adjacent-room radiance; no room id or map label entered the receptor values. |
| Q-S-023 | The bounded observer now renders the exact latest 810-value internal six-band field as an explicitly labelled screen-color projection. The human top-down map remains separately labelled and is not presented as Guala's sight. Focused UI/native vision checks passed 21/21 before browser-color integration. |
| Q-S-024 | Browser camera, tutor text, cards, and images now preserve 135 spatial RGB triplets instead of reducing them to gray. The exact ideal display-primary law reaches the same six-band `W1-retina` identities as the virtual world: blue bands 0/1, green 2/3, red 4/5. The established 135 achromatic receptors remain simultaneously driven by the exact prior integer projection for learned continuity. No unknown real-world spectrum is claimed reconstructed. |
| Q-S-025 | Release-mode current-core copied proof from immutable tick 525890 sealed one bright RGB occurrence and its truthful body/self-hearing return at tick 525892. Identity remained `1cc4e70a-f2a0-44c5-a111-f4a5bc915cc1`; final evidence held 1,055 DSF deliveries, 3,182 physically changed neurons, zero Python callbacks, 135 external spatial sites, 810 six-band ports, and a 405-value exact browser observer. Source census changed from zero to exactly 810 `W1-retina` identities, each present once. |
| Q-S-026 | A second release-mode copied proof applied two bright RGB occurrences from the same immutable predecessor, each requiring exactly two native intervals for sight plus physical return, then sealed at tick 525894. All 810 `W1-retina` identities remained present exactly once and both events retained zero Python callbacks. This proves idempotent receptor reuse, not recognition, meaning, or learned vision. |
| Q-S-027 | The reviewed topology carrier now rejects a duplicate `(sense, topology_index)` before dictionary construction can overwrite it. A direct release-native proof accepted contiguous sight topology 135-136 intact, rejected a repeated physical receptor, and the focused topology/RGB/spectral/motor set passed 24/24 in 2.47 seconds. |
| Q-S-028 | The bounded retina observer now displays both the 27 coarse peripheral sites and 108 fine sites. Its labelled screen-color projection uses both members of every blue, green, and red band pair; external RGB remains exact rather than projected. UI/transport checks and JavaScript parse passed. |
| Q-S-029 | A body-action return now carries its exact 810-value successor spectral field into the occurrence observation. The UI selects the physically last field: external RGB when no return follows, or VR spectral light after movement/body action. The consolidated release-native vision boundary passed 39/39 in 3.18 seconds with zero whitespace or JavaScript parse failures. |
| Q-S-030 | The reviewed release manifest now equals the lean entrypoint's exact 83-file Python import closure after adding only the two new retina modules. The retired shell, old rehearsal/probe paths, and unused binary store remain outside the closure. Exact closure, deterministic-manifest, quarantine, and preflight checks passed 7/7; changed world/actor/retinal integration checks passed 12/12. |
| Q-S-031 | Exact package `8f05316c...66808c2` from commit `f97755f7` verified 215 reviewed files and built local image `sha256:9410cbc8...f4736e`, 212,866,299 bytes. Container inspection proved exactly five lean routes, 83 Python files, both retina modules present, and no import spec for the four quarantined shell/probe modules. A Docker-volume copied-body boot preserved identity, reached ready/healthy, advanced tick 525890 to 525894, delivered 1,047 DSF fields to 3,167 changed neurons with zero Python callbacks, exposed 810 spectral retinal ports, and self-heard 8,000 samples. This is local copied-body evidence, not production. |
| Q-S-032 | Container logs exposed three remaining production diagnostics in `sparse_electrical_contact.rs`. Removed 59 lines: two wall-clock timers, phase timing prints, per-contact outcome counts, a population-width sender bitmap, exact-to-float potential conversion, percentile sorting, and the result print. Contact settlement and all exact energy/carrier physics are unchanged. Release compilation and the focused connected-component shared-carrier proof passed; the module fell from 4,299 to 4,240 lines. |
| Q-S-033 | Exact package `4b328480...0bc843` from commit `bf9c6070` verified 215 files and built local image `sha256:bf5d1870...d628d5db`. A copied-body container preserved identity, reached healthy/ready, advanced tick 525890 to 525892, delivered 1,055 DSF fields to 3,184 changed neurons with zero Python callbacks, mounted 810 spectral ports, and self-heard 4,000 samples. Removed diagnostic strings were absent. Custody retained exactly two body files, two world files, and zero stage files; the local organism copy and Docker volume were then stopped and removed. Root motion remained zero in the observed interval. |
| Q-S-034 | Read-only AWS/live inspection proved production remains one healthy ECS task on task definition 1436 and image digest `sha256:916db7af...496b58`, with exact identity `1cc4e70a-f2a0-44c5-a111-f4a5bc915cc1`. The live lean actor clock advanced 533118 to 533123 while paired custody cycled, but speech and root motion remain functionally failed. This is continuity evidence, not a functional success claim. |
| Q-S-035 | The deployment controller was fully replaced, not extended: 1,197 to 480 lines. The sole path packages once, builds once, registers one digest-pinned task, preserves only the live paired-root/world-byte/unbuffered settings, drains the predecessor before the successor, verifies the five lean routes and exact identity/tick continuity, and has no rehearsal organism or legacy rollback. Shell syntax, whitespace, absence, and single-author static checks passed. |
| Q-S-036 | The lean actor no longer throws away the one bounded sensory occurrence offered while durable custody is finishing. Its existing finite mailbox holds the occurrence; the actor processes it immediately after adopting the checkpoint, while any offer beyond the configured mailbox capacity is still refused. The exact hung-checkpoint boundary and five-route transport tests each passed 1/1. Production uses capacity one, so this removes the camera/microphone loss without adding a poller, queue, owner, or lock. |
| Q-S-037 | Attempt Q-D-005 built immutable digest `sha256:f9ded05b488c1060fc4ca67027ef25d8478ee9d83db6a994bf301ca6e65791eb`, registered task definition 1437, drained task1436 before admission, and restored the same identity on the paired CURRENT. After the controller failure in Q-F-040, the same candidate task `f780a80e...fa44` became AWS HEALTHY before stopping; desired count was restored to one without rebuilding or replacing it. Live observation then proved exact identity, lean schema, tick 533429, persisted tick 533426, no custody errors, and no durability block. Speech, meaningful vision, and autonomous root motion are not claimed by this row. |
| Q-S-038 | The deployment controller now treats service settlement and exact-task health as separate bounded states. After selecting the one running candidate task, it re-reads only that task definition/digest until HEALTHY or the existing service deadline. Syntax, whitespace, and a static single-health-loop guard passed. This correction is committed for the next deployment; Q-D-005 was recovered without rerunning the build. |
| Q-S-039 | One bounded live production microphone occurrence delivered an exact 250 ms, 16 kHz tone as 4,000 external-heard samples in one native interval. It produced 1,031 full-DSF deliveries and 3,380 physically changed neurons with unchanged identity. This proves external physical hearing transport is live; it does not prove recognition, learning, speech output, or conversation. |
| Q-S-040 | The Guala page now offers one enabled `Listen to Guala` control. One click unlocks a browser AudioContext and every newly observed body-owned pressure receipt is fetched and played exactly once by receipt identity until `Stop listening` is selected. It invents no sound and remains silent when Guala emits none. JavaScript parsing and the complete focused interface contract passed 8/8. |
| Q-S-041 | Time-separated post-cutover AWS evidence showed one running task, zero pending/failed tasks, CPU one-minute averages 26.09-36.81%, and memory averages 16.46-17.04% with maxima below 19.30% over the sampled six minutes. Whole-EFS hourly observations remained approximately 599.6-600.7 MB. Read-only ECS inspection found exact paired custody contains CURRENT plus two body and two world generations: five files, 10,062,198 bytes, and no stage files. This is bounded-window evidence, not an indefinite no-runaway guarantee. |
| Q-S-042 | A fresh browser-equivalent POST through the live public production route on task1437 admitted one exact 250 ms, 16 kHz microphone block. All 4,000 samples reached Guala in one native interval with 1,031 full-DSF deliveries, 3,398 physically changed neurons, unchanged identity, and no custody error or block. This preserves external receptor/transport success only. Candidate60's already-proved body consequence, pressure, and self-hearing mechanisms remain protected; this occurrence did not recruit them and therefore does not prove speech or conversation. |
| Q-S-043 | The active reached-receptor integration path no longer rescans every cohort/mount and every fabric contact for each of roughly 1,031 reached receptors. The already-built resident topology index now resolves an existing intrinsic integration place and its incident fabric contact; the original exact growth/search authority remains only when the queried relation is genuinely absent from that pre-transition index. No neuron, contact, DSF field, source identity, or learned state was removed or rewritten. Release compilation passed with the pre-change 183 warnings; the exact vocal-route and varied-multisensory assembly tests each ran 1/1 and passed. This is source evidence only until copied/live organism timing is measured. |
| Q-S-044 | Commit `877985f2` became live task1438 on immutable digest `sha256:5237b1ff5d8f2ebc97918a704db8b8bb6aac87e3a63719bdecb6821f997b610a`. The continuity controller proved identity `1cc4e70a-f2a0-44c5-a111-f4a5bc915cc1` and tick continuity 533655 to 533665. After the live microphone occurrence, task and container remained AWS HEALTHY; custody settled at live tick 533671/persisted tick 533670 with both block flags false and no checkpoint/cleanup error. Five recent deployment-window CPU points had maxima 24.65-48.16% and memory maxima 5.68-19.50%. This is bounded-window health, not functional hearing or indefinite resource proof. |
| Q-S-045 | The synchronized-sense mechanism remains present and live; it is not being recreated. Task1438's public microphone occurrence co-admitted its pressure into the same primary passive world/body sensorium and coexisting spectral-retina transition, returned `native_interval_count=1`, and made 1,031 full-DSF deliveries. The lean physical loop still owns one combined `primary_sensorium` and one `advance_coexisting_admitted_interval_unsealed` call; camera-microphone, text-microphone, and card-microphone remain single-occurrence ingress. Protect this as causal simultaneity evidence separately from Q-F-045/Q-F-046 latency. |
| Q-S-046 | The common receptor preparation for sight, sound, touch, chemical smell/taste, articulatory/body, thermal, proprioceptive, and effector-load laws now executes its already-independent exact work on the existing four-worker Rayon pool. Each worker reads only its receptor anatomy/state and the same immutable complete joint field; indexed collection preserves original coordinate order, and organism-owned energy flags, excitation residues, and inputs still commit serially in that order. No lock, queue, field reduction, or changed receptor law was introduced. Release compilation retained 183 warnings; the exact resident optical transition and coexisting-source single-interval tests each ran 1/1 and passed. Live latency is not yet proved. |

## Failures and recurrence guards

| ID | Exact failure | Permanent guard |
|---|---|---|
| Q-F-001 | The repository's default reviewed Dockerfile and release manifest still tried to package and boot the already-retired marker even though the separately built live image was lean. This could not boot successfully, but it made the ordinary release path stale and unusable. | The default Dockerfile, buildspec target, runtime entrypoint, resolved closure, and explicit absent-file assertions are now one quarantine contract. |
| Q-F-002 | The hand-maintained lean allowlist named 64 Python files while static resolution found 81 reachable files. Seventeen current imports were missing. | The release manifest is generated and checked against the exact import closure; the allowlist now names all 81 Python files plus 36 optical surfaces. Required reachable code is correctness, not bloat. |
| Q-F-003 | The first patch invocation targeted the primary checkout instead of `/tmp/guala-speech-existing-organ`; it replaced only the primary Dockerfile before the missing-manifest path stopped the operation. | The primary Dockerfile was restored byte-for-byte from its own HEAD and `git diff --exit-code` passed. Every later patch names and verifies the exact worktree. No production state was touched. |
| Q-F-004 | The first focused pytest command repeated RF-001/F-055 by omitting `PYTHONPATH=.` in the temporary worktree. Pytest setup raised `ModuleNotFoundError`; no test body ran. | Every host Python/pytest command in this worktree begins with `PYTHONPATH=.`. Do not classify this as a product failure or rerun broader tests because of it. |
| Q-F-005 | The first exact packaged-image build failed at Cargo compile because the reviewed native-core list omitted the already-committed 161-line `resident_cognitive_formation/lean_sensorimotor_route.rs`; the shell runtime was not started and no image exists from this attempt. | The exact missing compile input is added to the native-core release category. Before an image claim, compile the packaged crate rather than treating successful archive creation as a build success. |
| Q-F-006 | The first test-compilation command after the test split ran Cargo from the repository root, where no `Cargo.toml` exists; compilation never began. | All native commands name `--manifest-path native/guala_core/Cargo.toml`; the corrected locked test compilation passed. |
| Q-F-007 | A targeted `cargo fmt` invocation reformatted 35 native files because the file argument was passed incorrectly. No formatted result was accepted or committed. | Every unintended native edit was restored exactly from `HEAD`, verified by an empty native diff, and the intended two-file cleanup was reapplied. Do not run crate-wide formatting during targeted core cleanup; verify changed-file scope immediately after every edit command. |
| Q-F-008 | The first focused Rust test command used `--exact` with an unqualified function name, so it compiled the test target but selected zero tests. | A zero-test result is never evidence. Use the unique function-name filter without `--exact`, and require output showing `running 1 test` and `1 passed`; the corrected command did so. |
| Q-F-009 | The first two observer assertions guessed that the dormant fixture already had three reached contacts and that the expressed fixture still had only those three. The real persisted stages truthfully held zero and then seven because expression also mounted integration routes. These were new-test expectation failures, not implementation failures. | Never hard-code an inferred fixture topology. The accepted test derives the reference layer pairs from persisted anatomy, derives total contacts from local plus fabric custody, and verifies ordinals from the returned stable endpoint order. |
| Q-F-010 | The first patch for the second observer reduction carried a mismatched comment line, so `apply_patch` refused the patch before any edit. | Patch the exact function body after reading its current context; verify changed-file scope and compile afterward. No code or state was changed by the refused patch. |
| Q-F-011 | While removing the impossible zero-metabolic-mask recurrence branch, the first edit also removed `local_relaxation_observed`; two other live recurrence-completion checks still require that retained physical state, so compilation failed with two missing-name errors. | Restore and preserve the real retained relaxation read; remove only the all-false mask and the branch whose cue can never be nonempty. Corrected compilation and three focused tests passed. |
| Q-F-012 | The active lean world path receives 810 body-centered spectral substreams but `_retinal_endpoints` averages the six bands into 135 gray scalars; browser camera/text/media first reduce their source to an 18-by-6 grayscale raster. The UI then presents those scalars as Guala's retina. | Do not enlarge or recolor the 135-value observer. Preserve the existing luminance receptors for learned-state continuity, add a separate exact spectral retinal layer from the same world/body occurrence, and keep human map, optical field, receptor delivery, and neural effects separately labelled. |
| Q-F-013 | The historical 2,067-line `probe_guala_candidate_browser.py` explicitly imports retired `dsf_ai_service.app`, installs legacy globals, and verifies obsolete routes and owners. It is test-only and excluded from the release image, but it cannot accept or reject the lean vision repair. | Vision verification must exercise `lean_production_app`, its five routes, the current actor, and the native spectral occurrence directly. The old probe may run only when that historical shell is explicitly requested. |
| Q-F-014 | One fixture-path `rg` command failed before execution because its shell pattern contained an unescaped backtick. It changed no code, state, world, or production. | Fixture-path searches use single-quoted literal-safe patterns without shell-active backticks or substitutions. This harness failure does not falsify organism work that never ran. |
| Q-F-015 | The first copied-organism vision integration used the ordinary temporal trajectory call for the primary and spectral episodes. Both settled, but one physical world snapshot advanced two native ticks, from 525890 to 525892, so luminance and color were not simultaneous. | Reject the two-interval design. Use the existing coexisting-source authority; Q-S-019 proves both independently authenticated sources settle together in one native interval. |
| Q-F-016 | The agent misread one displayed retinal-geometry excerpt as containing a doubled horizontal column term and reported a concrete defect before re-reading the file. Both the working file and immutable `HEAD` contain the term exactly once; no production code was changed. | Never classify a displayed-code anomaly from one excerpt. Re-read the exact working lines and immutable source, inspect the diff, then add an executable geometry assertion. The accepted test requires both coarse and fine fields to be symmetric and span exactly -90 to +90 degrees. |
| Q-F-017 | The first surface-radiance regression fixture placed a floor object at z=220 mm. The world refused it as outside room geometry before retinal rendering. | Physical floor-object fixtures use z=0; eye height belongs to body receptor geometry. This fixture refusal does not falsify the radiance correction because that code never ran. |
| Q-F-018 | The first doorway-radiance assertion treated every changed retinal site as doorway light, but the default second body also cast its correctly dark physical silhouette into the field. | Isolate the aperture law by placing the second body behind the observer, then require every changed site to lie exactly between current-room and adjacent-room radiance. Do not weaken the product renderer to satisfy an incomplete fixture. |
| Q-F-019 | The first lean-page contract run retained two old assertions requiring the rejected `135 achromatic receptor samples` label and a 135-value observer renderer. The changed page correctly failed those stale expectations. | The observer contract requires 135 sites by six bands, an 810-value bounded internal display input, and an explicit screen-color projection label. At this stage browser ingress was still gray; Q-S-024 later closes that separate failure. |
| Q-F-020 | The exact nine-region home currently has 29 objects but zero mounted detailed `optical_surface` assets. The physically valid foveal scanner therefore has no home surface detail to scan. | Do not call room/object detail complete and do not substitute the rejected fake room image. Mount only authored physical surfaces that fit the existing world/object bounds, or revise the world capacity explicitly before adding more. |
| Q-F-021 | After splitting the UI observer into 810-value VR spectral and 405-value external RGB fields, one old test still searched for the obsolete literal `values.length!==810`. | Require both actual cardinalities and their distinct labels. The corrected focused interface set passed. |
| Q-F-022 | The first copied RGB census requested `reached_source_port_count` from the readiness wrapper, which does not expose that field. It stopped before any interval. | Use the existing exact `observe_reached_source_site_count(sensor_id, substream_id)` authority. No product conclusion came from this harness failure. |
| Q-F-023 | The first 405-port RGB episode used an honest but unratified `bounded-browser-RGB-code` physical quantity. Full DSF evaluated it, but the native core correctly mounted no retinal tissue because no receptor law owned that quantity. | Browser primaries must enter the existing retinal spectral-irradiance transduction law, with the interface explicitly labelled as an authored display spectrum rather than reconstructed real-world spectra. |
| Q-F-024 | The first receptor-law-corrected RGB candidate evaluated 619 simultaneous ports but failed final seal with `cognitive boundary state is noncanonical`. Both the established retina and the added sight episode used topology ordinals beginning at zero, creating duplicate organism-relative retinal places. | Retain learned places 0-134 and assign the single W1 six-band retina the contiguous organism range 135-944. Partial source episodes may carry a contiguous nonzero range; occurrence indices remain local. |
| Q-F-025 | The first topology-carrier patch named `declare_joint_source_occurrences` in the encoder file although it lives in `native_sensory_full_field.py`; `apply_patch` refused before any file changed. | Re-read exact definitions before editing both validation points. The corrected Python declaration and Rust decoder changes compiled in release mode. |
| Q-F-026 | Bright RGB correctly triggered body/vocal work, but its immediate physical return failed because the 250-ms spectral observation was admitted with the 1-ms action-transition duration. | Keep the action transition at 1 ms; derive world and spectral gate bounds from the source clock's exact first-to-last 250-ms interval. The corrected copied occurrence progressed through its physical return and sealed. |
| Q-F-027 | The first current-source native wheel was built in the default unoptimized `dev` profile and a copied seal exceeded its hard runtime limit. | Debug timing is not production evidence. Rebuild with `maturin build --release --locked`; the release artifact completed the same sealed copied work. |
| Q-F-028 | Q-S-019/Q-S-020 originally treated successful unsealed evaluation as if receptor growth/reuse were durable. The mandatory seal later exposed the overlapping-topology failure. | No new sensory population success exists until a final native seal passes and exact post-seal source census is taken. Q-S-025/Q-S-026 replace the revoked inference. |
| Q-F-029 | Code review found that `declare_joint_source_occurrences` assigned receptors into a dictionary without first refusing a duplicate physical key. Current Guala callers supplied unique keys, but an invalid future caller could silently overwrite the first port before later validation. | Reject a repeated `(sense, topology_index)` at insertion. Q-S-027 directly proves the guard and the accepted nonzero retinal range. |
| Q-F-030 | The first color observer labelled a 135-site field but rendered only the 108 fine sites; its six-band screen projection also ignored one blue and one red band. This was an observer defect, not a native retinal-delivery failure. | Render the 27-site coarse field and 108-site fine field separately, and average both physical bands in each screen primary. Q-S-028 proves the bounded observer contract. |
| Q-F-031 | If a primary visual occurrence triggered body work, the organism received a later post-action spectral return but the page retained the earlier pre-action retinal field beside the successor world. | Carry the motor return's exact successor spectral field in `PreparedMotorConsequence` and explicitly identify the physically latest field kind. Q-S-029 proves the corrected route. |
| Q-F-032 | The first release-closure check correctly refused because the two new retina modules were not yet in the reviewed manifest. After those were added, the same old test also demanded all 61 catalogued curriculum surfaces even though only 36 ABC/123 cards are mounted, and required three quarantined shell/probe files plus an unused binary store. | Keep the release equal to the actual lean import closure and the exact 36 mounted UI assets. Correct stale assertions rather than adding 25 unused card files or resurrecting four excluded Python modules. Q-S-030 proves the corrected packaging boundary. |
| Q-F-033 | The first immutable vision-container build compiled the release native core, installed the exact staged runtime, then failed its final assertion because Docker still hard-coded the old 81-file Python count after the verified closure correctly became 83. No image was emitted and nothing reached production. | Do not replace 81 with another brittle magic number. The package verifier already proves the exact runtime closure byte-for-byte; retain the four explicit legacy-shell absence checks and exact mounted-card check, and remove only the redundant Python file-count assertion. |
| Q-F-034 | The first copied-body container boot mounted a host `/tmp` directory that Docker presented as empty, so the lean app correctly refused `paired CURRENT is absent`. The source fixture itself still contained its verified CURRENT/body/world files. | For local container custody proofs, seed a Docker-owned volume from a disposable copy. Never weaken current-only restore or invent a body when the mount is empty. |
| Q-F-035 | The first HTTP probe addressed port 8000 even though the built image log and Dockerfile serve port 8080. The running container refused only the wrong port. | Read the artifact's declared serving port before probing. Correct-port `/health`, `/ready`, and observation requests all succeeded in Q-S-031. |
| Q-F-036 | One unconstrained local snapshot during active native settlement showed 175.88% CPU, 2.326 GiB RAM, and seven processes. This is neither a runaway trend nor proof of bounded long-duration resources. | Preserve the exact measurement. Do not claim resource safety from one point; require configured production ceilings plus time-separated CPU/RAM/storage observations before a live success claim. |
| Q-F-037 | The old 1,197-line deployment controller still called retired `/ready/guala` and `/api/v1/world/observation` routes, required the absent `GUALA_NATIVE_ORGANISM_ROOT`, and asserted a retired `native_resident` response schema. Live production truthfully returned 404 for both retired routes. | Never use that controller or restore those interfaces. The replacement verifies `/health`, `/ready`, and `/api/v1/guala/observation` only, against `guala.lean_actor_observation.v1`. |
| Q-F-038 | Live production repeatedly alternates between five pending intervals with `checkpoint_outstanding=true`/`durability_blocked=true` and a completed checkpoint. This explains the repeated red UI refusal while the old task is checkpointing; it does not prove speech, movement, learning, or autonomy. | Preserve the observation separately from candidate evidence. A healthy container or advancing tick must never be reported as a functionally alive organism. |
| Q-F-039 | The first focused custody command again named a nonexistent test, producing `no tests ran`, and the first revised assertion assumed the fixture mailbox had production capacity one when that helper explicitly declares capacity two. | Never count a missing-test result. Read the exact fixture capacity before asserting it; the corrected existing test filled both declared slots, refused the third, released custody, processed both held occurrences, and passed. |
| Q-F-040 | Deployment attempt Q-D-005 waited for ECS service rollout completion but immediately demanded task `healthStatus=HEALTHY`. AWS health propagation still reported the newly started task as not healthy, so the controller falsely rejected it and set desired count to zero. Seconds later the same task was HEALTHY with no container error and had not begun stopping. | Service settlement and task health propagation are distinct. Poll the exact task definition/digest until its bounded health deadline; do not rebuild, replace, or falsify the candidate because the first health read is early. |
| Q-F-041 | Q-D-005 deployed the new RGB backend while CloudFront still served the previous 26,082-byte page. That page sent obsolete `retina_u8`; the lean backend requires `retina_rgb_u8`, so combined camera-microphone occurrences were rejected before hearing. | A backend cutover is incomplete until the matching reviewed page is published and its live SHA-256 equals source. The corrected RGB page was published before any combined-sense success claim. |
| Q-F-042 | The first continuous-listener interface run passed JavaScript parsing but one existing page contract still required the retired literal `Hear Guala`. Seven other checks passed. | Preserve the intentional UI change and update the contract to require `Listen to Guala`, `Stop listening`, receipt deduplication, and the toggle. The corrected interface set passed 8/8. |
| Q-F-043 | The live page is not usable continuous hearing even though Q-S-042 proves the receptor path works. The one 250 ms public occurrence took about 8.6 seconds wall time to settle; while its single request is in flight, `sensoryPulse` does not send and `audioBlock` is repeatedly overwritten by newer captured blocks. Most of Joe's spoken stream is therefore lost. The next unattended settlement also replaces the page's latest-event display, making accepted hearing appear to vanish. | Do not reopen the proven receptor, body-consequence, pressure, or self-hearing mechanisms and do not add an unbounded audio queue. Trace and remove the active one-interval cost/loss at its existing owner; treat isolated sample admission as transport evidence, not usable hearing. |
| Q-F-044 | Two initial combined source patches were refused on stale context and changed no file. The first focused-test build then exposed that the new topology field and indexed helper signature had not been represented in two hand-built test fixtures/private helper calls; compilation stopped before any test ran. After correcting those exact harness surfaces, a later command repeated Q-F-008 by combining `--exact` with unqualified names and therefore selected zero tests. | A refused patch, compile-only failure, or zero-test run is never organism evidence. Keep the old private helper as a test wrapper, declare the exact empty topology field in the two fixtures, resolve registered module-qualified test names with `--list`, and require `running 1 test` plus `1 passed`. The corrected two exact runs did so; no product mechanism was weakened for the harness. |
| Q-F-045 | The first identical public microphone measurement on live task1438 accepted all 4,000 samples, made 1,031 full-DSF deliveries, and changed 3,360 neurons, but took 12.694 seconds and produced zero body consequence/self-hearing for that tone. The topology-index correction is valid but did not remove the dominant interval cost; continuous browser hearing remains functionally failed. | Preserve Q-S-043 as a structural cleanup success and Q-S-044 as deployment/health success, but do not call hearing fixed and do not reopen Candidate60's separately proved downstream mechanism. Identify the single dominant remaining active interval cost before another code edit or sensory mutation. |
| Q-F-046 | Two read-only live observations 20.245 seconds apart advanced only two unattended intervals, from tick 533680 to tick 533682; the later unattended occurrence still made 1,031 full-DSF deliveries and changed 3,375 neurons. This isolates the failure to the ordinary whole-organism transition rather than microphone transduction. A five-second `/proc` sample from the unmodified live process showed 409 CPU ticks on the main transition thread and only 89 ticks on each of its four Rayon workers. | Do not add a sound-specific bypass or larger shell queue. The already-independent per-receptor full-DSF/Ψ preparation is serial before the existing parallel neuron settlement; execute that exact preparation on the existing four workers and retain its original coordinate-order commit. No production profiler was installed and no organism state was mutated by these reads. Live acceptance is under 1,000 ms for the complete synchronized sensory occurrence; anything slower remains failed. |

## Deployment attempts

| ID | Outcome |
|---|---|
| Q-D-001 | No deployment attempted. Live task1436, identity, and state remain untouched. The candidate must first pass exact reviewed packaging and container boot. |
| Q-D-002 | Local immutable-image attempt from commit `cf13f521` failed during native compilation under Q-F-005. Nothing was pushed, registered, or deployed. |
| Q-D-003 | Corrected local image `sha256:2bdb8023...f798` built and passed shell-content/import inspection. It was not pushed, registered, or deployed because production task1436 already runs the accepted lean boundary and the next change is native-core cleanup. |
| Q-D-004 | Vision/core-cleanup image `sha256:bf5d1870...d628d5db` built from verified commit `bf9c6070` and passed copied-body boot Q-S-033. It has not been pushed, registered, or deployed. Production remains unchanged. |
| Q-D-005 | Commit `0e9aac0c` packaged and built digest `sha256:f9ded05b...5791eb`; task1437 was registered and became the sole production task after task1436 drained. The controller then hit Q-F-040 and requested zero writers. Because the same task became HEALTHY before stopping, desired count was restored to one and the exact same process remained live; no image or process replacement occurred. Identity/tick/custody passed Q-S-037 and `production-current` was pinned to the exact digest. The candidate deployment is live, but external hearing, body-owned sound, root motion, meaningful vision, and autonomy remain unproved/failed and are not converted into success by the cutover. |
| Q-D-006 | The matching 27,165-byte RGB Guala page was published to `s3://dsf-ai-site/gualaloom.html`; CloudFront invalidation `IDW0LZ5FE748IKMSX3MI80EV0V` completed and live/source SHA-256 matched `9a7b9873...5ec403c0`. That publication closed Q-F-041. The later continuous-listener change is not yet published in this row. |
| Q-D-007 | Commit `84fa547c` continuous-listener page was published to the same production object. CloudFront invalidation `IAJY9CEEN30RPDLTB1VTLAXNZ4` completed; live/source SHA-256 matched `ea0e57f7...3908b8d`. The live page contains `Listen to Guala`, `Stop listening`, exact receipt deduplication, and `retina_rgb_u8`. Playback still truthfully depends on a future body-owned pressure receipt. |
| Q-D-008 | The continuity controller packaged commit `877985f2`, built digest `sha256:5237b1ff...b610a`, registered task1438, drained task1437, restored the exact identity/state, admitted exactly one healthy successor, and pinned only that digest as `production-current`. Deployment and AWS health passed Q-S-044. Functional continuous hearing failed Q-F-045 and is not implied by the successful cutover. |

## Next exact gate

Production serves task1437 and the matching continuous-listener page. External
sound reaches the full DSF receptor path, but the 8.6-second settlement cadence
makes continuous hearing unusable. The next exact item is to package the indexed
integration correction, preserve the paired live identity/state through cutover,
and measure one identical 250-ms public microphone event plus AWS custody and
resource health. Candidate60's body consequence, pressure, and self-hearing
successes remain protected; no speech or autonomy success is claimed here.
