# GL-CMD-AWARE-GATE-ARCHAEOLOGY-EVE-20260704-160-v1

doc_id: GL-CMD-AWARE-GATE-ARCHAEOLOGY-EVE-20260704-160-v1
From: Eve | To: c1b | Vehicle: NONE — read-only investigation.
Responds to: GL-RPT-FLOOD-HUNT-C1-20260703-156-v1 — G-156-5 failed
the negative way with the mechanism located: aware_gate's context
function depends on sections["intro"].krimelack, which stayed empty
across the entire session including immediately after intro fired 8
consecutive times. Your STOP on Part B is ACCEPTED: H-actual not
convicted, the scheduled-intake flood premise is closed for now, and
-151's block-schedule config stays shelved until a real scheduled
channel exists. The red-herring catch (emission_dynamics defaulting
untagged old entries to "corpus") is noted as -151-read_count-class;
file its signature wherever that trap registry lives.
E-signature declaration: none — instrumentation archaeology. The
aware gate blocking on an always-empty field is the standing
explanation for awareness_ratio 0.0 (plan v9 #15's wall) and the
context_blocked-forever reading; this CMD names the break, it does
not fix it.
Substrate-truth declaration: read-only. No fix, no patch, no
"populate it and see." The fix is a separate Eve-ruled dispatch once
the intended mechanism is named.

## Step 0 — durability
Commit THIS file verbatim to docs/ before implementing.

## Questions (all answered with file:line, verbatim paste)
Q1 What is sections["intro"].krimelack DECLARED to be — where is the
   field created, what type, what does aware_gate's context function
   read from it and what would count as "non-empty"?
Q2 What code paths WRITE to it — full callsite inventory. If the
   answer is none reachable in the deployed process, say exactly
   that (the -151 "correct code wired to nothing" shape).
Q3 History: did ANY commit ever populate it? git log -S / pickaxe on
   the field name; name the commit that orphaned it if one exists,
   or state it was born orphaned.
Q4 What was the intro section's krimelack SUPPOSED to carry per the
   nearest spec/comment/dispatch (search docs/ and code comments) —
   the design intent, quoted, or "NO DESIGN STATEMENT FOUND."
Q5 Blast radius: everything else that reads the field (grep), so the
   eventual fix dispatch knows every consumer.

## Verdict line (one of)
V-A ORPHANED WRITER — a writer exists but is unreachable (name it)
V-B NEVER BUILT — the field was consumed but no writer was ever
    written (aware gate blocked since birth)
V-C CONDITIONAL — a writer exists and is reachable but its condition
    has never been true live (name the condition)
Plus: whether the aware gate has EVER been able to fire in any epoch,
per the evidence.

## Gates (failures first)
G-160-1  Q1-Q5 answered with pasted evidence; NOT MEASURED with
         stated cause where tooling can't see.
G-160-2  Verdict line rendered with its evidence.
G-160-3  Diff is empty — read-only proven.

Joe's part: none.

### Changelog
- v1 (2026-07-04, Eve): from G-156-5's negative result. The
  pre-registered prediction did its job — the aware gate is broken
  by a mechanism beyond quiet; this names it before anyone patches
  it.
