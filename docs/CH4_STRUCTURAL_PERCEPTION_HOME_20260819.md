# CH4 — home of the structural perception work (Joseph, 2026-08-19)

Joseph's word, this date: "This work can have a home — and that home
is CH4." Filed so the home is on the record, not in a conversation.

## What lives here as of tonight
- **The trash filters** (basics of investing, before any physics):
  price >= $5, median day-money >= $200k, trades every session, no
  destroyed shells (>=1000x collapse), >= 6 months of life. All three
  of the book's realized catastrophes die in these screens for free.
  Universe filed: artifacts/ch4_uf/population_universe_20260819.csv
  (4,303 validated lives).
- **The population reading layer**: every validated life through the
  canonical v2 chain, one reading per completed close, whole stored
  history. artifacts/ch4_uf/population_lanes/ (~813MB, not in git).
  Appended nightly by the CH4 runner after the store refresh
  (currently a full re-read pass ~30 min; state-carrying incremental
  append is an open engineering item).
- **The joint-structure census** (tools/ch4_joint_structure_census.py):
  eight facts per event slice — deaths presence + slope, channel
  integrity, push balance, buzz / strain / lock against the life's
  own trailing history, depth vs own peak — never single-signal,
  boundaries structural or self-relative. Decade result on file:
  13 structures pay in both halves (~10% of supply), 35 to avoid
  (~37%); the depth axis splits the lists; the collapsed-life corner
  is CLIFFED (one-fact steps swing +237 to -1011 per 100 events) —
  refused wholesale by curvature, not by cell values.
- **The field instrument** (tools/field_page.py): any of the 4,303
  lives drawn in the last session's 3D lane view on demand; recipes
  validated against that session's own shipped data (14/15 exact;
  the repaired-carry lane is a labeled reconstruction).
- **What CH4 already ran and keeps running untouched**: nightly
  store refresh, herd state export, herd_kgate_v1 (the surviving
  modest edge), its books and page.

## The missing layers, named by Joseph this date, in build order
1. **Peer assessment** — each event judged inside its same-day
   cohort, not alone (the census's own events form the cohorts).
2. **Market energy** — the herd machinery's field-level energy state
   conditioning every reading.
3. **News / events impacts** — the epoch news-signal bodies in the
   repo root, deep-read AS FOUND before wiring.
4. **Epoch predictions and impacts** — the 32-channel world-pressure
   field (Xi) of the spec. Later, per Joseph; place marked.

## Strategy scope
Both sides, earned separately. The assessment layer is side-neutral
by construction: the cliff that kills shorts is a discharge a gain
book could collect. Short harvests are live (CH3/CH6 door + laws).
Any gain harvest requires its own declared class-specific study
first — buying ALL spikes is already falsified on the decade record
(docs/CH3_CH6_CAPABILITY_AND_FAILURE_20260818.md); only structure-
specific long harvests may even be tested.

## Standing rules carried in
Declared constants before results; derive/confirm splits; true-gap
accounting; falsifications stay filed beside what replaced them;
Rule 11 adversarial review before physics-carrying code runs with
money; no single-signal evaluations; the verdict channel is
Joseph's.
