# GL-CMD-C2-WHOLE-BRAIN-EVE-20260704-168-v3

doc_id: GL-CMD-C2-WHOLE-BRAIN-EVE-20260704-168-v3
From: Eve | To: c1a | Supersedes -168-v1 AND -168-v2 (retained; do
not execute). FULL document. Vehicle: model work only — zero
live-path changes.
Correction record: v1 ignored the chat record; v2 fixed that but
kept the deeper error Joe just named — validating mechanism #1 in
isolation. The canonical architecture (-103) is DNA-GROWS-BRAIN:
the organism develops WHOLE, all fifteen mechanisms co-present and
co-shaping, immature everywhere rather than perfect anywhere. A
roof is not built before walls; recall is not perfected before the
brain that recall lives in exists.
Substrate-truth declaration: whole loom organism, real primitives
only; RNG in neuron identity banned; exp(1j·Δ) binding prohibited
(6/22 epitaph stands); no tuned constants; too-good STOP armed.

## Step 0 — durability
Commit THIS file verbatim to docs/ before implementing.

## The job: raise the organism, not bench the organ
A1 BOOT THE WHOLE BRAIN: the committed 8-hemisphere seed (Watts-
   Strogatz N=8, seed 8 neurons/ring, full per-neuron primitive
   stack, no-reset honored, contact inhibition in place). All
   mechanisms that exist in the module run TOGETHER from tick zero
   — nothing stubbed off to "later" that is already built.
A2 RAISE IT ON EXPERIENCE: feed a multi-modal curriculum (the
   sensory transducer on the codex branch + language), continuous,
   with sleep/replay cycles running inside the model — the organism
   lives a compressed childhood, it does not take a test battery.
A3 FIFTEEN GAUGES, ONE ORGANISM: measure ALL fifteen mechanisms as
   co-developing readouts on the SAME run — recall, association,
   cross-modal, habituation, recognition, attention, sequence,
   retention-through-model-sleep, and the rest — each reported as a
   development CURVE over the organism's life, not a pass/fail on
   an isolated bench. Immature numbers are the expected, honest
   output. The deliverable is the growth chart.
A4 THE RECALL REPRESENTATION QUESTION rides INSIDE the organism:
   event_count (champion, ~67% standing), Δ-rate (-134, resumed
   verbatim from the 6/22 record — appendix of -168-v2 applies),
   and Eve's subspace masks are compared as the LIVING brain's
   recall readout under the same development run — the winner is
   whichever the organism recalls best WITH, not best alone.
A5 WALLS NAMED, NOT HIDDEN: folding-during-experience remains
   blocked (n_eff 3.000 vs 2.943, hetero-krimelack attempt failed)
   — the organism grows within seed capacity this run; the blocker
   is stated on every growth chart it caps. Any mechanism with no
   code path yet (who-tags, etc.) appears on the chart as ABSENT,
   never simulated.

## Gates (failures first)
G-1 One organism, one run, fifteen curves — no mechanism reported
    from a separate bench.
G-2 STOPs: >95% on any degraded gauge = suspect hash; RNG in
    neuron identity; exp(1j·Δ) binding.
G-3 Every curve carries its scaffolding-vs-emergent label per -103.
G-4 Diff proves scope: model only.

Joe's part: none until the first growth chart — then it comes to
you as a picture of a developing brain, not a checklist.

### Changelog
- v3 (2026-07-04, Eve): Joe's correction — the whole brain at once.
  Organ-bench framing of v1/v2 retired; representation question
  demoted from the project to a gauge within the organism.
