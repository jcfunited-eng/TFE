# Hearing profile loss localization and upstream law — 2026-07-27

## Architecture honesty gate

1. Requested architecture: an authenticated hearing-only physical profile
   whose repeated, causally bound multisensory experiences grow one durable
   THING resonance basin across speaker variants.  A whole causal mosaic,
   never a signal match, may release a vocal action.
2. Current code reality: the canonical L0–L4 default remains byte-identical.
   The auditory peak neuron downstream of it identifies one ear/channel/
   component/field lane by four ternary peak relations.  Repetition increments
   a counter but does not change the neuron's receptive geometry.
3. Conflict with requested architecture: yes.  The present neuron cannot
   become the progressively defined vibrating thread described by Joseph.
4. Mechanisms not extended: exact complete-root retrieval, q IDs, the current
   four-trit neuron as meaning authority, set-intersection/L6 locks as word
   meaning, transcript or tutor labels, sign/ray/order identity, scalar
   scores, thresholds, nearest matching, ML, static word tables, and silent
   profile migration.
5. Single exact next item: test the smallest lawful full-field basin below.
   Promote it only if every predeclared held-out and conflict cell passes.
6. Field evaluation: the complete joint two-ear × cochlear receptor ×
   pressure/phase component × causal frame ×
   D/M/R/U/C/P/B trajectory is authoritative.
7. Reduced approximation: none is allowed to become identity.  A finite
   algebraic counter-witness may falsify a proposed full-field relation, but
   it cannot replace the retained trajectory.

## Exact loss boundary

Let one verified bilateral auditory episode be

\[
X:E\times C\times K\times I\times F\longrightarrow\mathbb{Q},
\]

where:

- \(E=(left,right)\);
- \(C=(erb_0,\ldots,erb_{15})\);
- \(K=(pressure,phase)\);
- \(I=(0,\ldots,99)\) is the authenticated physical receptor-frame order;
- \(F=(D_k,M_k,R_{rev,k},U^*_k,C_k,P_k,B_k)\).

The current auditory peak map is:

\[
\pi(X)=H(e,c,k,f,
 \operatorname{sgn}\Delta_2,
 \operatorname{ord}(|\Delta_2|,|\Delta_1|),
 \operatorname{ord}(x_3,x_1),
 \operatorname{ord}(\Delta t_2,\Delta t_1)).
\]

It is evaluated independently for one field lane.  Joint cross-ear,
cross-receptor, cross-component, cross-field, and whole-trajectory geometry
does not enter the neuron ID.

For a neuron already grown after independent witnesses \(w_1,w_2\), the
implemented reinforcement recurrence is:

\[
(w_1,w_2,s_n)\longmapsto
(w_1,w_2,\min(s_n+1,s_{max})).
\]

The support geometry is invariant.  Only strength changes.  Thus the third,
fifth, or twentieth experience cannot sharpen, widen, split, or otherwise
alter that neuron's receptive field.  The temporal assembly receives only
`(neuron_id, segment_index)` and cannot reconstruct geometry discarded by
\(\pi\).

This is the first proven architectural loss relevant to experience-grown
familiarity.  It occurs in the hearing-specific receptor-neuron encoding
after complete L0–L4 fields exist.  No canonical L0–L4 equation needs to
change to expose the missing structure.

## Typed input required by any replacement profile

`HearingBilateralFullFieldTrajectory`

- immutable profile ID and profile receipt;
- left/right calibration, transport, topology, and settlement receipts;
- exactly 44,800 ordered rational values:
  \(2\times16\times2\times100\times7\);
- every value linked to its explicit field name, causal frame, full L4 tuple,
  source interval, and L0–L4 trace receipt;
- authenticated object/context continuity and causal consequence receipts
  supplied separately from the acoustic field;
- typed state `observed`, `unresolved`, or `indeterminate_resource`.

Labels, transcript text, command names, filenames, speaker IDs, q IDs, and
legacy decision vectors are not typed inputs.

## Smallest physical basin candidate

Linear acoustic gain and source superposition supply one exact, non-tuned
relation.  If a THING \(T\) has causally bound bilateral trajectories
\(X_1,\ldots,X_n\), define:

\[
\mathcal B_T^{(n)}
=
\left\{
\sum_{i=1}^{n}\alpha_iX_i
\;\middle|\;
\alpha_i\in\mathbb Q_{\ge0}
\right\}.
\]

This is a positive rational projective cone over the complete joint field.
It has these properties:

- a new causally bound experience adds a generator and can only expand the
  basin;
- positive gain changes are admitted by the same equation;
- physical linear mixtures of retained generators are admitted;
- no distance, tolerance, score, nearest exemplar, tuned constant, label, or
  whole-root equality exists;
- every one of the 44,800 coordinates participates in membership;
- divergent object/context consequences own different cones; overlap is
  ambiguous and never resolved by a score.

The cone is the least restrictive basin justified by exact linear acoustics.
Its rational linear span

\[
\operatorname{span}_{\mathbb Q}(X_1,\ldots,X_n)
\]

is a superset.  Therefore:

\[
\operatorname{rank}_{\mathbb Q}[X_1\cdots X_n\;Q]
>
\operatorname{rank}_{\mathbb Q}[X_1\cdots X_n]
\;\Longrightarrow\;
Q\notin\mathcal B_T^{(n)}.
\]

The probe proves rank increase through a nonzero modular determinant after
exact denominator clearing.  This is an algebraic non-membership proof, not
a probabilistic comparison.

## Typed output if and only if the candidate validates

`AuthenticatedHearingProjectiveBasin`

- immutable profile ID
  `guala.hearing.bilateral_causal_projective_cone.v1`;
- ordered generator roots and their complete trajectory authorities;
- authenticated THING/context/consequence continuity roots;
- exact cone membership proof or exact non-membership rank witness;
- typed decision `resonant`, `unknown`, `ambiguous`, or
  `indeterminate_resource`;
- bounded generator and canonical-byte allocation;
- cold-restore receipt that reproduces identical canonical bytes.

`resonant` would be only a physical familiarity contribution to a whole
THING mosaic.  It would not itself name a word or authorize speech.

## Promotion rule

No production profile is implemented merely because the equation is lawful.
It must first resolve every predeclared held-out speaker only into the correct
causal THING, reject different commands and wrong object/context, preserve
unknown sounds, and remain ambiguous under conflicting consequences.

One held-out failure falsifies this candidate.  On failure, adding a distance,
epsilon, learned cutoff, nearest cone, partial-coordinate count, or
label-derived boundary is forbidden.

## Identifiability boundary

For a finite set of causally grouped experiences, an unheard trajectory
\(Q\) has no semantic membership given by physics alone unless a declared
physical equivalence or basin law relates it to the set.

To see this, hold all authenticated training bytes and \(Q\) fixed.  One
physically possible world causally binds \(Q\)'s vocal event to THING \(T\);
another binds the identical acoustic event to a different consequence or to
no known THING.  Before that consequence/context is observed, every admitted
input byte is identical.  A deterministic mechanism must return the same
state in both worlds, so it cannot be correct in both.

Therefore auditory-only held-out semantic resolution requires at least one
of:

- a proven physical invariance/basin law;
- an admitted distance or tolerance boundary;
- prior linguistic structure;
- or live causal object/context continuity.

The last three cannot be silently disguised as receptor physics.  If the
projective cone and every other predeclared exact physical law fail, the
truthful result is not a fabricated hearing profile; it is the exact missing
authority identified by this boundary.
