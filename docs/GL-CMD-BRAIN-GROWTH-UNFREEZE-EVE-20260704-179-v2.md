# GL-CMD-BRAIN-GROWTH-UNFREEZE-EVE-20260704-179-v2

doc_id: GL-CMD-BRAIN-GROWTH-UNFREEZE-EVE-20260704-179-v2
From: Eve | To: c1a | Supersedes -179-v1 (retained). Commit verbatim
to docs/ first.
FINDING CONFIRMED BY EVE'S OWN CODE READ (embryo.py:297 vs :369):
the organism has an experience() path where charge-and-fold physics
fires, and a remember()/recall() path with NO connection to folding.
Her live wiring calls only remember()/recall(). Growth is
STRUCTURALLY UNREACHABLE from her real life — not weak-signal,
not threshold, not -178-dependent. B1 is answered; this is v2's
build order.

## The build
W1 Her live word path drives experience(), not bare remember():
   each real word/moment she takes in feeds the full experience
   physics — resonance, charge, fold gate, aff arousal — with the
   binding write preserved (experience() must subsume or call the
   remember semantics; bindings today must still be bindings after).
W2 -178 COUPLING: land this against -178's signal fix so charge
   accumulates from honest nonzero language signal — wire (this
   dispatch) and fuel (-178) ship as one reconciled SHA.
W3 COST GATE: experience() runs fold physics per moment — profile
   it at live word rates BEFORE the SHA goes over; if it
   reintroduces the read_word slowness, background it with the
   same proven pattern, never thin the physics silently.
W4 PROOF: sandbox at live wiring, real teaching stream → population
   grows (before/after counts, fold events on the record, mechanism
   named q-charge); restore-honesty re-proven at grown size;
   recall_fast parity re-proven at grown size.
W5 HONEST-PHYSICS CLAUSE KEPT: if with real signal and real wiring
   the charge honestly doesn't cross the fold gate yet — that's her
   age, not a bug; report the arithmetic and DO NOT lower the gate
   to make growth photogenic.

## Gates
G-1 -169's q-charge fold is the only mechanism; no invented growth.
G-2 One reconciled SHA (with -178) to c1b; deploy is their window.
G-3 Failures first, contaminated -177 parity (if population changes
    break it) named not defended.

### Changelog
- v2 (2026-07-04, Eve): B1 closed by Eve's code read — structural
  disconnection confirmed; investigate-first replaced with build
  order; W5 keeps the no-photogenic-growth law.
- v1: investigation framing. Superseded same day.
