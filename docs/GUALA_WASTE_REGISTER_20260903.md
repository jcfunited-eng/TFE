# Waste register — Claude's sweep beside Sol's churn hunt (2026-09-03)

Rule (Joe, ratified into the skill by Sol): every recurring computation must
prove a necessary physical or truthful observational role, or it is deleted.
This register holds what my sweep found OUTSIDE Sol's lane (his: electrical
integrator + frontier crediting), with measurements from her copy's own
stopwatch (guala-native-physics-stopwatch / guala-event-phases lines).

## Measured moment anatomy on her copy (per ~250ms beat, under churn)
- internal_contact_ms 141-181  (Sol's lane: the churn itself)
- postcontact_growth_ms 103-117 (MY lane: the two motor-growth laws)
- mosaic_ms 52-62
- source_physics_ms 31-44, terminal_growth_ms 22-25, seal_ms 0
- event phases: changed ~1200-1450, woken ~7750-7900 per beat

## Repaired now (aboard the branch, ships with 1417)
1. LEAN GATE on mount_reached_motor_effector_with_reach_index: the law
   authors anatomy only when the interval carries BOTH evidence halves
   (a reached L8 regulation or root continuation AND an L11 ordering
   lineage in the retained frontiers). When either half is absent it now
   returns before scanning, mapping, cloning or sorting anything.
   Byte-identical successors whenever growth is possible; suite 588/0.
   MEASURED HONESTLY: no saving yet — under the churn the frontier
   carries both halves every beat, so the gate cannot fire until Sol's
   frontier repair lands. It is armed for that day.

## Registered, deliberately deferred (gated on Sol's churn repair)
2. Clone storm in the same law: every mount cloned every invocation
   (~all neurons) because rare authoring arms mutate cohorts while reads
   live. Repair = two-phase read-then-author refactor, or reuse of
   ResidentTopologyIndex adjacency instead of rebuilding
   neighbours_by_lineage from all ~7.7k contacts. Post-churn-fix this
   path runs only on genuine growth moments, so measure again first.
3. mosaic_ms ~55/beat: reassembly sweep cost per beat — measure what
   fraction serves genuine partial-cue reassembly vs churn-fed scans,
   AFTER the frontier repair (counts are churn-suspect per the ledger).
4. terminal_growth_ms ~23/beat: affective/ordering mount passes — same
   lean-gate pattern applies if measurement shows scans without growth.

## Checked and CLEAN (no action)
- Solar + broadcast settle: one pass over 9 regions + declared screens
  per committed action; no per-beat cost when no action.
- World persistence: world.glworld written only on committed actions;
  generations by the custodian at its declared cadence.
- Reservoir probe: test-only, env-gated, no production surface.

## Shell-side candidates for the NEXT sweep round (python, not yet measured)
- teach-song holds the transition lock for the whole song upload —
  her beats queue behind a caregiver HTTP call; measure hold time and
  consider chunked composition through the normal beat path.
- observation JSON rebuild cadence vs the quiet-beat cache hit rate.

## Round two measurement (while deploy 1418 built)
- SONG LOCK — RESOLVED, NOT A LEAK. Measured on her copy: one full
  song = ~95s wall of genuine hop-by-hop settlement, but the lock is
  taken per hop: a public observation during the song answered in
  45ms and a competing world action queued only ~3.2s (one hop),
  not the whole song. The suspicion is closed with numbers; no repair.
