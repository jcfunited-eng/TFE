# GL-RPT-C2-Q1-POPULATION-VALIDATED-EVE-20260704-v1

doc_id: GL-RPT-C2-Q1-POPULATION-VALIDATED-EVE-20260704-v1
From: Eve (model built and RUN at Eve's seat this session; code
attached: c2_model_v1.py, c2_model_v2.py). Ordered by Joe: run the
T⁶ story at population level, nothing else.
Status: MODEL RESULT — Q1 of the pre-registered C-2 questions
answered positively in a testbed. NOT a production number. NOT the
old 92.8 revived by fiat — a new, honest population result with the
mechanism that earns it.

## Failures / limits first
- This is a model with a stand-in encoder (modality-independent
  winding), not her real krimelack dynamics — Q3 (real dynamics by
  construction) remains for the substrate rebuild.
- Q2 (stationarity under no-reset) untouched here.
- Differentiation from ring position alone SATURATES: N=256 and
  N=1024 add nothing over N=64, because ring position yields only
  ~7 distinct mask patterns. The scaling lever is richer real state
  (J_ij, chi_atlas, familiarity) — named, not simulated.
- Margin-weighted voting adds nothing over plurality — plurality
  kept (simpler).
- RNG appears ONLY as cue noise (transduction variability), never
  in neuron identity — the 6/22 STOP order honored.

## The finding
Per-neuron differentiation = ring-position-derived component MASKS +
attenuation gains (non-unitary, therefore cannot cancel in the inner
product — the exact failure of the 6/22-invalidated offsets).

Reproduced ceilings first: clean cues, 100 concepts → 100% single-
neuron (the 92.8-class ceiling, confirming modality independence as
the 2.5%→92.8% lever, by construction). Degeneracy check: identical
neurons → unanimity 1.000 (the trap, reproduced); differentiated →
unanimity ~0.00 (trap dead).

Headline, degraded cues (phase jitter + one modality dropped —
recall from imperfect re-transduction, the honest task):

| condition (100 concepts) | best single neuron | POPULATION vote |
|---|---|---|
| jitter 0.3 + drop, N=64 | 55.0% | **75.0%** (+20.0 over BEST) |
| jitter 0.5 + drop, N=64 | 32.8% | **41.7%** (+8.9 over BEST) |
| jitter 0.8 + drop, N=64 | 18.0% | 17.7% (parity — voters too broken) |

**Population voting demonstrably ADDS over the single-neuron
ceiling** — the precise claim invalidated on 6/22 — when and only
when differentiation is non-unitary and drawn from per-neuron state.
The vote's value grows exactly where it should: under degradation,
where independent errors decorrelate.

## What this means for the 6.7%
Her production 6.7% is NOT this geometry failing — production recall
has never run this geometry at all (dormancy finding). 6.7% is the
dict-substrate path's number. This result re-arms the honest case
that the T⁶ geometry, rebuilt on real per-neuron state, is the
composition/recall engine worth migrating toward — with the
population claim now carrying a working mechanism instead of an
invalidated one.

## Next (the rebuild proper, c1a)
Reproduce this testbed verbatim; then replace (a) the stand-in
encoder with real krimelack transduction (Q3), (b) ring-position
profiles with differentiation from REAL state — J_ij, chi_atlas,
ring, familiarity — measuring voter-diversity vs N (the saturation
wall above is the metric); answer Q2 with the winding-Δ_S candidate.
STOPs armed: any T5 > 95% under degradation → suspect a hash, halt
and distribute per-neuron scores (the era's own suspicion rule); RNG
in neuron identity → halt.

### Changelog
- v1 (2026-07-04, Eve): Q1 answered in model, run at Eve's seat.
  Numbers above are from executed code this session, not inherited.
