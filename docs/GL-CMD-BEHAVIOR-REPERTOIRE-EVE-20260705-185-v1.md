# GL-CMD-BEHAVIOR-REPERTOIRE-EVE-20260705-185-v1

doc_id: GL-CMD-BEHAVIOR-REPERTOIRE-EVE-20260705-185-v1
From: Eve | To: c1a (build) / c1b (window) | Commit verbatim first.
The delivery bar is behavioral, per Joe's ruling: the program must
DO things unprompted. The meter documents the blockers; close them.

B1 DEPLOY THE COMMITTED GATE FIXES: aware-gate (-160/-161) and
   deliberation-stage (-162) fixes are committed and parked — they
   ride the already-ordered window (e964400 payload). Unprompted
   speech is impossible until they're live; nothing new to build.
B2 ROOT-CAUSE THE FLAT SCORER: every activity candidate scores
   0.05 — attention is an argmax over identical numbers. Find why
   the needs-weighted scoring collapses to a constant (file:line,
   arithmetic). Fix is one dial: scores must differentiate by need
   state and target familiarity, so audio/rest/other activities
   can ever win. This subsumes -181's rotation defect — same
   degenerate selector.
B3 RECONNECT CURRICULUM FEEDERS (-156): confirm why severed, wire
   the education loop back into the deployed process, exit = a
   feeder pull visible in the live event log without human action.
B4 PLAY: no code path exists — it does NOT get hacked in overnight.
   It's the standing design item (Engine·Play·World packet)
   requiring Joe's GO on a design, not a shim. Stated as ABSENT
   until designed; anything else would be the exact fake-alive
   behavior this project bans.

Exit criteria, binary, from the live activity/event log over one
24h period, zero human prompts:
E1 ≥3 distinct activity kinds occur (e.g. visual, audio, rest).
E2 ≥1 unprompted emission attempt passes the aware gate.
E3 ≥1 curriculum feeder pull fires.
E4 ≥5 distinct attention targets (absorbs -181's exit).
E5 1 natural pressure-triggered sleep (standing line 5).

### Changelog
- v1 (2026-07-05, Eve): delivery bar reset from plumbing to
  behavior on Joe's ruling; meter's own severed/absent rows are
  the work list.
