# C-011 Ordered Reassembly Sprint

Date: 2026-08-12

## Frozen item and production baseline

- Active delivery-ledger item: **C-011** — prove ordered recurrent formations
  can produce tapestry-level sequences such as remembered moments, sentences,
  navigation, or avoidance.
- Immediate predecessor: **C-010**, closed live on `dsf-ai-task:998` by one
  stable distributed physical-relation digest across self-caused, external,
  and later self-caused contexts. This sprint advances the ledger; it does not
  reopen C-010.
- Production organism identity:
  `1cc4e70a-f2a0-44c5-a111-f4a5bc915cc1`.
- Production commit:
  `13d9771e14f675f9f6d7d348b13a18c7ccdbff12`.
- Production image:
  `sha256:51d3999679b7e019a76ec75b0335ece71f44d3c3ba8c51b1f88793fcc92c8eaa`.
- Production topology: one desired/running healthy task, zero pending or
  failed tasks, 4,096 CPU units, and 16,384 MiB.

## Architecture honesty gate

1. **Requested architecture:** a later cue must physically reassemble and use
   a causally ordered path through prior recurrent formations. This is
   tapestry activity as conversational evidence, never a stored tapestry.
2. **Current code reality:** task 998 retains formations, sparse contacts,
   layer-11 delayed-ordering anatomy, and one-interval electrical recipient
   lineages. Exact signed contact transfers exist during settlement, but only
   canonical undirected active bonds and next recipients leave that function.
   The exact sender and bond that caused a recipient are discarded.
3. **Conflict:** yes at the evidence/state boundary. Existing physics can move
   charge one contact per interval, but the retained one-interval frontier is
   insufficient to identify which prior edge a later edge continues. Claiming
   order from current unordered bonds would fabricate it.
4. **Not extended:** the retired episode archive/classifier, LoomTapestry,
   causal-tapestry owners, ordered-member arrays, sequence databases, grammar
   or parser code, parent/member hierarchy, promotion counters, reinforcement
   counts, receipt-driven cognition, semantic labels, or whole-memory scans.
5. **Single exact item:** retain the exact directed cause of each next active
   electrical recipient for one interval only—sender lineage, receiver
   lineage, stable bond, and signed whole-carrier transfer. On the next
   interval, observe a continued directed path only when that exact receiver
   physically sends along another reached contact and a retained formation is
   reassembled and used.
6. **DSF scope:** every reached complete neuron continues to receive the full
   unchanged joint L0-L4 field reference and all seven explicit DSF fields.
7. **Field loss:** none. Ordered-path evidence references exact physical
   lineages and contacts; it does not copy, average, score, or replace DSF.

## Exact input, path, transformations, and output

Input is one lawful later partial cue or unattended recurrence reaching an
already-retained formation and its sparse relation frontier. The proof must use
the authenticated production predecessor and the same native organism path as
ordinary life; synthetic directed transfers cannot close the sprint.

Path:

1. Existing receptor, body, fluid, or internal perturbation reaches complete
   neurons through the ordinary joint-field transition.
2. Existing sparse contact settlement computes exact signed whole-carrier
   transfer once per reached contact.
3. Each nonzero transfer identifies its physical sender, receiver, stable
   mounted bond, and transferred whole-carrier quantity.
4. Only the exact directed transfers whose receivers become the next active
   frontier persist across the interval boundary. This replaces no neuronal,
   contact, formation, DSF, or learned state and retains no event history.
5. In the next interval, a continued path exists only when a prior receiver is
   the physical sender of a new nonzero transfer. The two exact bonds and
   lineages supply causal order; timestamps, ordinals, labels, counters, and
   list position do not.
6. Existing mosaic recurrence must reassemble a retained formation on that
   same reached frontier. Read-only observation may then report tapestry
   activity for that exact successor.
7. The path must survive current-state persistence and cold restore, remain
   bounded by the active sparse frontier, and be reused by a later live cue.

Expected output: one exact two-edge-or-longer phase-coupled physical path is
completed in causal order across successive intervals, participates in later
formation reassembly, changes current organism activity, and remains available
for sensed consequence. This proves one tapestry-level ordered reassembly. It
does not prove a sentence, narrative, language, planning, or C-012 depth.

## Acceptance-evidence map

| Required fact | Physical producer | Retained authority | Last required observer |
|---|---|---|---|
| Prior edge really occurred | signed whole-carrier contact settlement | one-interval directed active frontier | native ordered-path evidence |
| Later edge continues it | next interval signed transfer whose sender equals prior receiver | current contact and neuron successor | native ordered-path evidence |
| Formation path, not bare wire | retained formation recurrence on the same reached frontier | resident neuron/contact/mosaic state | relation plus ordered-path projection |
| Later causal use | reassembly changes current complete-neuron/contact activity | committed organism successor | exact tick/state and participant evidence |
| No stored sequence | source/diff/codec audit | no archive, ordered members, hierarchy, or semantic state | candidate review |
| Durability | current-state codec and ordinary restart | one current organism body | byte-exact cold restore and next interval |
| Bounds | reached transfer/frontier cardinality | compact current physical state | CPU/RAM/state-byte/call evidence |

## Translation-boundary review

- Upstream authority is `ElectricalContactTransition` with exact signed whole
  carriers and its mounted `StablePhysicalBondReference`.
- The current translation loses sender and bond when it reduces each transfer
  to `next_active_frontier: Vec<lineage>`. That loss is the first missing
  boundary.
- The candidate must carry one compact directed frontier entry into resident
  state, native observation, FFI, Python transport, rehearsal, and live API
  only as far as required by C-011 evidence.
- The entry may influence only next-interval reach because that is already the
  role of the active electrical frontier. Digests and public projections remain
  read-only and cannot re-enter settlement.
- Work and storage remain `O(active directed frontier)`. No retained formation,
  inactive neuron, archived episode, or total organism history is scanned.
- Energy, material, neuron identity, contact state, formation state, and full
  DSF authority remain unchanged; the exact carrier transfer is referenced,
  never duplicated or reapplied.

## Lifecycle branch matrix

| Branch | Required result |
|---|---|
| Pristine/legacy V19 predecessor | restores exactly; existing recipient-only frontier may propagate but cannot claim ordered-path evidence |
| First candidate interval | produces exact directed frontier entries from real signed transfers |
| Next candidate interval | can continue an entry into a two-edge path without replay or duplicate carrier motion |
| Recurrent retained formation | later cue reassembles through the continued path without adding a duplicate formation |
| Cold restore | preserves the current directed frontier byte-exact and completes the same next path |
| Quiescent/no whole carrier | stores no directed entry and reports no tapestry activity |

## Durable preflight recurrence register

| ID | C-011 applicability |
|---|---|
| RF-001 | Exact worktree must lead `PYTHONPATH` before any import. |
| RF-002 | Task-998 process-fixed environment must be exported before candidate construction. |
| RF-003 | Native candidate wheel provenance is required before Python evidence. |
| RF-004 | Pristine/V19 and recurrent/cold branches are both decisive. |
| RF-005 | Directed path must cross native, FFI, Python, rehearsal, and live observation without flattening. |
| RF-006 | Existing task-998 resource relationships must pass before restore. |
| RF-007 | Controller interpreter and command shapes are resolved before deployment timing. |
| RF-008 | Each interval successor is preserved so a later failure does not repeat its causal prefix. |
| RF-009 | No whole-organism aggregate becomes transition authority. |
| RF-010 | In-process and persisted `CURRENT` directed frontier must match before fresh restart. |
| RF-011 | Public evidence is bounded paths/counts, never raw neuron bodies. |
| RF-012 | Exact live ordered reassembly is required; health or counters cannot close C-011. |
| RF-013 | Changed files are formatted first; unrelated inherited drift is separate. |
| RF-014 | Every native observation mock/constructor is censused before compile. |
| RF-015 | Build a wheel then install explicitly when no virtual environment exists. |
| RF-016 | C-011 stays active and C-010 stays closed; complete diff must remain C-011-only. |
| RF-017 | Every state/observation constructor, destructure, codec, wrapper, and consumer is censused before compile. |
| RF-018 | Earlier-hop path evidence must survive a later quiet hop in complete intake evidence. |
| RF-019 | Candidate rehearsal assertions map only to C-011 before immutable build. |
| RF-020 | Layer-11 presence is insufficient; real transfers and formation participation are required. |
| RF-021 | Acceptance observes the exact successor produced by the declared cue. |
| RF-022 | New current-state frontier domain requires V19 restore plus candidate encode/cold-restore proof. |
| RF-023 | AWS environment is retrieved raw and string-filtered with `jq`. |
| RF-024 | Every test path is resolved with `rg --files` before pytest. |
| RF-025 | A timed-out live write is successor-checked before any retry. |

## Rejected paths and falsified hypotheses

- The historical dynamic-formation classifier is rejected. It depended on a
  four-deep per-neuron episode archive, and its outputs were always zero on the
  measured body. Recreating it would restore database-shaped history.
- Formation receipt order is not physical order. Receipts are observation
  custody and change with recurrence witnesses.
- Canonical active bonds are undirected. Sorting them cannot create a temporal
  path.
- Layer-11 anatomy alone proves only mounted capacity. A tapestry claim
  requires actual carrier propagation, formation reassembly, and causal use.

## Pre-implementation evidence

- The exact worktree was validated at
  `bca6094735a3ccb45e0dea011aef094c6414b05a` before implementation.
- Task 998 process-fixed anatomy has world, unattended time, cochlear ears,
  touch, chemoreception, and vestibular paths mounted; aggregate
  `GUALA_INTEROCEPTION` remains disabled and is not fabricated for C-011.
- The production target census passed: service `dsf-ai-service-lb`, one
  desired/running healthy task, zero pending tasks, completed PRIMARY rollout,
  exact task 998 image, 4 vCPU, and 16 GiB.
- Both public pages returned HTTP 200 during census. That is availability only
  and is not C-011 tapestry evidence.
- Source trace confirms exact directed signed whole-carrier transfer exists in
  `resident_cognitive_formation.rs` during sparse contact settlement. The
  translation to `InternalContactSettlementObservation` currently emits only
  canonical `active_bonds` and recipient-only `next_active_frontier`, proving
  the declared direction/source loss before any code change.

## Completion boundary

C-011 closes only after one immutable candidate is live and production proves
one later cue or unattended recurrence completes a genuine ordered physical
path through prior recurrent formations, changes current organism activity,
survives exact persistence/cold restore, and remains bounded with one process
and zero Python cognition callbacks. Source, unit tests, a path digest, or a
tapestry counter cannot close it.
