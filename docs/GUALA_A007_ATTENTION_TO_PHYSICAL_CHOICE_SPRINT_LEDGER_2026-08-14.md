# Guala A-007 attention-to-physical-choice sprint ledger

## Frozen scope

- Active item: `A-007` — prove one internally caused attention-to-choice
  transition with no authored goal, random selector, semantic command, or
  Python cognition.
- Immediate predecessors A-001 through A-006 are live-closed and are not
  reopened.
- This sprint adds no neuron, contact, choice object, retained state, command,
  score, winner, goal, planner, or Python decision. It may add only one bounded
  read-only proof over the physical transition already executed.

## Architecture honesty gate

1. Requested architecture: authentic available continuations must arise from
   current organism/world state; internally caused sparse attention and local
   physical competition must reach opposed motor preparation; exact antagonist
   settlement must leave one prepared continuation.
2. Current code reality: live task 1052 exposes changed transported/foregone
   routes, retained-formation causal propagation, two physical prediction
   alternatives, exact layer-11/layer-12 preparation, 144 motor recruitments,
   one nonzero signed yaw intent, one applied action, and returned body senses.
   The public observer still reports choice unavailable because it does not bind
   those facts into one exact attention-to-choice proof.
3. Conflict: yes, only at the bounded observation boundary. The physical path
   exists; current public evidence cannot distinguish causally settled choice
   from unrelated coexistence.
4. Mechanisms not extended: scheduler/timer choice, random or score selector,
   authored goal, semantic command, Python cognition, owner, lock, database,
   second whole-organism scan, copied native payload, or persistent proof state.
5. Single exact next item: derive one compact choice witness by intersecting the
   exact attention route witness with the exact motor preparation contacts,
   requiring internally reassembled causal lineage, nonzero opposed motor
   populations, and one nonzero signed prepared intent.
6. DSF scope: unchanged full joint L0-L4 remains authoritative inside every
   reached neuron; this sprint does not evaluate or reduce the field.
7. Lost field structure: none.

## Durable change-impact ledger

| Boundary | Exact value |
|---|---|
| Input | One committed ordinary unattended transaction containing changed simultaneous transported/foregone frontier routes, one internally reassembled formation-to-motor path, exact motor recruitments and preparation transfers, and the single prepared world intent. |
| Function/file path | `dsf_ai_service/native_production_app.py`: existing `_sparse_attention_route_facts` and committed `motor_action` evidence -> one new read-only physical-choice projector -> `_autonomy_record` and cognitive-capital observation. |
| State transformation | None. The projector intersects existing exact contact identities, sums already-authoritative outward motor carriers by the body anatomy's even/odd antagonist orientation, and verifies that their signed difference equals the already-produced yaw intent. |
| Expected output | One compact witness with exact causal origin, matched reached attention contacts, both antagonist carrier totals, nonzero settled signed intent, one causal intent receipt, and every prohibited authority explicitly false. |
| Production acceptance | An unattended live transition has changing reached/foregone attention; at least one transported attention contact is also exact motor preparation; retained internal formation activity reaches a recruited motor lineage; both physical antagonist populations discharge; exact signed settlement is nonzero and equals the one prepared intent. |
| Non-claim | A-007 proves preparation of one continuation. At-most-once execution belongs to A-008; full multimodal consequence re-entry belongs to A-009; repeated loop closure belongs to A-010. Existing action/consequence may support causal custody but must not silently close those later items. |
| Persistence | No schema or organism-state change. Keep at most one latest-tested process-local observation witness, as with the already-live A-006 projection. |
| Resource bound | One bounded pass over the transaction's already-projected sparse attention routes and motor recruitments; no native callback, history scan, or work proportional to total neurons. |

## Exact production baseline

- Task `dsf-ai-task:1052`, code commit `f2278fb2`, image digest
  `sha256:83452aab4f74e01ee4c0240d4b7043a44542857ca907ce78d9fbcb0d557a18f6`.
- Live tick `91004`, state
  `adebf581c82c19ddaa3b4a488ebfbb629637ddb30d9c3ca48c1bc6de7fc02e63`.
- The observed unattended transaction contained 882 qualifying attention
  routes: 877 transported and five foregone.
- Its 2,717 exact layer-11/layer-12 preparation-transfer observations share
  contact identities with the bounded attention witness, but the current
  public aggregate has discarded their individual hop boundaries. Contact
  identity across a transaction is coexistence evidence only and does not pass
  A-007.
- Both antagonist populations discharged: even-topology motor units carried
  1,641 outward elementary carriers and odd-topology units carried 1,865. The
  exact signed settlement was `1641 - 1865 = -224` millidegrees, identical to
  the single prepared yaw intent.
- The same transaction contains an internally reassembled retained-formation
  path into layer 12 and zero Python cognition callbacks.

## Applicable recurrence checks

| ID | A-007 application |
|---|---|
| RF-001, RF-002 | Put this exact worktree first on `PYTHONPATH` and use task-1052 environment before importing process-fixed Guala code. |
| RF-005 | Carry contact identities, signed motor quantities, internal-cause lineage, and the prepared intent unchanged to the public observer. |
| RF-011 | Publish bounded counts and one compact causal witness; do not copy 882 routes or 2,717 preparations into a new section. |
| RF-012, RF-020 | Close only from the real unattended production path; mounted populations, tests, and HTTP health are insufficient. |
| RF-016, RF-034 | Keep A-007 active; do not reopen A-006 or require its one historic witness on every release. |
| RF-018, RF-028 | Preserve an earlier attention/preparation event through the ordinary multi-hop transaction aggregate. |
| RF-021, RF-027 | Bind every assertion to the same predecessor/input/successor and the first exact qualifying interval. |
| RF-031, RF-032 | Resolve real test names and roots and require nonzero executed counts. |
| RF-042 | Treat signed yaw as signed: either nonzero direction is lawful; zero means antagonist cancellation and cannot prove one prepared continuation. |
| RF-043 | Follow antagonist settlement through the real body law; motor recruitment alone does not pass if opposing discharge cancels to zero. |
| RF-044 | Review route x recruitment cardinality and avoid a nested cross-product in the implementation; use canonical contact keys and linear passes. |
| RF-046 | Ensure later specialized consequence hops cannot erase the earlier choice witness from the ordinary aggregate. |

## Failed-hypothesis register

1. Rejected: relabel the two C-016 prediction paths as choice. They are possible
   consequence relations, not prepared opposed actions.
2. Rejected: call every motor discharge a choice. A discharge without authentic
   alternatives, internally caused preparation, and nonzero antagonist
   settlement proves action eligibility only.
3. Rejected: call aggregate reached/foregone counts choice. They prove sparse
   attention, but not that attention entered action preparation.
4. Rejected: retain a new choice object or winner. The exact settled body intent
   is already the physical successor and remains the sole authority.
5. Rejected: compare every route with every preparation. Canonical contact keys
   permit two linear passes and avoid repeating the observed 882 x 2,717 work.
6. Rejected during code review: match only a contact identity retained across
   the whole transaction. The same contact can move different quantities in
   different intervals. The corrected candidate admits only an exact directed
   transfer observed simultaneously as an attention route and motor
   preparation inside one native hop.
7. Rejected by the first production candidate on task 1053: require the route
   change classification to exist inside the same native hop as its exact
   motor transfer. The earlier hop cannot know that its route set changed until
   a later hop supplies the comparison frontier. The completed transaction is
   the smallest lawful observation boundary: it preserves one exact
   reached-and-foregone interval, its later distinct route set, and exact
   directed motor transfers without flattening their contact identities.

## Candidate and production evidence

- The corrected candidate records at most the first exact binding after the
  completed transaction supplies the later route comparison. It retains only
  attention counts, matched motor lineages, the transaction tick, antagonist
  totals, one formation reference, one causal-intent reference, and the current
  state reference. It copies no route or preparation body.
- Matching is linear: exact directed preparation transfers are indexed once,
  then the sparse attention routes are visited once. No route x recruitment
  cross-product exists.
- The focused attention, causal-trace, motor, unattended, and public-observation
  regression set passes 32/32. It directly rejects unrelated coexistence and
  exact antagonist cancellation, and proves that a later comparison frontier
  can classify an earlier exact route without losing its motor contact.
- Replaying live task-1053 transition tick 91524 through the corrected boundary
  yields 24 exact matched attention-to-motor contacts, 1,478 positive and 1,626
  negative antagonist carriers, and the already-observed `-148` signed yaw
  intent. This was the final pre-deployment model check.
- The corrected candidate cold-restored the exact production body at tick
  91797 and state
  `10a9a1dc5949f6aeb75860552796ab27177457470440d92d603ec403fd24c436`.
  Identity remained `1cc4e70a-f2a0-44c5-a111-f4a5bc915cc1`; the body contained
  313 active complete neurons and 196,416 developmental resting neurons; no
  migration or Python cognition worker/callback occurred.
- Live task 1054 runs commit `a4ae22e36331138213be585c534abd865bd3dfca`
  at immutable digest
  `sha256:947615882ac4631ac40d37560ab37c2a85310cb25c1b9a88ca39e40f7bb1dda5`.
  At tick 91862, 881 of 886 qualifying routes transported and five were
  foregone; 25 exact transfers also prepared motor cells. Even-topology motor
  units carried 1,362 and odd-topology units 1,484, whose exact difference is
  the one observed `-122` millidegree intent. The cause is retained-formation
  activity and all authored-goal, random, score, semantic-command, and Python
  cognition authority flags are false.
- The same live evidence is present under both `choice` and `autonomy.choice`.
  Cognitive capital credits exactly five `Deliberation and choice` dimensions:
  availability, participation, causal use, integration depth, and autonomous
  use. ECS settled with one running task, zero pending/failed tasks, and one
  completed primary deployment; the digest is pinned as `production-current`.

Status: **live-closed 2026-08-14**. A-008 is not claimed.
