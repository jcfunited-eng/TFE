# Guala A-015 unattended resource-soak sprint ledger

Date: 2026-08-16

Status: Active; continued activity, native action/consequence, recovery, one
process, and bounded state/storage are Live-Proven over the first production
falsification window. A-015 is not Live-Closed because this window does not
cover self-selected sleep/wake or autonomous tutoring, and it is not the full
P-011 long soak.

## Task identity

- Active item: `A-015` — live-soak unattended autonomy long enough to prove
  continued activity, recovery, bounded growth, and no duplicated processes.
- Production observed: task `dsf-ai-task:1093`, commit `50ba31d8`, image digest
  `sha256:6112924c7f6c217877969096445fe5e9a436c63623845d87d6d07e12f9066df1`,
  identity `1cc4e70a-f2a0-44c5-a111-f4a5bc915cc1`.
- Deferred work is unchanged: A-011.6 remains unresolved after its authorized
  time-box and A-011.7 remains untouched. A-014 is Live-Mounted but still
  requires one real-browser camera/microphone encounter.

## Architecture honesty gate

1. Requested architecture: one unattended native organism must keep living,
   recovering, and changing within finite compute, RAM, and storage.
2. Current code reality: task 1093 runs one native-resident serving process and
   advances the same persistent body from continuous world/sensory intervals.
   Public resource observation reports exact state bytes and zero Python
   cognition callbacks, but live CPU/RAM/storage rates are not yet mounted on
   that public surface.
3. Conflict with requested architecture: no conflict was observed in this
   bounded interval. Final acceptance remains absent because sleep/wake and
   autonomous tutoring did not occur in the window.
4. Mechanisms not extended: duplicate schedulers, owners, transition locks,
   Python cognition, archive/history databases, whole-brain scans, observer
   mutation, or polling that advances state.
5. Single exact next item: preserve this first live falsification result, then
   run a longer P-011 window only after self-selected sleep/wake and tutoring
   are truly available to be covered.
6. DSF scope: observation does not evaluate or alter DSF. Every live native
   occurrence continues unchanged full joint seven-field L0-L4 delivery.
7. Lost field structure: none.

## First live production falsification window

The only existing documented concrete minimum reading interval is ten minutes
in the runtime resource guidance. It was used here as a first falsification
window, not silently promoted into final P-011 acceptance.

Thirteen cached, read-only public observations were taken from
`12:14:00Z` through `12:25:05Z`. These reads neither acquire the transition
lock nor call native cognition.

- Generation/tick advanced from `122726` to `122990`: `264` autonomous native
  generations in `665` seconds.
- Every sample carried a distinct exact state SHA and a continuous-environment
  intake; sampled transitions physically changed `273` through `278` neurons.
- State bytes started at `58,441,123`, ranged from `58,438,798` to
  `58,442,490` (a `3,692`-byte envelope), and ended at `58,440,382`: `741`
  bytes below the start rather than append-only growth.
- Every sampled transition reported `energy_exhausted=false` and zero Python
  cognition callbacks.

The following live causal observation proved the activity was not mere clock
churn. By generation/tick `123074`, native attention and
motor physics had applied a `-22` millidegree body yaw at world revision
`6222`; its consequence returned at tick `123073` with one vestibular tick.
The same self-maintenance record reported `4,134` physically transitioned
neurons and `279` rest-recovered neurons, with zero Python cognition callbacks.

## Exact process and resource comparison

Read-only ECS Exec measurements bracketed the public series. They read only
PID 1 status, cgroup counters, and directory metadata; they did not read, copy,
decode, or scan the organism state.

| Measure | Start | End | Result |
|---|---:|---:|---|
| PID 1 | one `uvicorn`, 10 threads | one `uvicorn`, 10 threads | unchanged |
| PID 1 RSS | 937,300 KiB | 946,832 KiB | +9,532 KiB |
| PID 1 high-water RSS | 1,166,100 KiB | 1,235,928 KiB | +69,828 KiB |
| cgroup current memory | 1,062,236,160 bytes | 1,059,483,648 bytes | -2,752,512 bytes |
| cgroup high-water memory | 1,287,049,216 bytes | 1,344,442,368 bytes | +57,393,152 bytes |
| cgroup CPU time | 439.699442 s | 1,200.812600 s | +761.113158 s over 701 s, about 1.09 of 4 vCPUs |
| persistent root | 416,300 KiB | 416,300 KiB | unchanged |
| persistent files | 89 | 89 | unchanged |

The only other visible process names were the ECS Exec support agents and the
temporary shell created by this observation. There was one organism-serving
`uvicorn`, one healthy ECS task, service topology `desired=1`, `running=1`,
`pending=0`, and no overlapping deployment.

The modest PID high-water increase occurred while current cgroup memory ended
lower, state bytes oscillated in a 3,692-byte envelope, and persistent bytes
and files remained exact. This interval therefore does not show runaway RAM or
storage. It is too short to claim lifetime boundedness.

## Truthful observation defect surfaced by the soak

The same live snapshot contained an applied native action and returned sensory
consequence while the per-experience stage ledger said, “She has never done
anything.” The organism state was truthful; the observation prose incorrectly
turned absence in one experience into lifetime absence.

Candidate source now derives the intent, action, and consequence stage rows
from that experience's exact attention-motor binding, applied nonzero body
motion, and matching action/consequence receipt. When those records are absent,
the ledger says only “not observed in this experience.” It does not borrow the
lifetime autonomy card, add cognition, or infer meaning. Focused public/Loom
observation verification passes `47`; the optional local Playwright module is
not installed, so browser automation was not claimed.

## Closure rule

A-015 can Live-Close only after a longer unattended production interval covers
continued sensing, endogenous attention/choice, world action and returned
consequence, real recovery, bounded legitimate learning, self-selected
sleep/wake, and autonomous tutoring while identity, current-only persistence,
one process, zero Python cognition callbacks, and non-runaway CPU/RAM/storage
remain exact. Historical receipts or a short resource plateau cannot replace
missing live behaviors.
