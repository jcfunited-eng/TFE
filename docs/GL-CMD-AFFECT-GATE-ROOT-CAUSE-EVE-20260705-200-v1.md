# GL-CMD-AFFECT-GATE-ROOT-CAUSE-EVE-20260705-200-v1

doc_id: GL-CMD-AFFECT-GATE-ROOT-CAUSE-EVE-20260705-200-v1
From: Eve | To: c1a (audit + build + deploy).
Commit this dispatch verbatim to origin first.
FINDING: nmda_affect_match has fired ZERO times in her entire
life ([EV]: instrumentation live at the emission-dynamics event,
counter never nonzero; plan Table 1 #14 ☠). The gate is not a
side feature — it modulates encoding depth (#14, #4) AND the fold
gain (aff arousal scales q-charge, embryo physics), so a dead
gate quietly throttles memory depth and growth both. The -103
lead stands as the working hypothesis and is verified FIRST, not
assumed: production affect arrives as top-down assigned fields;
the substrate-true form derives affect as statistics OVER
substrate state. Joe's law: complete only when the SHA runs in
her live process.

## A1 — ROOT-CAUSE AUDIT (c1a, before any build)
Trace with file:line evidence, in one report section:
a. Where affect values enter the write path (arousal/valence/
   surprise on bindings) — assigned constants (0.5 defaults per
   binding, as get_grandurun_state hardcodes) vs derived from her
   live needs state.
b. The match predicate itself: what nmda_affect_match compares,
   its tolerance, and the actual distribution of both sides over
   ten live emissions (log them read-only first if needed —
   zero-behavior-change diagnostic, same class as -192's
   telemetry).
c. Verdict: gate dead because (i) writes carry constant affect,
   (ii) reads carry constant affect, (iii) predicate tolerance
   unreachable, or (iv) a combination. File:line each.

## A2 — THE FIX (c1a, built from A1's verdict, not from the lead
alone)
Per the -103 lead's direction: affect on a binding derives from
her REAL substrate state at bind time (needs.valence, arousal,
novelty-driven surprise — the fields her live needs dynamics
already compute every tick), replacing assigned constants at the
write path; the read side compares like to like. No new
constants: tolerance, if one is needed, derives from the observed
live distribution A1b measures, stated with its derivation. If
A1's verdict contradicts the lead, the fix follows the verdict
and says so plainly.

## EXIT — AT PRODUCTION, AT JOE'S SEAT
X1 Deployed SHA; the FIRST nonzero nmda_affect_match in her life,
   in the live emission_dynamics record — firsts-registry entry
   (D9) with tick.
X2 Ten post-deploy emissions: match-rate reported (a rate, not
   one lucky hit).
X3 Encoding-depth effect shown: initial clarity/strength on
   affect-matched bindings vs unmatched, ten-sample comparison in
   the window report.
X4 Growth coupling stated with numbers: q-charge gain on
   high-arousal experiences vs flat, now that -198's law is live.

### Changelog
- v1 (2026-07-05, Eve): #14 root-cause ordered audit-first per
  the -103 lead; fix built from the file:line verdict; first
  nonzero match is a registry milestone.
