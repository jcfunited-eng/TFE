# CH4 Schema-Memory Campaign — Waypoint (2026-07-30)

Standing order: iterate until ~91% WR / ~36.7% is demonstrated, validated,
repeatable. This files the climb to date; every number causal, declared
before measurement, misses included.

## The established law
Temporal-geometric species (mosaic-class bigrams over the conformant v2
gate stream) predict their own completions at **65.9%** across 11 years
(2016–2026), both directions symmetric, every year 62–71% including the
2022 bear; spectrum tail exceeds the binomial null (~160 excess
high-consistency species); species span 20–50 distinct symbols each.
Replicated out-of-window on 5 years the machinery had never seen.

## The harvest ladder (decade store, ~4,970 eligible symbols, declared
$100k / 10% slices / max-10 book, no costs, survivor-universe caveat)

| Read | per-trade WR | decade book | notes |
|---|---|---|---|
| next-gate steps (band ≥0.75) | 65.3% hit | +671% (5y run, yearly +17..+58) | the base law |
| cycle harvest (exit on first DOWN-majority) | 62.2% | +208% | boundary too early |
| **morphology harvest** (per-species gates-to-peak exits) | **65.5%** | **+503–630%** | every-year WR 59–71% |
| herd-conditioned tiers | 70.9–73.6% @ n=1093/303 | — | peer weather beats global |

## Conditioning verdicts
- Global field ("solar cycle") energy: mid-tier lift to ~71.8%@1239 —
  coarse but not nothing (control).
- **Herd (pedigree-cell) energy + greed: 73.6% @ 303 (deepest real-volume
  tier of the campaign)** — peers are the right conditioning frame, per
  the flare physics.
- Conditioning fragments per-species morphology history → hierarchical
  backoff build launched (most-specific earned record predicts;
  unconditioned species shapes collect).

## Falsified along the way (all filed raw)
Quant-signal frame in any form (~51% signature everywhere); strict ch06
mapping (arithmetically unreachable under pinned constants — spec-level
finding); trigram + pooled spellings; global meadow bands; arc phase
morphology (survivorship artifact); age-conditioned yield; naive cycle
boundary (band-DOWN too rare: decade-hold drift).

## Repairs caught by self-audit (diligence log)
Lattice-scale degeneracy (V axis invisible on normalized field);
1-bar-gate displacement ≡ 0; multiscale coarse-block look-ahead leak;
stale-feed entry in the unwired daily runner; process-death recovery
(overnight herd run relaunched).

## Open path
Hierarchical harvest (running) → deepest-tier entries × morphology
collection; DOWN-side energy collection; then validation & repeatability
protocol (deterministic rerun + live-forward on the side page, on
approval).


## Polarity-complete first-passage frontier (2026-07-30, decade store)

Ride-unbounded is FALSIFIED at every tier (even 85% WR bleeds: collapse
tail unbounded). With species-quantile targets, adverse-excursion stops,
and BOTH polarities collected:

| Cell (q25 targets) | n | WR | mean/trade | decade book | character |
|---|---|---|---|---|---|
| BOTH t75 stop | 1,925 | 61.1% | +0.53% | **+97%** | EVERY year positive incl 2022 (+9.0) |
| BOTH t85 stop | 271 | 67.9% | +0.66% | +18.5% | every year positive |
| NEG t85 stop | 88 | 79.5% | +0.74% | +6.7% | highest clean WR, positive mean |
| NEG t90 stop | 35 | 82.9% | +0.83% | +2.9% | WR champion, sparse |
| any ride cell | — | 78–85% | negative | negative | falsified |

Physics confirmed: negative-energy harvest is real (down-side symmetric
law collected short); bounded excursions are mandatory; the every-
condition property comes from polarity completeness. Open gap: the
highest-WR positive cells run ~80–83% at 35–88 events/decade — 91% at
density remains the target. Next levers: down-species density (mirror
alphabet is thinner), per-side book composition, finer time resolution
(daily bars may be the coarse rung of the flare physics), then the
validation/repeatability protocol and live-forward.
