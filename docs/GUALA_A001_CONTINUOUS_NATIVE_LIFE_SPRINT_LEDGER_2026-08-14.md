# Guala A-001 continuous native life sprint ledger

## Frozen scope

- Active item: `A-001` — replace endpoint-triggered existence with one
  continuously advancing native sensory/cognitive/action life while unattended.
- Immediate predecessor: `C-024`, live-closed in production task
  `dsf-ai-task:1050`, commit `e5b366c5`, image
  `sha256:31c7edeee93846dd5d87244f7e5499e0abdb88e76b0191d14d9751d0e4b67489`.
- Exact input: one bounded interval sampled from the authenticated persistent
  world, including the current body and mounted receptor roster.
- Exact path:
  `_unattended_time_loop -> _attempt_unattended_interval ->
  _unattended_interval_episodes -> _perform_admitted_intake_locked`.
- State transformation: one raw native CURRENT predecessor becomes one
  persisted successor through the same native organism transition used for
  admitted external experience.
- Exact output: truthful sensory settlement, optional native layer-12 motor
  discharge, applied world/body consequence, and vestibular re-entry.
- This item does not claim native deliberative choice or thought. Those remain
  explicit later autonomy items.

## Architecture honesty gate

1. Requested architecture: one persistent organism that advances from lawful
   sensory/body causes without an endpoint, operator, owner, or cognitive
   scheduler.
2. Current code reality: task 1050 already starts one process-owned transport
   thread at application lifespan startup. It feeds contiguous world intervals
   to the native organism; public reads consume a cached committed projection.
3. Conflict: the runtime mechanism does not conflict. Its dedicated regression
   test had become false and still referenced a deleted cadence variable,
   retired intake identity, retired energy quantities, and the obsolete claim
   that native action was unmounted.
4. Mechanisms not extended: the retired Python walk/stride controller,
   endpoint triggers, cognitive schedulers, owners, locks, semantic selectors,
   separate clocks, and Python cognition callbacks.
5. Single exact item: replace the false test contract and live-prove the
   already-deployed continuous native path.
6. DSF scope: A-001 does not reevaluate or reduce DSF. The existing native
   transition preserves the complete local seven-field delivery.
7. Lost field structure: none.

## Change-impact and acceptance map

| Boundary | Required fact | Evidence |
|---|---|---|
| Lifespan | continuous transport starts without an API request | `_lifespan` calls `_start_unattended_time` after native restore |
| Transport | time transports a world interval but selects no cognition | `_unattended_interval_episodes` samples the persistent world; `_unattended_time_loop` invokes only the admitted-intake boundary |
| Native state | the same native identity advances and persists | task 1050 retained identity `1cc4e70a-f2a0-44c5-a111-f4a5bc915cc1`; tick/state changed from `89604/5a15babe...` to `89617/94829750...` |
| Observation | reading cannot create life | live `observation_contract.read_advances_organism=false`; the public endpoint returns the cached committed body |
| Action/consequence | native physics may act without Python choosing an action | live interval `continuous-environment:b5ddff26-...` produced 136 layer-12 motor recruitments, 405 millidegrees of yaw, and vestibular return |
| Quiescence | no physical change is rest, not activity | corrected boundary test produces `no_internal_cause`, no action, and unchanged exact energy quantities |
| Preemption | external experience does not create a second writer | corrected boundary test proves unattended transport yields before an admitted external intake |
| Python authority | no Python cognition participates | live `python_cognition_callback_count=0` |
| Production topology | one settled task | ECS service desired/running/pending `1/1/0`; one completed PRIMARY deployment and one healthy container |

## Failed hypothesis retained

- The initial hypothesis was that A-001 required a new runtime mechanism.
  Source and live evidence falsified it: the mechanism had already shipped as
  part of task 1050. Adding another loop would have duplicated the organism's
  transport path.
- The old A-001 test failed before exercising behavior because
  `UNATTENDED_CADENCE_ENV` no longer exists. It also expected
  `unattended-interval:`, old fuel-delta fields, and no native action. The test
  was replaced rather than used to redesign correct production physics.

## Verification and live closure

- Corrected focused boundary suite: 13 passed, 0 failed.
- Live production task: `dsf-ai-task:1050`.
- Live commit/image: `e5b366c5` /
  `sha256:31c7edeee93846dd5d87244f7e5499e0abdb88e76b0191d14d9751d0e4b67489`.
- Live identity remained unchanged.
- Cached observation reads did not advance the organism.
- Across approximately 35 seconds, the native organism advanced 13 ticks and
  changed its current state while remaining on the same task and identity.
- The latest observed unattended successor included physical receptor ingress,
  native motor discharge, world/body action, and sensed vestibular consequence.
- Native deliberative thought and choice remain unavailable and are not claimed
  by A-001.

Status: **Live-Closed 2026-08-14**. No new runtime deployment was required
because the exact corrected mechanism was already the live production path;
deploying an unchanged runtime solely to alter its commit label would create no
new production behavior.
