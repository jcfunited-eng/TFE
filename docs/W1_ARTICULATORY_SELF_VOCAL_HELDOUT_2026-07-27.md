# W1 articulatory self-vocal heldout result — 2026-07-27

## Architecture honesty

- Requested architecture: self sound must be caused by a deterministic
  embodied larynx and vocal tract, with excitation, tract state, and heard
  pressure retained in one causal chain.
- Prior code reality: the self-vocal motor replayed authenticated PCM
  exemplars but had no independent excitation or vocal-tract authority.
- Conflict: yes.
- Mechanisms not extended: legacy TTS, text, phoneme or word labels,
  grounded-turn replay, chi/psi identity, reduced acoustic vectors, ML,
  thresholds, and L0–L4.
- Implemented item: an isolated physical articulatory motor and a
  custody-only bridge to the lived causal THING vocal route.
- Field evaluation: full explicit `D_k`, `M_k`, `R_rev_k`, `U_star_k`,
  `C_k`, `P_k`, and `B_k` for the laryngeal excitation and all eight tract
  sections. No reduced decision projection is used.

## Implemented physical chain

`articulatory_self_vocal_motor.py` contains:

1. an exact integer glottal flow/volume-acceleration actuator;
2. an eight-section fixed-point Kelly–Lochbaum loss tube;
3. initial/apex/final tract-area gestures;
4. independent BODY full fields for excitation and every tract section;
5. exact generated PCM admission to the existing bounded self-vocal motor;
6. one authenticated custody receipt spanning actuator full field, PCM motor,
   self-body execution, binaural acoustic propagation, and causal settlement;
7. bounded HMAC cold state that retains physical programs and zero generated
   PCM bytes.

`articulatory_thing_vocal_custody.py` admits a vocal relation only when the
articulatory custody and the settled self-acoustic occurrence are the same
typed lived event. It then delegates to the existing causal THING vocal route
and retains the missing larynx/tract authority in a bounded cold state.

## Passing proof

The focused and adjacent regressions passed:

```text
6 passed in 45.06s
```

Covered:

- deterministic self-babbling;
- complete excitation/tract L0–L4 fields;
- bounded cold restore with no retained generated PCM;
- recurrent mono auditory admission;
- self-body execution and binaural acoustic settlement;
- complete articulatory-to-THING custody;
- existing W1 self-acoustic and causal THING route regressions.

## Fresh heldout truth

Probe:

```text
tools/probe_articulatory_self_transform_external_distinction.py
```

Report:

```text
/tmp/w1_articulatory_self_transform_external_distinction.json
authority: 288530982fdfa2eae61b8ae95f82b81994509e5d1299d53582683e2fb10a4036
```

The substrate experienced eight physical self-babble programs before any
external recording. Those experiences grew 272 recurrent auditory neurons.
The frozen bank then fired read-only on 26 real recordings across eight
evaluator word classes.

- Nonempty firing: 26/26.
- Exact agreement across speaker variants of one word: failed.
- Pairwise distinct exact class signatures: failed.
- Hearing/word-recognition claim allowed: **false**.

The result proves that embodied self transformations now contribute
nontrivial structure to every external recording. It does not prove stable
word identity, arbitrary-room separation, or human-like hearing. Every
speaker variant still produced a different exact motif set. The next missing
authority is experience-grown causal consolidation/fission of these active
structures; it must not be replaced by a score, tolerance, nearest match, or
static word table.
