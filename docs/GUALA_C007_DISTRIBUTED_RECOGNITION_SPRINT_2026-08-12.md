# C-007 Distributed Recognition Sprint

Date: 2026-08-12

## Frozen item and production baseline

- Active delivery-ledger item: **C-007** — prove recognition is distributed
  physical pattern completion across sensory, association, retained, body, and
  affective participants.
- Immediate predecessor: **C-006**, closed live on task definition
  `dsf-ai-task:993`. C-007 does not reopen it.
- Deployed code: `b803f1a000d262b9ab4789e90e0e1582410ea803`.
- Deployed image:
  `sha256:666c19d27b3948e0b5a561b88dc754ff8c296ebb8ee9b0eb0798c832ebca37b4`.
- Organism identity: `1cc4e70a-f2a0-44c5-a111-f4a5bc915cc1`.
- Service at `2026-08-12T14:26:37Z`: one desired/running healthy task,
  zero pending tasks, and completed rollout.

## Architecture honesty gate

1. **Requested architecture:** a proper partial cue must change current
   distributed organism physics and complete a retained pattern through
   sensory, association/integration, recurrent-retention, body-regulation, and
   affective participants. Recognition is not a label, lookup, score, receipt,
   counter, or stored answer.
2. **Current code reality:** task 993 already carries a bounded one-contact-per-
   interval physical frontier, exact retained formation members, sparse
   layer-9 routes, exact active bonds, and physical participant layers. A live
   proper partial-cue reassembly reaches layer 6 local integration, but does
   not reach layer 7 cross-sensory association. The existing
   `partial_cue_reassembly_count` alone is insufficient evidence, because a
   count does not identify the participating populations.
3. **Conflict:** yes. The deployed path physically reassembles sensory,
   local-integration, retention, body, and affective participants, but not the
   required cross-sensory association participant.
4. **Not extended:** the retired hippocampal archive/navigator, semantic labels,
   similarity or nearest-match code, database memory, ownership, locks,
   successful-use counters, whole-brain polling, stored recognition answers,
   or UI claims.
5. **Single exact item:** carry the exact newly reached local-integration
   frontier across only the next causal interval so its already-retained sparse
   contact can reach layer 7; then require one live partial-cue component to
   contain layer 7 before closing C-007.
6. **DSF scope:** C-007 neither evaluates nor changes L0-L4. The live neurons
   continue to receive unchanged full joint seven-field DSF.
7. **Field loss:** none. No DSF field is reduced, copied, scored, or replaced.

## Exact acceptance path

Input: the authenticated task-993 `CURRENT` body followed by two ordinary
vestibular transitions, `0 -> 64` and `64 -> 0`, prepared and committed only
inside a disposable in-memory organism.

Path:

1. Cold-restore the exact live body under production-derived envelope, fabric,
   and logical-peak limits.
2. The first transition carries one retained member into its existing layer-9
   sparse route.
3. The second transition carries that same route into every exact member of
   one retained formation.
4. Build only the connected component of exact active bonds containing that
   layer-9 route; do not traverse the resting population or inactive fabric.
5. Classify that component by the already-declared physical projection layers
   and require nonempty sensory, layer-7 cross-sensory association, retention,
   body, and affective participation.
6. Commit the disposable successor, encode it, cold-restore it byte-exact, and
   discard it without publishing `CURRENT`.

Expected output: exactly one proper partial-cue reassembly whose one active
component contains all five requested participant classes, including layer 7,
changes organism state, remains bounded, cold-restores exactly, and invokes no
Python cognition.

## Production evidence

Schema `C007LIVE1`, captured directly inside the running task-993 image:

- predecessor tick/state: `54879`,
  `7e0ebf25ef091be0d93f3e142107b64d5207459883018217e7213748c1e08603`;
- disposable successor tick/state: `54881`,
  `7fd15485c5cef895dd902c51ac801f27d9110d92e15a2b9838b7ceb530967047`;
- retained formation receipt:
  `10f72d227550a26955e7a257bc17c410f5d959a7f4c67cb8130734c7031be91b`;
- recurrent-retention lineage:
  `474c4e4c494e4531000000000000008a`;
- exact formation members: 27;
- proper partial-cue reassemblies: 1;
- connected active component: 43 neurons and 76 bonds;
- canonical active-bond digest:
  `6913827b4464bbeb21593b41bc246bb76e58194c4deee96afffe58877bf7450b`;
- sensory: 27 neurons across layers 0 and 5, lineage-set digest
  `3077c4e081139d63d12322f6179c85ac4e5f668020f315c3f293182f92756063`;
- local sensory integration: one layer-6 neuron, lineage-set digest
  `a0132aed40a927abb9c0f92b3ab89109a98579b31da32bfa9b1f018a5dfb72f8`;
- recurrent retention: one layer-9 neuron, lineage-set digest
  `b885fb533ddbacdf4ca854b0e9b577811f6a27ba68921f3a36f1d40cbfe8fbc5`;
- body: two neurons across layers 5 and 8, lineage-set digest
  `75d0da7c85f2ab27d00ecd31755099893c995355ac22f734a93fb9afc50f2229`;
- affective: three layer-10 neurons, lineage-set digest
  `91b06e3c81508025ef7268aeaeac0dfc32a17a9875dd4d9167eee20717d9d871`;
- encoded successor delta: minus 36 bytes;
- successor cold restore: byte-exact;
- Python cognition callbacks: zero.

The production `CURRENT` pointer was not changed by this proof. The subprocess
restored the live body, advanced only its private in-memory copy, and exited.

## Durable preflight recurrence application

RF-001 through RF-019 remain applicable as recorded by the deployment skill.
This sprint added **RF-020**: mounted presence is not functional participation.
Before implementing a cognitive mechanism, inventory the authenticated live
predecessor's exact source-to-consequence participants and prove each required
population is reached and physically contributes. Population counts, layer
labels, counters, persisted records, and synthetic injection do not pass.

## Remaining boundary

The proof above does **not** close C-007. Its active component contains no
layer-7 cross-sensory association neuron. Source trace identifies the exact
cause: `settle_internal_contact_interval` advances one contact per interval,
but `next_active_frontier` currently retains only a newly reached layer-9
lineage. A receptor can therefore reach layer 6, but that layer-6 state is not
carried into the next interval to reach its already-mounted layer-7 neighbour.
The next change is limited to that causal frontier and must re-prove C-006's
boundedness before cutover.

## Recorded falsification

The first local implementation carried every newly reached layer-6 integration
lineage for one interval. The complete native suite rejected it: ordinary
first-use sensation then changed exact receptor state unexpectedly and could
advance into motor development without a retained recognition path. That rule
is discarded.

The corrected rule carries a newly reached layer-6 lineage for one interval
only when its current active-bond component also reaches an active layer-9
retention lineage. Layer 9 retains its existing one-interval behavior. Both
previously failing regressions now pass, and the full native result is 407
passed, zero failed, and 13 explicitly ignored frontier tests. This is local
test evidence only; candidate rehearsal and live production proof remain
required.
