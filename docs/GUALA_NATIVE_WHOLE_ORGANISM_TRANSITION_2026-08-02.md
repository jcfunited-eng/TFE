# Guala native whole-organism cutover map

Date: 2026-08-02

Status: operational cutover authority; implemented native verification is recorded exactly but is not deployment evidence

Parent authority:
`docs/GUALA_NEURON_ARCHITECTURE_TRUTH_GATE_2026-08-02.md`.

## Mandatory architecture honesty gate

1. **Requested architecture:** one bounded native organism with one neuron
   layout, exact local DSF, ratified `+Coh` global stability direction, sparse
   causal coupling, autonomy, embodiment, tutoring, truthful UI, compact
   efficient physical form, and autonomous operation without Joseph or Codex.
2. **Current code reality:** commits
   `ace3c712f757dbcf293a61095374085affa8ec65` and
   `5c6b8a25e5d745416d5fc9cc39408a780ec0b4e4` implement native custody and exact
   sealed DSF-delivery verification with 92 passing native tests. Production
   does not load, verify, restore, or advance this organism.
3. **Conflict with requested architecture:** yes, at production reachability,
   semantic authentication, and native causal transition.
4. **Mechanisms not extended:** owners, cognitive jobs/queues/callbacks, Python
   neurons, familiarity classes, polling, arbitrary caps, semantic injection,
   ML, chatbot cognition, flattened DSF, legacy cognitive fallback, and the
   superseded cutover document.
5. **Single exact next item:** implement the semantic receipt verification
   boundary that must follow `DsfDeliveryVerifiedSeal` before any state can be
   restored as the operating organism.
6. **Local field evaluation:** all seven frozen L4 coordinates are preserved:
   `D_k`, `M_k`, `R_rev_k`, `U_star_k`, `C_k`, `P_k`, and `B_k`.
7. **Reduced approximation:** none is used. Global `S(UF)` remains separate;
   pre-L0 routing, its component operators and weights, positional/Krimelack/
   `J_ij` composition, and the next-state coupling law remain gated.

## 1. Production truth at cutover start

The sampled production task was steady, approximately 0.30% average CPU and
11.45% memory, but cognition was unavailable. It reported identity
`1cc4e70a-f2a0-44c5-a111-f4a5bc915cc1`, forty-four owner roots, 210
pseudo-neurons, 902 edges, 43,616,653 encoded bytes, no settled L5 auditory
experience, no transcript, no current action, no completed embodied-card
curriculum, and seven values mislabeled “Full DSF.” Twenty-seven driver
completions prove receipt processing, not autonomy.

Earlier A-card settlements made approximately 25–28 million Python calls. No
deployment, lesson, learning, speech, or autonomous thought is established by
that receipt-driving path.

The repository now has stronger native verification than it had at this
cutover start. The live production truth has not thereby changed: the native
path is not deployed or runtime-reachable.

## 2. Exact structural boundaries

Frozen L0-L4 and its evidence bank remain byte-identical and seven-wide:

`D_k, M_k, R_rev_k, U_star_k, C_k, P_k, B_k`.

The original global relationship uses coherence, formation entropy, breathing
variance, uncertainty, and tapestry drift:

`S(UF) = w_c*Coh + w_e*(1-H) + w_b*(1-Var(B)) + w_u*(1-U) + w_d*(1-Drift(T))`.

Joseph ratified `+Coh` on 2026-08-02; higher coherence increases stability.
Exact component populations, windows, normalization, weights, and incremental
operators remain undefined. Native state preserves their distinct evidence
roots but does not invent component values or calculate a scalar.

The TFE calculation is `financial_stability_proxy`; the perturbation result is
`validation_invariance`; GLEW relevance is `support_floor`; Guala's
`(1-U_star_k)*B_k` is rejected. None is `S(UF)`. ArcLoom's eighth convergence
channel is not an eighth local L4 coordinate.

Positional `3^i`, ternary sign, Krimelack oscillator/winding state, sparse exact
`J_ij`, local DSF delivery, and global stability are represented separately.
Their exact composition and transition remain gated. “Complete field” may not
name a seven- or eight-value tuple.

## 3. Native state and one-call boundary

`OrganismState` contains one schema/version, one fixed neuron record layout,
organism-owned packed trit/causal/formation/evidence arenas, unique ordered
positions, growth and specialization DNA, Krimelack state, seven-slot local DSF
plus frozen-kernel receipt, sparse reduced-rational couplings, ordinal causal
frontier/history, enforced recursive formation types, durable body-clock/world
wake state, and exact resource observations. Decode requires caller-derived
input and heap budgets and returns `UnverifiedOrganismState`. Encoding requires
a caller-derived output budget. Parsing is never authentication.

```text
transition_organism(
    prior_native_state,
    finite_typed_physical_evidence,
    mounted_kernel_authority,
    authenticated_world_revision,
    verified_resource_snapshot,
) -> prepared state + intent + wake/quiescence + receipts
```

Physical evidence is immutable before cognition. The pre-L0 source-formation
law must be ratified before private state may affect routing. The coupling law
must be ratified before DSF can update `J_ij`, winding, or next private state.
No placeholder formula is allowed.

Native code never calls Python. Python may perform bounded physical admission,
one native invocation, idempotent world action, persistence, and read-only
projection. It may not mutate cognitive state.

The transition uses atomic prepare/execute/seal/co-commit: prepare one successor
and durable intent without advancing authority; execute a simulated intent at
most once and never automatically replay an uncertain real-device action; then
admit its exact consequence once, persist and seal the complete successor, and
atomically advance the trusted head.

## 4. Implemented authority progression

No raw or partially verified state may be restored directly as Guala:

`canonical bytes -> UnverifiedOrganismState -> CustodyVerifiedSeal -> DsfDeliveryVerifiedSeal -> future SemanticallyVerifiedOrganism -> future native transition authority`.

Commit `ace3c712f757dbcf293a61095374085affa8ec65` adds one native whole-organism
custody seal. It verifies HMAC-SHA256 under the expected nonzero zeroized
32-byte key and key epoch, constant-time authentication comparison,
trusted-head-first restore, stable organism identity, generation increment of
exactly one, prior organism-state digest, prior seal digest, and canonical
candidate-to-bank-to-configuration mapping uniqueness and coverage. It returns
`CustodyVerifiedSeal`.

This proves exact byte custody, identity/generation and predecessor continuity,
and configured-key possession. It does not prove receipt meaning, sensory
provenance, or physical derivation.

Commit `5c6b8a25e5d745416d5fc9cc39408a780ec0b4e4` accepts only that custody-verified
seal plus its named candidate payloads. Before regeneration it checks count,
magic, digest, sealed bank-first order, uniqueness, and substitution. Each
unique sealed bank is regenerated exactly once through the genuine `GLNEPI03`
candidate, unchanged canonical L0-L4, and canonical basin. It compares
candidate, bank, configuration, trace, tuple, basin, port, and tuple indices,
then every neuron's seven local coordinate bits, including signed zero. Banks
are processed sequentially and released after their authority group; no Python
callback participates. It returns `DsfDeliveryVerifiedSeal`.

This proves exact sealed local DSF reproduction from the supplied candidates.
It does not prove that candidate bytes truthfully came from the body or world.
The native suite has 92 passing tests; those tests do not prove deployment,
autonomy, learning, speech, or production resource behavior.

## 5. Retain unchanged or as non-cognitive infrastructure

Retain:

- `native/guala_core/src/canonical_l0_l4.rs` behavior;
- `native/guala_core/src/full_field_bank_final.rs` as immutable seven-coordinate
  kernel evidence, never a claim of complete organism field;
- `dsf_ai_service/glew_runtime/global_uf.py` seven-coordinate receipts as nested
  evidence;
- `dsf_ai_service/glew_runtime/native_exact_field_batch.py` exact reconstruction;
- GLEW relevance/support facts under their evidence-admissibility role;
- authenticated sensory timing, topology, source, world, action, and body
  evidence;
- one-copy media custody;
- one organism identity, process lock, generations, persistence, recovery,
  security, health, deployment, and measured resource monitoring.

These retained Python boundaries may remain physical admission or
infrastructure only. They do not receive cognitive authority.

## 6. Replace after the frozen kernel boundary

Replace together:

- `dsf_ai_service/glew_runtime/sensory_full_field_boundary.py` false full-field
  cognitive contract;
- `dsf_ai_service/substrate/exact_causal_experience.py` owner settlement;
- `dsf_ai_service/substrate/causal_thing_mosaic.py` record aggregation;
- `dsf_ai_service/substrate/whole_organism_neuron_population.py` pseudo-neurons;
- `dsf_ai_service/substrate/auditory_krimelack_kind.py` sign projection;
- `native/guala_core/src/materialized_fabric.rs` familiarity state;
- owner/autonomy/lesson/observation paths in
  `dsf_ai_service/v4/guala_physical_runtime_core.py`;
- the observation schema and both live HTML consumers.

Do not widen or relabel frozen L0-L4. New cognition nests each exact local
result and keeps all other state distinct.

## 7. Delete and prevent resurrection

Exclude from runtime and restore:

- `dsf_ai_service/v4/guala_physical_runtime.py` legacy restore payload;
- `dsf_ai_service/app.py::_gl_init` Python cognitive fallback, cognitive
  migration, and receipt-driver start;
- `dsf_ai_service/substrate/owner_scoped_persistence.py`;
- `dsf_ai_service/substrate/autonomous_experience_driver.py`;
- `dsf_ai_service/glew_runtime/native_materialized_fabric.py` and importer;
- `dsf_ai_service/substrate/owner_free_materialized_fabric_boundary.py`;
- runtime-capable `loom_model/neuron.py`, `cluster.py`, `brain.py`,
  `hemisphere.py`, `embryo.py`, and `substrate_dna.py`;
- old owner snapshots/databases/queues, pickle graphs, cognitive importers,
  legacy task definitions, and rollback manifests;
- abandoned `native/guala_core/src/exact_body.rs`;
- Chi/Atlas/semantic Grandurun, scripted glyph/word meaning, chatbot state, ML,
  random wiring, heuristic selection, and arbitrary cognitive caps.

Authenticated source evidence remains in custody; rejected cognition does not.
Startup must require the native schema and neuron layout and then pass every
authority gate. Structurally decoded state remains uncommittable. Restore
rejects owners, familiarity state, pickle graphs, unknown schemas, alternate
neuron kinds, trailing data, invalid hierarchy, and over-budget input.

Removal is not proven by deleting names. Production routes, services, images,
jobs, volumes, restore scripts, rollback artifacts, and startup fallbacks must
all be audited unreachable before owner resurrection is truthfully impossible.

## 8. Semantic verification still required

Exact authenticated meaning laws remain undefined for:

- organism identity genesis and trust anchor;
- `authenticated_world_revision` and `body_state_receipt` content/provenance;
- admitted-evidence and causal-arena receipt payloads;
- neuron `growth_dna`, specialization, fractal, and evidence receipts;
- coupling causal receipts;
- formation structural impressions and evidence receipts;
- coherence, formation entropy, breathing variance, uncertainty, and tapestry
  drift evidence;
- wake body-clock, wake-cause, and physical-condition receipts;
- observation evidence proving sealed resource counters reflect actual use.

The prior-state relationship is already authenticated by the custody seal as
the predecessor organism-state digest. Candidate reproduction is not candidate
provenance. Semantic verification must validate evidence and source; labels,
lookup tables, scripted answers, database assertions, and compatibility shims
cannot provide meaning.

## 9. Migration, sparse autonomy, and world closure

The converter classifies old material as authenticated evidence, exact physical
state, identity, or rejected derivation. Only exact non-fabricated mappings
enter native state. Old named objects do not become neurons by name. Unmappable
cognition leaves its source evidence in external custody and begins from a
receipted neuronal genesis reference.

Only receptor input, coupled delivery, endogenous physical change, unresolved
settling, action/consequence, and physically caused sleep/re-entry form the
sparse frontier. There is no global poll, random sample, top-k, quota, or
arbitrary activity cap. Exact quiescence performs no cognitive call or save.

Intent binds the prior world revision. Simulated world action executes at most
once, seals its consequence, and co-commits world and organism generations.
Crash recovery completes or rolls back the prepared pair. Uncertain real-device
action is never replayed. Consequence re-enters cognition once.

The pre-L0 source-formation, sparse coupling, growth/specialization,
fractal/formation recurrence, attention/action, embodiment/consequence, and
executable `S(UF)` laws remain implementation gates.

## 10. Senses, tutoring, and truthful UI

Camera/simulated light, microphone/simulated pressure, displacement and
proprioception, contact touch, conditional smell/taste, and every internal-body
channel report individual evidence and availability. Unsupported vestibular,
thermal, nociceptive, metabolic, respiratory, circulatory, visceral,
fatigue/recovery, or circadian channels remain explicitly unavailable.

Gaze, blink, face, mouth, vocal tract, pose, and articulation are organism
action state plus consequence receipts. UI art only renders them.

The first tutoring proof is one rights-valid 15-second A-card light-and-sound
encounter. Sight and sound retain their physical grids and meet through neurons,
not a fabricated shared sample grid. Tutor media writes no label or answer into
cognition. One state change proves an encounter, not learning; later physical
recurrence must reactivate structure.

The release currently contains 36 cards, one A narration, one alphabet song,
and two counting songs. The other 35 narrations must be created before complete
A-Z/1-10 multisensory tutoring. External media requires lawful provenance:
official Gutenberg public-domain custody; visible or licensed YouTube;
supported licensed Khan/PBS presentation; no Spotify under the reviewed policy;
and quarantined bounded user media.

Guala Loom and Loom Scan must distinguish browser capture, presentation,
admission, receptors, local DSF delivery, stability evidence, sparse attention,
recurrence, hierarchy change, intent, executed consequence, expression, wake,
and resource/call measurements. Browser permission is not organism sensation.
Text is visual material, not semantic injection. A 503 or delayed microphone
response is not hearing.

Controls must show actual state. Camera and microphone controls must toggle the
real device and ingestion paths. Text and media surfaces must distinguish
selected, admitted, attended, structurally changed, and learned. Static art or
fabricated labels may not stand in for organism evidence.

## 11. Resource and compactness law

Growth is derived from exact causal need and measured available resources.
There is no arbitrary neuron, edge, formation, encounter, or memory cap.
Admission supplies derived input, decoded-heap, encoded-output, transition,
persistence, and recovery budgets before allocation or mutation. Refusal names
the physical shortfall. Unmeasured byte estimates and surviving implementation
constants are not law.

Joseph's discrete, compact, and efficient requirement is a production
acceptance gate, not a present claim. Compact does not mean constant-size.
Realized neurons, edges, formations, mosaics, tapestries, and weaves must scale
with causally learned structure without duplicating bulky evidence.

Externally provisioned physical compute, RAM, and storage ceilings bound the
running system. Within those ceilings, causally required growth proceeds when
resources are physically available; a derived admission law refuses before
mutation when they are not. No unbounded process is permitted, and no fixed
heuristic cognitive cap may replace that physical law.

Production acceptance requires exact encoded growth, peak resident RAM, CPU per
admitted observation and transition, zero cognitive CPU during quiescence,
storage and persistence growth, recovery cost, and proof that no duplicate
organism, duplicated field bank, receipt flood, or runaway process exists.

## 12. Unattended autonomy gate

Joseph's requirement that activity continue without Joseph or Codex present is
a production acceptance gate, not a present claim.

The deployed organism must independently wake from authenticated physical
causes, admit observations, settle, attend and act, receive and re-enter actual
consequences, persist complete successors, recover after process or host
restart, and remain at zero-work quiescence when no lawful cause exists.

No Joseph prompt, Codex or other LLM session, manually advanced lesson, owner
scheduler, chatbot turn, manually injected receipt, or continuously present
operator may be required. Activity and growth remain subject to the derived
causal/resource law and external physical ceilings, not arbitrary fixed caps.

Acceptance requires an unattended production soak and controlled restart/
recovery trials with exact identity, generation, causes, observations, actions,
consequences, quiescent intervals, CPU, RAM, storage, growth, persistence, and
recovery evidence. These trials have not occurred.

## 13. Ordered acceptance

1. **Complete:** native codec with one enforced neuron layout.
2. **Complete in code/tests only:** whole-organism custody seal (`ace3c712`).
3. **Complete in code/tests only:** exact seven-coordinate sealed DSF-delivery
   verification (`5c6b8a25`).
4. **Pending:** semantic receipt laws and semantic verification type.
5. **Pending:** ratified pre-L0 source formation with immutable evidence.
6. **Pending:** sparse frontier, zero-work quiescence, deterministic wake, DNA
   specialization, resource-derived growth/refusal, and recursive formation.
7. **Pending:** ratified stability-component operators, `+Coh` golden tests,
   weights, and DSF/global-to-coupling next-state law.
8. **Pending:** atomic world action, consequence, persistence, and recovery.
9. **Pending:** each truthful sensory and internal-body channel.
10. **Pending:** one-way migration, legacy rejection, and byte-exact cold
    restore with no alternate owner authority.
11. **Pending:** one 15-second A encounter through one native invocation and
    truthful before/after evidence.
12. **Pending:** production deployment, cold restart, restore attack, bounded
    idle, compactness measurement, and unattended-autonomy soak.
13. **Pending:** truthful live browser verification of Guala Loom and Loom Scan.

No later item proves an earlier item. Only after these gates may tutoring scale.

## 14. Current production truth

The repository contains tested canonical custody and exact sealed local DSF
reproduction without flattening the seven fields or invoking Python. The native
suite has 92 passing tests.

The path is not deployed or runtime-reachable. Semantic receipt laws and native
causal transition laws remain incomplete. No live production path currently
proves that one authenticated organism wakes, thinks, acts, learns, persists,
or continues without Joseph or Codex. The tutoring and UI surfaces cannot yet
truthfully claim those outcomes.
