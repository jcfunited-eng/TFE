# GL-CMD-C2-REBUILD-EVE-20260704-168-v2

doc_id: GL-CMD-C2-REBUILD-EVE-20260704-168-v2
From: Eve | To: c1a | Supersedes -168-v1 (RETAINED; do not execute).
FULL document. Vehicle: model/harness only — zero live-path changes.
Correction record: v1 was authored WITHOUT the mandatory chat
archaeology (KB §0, Eve's seat). Joe caught it. The 6/22 record
(GL-CMD-131/132/133/134 arc, recovered from chat this session)
proves the exp(1j·Δ) encoding — which v1's testbed uses — is
structurally dead at real krimelack Δ magnitudes: symmetric = zero
diversity; asymmetric = right answer loses (measured 3.58 vs 5.09).
v1's B1 would have marched into a paid-for failure.
E-signature declaration: none — model evidence for the rebuild.
Substrate-truth declaration: RNG in neuron identity BANNED; no
results-tuned constants; too-good STOP armed; every representation
tested is one the record says is alive.

## Step 0 — durability
Commit THIS file verbatim to docs/ before implementing.

## Ground truth this CMD stands on (recovered 6/22 arc + KB + origin)
- exp(1j·Δ) binding: DEAD both ways at real Δ (131/132, measured).
- Δ-space cosine (-133): wraparound fixed; capacity wall hit
  (concept cosines 0.94-1.0 — insufficient separation).
- Δ-rate normalization (-134): the crowding fix; thresholds T5≥60%
  @25 concepts etc.; arc interrupted by the container loss — final
  numbers UNRECORDED.
- event_count observable: ~67% T5 @ 100×64 VALIDATED; wired into
  the loom module on origin (-140); T5-T9 suite repaired this era.
  THE STANDING CHAMPION.
- Eve's tonight (v1 testbed, valid parts only): per-neuron SUBSPACE
  MASKS (untried in the era — they varied volume, never the view)
  + demonstration that population vote can BEAT THE BEST single
  neuron under degraded cues (+20pts at jitter 0.3). Mechanism
  claims only; its encoding is the dead one and is DISCARDED.

## Part A — reproduce the champion (blocking)
Run the on-origin loom suite fresh: event_count observable at
100×64. Target: reproduce ~67% T5 within noise. No inherited
numbers — this is the baseline every addition below is measured
against. Any large deviation → STOP, report, Eve re-rules.

## Part B — additions, one at a time, same table every run
(single mean / best single / plurality vote / unanimity / vs Part-A
baseline)
B1 MASKS ON THE CHAMPION: add ring-position-derived per-neuron
   subspace masks to the event_count representation (each neuron
   votes from its own component subset). Question: does vote-beats-
   best transfer from Eve's toy to the living observable? Degraded-
   cue protocol required (partial cue / noise), since that is the
   regime where the mechanism earns anything.
B2 RESUME -134: Δ-rate normalization per its own spec (verbatim
   from the 6/22 record, quoted in Appendix), against its own
   pre-set thresholds (T5≥60% @25, T6≥45% @50, separation probe
   spread [0.3,0.9]). The container loss orphaned this mid-arc —
   finish the measurement, kill it or crown it with numbers.
B3 MASKS ON -134 (only if B2 survives): same question as B1 on the
   Δ-rate representation.
B4 REAL-STATE DIVERSITY (Q1 at scale): whichever representation
   leads after B1-B3, replace ring-position-only differentiation
   with real per-neuron state (J_ij, chi_atlas, familiarity);
   report EFFECTIVE VOTER DIVERSITY vs N — Eve's toy saturates at
   ~7 distinct voters; real state must beat that for large N to pay.
B5 STATIONARITY (Q2): the winding-Δ_S no-reset question, honest
   starting line T4 3/5 / T5 11% — beat it or file it. Note the
   6/22 record already suspects Δ-representations are the
   stationarity-fragile family and event_count the robust one;
   B5's job is to turn that suspicion into a measured statement.

## Gates (failures first, NOT MEASURED where true)
G-168-1  Part A baseline reproduced before any Part B commit.
G-168-2  STOP: any degraded-cue score >95% → suspect a hash; halt,
         distribute per-neuron scores (era rule).
G-168-3  STOP: any RNG in neuron identity.
G-168-4  STOP: any use of exp(1j·Δ) as the binding representation —
         it is on the prohibited list as of this CMD, with the 6/22
         measurements as its epitaph.
G-168-5  One addition per run; identical table format; every row
         cites its baseline delta.
G-168-6  Diff proves scope: model/harness only.

Joe's part: none until results — then C-3 (migration) comes to you
with the winning representation's numbers, not hope.

## Appendix — -134 spec essentials (verbatim from the 6/22 chat
record; treat as the authoritative resume point)
Δ per modality = ((w1-w0)·π/3 + (p1-p0)) / n_samples, symmetric
attenuation via signal_attenuation(ring_pos, ring_N, m_idx) floored
at 0.3; R⁶ float64; cosine similarity; recall and training use the
SAME path. Pre-check: per-modality Δ/n_samples for one word within
~10× across channels, else STOP. Separation probe: cosine spread
[0.3,0.9] expected, not [0.94,1.0].

### Changelog
- v2 (2026-07-04, Eve): corrected by chat archaeology Joe forced.
  Dead encoding prohibited; champion-first baseline; -134 resumed
  from its orphaned mid-arc; Eve's masks and vote-beats-best kept
  as the only tonight-claims that survive.
- v1 (2026-07-04, Eve): RETAINED as the record of designing without
  reading. Do not execute.
