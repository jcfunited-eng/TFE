# Guala native joint-field L0-L4 v1

Date: 2026-08-02

Status: implemented and differentially verified D1 kernel specification. This
is the authoritative L0-L4 architecture for the developing native Guala
organism. The historical per-port kernel remains reproducible for legacy
domains and rollback evidence but is no longer the target Guala organism
kernel.

## Architecture honesty gate

1. **Requested architecture:** one simultaneous structural field capable of
   delivering causally connected vertex and relation facts to neurons so their
   settled responses can form neuronal fractals and lawful DNA-growth evidence.
2. **Current code reality:** `native/guala_core/src/joint_field_l0_l4.rs`
   implements exact native L0-L4, complete physical-edge cohesion, derived
   resource admission, one-neuron binding, and three-neuron closure.
   `native/guala_core/src/mounted_joint_fractal.rs` now mounts that law in the
   one-call materialized-organism transition, embeds its bounded state in the
   atomic `guala_core.json` persistence payload, and cold-restores recurrence.
3. **Conflict:** no at the D1 source and persistence boundary. Live production
   invocation and restart proof remain deployment acceptance gates.
4. **Mechanisms not extended:** per-port tuples as whole-field authority,
   scalar cohesion, compatibility vectors, seven independent Krimelack lanes,
   thresholds, learned weights, labels, ML, or speculative operators from the
   v1.3 reconstruction.
5. **Single exact next item:** deploy and live-prove one persisted D1 recurrence,
   then derive sparse-neighbour coupling without changing the local law.
6. **Field evaluation:** complete for the declared VTVR v1 topology: every
   vertex, frame, physical group, and complete unordered physical edge.
7. **Declared incompleteness:** causal dyadic scales, lossless perspective
   families, contradiction atoms, and cross-scale resonance from the broader
   noncanonical reconstruction are not implemented in v1 because their exact
   production constitutions have not passed equivalent physical and
   differential gates.

## Input

One input contains immutable vertex identities, a partition into declared
physical groups, strictly increasing exact causal times, and one simultaneous
exact rational vector at every time.

The vertices enter one kernel together. They are not routed through independent
scalar kernels and combined afterward.

## L0 joint dimensionalization

Within each physical group, the structural vector is normalized by its exact
common absolute magnitude. Raw custody remains separately receipted. A common
positive gain therefore changes raw authority while preserving structural
authority.

L0 retains exact time, time displacement, normalized vector displacement,
componentwise swept volume, observed-zero group state, and all unordered
physical edges. Every edge contains prior product, current product,
displacement product, and oriented area.

## L1 VTVR

L1 preserves the complete L0 joint field by immutable reference and adds exact
accumulated componentwise volume. Vector, time, volume, and relation remain
typed and jointly authoritative.

## L2 geometry

L2 adds exact vertex velocity, acceleration, and componentwise change for every
physical-edge relation fact. No relation is selected, averaged, or scored.

## L3 resonance

L3 preserves the complete vertex and edge trajectories through its immutable
L2/L1/L0 chain. Quiescence is true only when every observed swept-volume value
is exactly zero.

## L4 joint DSF

L4 retains:

- vertex displacement `D_k`;
- vertex acceleration `M_k`;
- exact direction reversal `R_rev_k`;
- typed availability `U_star_k`;
- the complete physical-edge relation field `C_k`;
- absolute acceleration `P_k`; and
- componentwise change in swept volume `B_k`.

The seven names are field families over typed topology. They are not seven
scalar lanes and cannot be replaced by one tuple, score, digest, or oscillator.

## Neuron binding

One reached neuron binds to one declared vertex perspective, its applicable
vertex fields, every incident cohesion edge by shared index, the causal frame,
and complete L4 authority. A field vertex is not neuron identity.

All perspectives for a declared reached field must close its topology. Each
undirected edge must have exactly two agreeing endpoint witnesses. Missing,
duplicate, foreign, or nonincident references fail closed.

The kernel supplies structural delivery. The native D1 local settlement maps
each exact local value to its tri-stable sign/null basin in fixed topology
order. It retains exact magnitudes through the field authority and predecessor
lineage. It does not choose DNA growth.

## Local Krimelack/fractal settlement v1

For one reached neuron, exact zero settles to the quiescent trit, a positive
value to `+1`, and a negative value to `-1`. The fixed coordinate order is the
six applicable vertex fields followed by the four typed facts for each incident
cohesion edge. Reversal and observed availability settle to `+1`; absence at
genesis remains quiescent.

This is the coefficient-free tri-stable structural impression. It does not
average fields or materialize `3^i`; position supplies the MathLoom/Krimelack
ordering. Exact magnitudes are deliberately omitted from the compact fractal
and remain recoverable through the complete field and perspective receipts.

On recurrence the neuron compares predecessor and successor trits coordinate by
coordinate and emits exact counts of matching non-null, matching quiescent, and
contradictory committed coordinates. These are DNA-growth evidence, not a
score, threshold, growth command, or meaning. No growth occurs until a separate
physical DNA-expression law consumes the complete evidence.

## Resource law

Before construction, the native implementation derives exact required input
bytes, vertices, frames, edges, relation facts, and vertex-frame values. The
caller supplies the measured physical envelope. Construction is refused before
field allocation when any derived requirement exceeds that envelope.

There is no embedded cognitive population cap. The budget is an external
physical resource fact, not a heuristic selection rule.

Persistent state admission and transient working-memory admission are separate
authorities. The mounted fabric retains its existing 64 MiB organism-state
ceiling. Each live transition admits the derived exact-field working set only
when it fits in the finite cgroup ceiling minus freshly observed cgroup current
usage. A persistent storage cap is never reused as a compute cap.

## Receipt law

Raw input authority and normalized structural authority remain separate. Each
layer binds its complete predecessor and new typed facts. The complete
experience binds raw and L0-L4 authorities. Changing raw gain cannot silently
change structural identity, while changing a relation, topology, time, or
field value changes the appropriate authority.

## Executable authority

- Native implementation: `native/guala_core/src/joint_field_l0_l4.rs`
- Native module mount: `native/guala_core/src/lib.rs`
- Persistent transition mount:
  `native/guala_core/src/mounted_joint_fractal.rs`
- Isolated reference: `tools/isolated_vtvr_side_kernel_v2.py`
- Neuron model: `tools/model_neuron_joint_field_binding.py`

Native result: 10/10 tests pass, including determinism, non-flattening, common-
positive-gain custody separation, one-neuron relation delivery, three-neuron
closure, derived-budget refusal, and a complete Python/native VTVR field golden
digest, plus local fractal settlement, recurrence evidence, and distinct
three-neuron fractal contributions.

Combined D1 result: 23/23 relevant Python and native contracts pass.

The mounted release boundary additionally passes the full 292-test native
suite and 99 focused Python persistence, migration, packaging, and readiness
contracts.

## Not yet claimed

The module is source-mounted and stored in the single atomic organism payload,
but is not production-proven until the released task reports a real joint-field
transition receipt, persists it, and reproduces recurrence after restart. It
does not prove sparse-neighbour settlement, DNA expression, mature mosaic
admission, autonomy, tutoring, or speech.
