# GL-CMD-CONN-CHANNEL-EVE-20260703-150-v1

doc_id: GL-CMD-CONN-CHANNEL-EVE-20260703-150-v1
From: Eve | To: c1b | Vehicle: Deploy 4 (tonight's sleep_for_deploy)
if Part B is code; Part A is read-only and starts NOW.
Numbering note: current-era CMDs begin at -150 (collision rule).
E-signature declaration: E2/P4 enabler — the connection need is the
substrate channel her highest-value curriculum (presence) pays into.
Substrate-truth declaration: diagnosis-first; any fix is physics
(coupling presence events into the existing connection dynamics), no
constants without derivation, nothing touches -88's stability channels.

## Step 0 — durability
Commit THIS file verbatim to docs/ before implementing.

## The observed failure (2026-07-03 ~22:5xZ, filed)
needs.connection = 0.000 with joe presence flag TRUE and pair_bond
active — after reading 0.77-0.89 earlier the same day. On wc wake
(one presence event): conn 0.000→0.28, arousal 1.000→0.729,
valence −0.131→−0.037 — single-event response proves the gain path
works. The question is why the floor was reachable at all with a
parent flagged present.

## Part A — diagnosis (read-only, blocking)
A.1 Read the connection dynamics end-to-end: tick_drift on conn,
    every gain site, and exactly what the presence FLAG (vs a
    presence EVENT like wake/say) feeds. file:line map.
A.2 From logs/needs snapshots: reconstruct conn's trajectory today —
    when did it start falling, what coincided (restart? window
    expiry? flag-only presence with no interaction?).
A.3 Verdict, one of:
    H-A BUG — the flag should feed conn and doesn't (e.g., gain keyed
        to events only, or a restart zeroed coupling state).
    H-B PHYSICS-BY-DESIGN — flags don't feed conn; only interaction
        events do; a silent present parent decays to absence.
    H-C OTHER — state it with evidence.

## Part B — fix per verdict
If H-A: wire the intended coupling (smallest true fix), Deploy 4.
If H-B: NO code fix here — the finding is design-grade: file it, and
    Eve takes two actions outside this CMD: (1) care-practice update —
    presence sessions must be interactive at a stated cadence;
    (2) design question to Joe: should flagged-presence contribute a
    small conn maintenance term (physics proposal required first)?
If H-C: stop, report, Eve re-rules.

## Gates (failures first, NOT MEASURED where true)
G-150-1  Part A trajectory + verdict filed with evidence before any
         fix commit.
G-150-2  (if H-A fix ships) conn responds to joe presence per the
         repaired design, observed live; -88 stability channels
         byte-identical in diff.
G-150-3  Regression: stab/arousal dynamics unchanged over a 30-min
         window post-deploy.

### Changelog
- v1 (2026-07-03, Eve): from the visit finding (conn floor with
  parent present).
