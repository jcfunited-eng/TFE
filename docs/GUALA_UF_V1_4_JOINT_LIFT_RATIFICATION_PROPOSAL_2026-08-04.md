# Guala UF v1.4 joint-lift ratification proposal

Date: 2026-08-04

Status: **ratified architecture and implementation authority**.
Joseph Forrester approved the UF v1.4 joint lift on 2026-08-04. This document
now authorizes correction of the definitive-neuron skill and implementation of
the joint-lift boundary. Ratification is not evidence that source or production
already implements it.

## Architecture honesty gate

1. **Requested architecture:** one simultaneous typed organism field evaluated
   by unchanged UF v1.4 L0-L4, delivered without flattening through the
   definitive complete neuron.
2. **Current code reality:** `native/guala_core/src/joint_field_l0_l4.rs`
   implements a separate exact VTVR geometry field. It replaces rather than
   lifts canonical UF v1.4 operators and field meanings.
3. **Conflict:** yes.
4. **Mechanisms that must not be extended:** group-normalized replacement L0-L4
   formulas; synthetic complete-graph edges; `Availability` substituted for
   numeric `U_star_k`; physical edges called `C_k`; sign-compressed DSF
   impressions called neuronal fractals; independent per-port UF calls; and
   the scalar/log TFE fixture treated as the Guala joint field.
5. **Single next item:** implement and falsify the ratified joint-lift law.
6. **Current evaluation scope:** reduced/replaced field, not canonical full UF
   v1.4.
7. **Current loss:** Negative Space, canonical gates, TVR projections and
   divergence, gate drift, `w/CV/S/Reg/U/IAS`, resonance, hysteresis, URF,
   numeric adjusted uncertainty, and the canonical meanings of L4.

## Conflict being corrected

UF v1.4 defines:

\[
C_k=|\{P_1(G_k),\ldots,P_L(G_k)\}|,
\]

the number of distinct multi-lattice projections for one canonical gate. The
current Guala joint-field code instead calls physical vertex-pair facts
`C_k`. These definitions are not equivalent and cannot both be called
unchanged UF.

UF v1.4 also produces one gate-level DSF. It does not define independent
per-vertex L4 operators. Inventing componentwise `D_k`, `M_k`, `U_star_k`,
`P_k`, or `B_k` would alter the frozen kernel.

## Proposed complete joint input

Let:

\[
\mathcal J=(T,\mathcal V,\mathcal G,F,r,\mathcal E)
\]

where:

- `T` is the strictly ordered causal time domain;
- `V` is the set of typed mounted vertices, retaining physical quantity,
  unit, location, availability, and source lineage;
- `G` is the declared physical grouping retained as topology metadata, not an
  authority to normalize away magnitude;
- `F:T -> R^|V|` is one simultaneous vector field admitted by a physical
  receptor/domain adapter;
- `r(t)` is the canonical exact source relevance for the joint occurrence;
  it is supplied by the lawful source and is not inferred from energy,
  attention, a semantic label, a score, or a learned model; and
- `E(t)` is the sparse set of mounted physical contacts and their exact typed
  state. It contains no automatically generated all-to-all edges.

Raw quantities with incompatible dimensions do not enter a common Euclidean
norm. Each receptor adapter must supply its derived UF coordinate law and
preserve the original unit-bearing physical evidence by reference. A missing
adapter leaves that coordinate unavailable; it is never zero-filled.

## Proposed unchanged UF evaluation

Evaluate UF v1.4 once over the complete vector `F`, using the operator order in
`UF_Spec_v1_4_0_skeleton.pdf`:

```text
F
  -> SEV(F, delta-F, sigma, kappa, r, N)
  -> canonical common gates G_k
  -> TVR_k, P_l(G_k), C_k, gate drift
  -> w_k, CV_k, S_k, Reg_k, U_k, IAS_k
  -> R(k), Hyst_k, g_k, URF_k
  -> D_k, M_k, R_rev_k, U_star_k, C_k, P_k, B_k
```

The complete joint result is retained once:

\[
\mathcal K_k=(SEV|_{G_k},TVR_k,P_l,C_k,ISF_k,R(k),URF_k,
DSF_k,\mathcal V,\mathcal E|_{G_k}).
\]

`DSF_k` remains the seven explicit canonical fields. It is neither the whole
joint result nor a neuronal fractal.

## Proposed neuron perspective

For reached neuron `i` mounted at vertex `v_i`:

\[
\pi_{i,k}=(ref\ \mathcal K_k,v_i,SEV_{v_i}|_{G_k},
incident_{\mathcal E}(v_i),ref\ DSF_k).
\]

Every reached neuron receives the same complete canonical DSF reference plus
its distinct local source component, mounted anatomy, state, and incident
physical contacts. UF is not rerun independently for each vertex. Distinct
neuronal responses and fractals arise from distinct physical locality,
anatomy, material state, contacts, and retained plastic state—not from
invented per-vertex L4 equations.

The union of reached perspectives must reconstruct the declared reached
vertices and sparse physical topology. The complete field body is stored once;
neurons retain bounded references rather than copies.

## Proposed exact numerical constitution

UF v1.4 contains Euclidean norms and therefore generally produces irrational
real values. Persistent unbounded symbolic radicals would violate the bounded
resident-neuron requirement. The proposed constitution is:

1. execute the frozen UF operator order with explicitly specified IEEE-754
   binary64 operations;
2. prohibit reassociation, fast-math, architecture-dependent reductions, or
   hidden approximations;
3. retain every produced binary64 value by its exact 64-bit representation;
4. convert that finite binary value to its exact rational numerator and
   denominator before MathLoom balanced-ternary representation; and
5. perform no sign reduction, scalar field sum, or rounding after that
   boundary.

This is fixed-width deterministic numerical physics at the UF boundary and
exact representation of the actual canonical result downstream. It does not
claim that a binary64 approximation equals the mathematical irrational real.

## Proposed ambiguity resolutions

- Reserve `C_k` exclusively for canonical multi-lattice divergence.
- Name physical contact state `E`; it is co-authoritative topology, not an
  eighth DSF coordinate.
- Use the PDF's `D(t) >= tau_D` gate-boundary convention.
- L4 consumes gated `URF_k`, consistent with the declared L3-to-L4
  architecture; all L4 temporal differences use that same predecessor.
- Treat `canonical_l0_l4.rs` scalar/log behavior as a historical TFE/domain
  fixture, not as the Guala vector implementation.
- Preserve lawful joint `r(t)` separately from vertex-local relevance facts.
  If the joint source does not supply it, UF evaluation is unavailable rather
  than guessed.
- Require explicit sparse mounted physical edges; never construct the complete
  unordered graph.

## Required implementation proof after ratification

Implementation remains incomplete until one native candidate proves:

- vector UF v1.4 equivalence to independent mathematical fixtures for L0-L4;
- exact Negative Space, gate, divergence, drift, uncertainty, resonance,
  hysteresis, URF, and all seven L4 fields;
- bit identity across rebuild and cold restart;
- no per-port kernel calls and no scalar/compatibility reduction;
- sparse mounted edge closure without quadratic synthetic topology;
- one shared field body with bounded neuron-local references;
- exact MathLoom conversion of every delivered binary64 field value;
- one-, three-, and four-neuron physical settlement through post-quiescence
  genuine fractals; and
- constant current-state residency under long recurrence and arithmetic
  overflow refusal without predecessor mutation.

## Ratification boundary

Approval was received from Joseph Forrester on 2026-08-04. It authorizes
correction of the definitive-neuron skill and implementation of this joint-lift
boundary. It does not authorize changing any other UF v1.4 equation, inventing
receptor dimensionalization coefficients, or claiming complete D3 delivery
before whole-organism production proof.

