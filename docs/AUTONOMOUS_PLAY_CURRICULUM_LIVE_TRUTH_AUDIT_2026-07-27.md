# Autonomous play and curriculum live-truth audit — 2026-07-27

This report separates current local executable proof from live-production
truth. It is not a deployment record.

## Architecture honesty gate

1. Requested architecture: Guala autonomously acts in a truthful embodied
   world, experiences the physical consequence through the complete DSF field,
   grows causal THING memory, and remains bounded and cold-restorable.
2. Current local code reality: one exact, closed, physically demonstrated
   action relation can enter a one-use autonomous `PLAYING` activity and
   complete through the W1 world. Novel motor-program invention and a live
   tutor curriculum are not yet present.
3. The proved local action-replay mechanism is aligned with causal embodied
   play. The deployed lexical imagination path is not.
4. The implementation does not extend lexical `read_sentence` imagination,
   Chi/Atlas identity, scalar salience as play authority, transcript/corpus
   curriculum, or reduced DSF projections.
5. The next item is to let bounded endogenous physical motor exploration
   create candidate actions and let experienced consequences, rather than a
   tutor-created command relation, govern which action becomes reusable.
6. The local path preserves explicit `D_k`, `M_k`, `R_rev_k`, `U_star_k`,
   `C_k`, `P_k`, and `B_k`.
7. No reduced field is the decision authority in that path.

## Definitive local proof now present

The following tests passed against the current worktree:

```text
3 passed in 7.46s
```

Covered files:

- `tests/test_engine_autonomous_play_single_settlement.py`
- `tests/test_engine_autonomous_play_thing_expansion.py`

The proof performs the following physical chain:

1. Capture an authenticated W1 trigger settlement.
2. Execute one real self-body motion through the embodiment port.
3. Capture the changed W1 outcome and close the exact relation.
4. Restore the body to the trigger state as test arrangement.
5. Let `AutonomousCausalPlayOwner` independently admit the single available
   closed relation.
6. Start one `PLAYING` activity from its authenticated one-use ticket.
7. Re-settle the current W1 trigger and execute the physical action once.
8. Settle the physical outcome once through shared custody.
9. Grow the same contact-grounded causal THING with that outcome.
10. Persist the world, relation owners, consumed play opportunity, full-field
    prediction state, and expanded THING.
11. Cold-restore byte-identical durable authority and prove the consumed
    opportunity cannot execute again.

The assertions include:

- the self body moves from `(1000, 1000, 0)` to `(1000, 1400, 0)`;
- world revision advances exactly once;
- one current observation and one outcome are committed;
- each source mount enters settled custody exactly once;
- every observed tuple retains the complete DSF field;
- one action relation, two physical episodes, and no passive relation are
  added;
- the same THING gains exactly one causally chained partition;
- the autonomous-play state remains within `128 KiB`;
- cold restore preserves the consumed state and changed world;
- no second opportunity is released.

This is meaningful proof of bounded autonomous reuse of learned physical
experience. It is not proof that Guala originates a novel motion, chooses
among unexplored motor acts, or learns a curriculum without an already-closed
tutor relation.

## Curriculum reality

The active local tutor-facing method
`durably_experience_embodied_action` records a real trigger/action/outcome
experience but deliberately creates no learned binding. That is safer than the
retired demonstration shortcut, but it leaves no current production path by
which a tutor can grow the exact closed relation consumed by autonomous play.

The local curriculum owners and manifests are not sufficient evidence by
themselves. A curriculum is active only when an authenticated physical
experience reaches the causal owners, changes durable learned state, and can
later alter Guala's own action.

## Live-production truth

The last measured production task served the old observation schema and did
not contain the local autonomous-play owner. Its most recent play record was a
zero-step boot reconciliation. Production also ran a lexical dream path that
fed developer-shaped word pairs into `read_sentence`.

No deployment has occurred from the current worktree. Therefore:

- autonomous physical play is locally proved but not live;
- spontaneous novel play is not implemented;
- tutored physical curriculum growth is not wired;
- deployed lexical imagination remains prohibited legacy behavior until the
  production cutover replaces it.

## Exact remaining acceptance

Before autonomous play and curriculum can be called complete, a clean
immutable build must prove:

1. Guala creates bounded physical candidate actions from her own actuators.
2. Candidate attempts are lived experiences and do not auto-bind.
3. A later physical consequence closes only the exact preceding occurrence.
4. Causally useful acts become reusable; unclosed acts do not.
5. The mechanism survives cold restore without replay or unbounded history.
6. The live scheduler performs the action and Loom reports the same physical
   receipts and full fields.
7. The deployed lexical imagination mutation no longer runs.
