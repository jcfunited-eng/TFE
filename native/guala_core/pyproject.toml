[build-system]
requires = ["maturin>=1.4,<2.0"]
build-backend = "maturin"

[project]
name = "guala_core"
version = "0.1.0"
description = "Native deterministic organism kernels for Guala."
requires-python = ">=3.9"

[tool.maturin]
bindings = "pyo3"
features = ["python-extension"]
