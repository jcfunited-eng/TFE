# guala_core

`guala_core` is the native deterministic boundary for Guala's exact sensory
source admission, corrected joint-field kernel, persistent neuronal fractals,
materialized fabric, and organism-state structure.

The mounted organism accepts only `NativeJointSourceEpisode`. It independently
constructs the complete joint L0-L4 field and cannot accept the historical
per-port field bank. The old bank remains temporarily available only to the
separate observation consumers that have not yet been replaced; it has no
source-view bridge into mounted neuronal state.

The crate does not export the retired Python-compatibility kernels formerly
named `krim_feed`, `biquad_bandpass`, `cochlear_feed`, `fovea_feed`,
`fingerprint`, `compute_dsf`, `map_inject`, or `psi_settle`. Those functions
implemented tuned oscillators, event-statistic DSF, Chi injection, and complex
psi settling that conflict with the accepted native organism architecture.

Build and verify from this directory:

```bash
cargo test --locked
maturin build --release -o dist
```

Python reaches the wheel through explicit native boundaries. No startup hook
monkey-patches Python cognition objects.
